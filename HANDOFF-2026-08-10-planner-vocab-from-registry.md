# 交接 · 让规划器的可选值从登记表生成（四批全部完成）

分支 `codex/claude-restaurant-generic-executor`，4 个 commit 已推。

---

## 一句话

**规划器能指到的格子从 147 (4%) → 2992 / 3168 (94%)**，办法是把 prompt 里「你可以选这些指标/维度/聚合」改成**由登记表渲染**。登记表加一行，规划器当场能指到它，永远不用再改 prompt。

回归电池 **81/85**（基线 80），单元测试 396 → **473 passed**。

---

## 一、四批的数

| 批 | 做了什么 | 可达格子 | 电池 |
|---|---|---|---|
| 起点 | — | 147 (4%) | 80/85 |
| 批 1 | 规格加 `aggregation` 槽；聚合从 `AGGREGATIONS` 渲染 | **322** (10%) | 80/85 |
| 批 2 | 维度从 `DIMENSIONS` 渲染（6 → 16） | **952** (30%) | **83/85** |
| 批 3 | 指标从 `METRICS+DERIVED` 渲染（15 → 22） | **2992** (94%) | 81/85 |
| 批 4 | resolver 逐个与格子比数字 | 2992 | 81/85 |

聚合 5/9 → **9/9**，维度 6/16 → **16/16**，指标 7/22 → **22/22**。
剩下 176 个不可达的是**真的不该成立**的组合（如客流×菜品会扇出）。

---

## 二、这一轮最值钱的四条判据

### 1. 只写规则不写示例，模型一次都不产出

批 1 第一版只在 prompt 加了「规则 4b: aggregation 只能使用…」，真模型实测 **0/7 全是 None**。模型是照**示例 JSON 的字段集**输出的——键不在示例里它就不填。补进 10 处示例后 **7/7 全部填对**。

> ⛔ 改 prompt 加槽，**规则和示例都要改**。只改规则等于没改，而且失败形状是「静默永远为 None」，不报错。

### 2. 给两套写法 = 邀请模型输出两套

批 2 第一版在 prompt 写了「dish 等同 product，两种写法都接受」，模型当场**两个都写**：`dimensions=('product','dish')` → resolver 能力子集判断失败 →「本月米饭的销量」这种最基础的问句被判成「问题对象与分析范围不一致」→ **整个回归电池连飞行前检查都过不去**。

### 3. 归一的方向和位置，两个都错过

- **方向**：管线里有 8 处以上消费者直接写 `"dish" in dimensions`。正解是**新写法归一到管线的旧写法**（只动一处）；反过来要改 8 处，漏一处的症状是「那条路径悄悄按旧写法比对，比不上就拒答」。
- **位置**：第二版把归一放在构造 spec 的那一行（出口），而契约修复、意图规划都在它**之前**读那个局部变量。⛔ **归一要放在第一个消费者之前，不是放在出口。**

这条当时**没有任何单元测试能抓到**，是 prod 电池飞行前失败才暴露的。已补源码顺序断言。

### 4. 可达性闸自己量错了域

批 3 扩完之后闸报「一点没变」（952/952）——因为脚本量的是 `_REQUEST_METRIC_RULES`（确定性**关键词编译表**），而批 3 扩的是 `_SEMANTIC_METRICS`（LLM **输出**的取值域）。

> ⛔ **量错了域，闸会对真实的进展沉默。** 与「读任何比值前先问分母是什么」是同一条判据。

---

## 三、放开维度会把「近似回答」变成「拒答」

放开 `meal_period`/`hour`/`staff` 当天，三条排班/人效用例挂了——规划器开始给问句打新标签，而没有 resolver 声明它们。逐条查真因后**分三类处理**：

| 维度 | 真因 | 处理 |
|---|---|---|
| `meal_period` | resolver **本来就按餐段出数**（注释写着 "keyed by store and daypart"），能力表里没这个名字 | 补进声明（说真话） |
| `hour` | 我写的说明是「按钟点/**时段**分」——「时段」是餐段的词，撞了 | 收紧成「按小时分(几点钟)；⛔ 问餐段用 meal_period」 |
| `staff` | 「各岗位」被打成 staff，而系统**根本没有岗位维度** | 说明里写「⛔ 问各岗位/工种时不要用它，dimensions 留空」 |

> ⛔ **每放开一个维度都要做一次决定**：要么某个 resolver 真能出它（补进能力表），要么明确接受它拒答（登记进已知名单）。不做决定的后果是静默的。
> 已补 `test_every_new_dimension_has_an_explicit_decision` 强制这个决定。

---

## 四、批 4：结论是「不删 resolver」，而且「倒转路由」被证据否掉了

### 数字口径已证等价（prod MOCK_REST 逐字核对）

```
总营收        resolver ¥6,490,180.61  ==  revenue×all×summary   6490180.61
门店营收榜首  resolver ¥663,083.12    ==  revenue×store×rank     663083.12
堂食营收      resolver ¥4,114,412     ==  revenue×channel×share  4114412.00
```

### 但结论是不删

7 个取数型 resolver 在**取数层面**确实是格子的特例，但它们的答案里还有格子给不了的东西——建议动作、KPI 卡、顺带提示、门店中位数对比。**删掉 = 用一个更薄的答案换一个更整洁的架构。**

### 「倒转路由」的收益上限很低

重放语料里剩下的 **20% 契约失败全是**「差异的**根本原因**」「下一步**该做什么**」「该重点**改善**哪个指标」——`analysis_action=diagnose/optimize`，**结构上就不是取数问题**。登记表答不了「为什么」，倒转路由一条都救不回来。另 34% 是反问澄清，多数合理。

> ⛔ 判据：**动手改主路由前，先量一遍剩下的失败到底是什么形状。**「架构上更干净」不是收益。

### 真正起作用的是契约失败兜底

挂在 `answer_contract.validate` 判失败的分支（单一调用点、每个答案都过）。
⛔ **不挂 resolver 层**：18 个里 **14 个没有失败出口**，它们把答不出来写进答案文字而不是给信号。

效果：重放语料「答上了」41% → **44%**；电池 `[56]` 的实际答案已经是通用执行器的输出格式——**它在 prod 真的接球了**。

---

## 五、没做 / 遗留

| 项 | 状态 |
|---|---|
| 电池剩余 4 条 | `[50][52]` 排班的「大模型解读」段需模型调用，**当前额度耗尽**（池 6/31）；`[56]` 多指标同问；`[66]` **跨天造成**——演示数据止于 08-09，而「这周」已从 08-10 起算，区间里 0 行 |
| 数据缺口 5 项 | 净利润/翻台率/人效/上座率/复购率——**故意不进 prompt**，走「如实说没有」。要不要补数据由 Steve 定 |
| `_REQUEST_METRIC_RULES` | 仍是关键词表（goal 说「批 3 之后应缩小」）。本轮**没缩**——它是确定性路径的入口，缩它要单独验证 |
| 字段登记表切 prod keruyun | 仍未切 |
| 写路径「确认执行」 | 仍只验过预览 |
| `_SAFE_MODELS` 白名单 | 08-16 过期。⛔ 只有 Steve 能在控制台确认「用完即停」 |

---

## 六、环境备忘（本轮新增）

- **熔断键是 `(账号, 模型)`**，阈值连续 2 次失败、冷却 60 秒，**403 额度耗尽也计入失败**
- ⛔ **排一个会超时的候选在链前面，等于把它后面的健康候选一起拖下水**（串行链共享总预算）。已把 zhipu 挪到 MAPPER 链尾
- ⚠️ **别在电池运行时跑探针**——它会打开熔断器污染你的测量（本轮踩了一次）；也别在电池运行期间重启服务（那轮 74/85 已作废）
- 可达性闸：`python -m smartbi.scripts.registry_reachability --detail`，也可 `--min N` 给 CI 当闸
