# HANDOFF 2026-08-10 — 链按实测能力排序 + 三条「答非所问」收尾

**全部已合并并部署 prod。** PR #2455 / #2456 / #2458 / #2459 / #2460 / #2461 / #2462。

> ⚠️ **本文按时间顺序写，中间有两次自我推翻。**
> **要看当前实际生效的排序规则，直接跳到文末「附：链排序的最终形态」** ——
> 第 1 节里那个「(地板置底, 能力档, 中位延迟, 到期日)」当天就被 #2459 撤回了，
> 保留它是为了记录**为什么**那样排是错的。

---

## 1. owner 拍板：链按能力排，不按免费额度到期日排

### 原策略的失败形状（很具体）

`_build_chain` 原本纯按免费额度到期日升序（use-it-or-lose-it）。它对
**「这个模型今天还活着吗」完全沉默**：

| | |
|---|---|
| `aliyun_c/deepseek-v3.2` | 契约打分 **0/6，全 403 quota** |
| 它的到期日 | 08-13（全池最早） |
| 于是它在链里的位置 | **REVIEW / CHAT / MAPPER 三个槽的链头** |

每次调用都先在一个已耗尽的模型上撞一跳。

### 新增：`smartbi/scripts/llm_capability_rank.py`

三处都同源，**没有手写答案表**：

| | 来源 |
|---|---|
| prompt | 生产自己的 `_build_t3_prompt` |
| 判定 | 生产自己的 `_t3_contract_violation`（`call_chain` 真正用来收/拒的那个） |
| 题目 | 回归电池 `CASES` 每条链首问 + 无上下文单问，**选法算出来不手挑** |

重测命令：

```bash
ssh root@47.100.235.168 "cd /www/wwwroot/cretas/code/backend/python \
  && set -a && . /www/wwwroot/cretas/.env.prod && set +a \
  && ./venv-current/bin/python -m smartbi.scripts.llm_capability_rank \
     --slot review --n 6 --emit-table"
```

### ⚠️ 这张表能区分什么，不能区分什么（先读这段）

**19 个 REVIEW 候选，18 个 6/6 满分。** 契约是**地板题**，不是能力题——它只能
区分「能不能用」。

⛔ **不要因为某个模型在这里 6/6 就说它能力强。** 要真正区分强弱需要一道会让
好模型和更好的模型分开的题，**现在没有，这是已知缺口**（写进了 `_CAPABILITY`
的注释）。

### 排序键

```
(地板置底, 能力档, 中位延迟, 到期日)
```

- **地板置底先于能力档**是**结构契约**不是排序偏好：`chain[-1]` 必须是永不过期
  的地板，否则全部 aliyun 过期那天该槽变空。旧版靠地板的 `_FAR_FUTURE` 到期日
  **隐式**实现——能力档一旦能把某个条目沉到底，那个隐式保证当场失效
- **到期日仍在键里**：同能力同速度时依旧优先榨干快到期的免费额度。它只是不再能
  把一个已耗尽的模型顶到链头
- **能力表超龄（21d）** → 退回纯到期日排序，**且 `llm_pool_health` 翻转退出码
  告警**（每 15 分钟经 `capability-watch.sh` 跑）。静默退化会把刚修掉的死链头
  原样放回来，而它没有任何自然症状

### 代价（owner 已知情）

08-13 到期的 aliyun_c 那批（6 个）大部分被排到链中后段，那批免费额度大概率用不完。
这是「按能力排」换来的，不是 bug。

---

## 2. 三条「答非所问」

### [56] 多指标问句 —— 数算出来了，被契约整份扔掉

`_scoped_dish_metric_answer` 结尾是一串互斥 if，前三条都带 `not asks_margin`。
问「米饭的**销量、毛利率和成本**」时它们被全部排除，只剩毛利分支——而那条
**原先不含销量**。于是 Answer Contract 判 `missing=['request_coverage']`，
**扔掉整份答案**，用户看到拒答。飞轮 miss 台账累计 **47 次**。

> 判据：**互斥 if 链表达不了「多选」**。每加一个可问指标组合数翻倍，漏掉的
> 组合表现为「答非所问被契约拦下」，不报错不告警。

根治是走登记表的多指标路径（`spec_to_cells`），**那是独立一件事，未做**。

### [03] 澄清延续轮 —— 拿半句话当整句判

```
turn1 「米饭的销量是多少」 → 反问时间
turn2 「本月」             → 反问门店
turn3 「全部门店」          → planner 正确继承出 sales_volume
```

```
[restaurant-contract] 去掉 planner 自造的指标要求: ('sales_volume',) -> () query='全部门店'
```

`_drop_planner_invented_metrics` 拿**这一轮的半句话**去判「原句一个词都没沾」。
后果不是少一个指标，是整轮**换了个问题回答**——用户收到全店营收概览，「米饭」
不见了。而按钮里还带着「上个月米饭的销量是多少」，**上下文一直在，只是回答那一步
没用**。

> 🔑 本函数自己的 docstring 写着「要求覆盖的应该是**用户问了什么**」。多轮澄清里
> 用户问的是**累积的那句话**，不是最后那个片段。

### [66] 那条不变式**永远不会红**

```python
"空周期必须给相邻周期": (["没有可用的经营数据"], []),
```

`invariant` 的结构只有 `(need → forbid)` 一个方向。「必须给相邻周期」是个
「必须出现」，表达不了，于是 forbid 填了空表——**内层循环一次都不执行**，而
旁边的注释信誓旦旦说它在检查相邻周期。

> 判据：**写完一条断言先问「它靠什么变红」。答不上来就不是断言，是注释。**

「闸不可能变红」的**第四种形状**：①闸没跑 ②左右同源恒真式 ③被测数据里没有可
失败的东西 ④**规则表达不了这条判据，于是被填成空操作**。

连带修法：判定逻辑原本内联在一个要发 HTTP 的函数里，**没有任何办法被单测覆盖**
——抽成纯函数 `invariant_problems` 后才谈得上补闸。**「不可测」本身就是「不会红」
的一种成因。**

---

## 3. 🔴🔴 自我推翻（PR #2459）：我说的「同版本两轮」不是同版本

上面第 1 节那条**中位延迟排序键，我当天就撤回了**。经过：

我拿到 80/85 和 61/85，写成「同版本读数抖 19 条」，并把它写进了 PR 描述、这份
交接和 memory。把时间戳拉齐才看清：

| 时间 | 版本 | 结果 |
|---|---|---|
| 08-10 **04:40** | 下午所有部署**之前**（cron 定时跑） | 80 / 5 |
| 08-10 16:57 | #2455 之后 | 61 / 24 |
| 08-10 17:09 | **真正的同版本** | **61 / 24，失败集逐条相同** |

**真正的同版本两轮一点都不噪**——逐条相同。19 条差全部来自我自己夹在中间的部署，
那是**一次真实回归**。

> 判据升级：**「同版本复测」比的是【两个读数各自的时间戳 vs 最后一次部署的时间戳】，
> 不是「我特意连着跑了两次」的感觉。** cron 产出的读数最容易被误当成「刚才那次」。

### 回归的根因：我拿一个刚判定为「区分不出能力」的量去排能力

`_CAPABILITY` 的注释是我自己写的：「契约是地板题，18/19 满分，区分不出强弱」。
**然后我还是把它当排序键用了。** 18 个满分并列后，排序退化成**纯延迟升序** =
「最小最快的排最前」，链头从 `glm-4.6` 换成 `qwen3-next-80b`（0.8s，规划更弱）。

> 🔑 **一个量，你刚判定它区分不出 A，就不能拿它去给 A 排序。**
> 延迟是**约束**（超预算不能进链），不是**能力的代理**。快 ≠ 会规划。
>
> 📌 **「我知道」和「代码知道」是两件事**——把判据写进注释不等于把它写进排序键。

处置：**保留有证据支撑的那半**（实测 0/6 全 403 的模型继续沉底），
**去掉没证据的那半**（延迟不再排序）。不是整个回退——整个回退会把「死模型占着
三个槽的链头」一起放回去。

## 3b. 失败条数 ≠ 缺陷数

那 24 条**内部**确实是一个缺陷的爆炸半径：菜品链第 3 轮断了上下文，后面同链
6+ 轮全部连坐。

> 判据：**看到一长串失败，先问「这些失败之间有没有父子关系」。**
> 按条数估工作量会把 1 个缺陷估成 24 个；按「最早那条」定位只需修一处。

⚠️ 但**别拿这条去解释 80 vs 61**——那是跨版本。爆炸半径解释的是**一次失败内部
的结构**，不是**两次读数之间的差**。这是我第一版犯的第二个错。

---

## 4. 我自己在这一轮里踩的两个坑

1. **变异没生效我差点当成「闸抓不住」**：把排序 key 从 `(a,b,c)` 改成
   `(0,0,0,a,b,c)`，**排序结果完全没变**，测试当然全绿。
   → **变异跑完先验「被测行为真的变了」，再看闸红不红。** 我打印了链头，看到
   它变回 `deepseek-v3.2` 才确认变异生效——然后闸就红了。

2. **我读了「不存在的日志」当证据**：`grep -c 'trusted_context'` 得 0，差点推出
   「那条路径没走」。实际是**那条路径根本没有日志语句**。
   → **拿「日志里没有 X」当证据前，先确认代码里真的会打 X。**

---

## 4b. 🔴 淘汰 5 个 403 模型（PR #2460），以及「先用快过期的」的代价

全量探针判定 5 条 403 `AllocationQuota.FreeTierOnly`，已全部移出注册表：

`aliyun_b/qwen3.7-max-preview`、`aliyun_c/deepseek-v3.2`、`aliyun_c/glm-4.6`、
`aliyun_c/qwen3-next-80b-a3b-instruct`、`aliyun_c/qwen3-vl-flash-2026-01-22`

⚠️ **VL 槽因此变成空链**（那是唯一的 VL 模型）。它本来就 403——图片请求两种情况
下都不工作，差别只在后者报错更清楚。与 §9.1 已拍板的处置一致。

### 同一天之内 REVIEW 链头连死三个

```
deepseek-v3.2  →  glm-4.6  →  qwen3-next-80b     全是 403 FreeTierOnly
```

它们恰好就是「按到期日升序」排最前的那三个。

> **use-it-or-lose-it 把「最快烧完」和「排最前」绑成了同一件事**——链头位置本身
> 就是最大的消耗源，于是这条策略在持续地把自己的头部烧掉。

而长跑道的强模型（`qwen3.8-max` 11-01、`deepseek-v4-flash-0731` 10-31）全被压在
链尾，**一次都没被用过**。

### 实测：这些模型在难题上的表现（`llm_capability_rank --compare`）

| 模型 | 「哪道菜卖得最差」判成 |
|---|---|
| deepseek-v4-flash-0731 / qwen3.8-max / qwen3.7-max ×2 | `GROSS_MARGIN`（菜品级） |
| qwen3.7-flash ×2 | `SALES_SUMMARY` |
| **qwen3.5-plus / qwen3.6-plus** | **`RECIPE_COST`（食材成本排行）** ← 问销量答成本 |

**这道题是有区分度的**，早上那把「契约合格率」的尺子没有（19 选 18 满分）。

📌 另一发现：「明天怎么排班 / 下周需要多少兼职」上所有模型的 `time_range` 都乱
（`tomorrow` / `next_week` / `yesterday` 都不在提示词允许的 `today|this_week|this_month` 里）
——**这不是模型弱，是 schema 没有「未来 horizon」这个槽**。

## 5. 验证状态

- CI 闸全量：**7390 passed / 10 failed**，10 条与 clean `origin/main` 基线逐条
  相同（本地无 PG / 无在跑服务），**新增 0 条**
- 路由测试 5 个文件 **90 passed**
- 所有新闸均**双向变异验证**（撤回修复 → 红；无条件豁免 → 阴性对照红）
- prod 部署后核对过运行代码：链头 = `aliyun_c/qwen3-next-80b-a3b-instruct`，
  地板 = `zhipu/glm-4.5-air` 在末位，`cap_age=0d`，`llm_pool OK usable=20/21`

---

## 6. 未做 / 待办

| 项 | 说明 |
|---|---|
| **能力题** | 契约是地板题，18/19 满分。需要一道能区分强弱的题才谈得上真正「按能力」 |
| **多指标根治** | [56] 现在是把销量补进最完整那条分支；根治是登记表的 `spec_to_cells` 多指标路径 |
| **CHART / REASONING 槽未测** | 它们的任务形状不同（图表 JSON / 长推理），T3 prompt 测不了；这两个槽的顺序仍按到期日 |
| **能力表 21 天后过期** | 2026-08-31 起 `llm_pool_health` 会开始告警要求重测 |

---

# 附：链排序的最终形态（本轮反复三次才定下来）

## 排序键

```
(地板置底, 存活档, 计划合法档, 延迟粗档, 到期日)
```

| 位 | 判据 | 来源 |
|---|---|---|
| 地板置底 | `_TEXT_TAIL` 永远最后 | **结构契约**：全部 aliyun 过期那天槽不能变空 |
| 存活档 | 实测能不能调通 | `probe_llm_registry` 探针 |
| 计划合法档 | 有没有编造提示词里没有的枚举值 | `llm_capability_rank --schema`，合法值从 `_build_t3_prompt` **现解析** |
| 延迟粗档 | 快≤5s / 未测 / 可接受≤20s / 超预算 | `_CAPABILITY` 的 p50 |
| 到期日 | use-it-or-lose-it | `_SAFE_MODELS` |

「没测过」在每一档里都单独占一位，排在「实测好」与「实测差」之间——**缺席不构成任何一侧的证据**。

## 三把尺子，前两把都不行

| 尝试 | 尺子 | 为什么废 |
|---|---|---|
| 一 | 契约合格率 | 19 选 **18 满分**，地板题。满分并列后退化成纯延迟升序＝「最小最快的排最前」，**电池 83→61** |
| 二 | 与参照模型一致率 | 参照自己会烧完额度而死（glm-4.6 当天 403），死了全 0 |
| **三** | **计划合法性** | 判「有没有编说明书里没有的值」——**客观、有区分度、不需要人写标准答案** |

> 🔑 **一个量，你刚判定它区分不出 A，就不能拿它去给 A 排序。**
> 我在 `_CAPABILITY` 注释里亲手写了「契约区分不出强弱」，然后还是把它当了排序键。
> **「我知道」和「代码知道」是两件事。**

## 延迟：连续键 ❌ / 粗档 ✅

- **连续键**在上一位全平手时退化成「最小最快的排最前」——那是 83→61 的全部内容
- **粗档**不会：1.7s / 2.7s / 2.8s 同属一档，它们之间仍由到期日决定；只有跨档（2.8s vs 19.2s）才重排

## 单跳预算 10s → 20s（owner 拍板）

放宽前逐层核对上游没有更短的闸：前端 75~180s、nginx 默认 60s、Java `readTimeout(0)`。**Python 原本就是瓶颈。**

定 20 不定 25 是因为**先立了规则**：总预算至少要放得下两次满额尝试，否则链事实上只有一跳、后面全是死代码——而这件事没有症状。25×2=50 > 45 当场判红。

⚠️ kimi-k2.7-code 在 aliyun_c 上实测 19.2s，**正好卡在线上**。

## 🔴 今天四个模型死在链头上

```
deepseek-v3.2 → glm-4.6 → qwen3-next-80b → qwen3.5-plus     全是 403 FreeTierOnly
```

**「按到期日升序」把「最快烧完」和「排最前」绑成了同一件事**——链头位置本身是最大消耗源，而排在链头的恰恰是额度最少的那个。排序策略在持续地把自己的头部烧掉。

08-13 那批 aliyun_c 还剩 8 条，未来三天还会有。

## 同一天交了三次同一笔维护税

`test_llm_router_budget` / `test_refuse_hard_drops_expired` / 三条写死预算数值的测试——全是**夹具引用了会随运维变动的东西**。

> **判据：测试夹具引用的东西如果会随运维动，就从权威表里现算，不要写死。**

⛔ 一个例外：`test_negative_confidence_pill_...` 的**主语**就是那颗毒丸模型，它被淘汰后我**没有**随便挑另一个顶替——改成钉住真正承重的那件事。**夹具可以换，主语不能换。**

## 仍然开着的

| 项 | 状态 |
|---|---|
| [45] 本月社区店的营收 | 近几轮稳定失败，未查 |
| [51] 下周需要多少兼职 | schema 层面已定位（模型编 `next_week`），但**根因是 prompt 没有「未来 horizon」这个槽** |
| `factory_ai_eval.py` | 同一个 cron 里在崩（Traceback），未查 |
| 能力表 21 天后过期 | 2026-08-31 起 `llm_pool_health` 会告警要求重测 |
| CHART / REASONING 槽 | 任务形状不同，T3 prompt 测不了，顺序仍按到期日 |
