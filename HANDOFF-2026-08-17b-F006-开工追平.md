# 交接 2026-08-17（第二轮）—— F006 开工追平：七条判据逐条结账

**授权**：Steve 全权（部署／合并／F006 真实写入与冲销／自行做产品决策）
**生产写入**：**两轮**全部冲销，59 行基线快照 `diff` **两次都完全为空**；跨租户阴性对照两侧均未变

> **第二轮冲销（仓储走查）**：采购单 / 采购行 / 审批实例 / 审批历史 / 收货单 / 收货行 /
> 入库批次 / 附件登记 / 发货单 ×2 / 发货行 ×2 —— 全部删除，**9 条计数断言逐项核回基线**，
> 阴性对照 `mb_other=181 / usr_other=90` 两侧一致。
>
> ⚠️ **冲销的 FK 顺序（两条，都是实测撞出来的）**：
> ① 先删 `semi_finished_inventory_transactions` 再删 `semi_finished_inventory`；
> ② **先删 `purchase_receive_items` 再删 `material_batches`**
> （`purchase_receive_items.batch_id → material_batches.id`，反过来撞 `fk_pri_batch`）。
> 第二条当场撞了一次 —— 因为整段在一个事务里配 `ON_ERROR_STOP`，**整体回滚，什么都没提交**。
>
> ⚠️ OSS 上那张 70 字节的收货凭证图留着没删（只清了库里的附件登记），不影响任何读数。
**上一份**：`HANDOFF-2026-08-17-报工库存与退役.md`（本文件订正了它的两处结论，见第九节）

---

## 零、一句话

**工厂还不能开工，但阻塞点变了** —— 不再是「等有人去手机上试一下」，
而是查出了根因：**那台设备跑的是 runtimeVersion 1.0.3，而 8-09 之后所有 OTA 包都发到 1.0.4 树**。
重开一百次也没用，两边问的不是同一棵树。

同时，**仓储出入库在手机上目前走不通**（判据四），这一条上一轮完全没碰过，现在有了三条具体成因。

---

## 一、七条判据结账

| # | 判据 | 结论 | 关键读数 |
|---|---|---|---|
| 1 | OTA 到手机并经确认 | 🔴 **未达成，但根因已定位**（不是「等现场」） | 设备报 `rv=1.0.3`；1.0.3 树最后一次发包 **08-09 12:12:57**，此后 5 次发包全在 1.0.4 |
| 2 | 真实批次从头走到尾 | ✅ **达成**（批次 10761，两道工序全 COMPLETED） | 报工落在点的那一道 ✅／产出进半成品库 ✅／下一道领得到 ✅／扣减+OUT 流水 ✅ ／**IN 流水缺失** ⚠️ |
| 3 | 单位这一族收口 | ✅ **达成** | 棘轮闸 PR **#2780**（冻结 78 键/85 处）＋ 两条路径 prod 实跑通过，带阴性对照 |
| 4 | 仓储出入库手机端可用 | 🟡 **入库端到端跑通；出库卡在成品库 0 行** | 三处死路已修(#2785)，另查出四处，见第四节。⚠️ 含**仓库员看得见发货单点不开(403)** |
| 5 | 已合未验的 PR 补验证 | ✅ **达成** | `DEPLOY_EXIT=0`／`RELEASE_FINAL_STATUS` 恰好 1 次／进程 18:15:20 晚于 jar 18:14:55／端点真跑 |
| 6 | 生产数据干净 | ✅ **达成** | 59 行快照 `diff` 为空；阴性对照 `mb_other=181 / usr_other=90` 两侧一致 |
| 7 | 交接件最新 | ✅ 本文件 | 含**被推翻的结论**（第九节） |

---

## 二、判据 5 —— 部署与四条判据（已完成）

上一轮合的四个 PR（#2760 / #2771 / #2775 / #2776）当时**都没上线**：
运行中的 jar 建于 **08-17 13:10 CST（05:10Z）**，早于全部四次合并（09:24Z / 09:47Z）。
不是靠时间戳推的 —— **#2776 新增的两个 DTO 类当时不在 jar 里**，
阳性对照 `ProcessWorkReportingServiceImpl.class` 读出 1（仪器活着）。

部署后四条判据：

| 判据 | 读数 |
|---|---|
| `DEPLOY_EXIT=0` | ✅ |
| `RELEASE_FINAL_STATUS` 恰好 1 次 | ✅ 第 88 行 `RELEASE_FINAL_STATUS=deployed` |
| 进程启动晚于制品 mtime | ✅ 进程 `18:15:20` > jar `18:14:55`；**蓝绿已换到 10020** |
| 真跑一次那条路径 | ✅ `/summary` 200、`/reports/historical-average` 200、`/reports` 200；`case→箱` 建计划 200 |

⚠️ **两个坑，下次照做的人会踩：**

1. **`--base-sha` 传成当前 tip = 白跑。** 我第一次传了 `1d23430ac8`（就是当时的 origin/main），
   diff 为空 → `DETECTED_JAVA_CHANGED=false / RELEASE_SELECTION=none`。
   Java 段其实**部署成功了**（140s SUCCESS），但整条流水线以 `DEPLOY_EXIT=1` 收尾、
   `RELEASE_FINAL_STATUS` 一次都没出现。**正确值是上次部署对应的 commit**（这次是 `d79c2aade5`）。
2. **worktree 会被并发 session 的 `--all-merged` 清理顺走。** 我的部署 worktree
   在 `deploy-web-admin.sh` 那一步**整个目录消失了** —— 因为它正好「已合并 + 干净 + 未锁定」。
   ⇒ **新开的 worktree 立刻 `git worktree lock`**，本轮之后所有 worktree 都上了锁。

---

## 三、判据 2 —— 批次 10761 从第①道走到最后一道

在真实 RN 界面（Expo Web 打 prod，控制台实证 `REACT_APP_API_URL = https://www.cretaceousfuture.com`）走完。

| 步骤 | 界面读数 | 生产库读数 |
|---|---|---|
| 第①道 原料处理 · 投入 10 kg（领 `MT-20260817-8432`） | 「投入已提交」 | 报工 23817 落在 **task 1788** ✅ |
| 第①道 完工产出（故意填 99 kg 测越界） | 出成率 **990%**，事后弹「系统不阻塞」 | task 1788 `COMPLETED`，SFI **338** 建行 99 kg |
| 第②道 定量包装 | **「上道半成品有 1 笔 · 可领 99 kg」** ✅ | 领用后 SFI 338 → `DEPLETED`，OUT 流水 `balance_after=0` |
| 第②道 完工产出 80 盒 | 出成率 **64.65%** | task 1789 `COMPLETED`，SFI **339** = 80 盒 |

✅ **客户那句「上工序不报工、下工序就没库存」的链，两道工序之间是通的。**

**出成率 64.65% 值得单记**：= (80 盒 × 800 g = 64 kg) ÷ 99 kg。
**后端会做跨单位换算，而且算对了** —— 这一条直接决定了下面那个 RN 缺陷只是显示层的。

### 判据 2 下查出的问题（按严重度）

**① 半成品「入库有、入账流水没有」**（形态 B：机制在、没接上）

`upsertProducedWip`（`YieldReportServiceImpl.java:815`）只写 `semi_finished_inventory`，
不写流水。真正写 `IN` 流水的 `postSemiOutputLedger`（`WipInventoryServiceImpl.java:171`）
守在 `semiCode` / `semiOutputQuantity` 上：

```java
if (semiCode == null || semiCode.isBlank() || inQty == null || inQty.compareTo(BigDecimal.ZERO) <= 0) return;
```

而三阶段报工**从不设这两个字段**（实测报工 23819：`semi_output_quantity` / `semi_code` 全 NULL）
⇒ 那段代码永远够不着。

🔴 **阳性对照在同一轮里**：OUT 流水（txn 82）**写出来了**。
所以「IN 缺失」不是仪器坏 —— 流水表这一轮就写得进去。
后果：半成品余额对得上，但**账上查不到这 99 kg 是哪笔报工进来的**，
第一条 OUT 的 `balance_after` 凭空成立。

**② 报工领料不扣原料库存**（需要业务口径，见第七节）

实测：报工引用批次 `MT-20260817-8432`（10 kg）后，
`material_batches.used_quantity` 仍是 **0**，`material_consumptions` 无新增行。
读代码：投入报工只把 refs 写进 `production_reports.material_batch_refs`，
再调 `checkAndAutoSettle` —— 而它**只在批次已经是 `USED_UP` 时标记结清，不做扣减**。
F006 `factory_material_requisitions` **0 行**，即当前没有任何路径在扣。

阳性对照：F006 唯一两条 `used_quantity > 0` 来自 `TRANSFER_OUT`（调拨出库），不是报工。

**③ 产出 > 投入 不事先拦，且损耗行的公式在说谎**

- 投入 10 kg、产出 99 kg：客户端只弹常规确认，服务端接受，锁定 990% 出成率并入库 99，
  事后才提示「物料平衡偏差 890% — 请核对，**系统不阻塞**」。**是提交后解释，不是事先拦住。**
- 同屏损耗行印的是 `损耗 (自动) = 投入 10kg − 产出 99kg = 0kg`
  —— `Math.max(0, …)` 把 −89 夹成 0，**等号照印**。
- 第②道更明显：`损耗 (自动) = 投入 99kg − 产出 80盒 = 19盒`，**99 kg 减 80 盒得 19 盒**。
  代码在 `YieldStepReportScreen.tsx:2682-2696`，三个单位变量
  （`reportedInputUnit` / `outUnit` / `unit`）各不相同，结果套的是第三个。
  ⚠️ 后端算对了（64.65%），**这纯粹是前端那一行**。已派修复。

---

## 四、判据 4 —— 仓储出入库（🔴 未达成，三条成因）

以 `f006_warehouse_worker` 登录真实 RN 界面走查。

**① 「新建入库」是过期标签，点了到一个空列表**

按索引精确点击（按钮自身文本已核对）：按钮 0「新建入库」和按钮 2「待收货任务」
`onPress` 是**同一行代码** —— `navigation.navigate("WHPurchaseReceiveList")`
（`WHInboundListScreen.tsx:256` 与 `:275`）。
导航器注释说明真正的新建入库屏已删（它调停用的 `POST /material-batches` 会 409）。

而那个列表在 F006 上是空的，且**不是接口坏**：5 个请求全 200
（`purchase/orders/by-status` × 3 + `warehouse/receiving/tasks`）。
库里核实：F006 采购单只有 2 张 `COMPLETED`，客户来料通知 1 条已 `RECEIVED`。
**阳性对照：F002 有 1 张 `APPROVED`** ⇒ 查询能读出非 0，是 F006 真的没有。

另一条正规入库 `WHUnorderedInboundReceive` **必须带 `noticeId`**，
唯一入口是那个空列表里的预告项 ⇒ **仓管在 App 上无法发起任何入库**。

**② 出库同理，且手工出货登记连「新建」按钮都没有**

「发货确认」要销售员先建发货单（F006 待确认 **0 单**）；
「手工出货登记（旧）」列表 0 单、页面上只有「扫码出库」一个按钮，**没有新建入口**，
页面自己注明「不自动扣成品库存」。

**③ 扫码入库的「手动输入」在 Android 上是死路**

`WHScanOperationScreen.tsx:175-192` 用 `Alert.prompt` —— **iOS 独有**。
Android / Web 上 `typeof promptFn !== "function"` ⇒ 走 else 分支弹
「当前平台不支持手动输入弹窗, 请使用扫码功能」。
而同屏说明里白纸黑字写着「**如无法扫码, 可点击"手动输入"**」。
⇒ 标签牌照扫不出来时，工人**没有退路**。

**④ 🔴 附带查出：手机端「库存调拨」是个会写脏数据的原型屏**

`WHInventoryTransferScreen.tsx`（文件头注释：`对应原型: warehouse/inventory-transfer.html`）：

- 调入库位下拉是**写死的三条示例**：`A区-冷藏库-02 (剩余: 150kg)` / `A-03` / `B-01`
  —— F006 真实库位是 `WH-RAW` / `WH-WKS` / `WH-FG` …
- 22 处硬编码 `kg`；批次下拉把 片/卷/盒 全印成 kg
- `executeTransfer` 只调 `materialBatchApiClient.updateBatch(id, { storageLocation: toLocation })`
  —— **把批次库位改写成 `A-02` 这种不存在的码**，
  **输入的调拨数量完全不参与**，也不产生任何调拨单，根本不走 `TransferServiceImpl`

⛔ **我没有按那个确认按钮**（按了会往生产库写一个不存在的库位）。

### ✅ 这三条已经修掉了（#2785 已合进 main）

| 原症状 | 改法 |
|---|---|
| ①「新建入库」是过期标签牌 | 删掉那块牌子。⛔ 不新放一个 —— App 侧目前**没有**无预告直接建批次的路径，少一个按钮好过一个说谎的按钮 |
| ③ Android 上「手动输入」是死路 | 换成组件自己的受控 Dialog，三端一致；空值 `disabled`（事先拦住，不是提交后报错） |
| ④ 原型调拨屏 | 摘掉两个入口，屏文件保留待接；行内那条把内部屏幕名弹给仓管的 Alert 改成人话 |

7 条断言 + 3 条变异对照（每条**先证明变异真的进了代码**再看红不红，回退后计数回到变异前）。
⚠️ 闸的第一版把①写成「全文件不许有重复 navigate 目标」，**实测误报** ——
`WHInboundDetail` 从卡片点击和「查看详情」各跳一次是合理的。已收窄到 actionBar 那一段。
一道会误报的闸最终会被关掉，那时它覆盖率归零。

### ✅ 补做：造出上游单据后，入库/出库都在手机上真走了一遍

第一轮我停在「F006 没有上游单据所以发不起来」。**那是数据缺口，不是不能验** ——
造一张采购单、造一张发货单，两条链都能走。全部写入已冲销（第六节）。

#### 入库：**端到端跑通** ✅

| 步 | 读数 |
|---|---|
| 造采购单 | `PO-20260817-0003`，外箱 20 **片** @5，DRAFT |
| 提交 | 「采购订单审批流程已自动完成」→ 直接 `FINANCE_APPROVED`（就是那个自动盖章，Steve 已裁定不改） |
| 手机「待收货任务」 | 出现 1 条：`财审通过 / 待收 20 片` ✅ 单位对 |
| 收货录入 | 数量与日期**已预填**（20 / 今天），物料名显示正确 |
| 提交 | 5 个请求全 200：建收货单 → 取上传地址 → 传凭证到 OSS → 登记附件 → **确认收货入库** |
| 结果 | 采购单 → `COMPLETED`；新批次 `MT-20260817-0643` = **20 片**；收货单 `RCV-20260817-9370` → `CONFIRMED` |
| 阴性对照 | `mb_other` 181 → 181 未变 |

#### 三条「事先拦住」的路径（判据要求的那一条，实际拿到三条）

| 路径 | 读数 |
|---|---|
| 收货数量清空 | 按钮文案从「提交入库 (1 行)」变成「(0 行)」且 **disabled**（Playwright: `element is not enabled`） |
| 没拍收货凭证 | `photos.length === 0` 时**在发请求之前**拦住，并说清去哪补。这是 08-16 特意加的，为的就是别让人填完才被后端 409 拒 |
| 出库超发 | 字段下方标「不能超过计划 1」，按钮 `aria-disabled=true`，按钮下方还写「请修正数量后再确认」 |

⚠️ **超收（收货填 999 而订单只有 20）是被接受的** —— 但屏幕自己写着
「按实际到货填写即可，差异由采购处理」，那是**有意的设计**，不算缺陷。

#### 出库：走到最后一步被拒，卡在成品库

| 步 | 读数 |
|---|---|
| 造发货单 | `DLV-20260817-6352`，状态 `PENDING_SPLIT` |
| 手机「发货确认」 | **没出现**（列表只认 `DRAFT / PENDING_WAREHOUSE_CONFIRM / PICKED`） |
| 拆子发运单 | `DLV-20260817-6352-01` → `PENDING_WAREHOUSE_CONFIRM` |
| 手机「发货确认」 | 出现了 ✅「待仓库确认 1 单」 |
| 点「确认并扣库存」 | **被拒**：「发货行 209 未完成批次分配，无法确认发货（请在「发货记录」Tab 点击「分配批次」…）」 |

🔴 而**批次分配要成品批次，`finished_goods_batches` 全库 0 行**（含软删、跨所有租户）。

⇒ 成品那一步**有**写入路径（质检通过 / 期间结算 / FG feed 三处），
**只是从来没有被执行过** —— 所以发货这条链在生产上至今没有输入。
⛔ 这不是代码坏了，是流程里那一步（质检或期间结算）没走。

### 🔴 判据 4 新查出的四处

**① 仓库员看得见发货单，点不开（403）**

| 角色 | 列表 `/warehouse/deliveries/pending` | 详情 `/sales/deliveries/{id}` |
|---|---|---|
| `warehouse_worker` 仓库员 | 200 | **403**「您的角色 [仓库员] 在 [销售管理] 模块无 [读写 / 读取] 权限」 |
| `warehouse_manager` 仓库主管 | 200 | 200 |
| `factory_super_admin` | 200 | 200 |

列表要 `warehouse:read*`，详情要 `sales:read*` —— **同一个功能的列表与详情权限范围不一致，
而受影响的恰好是真正去发货的那个角色**。
⛔ 我没有擅自放宽权限（该给仓库员 `sales:read`，还是该让详情接受 `warehouse:read`，
是权限模型决策）—— 见第七节第 6 问。

**② 「发货确认」的空态文案是假的**

写的是「销售员创建发货单后会出现在这里」，实际**创建并不够** ——
父单是 `PENDING_SPLIT`，要先拆成子发运单才会出现。
⚠️ 代码里那句注释还写着「销售创建后直接进入 `PENDING_WAREHOUSE_CONFIRM`」，
而同一行代码设的是 `PENDING_SPLIT` —— **注释过期，方向还相反**。

**③ 「待收货任务」列表把物料 UUID 给了仓管看**

明细行显示 `RMT_458890b0-be7e-478f-843f-5bbe216674a2: 待收 20 片`。
点进收货录入页之后物料名是对的（`SOP-20260817-01-黄油鸡-外箱`），
说明数据拿得到，只是列表那一行没取名字。

**④ 「确认并扣库存」被拒时指向手机上没有的入口**

错误文案让人去「发货记录」Tab 点「分配批次」—— 而手机出货页只有
「发货确认」和「手工出货登记(旧)」两个入口，**没有「发货记录」Tab**。
文案本身很好（点名了行号和产品），指的地方在 web-admin。

### 判据 4 里唯一「事先拦住」的读数

不是业务校验（发不起来），而是 ③ 那条：点击后**在提交前**就给了提示。
但那个提示是**死路**而不是可执行的下一步，所以按判据的本意仍算不合格。

---

## 五、判据 3 —— 单位这一族收口（✅）

### 5.1 棘轮闸 PR **#2780**

冻结存量 **78 个键 / 85 处**，31+ 个文件，**只禁新增**。

口径写在类注释里：只管「**两侧都是单位表达式**」的同一性比较；
刻意不管 `"kg".equals(unit)`（量纲分类）、两侧 `.code()`（已过契约，是正解）、
`unitPrice`/`gramsPerUnit`/`unitReviewRequired`（以单位为定语）。

仪器自检三条：
- **阳性对照** 4 种已知违例形状必须被检出 —— 没有它，扫描器坏掉时读数是「0 违例」，
  和「全仓修干净了」长得一模一样
- **阴性对照** 7 种不该报的必须不报，含 `OpportunityStage` 的 enum 比较
  （实测教训：用 `\w*unit\w*` 子串匹配会把 Opport-**unit**-y 数进来，误报过 3 条）
- **仪器活着**：扫到的 java 文件数 > 3000 且违例总数 ≥ 30

**变异对照**：在 `YieldAnalysisServiceImpl` 加一处 `probeInputUnit.equals(probeOutputUnit)`
→ 先 `grep -c` 证明变异真的进了代码 → 闸 `Tests run: 5, Failures: 1`，
失败信息精确点名 `+ …|probeInputUnit|probeOutputUnit (基线 0 处 → 现在 1 处)` → 回退后 5/0。

⚠️ 实现上两个坑已留痕：基线键里把字符串字面量掏空成 `<str>`
（`PurchaseServiceImpl` 有两处比较的参数带中文提示语，Maven 在 Windows 走 GBK 输出，
从 stdout 复制回源码就是乱码，且带内层引号编译不过）；`printBaseline` 改成落 UTF-8 文件。

### 5.2 两条已知会撞的路径，prod 实跑

**甲、以销定产（`case → 箱/盒`）**

挑的是**能让判据变红的脏样本**：`SO-20260817-0001` item 766，
`unit='case'` 而包装快照 `packagingUnit='箱'`（同一单位的中英两种写法）。

```
POST /api/mobile/F006/production-plans/batch-from-so
→ 200  plannedQuantity=80.0  plannedUnit='盒'
       sourceDisplayQuantity=10.0  sourceDisplayUnit='case'
```
10 箱 × 8 = 80 盒 ✅

**乙、调拨（基本单位「片」用英文别名 `slice` 提交）**

```
POST /api/mobile/F006/transfers   items:[{materialTypeId: 外箱, quantity:10, unit:"slice"}]
→ 200  TRF-20260817-1232   item.unit="片"  packageUnitSnapshot="片"
```
放行 ✅ 且快照**收敛成主数据写法**「片」，不是客户端送来的 `slice` ✅

**🔴 阴性对照（关键 —— 200 不等于闸还在）：**

| 提交的单位 | 结果 |
|---|---|
| `kg`（物料基本单位是「片」） | **422 拒** ✅ |
| `只`（只/个/件 不该被合并） | **422 拒** ✅ |
| `片`（中文原写法） | 200 ✅ |
| `slice`（英文别名） | 200 ✅ |

⇒ **#2775 只放宽了「同一单位的两种写法」，没有把校验改成恒真式。**

### 5.3 「只」那个物料走不通，原因是数据不是代码

F006 唯一 `unit='只'` 的物料 `RMT_1784220443823`（黄油鸡）**0 条库存批次**
（阳性对照：冻猪蹄同形状查询 3 行）⇒ 这条路径现在没法真写，换用了外箱（片）。

### 5.4 第三处裸比较：登记，但**不改**

`TransferServiceImpl.java:562` 抄码分支仍是
`Boolean.TRUE.equals(material.getIsAbacaPackaging()) && !transactionUnit.equals(baseUnit)`。

实测 F006 只有 1 个抄码物料（`RMT_1777690110201` 牛肉前腱子，单位 `g`，MASS 维度会先归一）
⇒ **对 F006 现状咬不到人**，且该物料 0 库存，无法用真实 payload 验证。

⛔ 这是**显式登记的「故意不改」**，不是漏了：
只要别的工厂出现「抄码 + 计数/包装单位」的物料，这一行会重演同一个死路。

---

## 六、判据 1 —— OTA（🔴 根因已定位，不是「等现场」）

### 上一轮说「OTA 服务端不记 manifest 请求日志」—— **假的**

nginx 在 **139** 上完整记录（`/www/wwwlogs/ota.cretaceousfuture.com.log`，未轮转，覆盖 6/4 至今）。
8/16 **62 条**、8/17 **64 条** manifest 请求。

### 真正的问题：设备和包不在同一棵树上

唯一可观测的常驻设备 `121.228.208.154` 自 **2026-08-11 19:32:02** 起，
828 条请求**全部是 585 字节**（= `noUpdateAvailable`），跨越此后 **5 次**真实发包，
**一次「有新版本」都没再收到过**。

用真实等价 header 两个方向都复现过：

```
rv=1.0.3 + current-update-id = 1.0.3 树最新 id  → 585  (directive: noUpdateAvailable)
rv=1.0.4 + 同一个 id                            → 4700 (manifest: 有新版本)
```

各 runtimeVersion 树的最新包时间：

```
1.0.3 → 2026-08-09 12:12:57   ← 最后一次，此后再没发过
1.0.4 → 08-10 01:45 / 08-15 23:46 / 08-16 14:48 / 08-16 15:23 / 08-16 19:26 / 08-17 15:58
```

`app.json` 的 runtimeVersion 在 `1fd027ff62`（**2026-08-09 11:46:41**）从 1.0.3 bump 到 1.0.4
—— 比 1.0.3 树最后一次发包早 26 分钟，时间线完全吻合。

⇒ **那台设备卡在 1.0.3，服务端往 1.0.4 发。杀 app 重开多少次都没用。**

### 还不能确定的一件事（如实说明）

**无法从服务器侧读出工人 APK 内嵌的 runtimeVersion** —— APK 走阿里云 OSS+CDN
（`dl.cretaceousfuture.com`），不在 47/139 上。bundle 下载日志同理，**结构性不可测**。

所以有两种可能，需要现场一句话就能分开：

| 可能 | 现场怎么验 | 如果是它 |
|---|---|---|
| (a) 工人手机是 1.0.4，`121.228.208.154` 是别人的旧开发机 | 工人手机「关于/版本」看 runtimeVersion = 1.0.4 | 那 OTA 应该能到，回到「杀 app 重开两次」 |
| (b) 工人手机就是 1.0.3 | 同上，读到 1.0.3 | **OTA 永远到不了，必须重装 APK** |

### 🔑 补测：不用去现场就把两种可能分开了大半

**读数一 —— 强制更新墙卡在 1.0.4：**

```
GET /api/mobile/version/check?currentVersion=1.0.3&platform=android
→ minimumVersion=1.0.4, updateRequired=true, updateAvailable=true
GET /api/mobile/version/check?currentVersion=1.0.4&platform=android
→ updateRequired=false
```

而 `MandatoryUpdateGate.tsx` 正是 `1fd027ff62`（1.0.3→1.0.4 那次 bump）**同一个 commit** 加的。
⇒ **跑 1.0.3 APK 的手机会被更新墙挡住，根本用不了报工。**

工人这几天一直在用（我们修的「点错工序」等缺陷就是从他们的生产数据里读出来的）
⇒ **工人手机的 APK 版本 ≥ 1.0.4**，
那台一直报 `rv=1.0.3` 的设备**多半是一台还装着旧包、已经被挡在更新墙外的机器**，
不是正在干活的那台。

⚠️ 这不是 100% 的证明 —— 我没能从服务器侧读出 APK 内嵌的 runtimeVersion（APK 在阿里云 OSS/CDN 上）。
但它把现场那一步从「猜」变成了「看一眼就知道」。

**读数二 —— 1.0.3 与 1.0.4 之间没有原生差异：**

从 `1fd027ff62` 到今天的 `origin/main`，`frontend/CretasFoodTrace/package.json`
**只多了两个 npm script，依赖一行没动**；`android/` 与 `app.json` 也没有再变。
⇒ 万一真有一批手机卡在 1.0.3，**往 1.0.3 树补发一个包在技术上是可行的**（JS 侧没有依赖差）。
⛔ 但我**没有这么做** —— 那是往生产设备推东西的风险决策，而且更新墙会先把它们挡下来，
先按下面那一步确认清楚再说。

**读数三 —— 一处死配置，顺手记下：**
`.env.prod` 里有 `APP_VERSION_MIN=1.0.0`，**它没有被接线**。
真正生效的是 `application.properties:149` 的
`cretas.app.min-version=${CRETAS_APP_MIN_VERSION:1.0.0}`，取的是 `CRETAS_APP_MIN_VERSION=1.0.4`。
两个名字相近的变量、值还相反，下次改最低版本别改错那一个。

### ⛔ 现场要做的事已经变了

**不要**上来就让人「杀掉 app 重开两次」然后看第②道 ——
那个动作在 (b) 情况下**必然失败，而且失败得像是我们没修好**。

**第 0 步（2 秒，看一眼就分得开）**：拿工人手机打开 App。

| 看到什么 | 说明 | 下一步 |
|---|---|---|
| **弹出「必须更新」的墙，进不去** | 这台是 1.0.3 的旧包 | 去 https://download.cretaceousfuture.com/ 装新 APK，装完再走下一步 |
| **正常进到首页/工序任务** | APK ≥ 1.0.4（强制更新墙会挡 1.0.3） | **杀掉 app 重开两次**（第一次后台拉、第二次生效），然后点第②道工序，确认打开的就是第②道 |

⇒ 按上面的补测，**大概率是第二行**。第一行那条路留着，是因为我没能 100% 证死
（APK 内嵌的 runtimeVersion 在阿里云那边，服务器侧读不到）。

顺带：如果方便，在「关于/设置」页把**版本号**也念一下，能把这件事彻底钉死。

### 一个只报不做的改动建议

139 上 `ota.cretaceousfuture.com.conf` 的 `log_format` 里加
`$http_expo_current_update_id` / `$http_expo_runtime_version` / `$http_expo_channel_name`，
以后一眼看出设备卡在哪个 id / 哪棵树，不用再靠「按字节数反推分支」。
⛔ 本轮**没有执行**（只取证，不改生产配置）。

---

## 七、需要你一句话的（业务口径，不是技术问题）

1. **报工领料到底该不该扣原料库存？**
   现在：报工只记「链接了哪些批次」，不扣 `material_batches.used_quantity`，
   也不写 `material_consumptions`。F006 一张领料单都没有 ⇒ **当前没有任何路径在扣**。
   如果口径是「仓管另外做领料出库单」，那是设计；如果不是，那仓里的原料数字会一直不动。

2. **产出 > 投入 该拦还是该放？**
   现在是「事后提示、系统不阻塞」，990% 出成率会被锁定并入库。
   我倾向**保持不阻塞但把提示前移到确认弹窗里**（工人现场确实会遇到合法的异常批次），
   但这是你的生意判断。

3. **仓储入库要不要一个「无预告直接建批次」的入口？**
   现在入库必须先有采购单或预告。F006 没有 ⇒ App 上做不了入库。
   （上一轮的 UIN-2026… 批次是 8-14 从别处建的。）

4. **驳回常用语**（上一轮遗留，仍缺车间自己的说法）。

5. **RN 离线草稿**（上一轮遗留，仍缺「车间手机连 wifi 还是 4G」这个读数）。

6. **仓库员该不该能打开发货单详情？**（本轮新查出）
   现在列表能看见、点进去 403。两条修法，选一条：
   (a) 给 `warehouse_worker` 加 `sales:read` —— 简单，但把销售数据的读权限放宽了；
   (b) 让 `GET /sales/deliveries/{id}` 也接受 `warehouse:read*` —— 只放宽这一个端点。
   我倾向 (b)，但这是权限模型决策，⛔ 没有替你定。

7. **成品那一步（质检 / 期间结算）现在谁来做？**
   `finished_goods_batches` 全库 0 行 ⇒ 发货永远没有输入。
   报工产出进的是半成品库；要变成成品，得走质检通过或期间结算。
   这一步在六膳门的实际作业里由谁在什么时候做，决定了要不要给它做入口。

---

## 八、本轮产出的 PR / 改动

| PR | 内容 | 状态 |
|---|---|---|
| **#2780** | 单位裸字符串比较棘轮闸（78 键/85 处冻结，含阳性/阴性/变异对照） | ✅ **已合** |
| **#2782** | `#2727/#2766/#2769` 收敛成一个 PR —— 解禁 integration/screens 4 个 suite | ✅ **已合**（126 suites / 1460 tests / 0 failed，tsc 0） |
| **#2785** | 仓储三处「点了没用」的死路（见第四节）+ 7 条断言 + 3 条变异对照 | ✅ **已合** |
| **#2786** | RN 损耗行跨单位假等式 + 领料批次选择器套错单位 | ✅ **已合** |

### 🚀 已发 OTA —— 今天这五个 PR 的 RN 改动已经在包里

发之前先用最新 main 重起 Expo Web 打 prod，**在真实界面上把每一处都看过一遍**才发的：

| 验的东西 | 前 | 后 |
|---|---|---|
| 入库列表的按钮 | 新建入库 / 扫码入库 / 待收货任务 / 餐饮待验收 | **新建入库已消失**，剩三个 |
| 扫码入库「手动输入」 | 点了毫无反应 | 弹窗打开；空值时确定 `aria-disabled=true`（**事先拦住**），填入后变可点（阳性对照） |
| 库存快捷操作 | 盘点 / **调拨** / 库位 / 过期 / 中转确认 / 库存预警 | **调拨已消失**，全页无「调拨」字样 |
| 领料批次单位 | 成品盒 `剩余 1000 kg`／封膜 `10 kg`／外箱 `100 kg` | **`1000 盒`／`10 卷`／`100 片`**；原料 A–D 仍是 kg（没被误改） |

⚠️ 损耗行那条**没有在真机上复验** —— 它要先提交一次投入才显形，而生产数据已经核回基线，
我不想为看一行字再写一遍再冲销一遍。它由 #2786 的渲染测试 + 变异对照覆盖（3 条断言，变异下 2 红 1 绿）。

**四步验收（全部带对照）**：

| 步 | 读数 |
|---|---|
| a 命令成功 | `PUSH_EXIT=0`，总耗时 98s |
| b manifest 发了新的 | id `17bdd709-e0ca-e533-b518-05cf16c68c05` → **`503200be-5c62-f4db-0bd4-253c5b2313ec`**；createdAt `2026-08-17T07:59:34Z` → **`2026-08-17T12:17:27Z`** |
| c app 会去拉 | 用 app 那套 header（`rv=1.0.4` / `channel=production` / `platform=android`）问，拿到的就是这份 |
| d 包里有改动 | 下载 6,977,029 B → gunzip 15,883,045 B |

第 d 步的逐条读数：

```
今天的标记(必须>0)    wh-scan-manual-dialog 1 | wh-scan-manual-confirm 1 | quantityUnit 1
昨天的标记(必须仍>0)  resolveInitialStepIndex 1 | resolveInputUnit 1 | resolveStepPhase 1
阳性对照(仪器活着)    YieldStepReportScreen 1 | AWAITING_INPUT 1 | yield-output-qty 1
阴性对照(必须=0)      MUT_NEW_INBOUND 0 | probeInputUnit 0
⇒ VERDICT = PASS
```

⚠️ **昨天那三个标记我特意也数了** —— 新包不许把昨天的修复弄丢。
⚠️ 两个坑照旧（本机没 `strings`、bundle 是 gzip），探针用 Python 直接扫字节 + 先 `gunzip`。
⚠️ 第三个坑（本轮新踩）：`dl.cretaceousfuture.com` 的 CDN **拒了 Python 的 urllib**
（`SSL: UNEXPECTED_EOF_WHILE_READING`），换 `curl` 落盘就好了。别把它读成「包不存在」。

回滚：`./scripts/ota/rollback.sh 1.0.4 production 1786953489878`（回到昨天那版）。

`#2727 / #2766 / #2769` 已 close，并在评论里注明收敛去向。
⛔ 不要再 merge 那三个 —— 同样的 commit 会被合第二遍（本仓 squash 合并会让旧分支不再是 main 的祖先）。

### #2782 那条链值得单记一笔

在 `origin/main` 上直接跑 `integration/screens`，jest 打的是
`No tests found, exiting with code 0` —— **`success=true`，`numTotalTestSuites=0`**。
⇒ **只看退出码完全看不出一个测试都没跑。** 成因是 `jest.config.js` 的
`testPathIgnorePatterns` 里那行（2026-05-09 留下的），不是「编译了没跑」。

⚠️ 顺带订正两条转述：`#2727` 落后 main 是 **61** 个 commit（不是 47）；
「基在 main 上断言只编译不运行」这个描述会把人引去找编译产物，而真相是 jest 连收集都没收集。

### #2771（web-admin）已在 prod 上行为验证 —— 两半都验了

上一轮只到「部署了」，这一轮真跑了那条路径：

| 半 | 读数 |
|---|---|
| 越权角标不发请求 | 以 `f006_workshop`（无 sales 权限）打开报工审批页，`/sales/orders` **0 次** |
| 阳性对照 | 同一次加载里 `unorderedInbound` 角标的 `/operations/customer-material-arrivals` **发出去了** ⇒ 角标 store 确实跑了，不是整个没执行 |
| 后台轮询会停 | 站内切到别的页面后等 ~50 秒（远超 30s 间隔），`pending-approval` **仍只有 1 次** |
| 阳性对照 | 同期静态请求从 61 涨到 64 ⇒ 网络记录仍在工作，「没有新请求」不是仪器停了 |

---

## 九、⚠️ 被推翻的结论（只写正确答案会让下一个人重犯）

| 曾经的说法 | 真相 | 怎么发现的 |
|---|---|---|
| 「OTA 服务端不记 manifest 请求日志，所以设备侧不可测」 | **错**。nginx 在 139 上完整记录，未轮转，6/4 至今 | 去 139 上 `grep -ril cretaceousfuture /www/server/panel/vhost/nginx/` |
| 「剩下唯一未知是设备侧有没有人去拉」 | **不准确**。设备**一直在拉**（8/17 共 64 条），但它问的是 1.0.3 树 | 按字节数分类 + 两个方向的真实请求复现 |
| 「#2727 落后 main 47 个 commit」 | **错，是 61** | 当场 `git rev-list --count` 重算 |
| 「基在 main 上断言只编译不运行」 | **结论对，描述错**。实测 `numTotalTestSuites=0` —— jest **连收集都没收集**（`testPathIgnorePatterns` 踢掉了路径），不是「编译了没跑」 | 读 jest `--json` 报告 |
| （我自己差点写错）「F006 有 13 条活报工，和上一轮的 0 对不上」 | **是我的口径少了 `deleted_at IS NULL`**。13 条**全是软删的**，活的确实是 0 —— 上一轮是对的 | 加过滤重数一遍 |
| （我自己差点写错）「第②道把 99 kg 当成 99 盒」 | **过头了**。选中半成品后字段正确变成「半成品领用量 99 kg」；「99 盒」只出现在**未选半成品前**的默认态 | 选中后再读一次界面 |

| （我自己）「F006 上游没单据 ⇒ 入库/出库没法验」 | **过头了**。那是**数据缺口不是不能验** —— 造一张采购单、造一张发货单，两条链都走得通。入库端到端跑通了 | 自己造上游数据再走一遍 |
| （我自己两次）「这个按钮点了没反应」 | **错**。`Alert.alert` 在 **Expo Web 上是 no-op**，Android 上会真弹。两次都是**事先拦截**被我读成死按钮 | 读源码找到 `return` 的位置 |

**这一批的共同形状**：读数本身没错，错在**口径没写清**或**只读了一个时刻的状态就下结论**。

### ⚠️ 一条给下一个在 Expo Web 上做 E2E 的人

**`Alert.alert` / `Alert.prompt` 在 react-native-web 上是 no-op。**
今天被它骗了两次：
- 点「手动输入」毫无反应 —— 真相是 `Alert.prompt` 只有 iOS 有，else 分支弹的 `Alert.alert` 在 web 上也不显示；
- 点「提交入库」毫无反应 —— 真相是 `photos.length === 0` **事先拦住了**，提示同样没显示。

⇒ **在 Expo Web 上，一个用 `Alert` 说话的事先拦截，长得和死按钮一模一样。**
判据：点了没反应时，先 `browser_network_requests` 看有没有发请求；
没发就去源码里找那个 `return`，⛔ 不要下「按钮坏了」的结论。

---

## 十、下一轮从哪开始

1. **现场那一步（唯一还卡着的）**：拿工人手机打开 App，按第六节那张二选一表走。
   ⚠️ 现在包里装的**不只是昨天那三个修复**，还有今天这五个 PR
   （点错工序 / 单位显示 / 仓储三处死路 / 未设计划量不再编成 0）。
   所以那一次「杀 app 重开两次」拿到的东西比昨天多，值得做。
2. **判据 4 三条成因**：先修「新建入库」的过期标签和 Android 手动输入（都很小），
   再决定要不要给无预告入库开入口（等你第七节第 3 问的答复）。
3. **半成品 IN 流水**（第三节①）—— 这是账本缺一半，越早补越便宜。
4. 合 #2780 与两个在飞 PR。
5. legacy 退役第 3 步起：`docs/decisions/2026-08-17-legacy报工栈退役.md` 第六节。

---

## 十一、这一轮最该记住的

上一轮写的是「**对抗性核对的收益，高于多写一个修复**」。这一轮的版本更具体：

**三次差点写出反向结论，救回来的都是「同一类里必然存在」的那条对照。**

- 「F006 有 13 条活报工」→ 加上 `deleted_at IS NULL` 才发现全是软删的
- 「半成品流水表是空的 ⇒ 流水没写」→ 同一轮里 OUT 流水**写出来了**，才敢说是 IN 缺失
- 「待收货任务是空的 ⇒ 接口坏了」→ F002 有 1 张 `APPROVED`，才敢说是 F006 真的没有

⇒ **任何会读出「没有 / 全 0」的读数，配一条同类里必然为「有」的对照，再下结论。**

---

# 十二、深夜追加（22:00–23:40）—— 两件「所有判据全绿而东西根本不可用」

这一节是**当天最贵的两个发现**，都不在原七条判据的计划里，是做判据 1 的路上撞出来的。
两件事形状完全相同：**每一道既有判据都绿，而产物对用户完全不可用。**

## 12.1 分发中的 v1.0.4 APK 一启动就崩（任何设备）—— ✅ 已换包

```
java.lang.NoSuchMethodError: No static method getDirectConverter(Ljava/lang/Class;)
  Lexpo/modules/kotlin/types/JSTypeConverter; in class Lexpo/modules/kotlin/types/ReturnTypeKt;
  at expo.modules.font.FontLoaderModule.definition(FontLoaderModule.kt:98)
```

**不需要设备就能证死**（`classes*.dex` 全 ABI 共用）：调用方在（1 处 `invoke-static`），
被调方不在（`ReturnTypeKt` 只声明 `toReturnType`），阳性对照 `toReturnType` 2 处 /
`FontLoaderModule` 92 处 ⇒ 尺子活着。

**成因**：两个打包脚本都是「`node_modules` 存在就整个跳过安装」+ `npm install`，
且**没有任何 CI workflow 打 APK**（本地人工打）。⚠️ **lockfile 一直是好的** ——
问题不是没锁，是**没按锁装**。

**已修 + 已验证**：
- `npm ci --legacy-peer-deps`（PR #2790）
- 新增 `scripts/smoke-android-apk.sh`：装到设备 → 启动 → 断言 ①无 FATAL/NoSuchMethodError
  ②进程还在 ③`topResumedActivity` 是自己。**三态退出码**，rc=2「这次没量到」单独一态
- 接进**三个**调用点，最重要的是 `deploy-apk.sh`（分发入口）；rc=1/rc=2 都拒绝发布
- 线上包已替换，并从 **CDN 下载那个文件本身**再冒烟一次：`SMOKE=PASS`

| | 旧（坏） | 新（已上线） |
|---|---|---|
| 字节数 | 122,521,709 | 122,303,422 |
| 启动 | `SMOKE=FAIL` rc=1，掉回桌面 | `SMOKE=PASS`，前台是 MainActivity |
| 签名 SHA-256 | `2ee293fd…9652da` | **同一把 release key**（老用户可覆盖装） |

## 12.2 OTA 自 8-09 起**一次都没送达过任何设备**，而每次推送都报成功 —— ✅ 已修

判据 1 原本卡在「等现场」。埋点上线后十分钟，现场读数直接把它解开了：

```
10 分钟内 5 次真实拉取 / 3 个公网 IP，全部 runtime=1.0.3
runtime 1.0.3 树  最新 bundle = 2026-08-09 12:14   ← 设备只够得到这个
runtime 1.0.4 树  最新 bundle = 2026-08-17 22:44   ← 我们一直在推这里
```

**根因**：一个 App 的版本号在**四个**地方，8-09 的 bump（`1fd027ff62`）改了三处漏了一处：

| 位置 | 值 | 用途 |
|---|---|---|
| `app.json` version / runtimeVersion | 1.0.4 ✓ | 推送脚本读它决定推哪棵树 |
| `android/app/build.gradle` versionName | 1.0.4 ✓ | 设置里显示的版本 / **强制更新判断** |
| `android/.../res/values/strings.xml` `expo_runtime_version` | **1.0.3** ✗ | **只**用来问 OTA 要**哪棵 bundle 树** |

`android/` 是**提交在 git 里**的，`expo prebuild` 看到目录已存在就不覆盖 ⇒ 升版本时它不会跟着改。

⇒ 设备问 1.0.3 树、推送进 1.0.4 树，**零交集**。而且症状是
「版本显示正常、强更正常、只有 OTA 悄悄失效」。

**已修 + 已在真机上端到端验证**（PR #2795）：
1. `strings.xml` → 1.0.4，并在文件里写明它为什么不会自动更新
2. 闸 `nativeVersionConsistencyContract`（4 条）钉住四个来源两两一致
3. `push-bundle.sh` 加 `OTA_RUNTIME_VERSION_OVERRIDE`，迁移期能推到「设备实际在要的那棵树」
4. 用它把今晚的 bundle 推了一份到 1.0.3 树 —— **现场设备不用重装就能拿到今晚的改动**

真机读数（同一台 Mi 11 Pro，服务器侧逐条对上）：

```
23:07:08  current=e9631105  outcome=update      ← 拉到更新
23:07:41  current=e12dd330  outcome=no-update   ← current 变了, 新 bundle 已装上
23:08:21  current=e12dd330  outcome=no-update   ← 再杀再开仍是新的 ⇒ 持久化
手机 logcat 对应: restartCount 0→1 + "Updates state change: reset"
```

**⇒ 判据 1 达成。** 不是「等现场」，是当场在真机上跑通了。

### ⚠️ 迁移期（现在起到现场全部换包为止）

现场设备在 **1.0.3 树**，新装的走 **1.0.4 树** —— **两棵树都要推**：

```bash
./scripts/ota/push-bundle.sh production android                              # 1.0.4 树
OTA_RUNTIME_VERSION_OVERRIDE=1.0.3 ./scripts/ota/push-bundle.sh production android   # 1.0.3 树
```

什么时候可以停推 1.0.3 树（判据，不靠猜）：

```bash
ssh root@47.100.235.168 "grep OTA_PULL /www/wwwroot/cretas/python-prod.log \
  | grep -o 'runtime=[0-9.]*' | sort | uniq -c"
```

⚠️ 往旧树推之前必须确认 `package.json` 的 `dependencies` 没漂
（实测：1.0.3 时代到今天**一个字没动**，所以今晚的 bundle 对旧二进制原生兼容）。
否则就是把不兼容的 JS bundle 推给旧二进制 —— 与 12.1 是同一种病。

## 12.3 CDN 同名覆盖不刷缓存，重发的包**发不出去**

重发 v1.0.4（同文件名）时，源站已是新包而 CDN 边缘仍返回旧包：

```
OSS  Content-Length 122302626  Last-Modified 22:36:30   ← 新
CDN  Content-Length 122521709                           ← 旧(一启动就崩的那个)
```

原脚本只刷 latest 别名（注释写「版本化文件名是新对象，无需刷新」—— 同名覆盖时不成立），
且只在有 `aliyun` CLI 时刷，而发布机没装 ⇒ **这一步从来没跑过**。
救场的是紧随其后的 Content-Length 比对闸，它两次都正确报红。

已修（PR：`codex/claude-apk-cdn-refresh`）：两个 URL 都刷 + 无 CLI 时走
`scripts/deploy/cdn-refresh.py`（OpenAPI 直发，三态退出码）+ 验证步骤加重试（不放宽判据）。

## 12.4 本轮新增的闸与埋点

| 名字 | 守什么 | 变异对照 |
|---|---|---|
| `scripts/smoke-android-apk.sh` | 包装上去打不打得开（三态） | 拿**真实坏包** v1.0.4 跑：`SMOKE=FAIL` rc=1 |
| `rnDependencyLockContract`（5 条） | 打包按 lockfile 装 + 冒烟闸接线 | 退回接线前：引用 5→0，恰好 1 条红 |
| `nativeVersionConsistencyContract`（4 条） | 四个版本号两两一致 | `strings.xml` 退回 1.0.3：恰好 1 条红 |
| `apkPublishCdnRefreshContract`（4 条） | 刷版本化 URL + 有无 CLI 的退路 + 三态 | 退回只刷 latest：恰好 1 条红（**第一版恒真，被变异抓出来**） |
| `OTA_PULL` 埋点（Python，已上线 prod） | 每次拉取的 runtime/channel/current/ip | 拿掉「已是最新」那条：`_audit(` 5→4，恰好 1 条红 |

## 12.5 ⚠️ 被推翻的结论（补到第九节那张表）

| 曾经的说法 | 真相 | 怎么发现的 |
|---|---|---|
| 「设备和包不在同一棵树上，**因为设备装着 1.0.3 的旧包**」 | **成因错了**。设备装的就是 **1.0.4**，是它**自己报 1.0.3**（`strings.xml` 漂了）。按旧结论推出的修法（让大家装新包）**不管用** —— 新包也报 1.0.3 | 真机 `dumpsys package` 读到 `versionName=1.0.4`，而同一秒服务器记到 `runtime=1.0.3` |
| （我自己）「JS lockfile 从来没进过 git」 | **错，一直在版本控制里**。是我用 `git ls-files \| grep lock \| head -3` **被截断**了 | `package-lock.json` 既不在 `git status` 也没被 `check-ignore` 命中，这个矛盾读数拽回来的 |
| 「`aliyun` CLI 没装 ⇒ 跳过 CDN 刷新，版本化文件名无需刷新」（脚本注释） | **同名重发时不成立**，CDN 一直发旧包 | Content-Length 比对闸报红 |

**这一批的共同形状**：判据全绿 ≠ 东西可用。
三件事里有两件的判据问的都是「**这个文件对不对**」，没有一条问「**它对用户能不能用**」。

## 12.6 下一步

1. `codex/claude-apk-cdn-refresh` 的 PR 合并（GitHub 当时 503，push 已完成）
2. 现场换包：让工人从 `download.cretaceousfuture.com` 重新下载安装
   —— 换完之后他们进 1.0.4 树，`OTA_PULL` 里 `runtime=1.0.3` 归零就可以停推旧树
3. ⚠️ MIUI/HyperOS 真机 `adb install` 被 `INSTALL_FAILED_USER_RESTRICTED` 拒
   （需设备端开发者选项里的「USB 安装」，且要求登录小米账号 + 联网）。
   **做 APK 冒烟一律用模拟器**，别指望真机

## 12.7 🔴 更正：12.2 里「5 次真实拉取 / 3 个公网 IP」是错的

发布后复核 `OTA_PULL` 逐条归属时发现：`57.183.75.185` **是我这台开发机的出口 IP**
（实测 `curl https://api.ipify.org` = `57.183.75.185`），那些拉取来自**我自己的模拟器**
（做 APK 冒烟时每次启动都会拉一次）。我把自己的流量算进了「现场设备」。

正确归属：

| IP | 是谁 | runtime | 说明 |
|---|---|---|---|
| `198.51.100.42` | 我伪造 X-Forwarded-For 的自测 | 1.0.4 | 埋点上线时的验证 |
| `57.183.75.185` | **本机模拟器** | 先 1.0.3、后 1.0.4 | 冒烟时启动 App 触发 |
| `117.143.178.153` | **owner 的 Mi 11 Pro** | 1.0.3 → 拉到新 bundle | 12.2 那段端到端就是它 |
| `112.0.153.54` | **唯一一台真正的现场设备** | 1.0.3，`no-update` | 22:39 时它已在 1.0.3 树最新（8-09 那个） |

⇒ **现场设备实测只观测到 1 台**，不是 3 台。

⚠️ 但根因结论**不受影响** —— 它不是从这个计数推出来的，是从
「同一台机器 `dumpsys` 读到 `versionName=1.0.4`，而同一秒服务器记到 `runtime=1.0.3`」
对出来的。而且本机模拟器给了一组更干净的**同机前后对照**：

```
装我今晚打的第一版 APK(strings.xml 未修) → 报 runtime=1.0.3
装修好之后重打的 APK                     → 报 runtime=1.0.4
```

**这条更正本身的教训**：`OTA_PULL` 里的 `ip` 是**我加的字段**，
而我读它的时候没有先问「哪些是我自己」。
⇒ **任何按来源分组的读数，先把「我自己」这一类剔掉再下结论** ——
否则做验证的动作本身会污染被验证的量。
（形态 A 的又一个变体：我想知道的 X 是「现场有几台」，实际量的 Y 是
「有几个 IP 拉过」，而我自己也在里面。）

📌 `112.0.153.54` 那台在我推 1.0.3 树（23:04）之前读的是 `no-update`，
所以它下次开 App 会拿到今晚的 bundle —— 明天可以用它验证「现场真的收到了」。

---

# 十三、真机端到端走查（2026-08-18 00:20–00:40，Mi 11 Pro 实机）

用 `f006_worker1` 在**真手机**上把报工从头走到尾。这一节的每条读数都来自实机 + 生产库，
不是 Expo Web、不是单测。**写入已全部冲销，基线核对见 13.5。**

## 13.1 ✅ 判据 1 后半句达成 —— 点第②道，打开的就是第②道

工序列表：「你有 2 道可以报工」，第二张卡写着「现在可报：**第 2 道 · 卤制**」。
点它的「可报工」→ 打开的页面：

```
当前分配工序 · ① 投入阶段
批次 PB-PLAN-1786724332978-6013FEBE-75827 · 卤制     ← 第②道
步骤点 ● ●(橙) ●                                     ← 第二个高亮
```

**是第②道，不是第①道。** 那个「点②开①」的缺陷在真机真入口上确认修好。

## 13.2 ✅ 判据 2 全链路 —— 报工落到点的那道 / 产出进半成品库 / 下一道领得到

走完三段（投入 2kg → 工时段 07:00~15:00 1人 → 完工产出 1kg）后，库里逐条对上：

```
production_reports(活)   0 → 4
work_process_tasks 1786  0.0000/PENDING → 1.0000/COMPLETED    ← 落到我点的那一道
semi_finished_inventory  1 → 2
新增 sfi 行  PT_F006_LSM_d25d73da30f338d4-B10759-S2-1786 = 1.00
             (批次10759 / 第2道 / 任务1786 —— 编号本身就带着来源)
```

回到列表，同一张卡自动变成「现在可报：**第 3 道 · 拼装分装**」。进去看到：

```
上道半成品有 1 笔, 请选择本道领用哪一笔
  PT_F006_LSM_d25d73da30f338d4-B10759-S2-1786
  卤制        可领 1 kg
← 上道产出 1 kg, 请确认实际投了多少
投入量  1 [盒]                                  ← 本道单位是盒, 没和上道的 kg 糊成一个
```

对比第②道当时显示的是「上道产出量暂时取不到，请按本道实际投入量填写」——
**有上道数据时给数据，没有时说实话**，两种都不编。

## 13.3 ✅ #2789 在真机上第一次真实触发（前置不平衡确认）

点「标记完工（锁定出成率）」时弹出：

```
⚠️ 投入产出不平衡 — 完工出成确认
投入 2kg, 产出 1kg, 偏差约 50%, 请核对数量是否填错。
完工后本道出成率锁定, 确认要完工出成吗?
(若还要继续产出, 请改用"保存, 稍后继续")
```

**事先拦住 + 说清偏差 + 给出正确的替代动作**，不是提交后报错。数字也对。

## 13.4 🔴 走查中发现的三个新问题

### (a) 同一工时段可以重复提交，无去重无提示 —— 工时会被算两倍

冲销时逐条列出这次写的 4 条记录：

```
23823 INPUT   2.00 kg    00:30:08
23824 SEGMENT            00:31:14
23825 SEGMENT            00:31:43   ← 同一时段(07:00~15:00, 1人)第二次提交, 照单全收
23826 OUTPUT  1.00 kg    00:34:41
```

成因是**第一次提交没有可见反馈**（没有 toast、没有弹窗，只在页面顶部多出一行小字
`· 07:00~15:00 1人`，而那行当时在屏幕外）。我以为没成功就又点了一次。
**工人手一抖，这一段的工时和人工成本就翻倍。**

⇒ 建议：提交本段后给明确反馈；后端对 (task, 起止时间, 人数) 相同的段做幂等或提示。

### (b) 界面说的「损耗」和后端量的「损耗」不是同一件事

界面（完工出成页）：

```
损耗 (自动) = 投入 2kg − 产出 1kg = 1kg
```

后端提交后返回：

```
物料平衡偏差 50% (投入 2, 产出 1, 副产物 0, 损耗 0, 留样 0) — 请核对, 系统不阻塞
```

界面把「投入−产出」显示成**损耗**，后端把它算成**未解释的偏差**（损耗字段仍是 0，
因为那是另一个要单独填的输入项）。工人刚被界面告知「损耗自动算出来是 1kg」，
提交后立刻被告知「偏差 50%，请核对」——**同一个 1kg，两套说法**。

⇒ 这正是本仓规则里「写用户可见的判断句时，把『我在量什么／我说了什么』并排读一遍」
   那一条。要么界面别把差额叫「损耗」（叫「未记录差额」），要么提交时把它带进损耗字段。

### (c) 半成品库存行写了，入库流水没写

```
semi_finished_inventory              1 → 2      ✅ 库存行有了
semi_finished_inventory_transactions 0 → 0      ❌ 流水一条没有
```

上一轮记的「IN 流水缺失」在这里拿到确证 —— 这次是**同一笔操作**的前后对照，
不是跨时间推断。追溯链上少了这一环。

### (d) 🟡 登录页显示的版本号是 `v1.0.0.1`，而实际是 1.0.4

小事，但它是**第五个**版本号来源（前四个见第十二节）。既然刚上了
`nativeVersionConsistencyContract` 闸，这一处也该纳进去。

## 13.5 ✅ 数据已冲销回基线

单事务 + 硬断言（活报工归零 / 半成品库存行回到 1 / task 1786 回 PENDING+0.0000 /
跨租户原料批次仍 24 / 全库成品批次仍 0），任一不符即整体回滚。

冲销后与走查前快照 diff：

```
唯一差异: production_reports(全) 13 → 17
```

即那 4 条**软删**记录（与既有 13 条同口径）。**所有活数据读数逐条相同。**

## 13.6 关于「小结 vs 结单」的订正（我上一轮说错了）

上一轮我说「成品批次全库 0 行，是因为 interim-settle（小结）这一步没人做」。**成因说错了。**

`InterimSettleServiceImpl` 第一道守卫：

```java
if (plan.getSourceType() != PlanSourceType.SAFETY_STOCK)
    throw new BusinessException(400, "仅存货生产计划可小结, 当前来源类型: " + ...)
        .withHint("非存货生产计划请走「结单」")
```

而 F006 在用的那条计划是 **`CUSTOMER_ORDER`** ⇒ **它根本不能小结**，点了会 400。

成品批次真正的写入点（grep 了所有 `finishedGoodsBatchRepository.save`）：

```java
createFinishedGoodsFromReceipt(...)   // remark: "生产结单仓库确认入库: {planNumber}"
    batch.setStorageLocation("仓库确认入库");
    batch.setWarehouseId(warehouseResolver.resolveFinishedGoodsId(factoryId));   // 直接落成品库
```

⚠️ 并且与「先入生产库、再调拨到成品库」的心智模型**不一样**：代码在
「结单 → 仓库确认实收」那一刻**直接落成品库**。半成品那条才走 SFI。
（小结那条路支持「同次既出半成品又出成品」，但只对备货计划开放。）

⇒ 出库那条链缺的是**「结单 + 仓库确认实收」**，不是小结。
