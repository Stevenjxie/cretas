# 交接 · 2026-08-16 · 餐饮澄清按钮 + 时间词语料

**写给下一个 chat。** 你接手时的状态：**计划 1 已上线并在生产上验证通过；计划 2 只有设计卡和实现计划，一行代码没写。**

---

## 一 · 你的角色与判据（owner 原话转录，⛔ 非逐字稿）

你是餐饮 AI 读路径的**唯一 chat，全权，没有红线**。

▎**让老板打开之后，能自己做出一个决定。**
▎取舍排序：**能不能决定 > 数字准不准 > 覆盖得全不全**

**五条自查动作是唯一的闸**（每条改动都要过）：
1. 变异对照 —— 破坏被守的行为，断言必须红
2. 阳性对照 —— 证明仪器活着（读出「0」之前先证明它能读出「非 0」）
3. 全量对**当场重跑**的 baseline —— ⛔ 不引用记忆里的数
4. **跑一次**，⛔ 不从代码推
5. 每条改动能回答「他哪句话要求了这个」

**唯一的提醒**（⛔ 不是禁令，自己判）：`MOCK_REST` 那张 ¥81.00 米饭卡、青花椒那张 ¥167.20 卡，是目前唯二能验「产品自己指着坏数据说话」这条能力的样本 —— 动它们之前先确认还有别的样本。

**台账** `docs/dispatch/ACTIVE.md` 归你，写给明天的自己。
**方法学**读 **origin/main 那份** `.claude/rules/measurement-and-wiring.md`，⛔ 不凭记忆重建。

---

## 二 · 现在的状态（全部实测，⛔ 无推断）

```
分支     codex/claude-time-corpus-2 @ aab1eb250e   (已推 origin, 落后 origin/main 0)
worktree C:\Users\Steve\cretas-tpc-2                (off origin/main, 物理隔离)
计划1    PR #2723 已 squash 合并 = origin/main f52ee5c30d, 已部署 prod 并验收
计划2    只有设计卡 + 实现计划(aab1eb250e), **零代码**
生产     Python 8083  PID 2690874  启动 Sun Aug 16 22:15:01 2026
全量基准 8 failed / 6007 passed  —— 那 8 条在 origin/main 上就有
         (chart API / agent_runtime / conv_memory), ⛔ 不是你造成的
```

**全量命令**（照抄 CI 的，⛔ 不自拟目录）：

```bash
cd backend/python
python -m pytest tests/ smartbi/api/tests/ -q --continue-on-collection-errors \
  > /tmp/after.log 2>&1; echo "EXIT=$?"
grep -cE "^FAILED |^ERROR [a-z]" /tmp/after.log
```

baseline 要**当场重跑**：`git worktree add -q --detach ../cretas-base origin/main` → 同一条命令 → `comm -13` 逐条比名字。⚠️ 两个全量**串行跑**，⛔ 不并发（互抢 CPU 会让 baseline 撞超时，读数作废）。

---

## 三 · 计划 1 做了什么（已上线）

### 缺陷

生产日志实测，一条对话的计划被另一条对话命中：

```
17:13:10  zero-token plan-cache hit: authority=validated_plan_cache
          intent=RESTAURANT_OPS_WASTAGE_TOP  clarification=False  query=本月
```

计划缓存键 `(factory_id, 归一化问句, 版本)` **不含会话上下文**，而「本月」是产品自己发的澄清按钮 —— `question` 就是那两个字。

▎**最容易被缓存的那批串，恰好是最不该被缓存的那批。**

### 改法

澄清按钮的 `question` 改成**合成的完整问句**，`label` 仍是短词：

```
{"label": "本月", "question": "本月哪个菜卖得好"}
```

准入两条（任一不过就退回原样）：① 种子非空 ② 合成句自足 —— **按按钮种类分**：时间查「解出的窗口 **==** 用户点的那个」（⛔ 不是 `!= "全部历史"`），门店查「前缀能被 `_split_store_scope` 原样切回来」。

接收侧加了共用的 `_button_answer_fragment(original_query, answer)`，在判之前把合成句还原成片段，**四个消费点共用一份**。合成种子用 `spec.resolver_query_seed or query`（多轮时与 pending 存的那个串由构造一致）。

前端 `mobile-rest-ai`：`normalizeFollowups` 返回 `{label, question}`，chip 显示 `label`、点击发 `question`、`max-width: 220px`。

### 生产验收读数（⛔ `DEPLOY_EXIT=0` 不是判据）

```
① md5 逐条一致   b15fca63… / 0a3bf747…
② 进程重启      2603504 → 2690874, 启动 22:15:01 晚于制品 22:13:26
③ 真跑那条路径(在生产 venv 上):
     tiered_answer 返回 4 个按钮, label='本月' question='本月哪个菜卖得好'
     仍在发光秃秃的词: 0 个
     消费侧认得: 4/4          ← 零 LLM 延续活着
     阳性对照·乱输入被拒: True  ← 没有放宽授权
   门店那条: 阳性对照 9/9 · 修后 9/9 · 修前 0/9
```

探针在 `D:\Temp\claude\...\scratchpad\`（`verify_c1.py` / `verify_store.py` / `verify_prod_e2e.py`），已 scp 到服务器 `/tmp/`。⚠️ **`verify_c1.py` 最后一行的判据是按「坏状态」写的**，现在打印「与 review 的读数不一致」是**正常**的。

---

## 四 · 🔴 这一轮最贵的四条教训（⛔ 别再犯）

### 1. 我改了产出侧「发什么」，没查谁按精确相等消费它

设计卡和计划里 grep「continuation / 延续 / `APPROVED_TIME_ANSWERS`」= **0 hits**。而接收侧是 `answer_normalized in {…}` 精确相等。实测 **4/4 HIT → 0/4**：零 LLM 延续整条失效，每次点按钮退回 T3 编译（~2.1s）。

▎**为省 60 毫秒的缓存命中，换来每次点击 2.1 秒 —— 净效果是负的**，而 221 条测试全绿。

判据：**grep 那个值的字面量，⛔ 不是 grep 函数名**（消费方永远不会提到产出方的名字）。三类消费都要找：**精确相等 / 包含 / 长度阈值**。

### 2. 我为这件事建的闸，在半吊子上是绿的

修的时候只换了闸的**实参**，漏了两行之下的**载荷**。门店按钮仍是 0/9，而 AST 闸只钉实参 ⇒ 它绿着，还声称已修。

⇒ 硬约束 8 的原形：**我数的是「那个名字出现几次」，不是「这个结构涉及的所有地方」。**

### 3. 「测了 helper 不是接线」，一个分支上犯两次

两次都是「自己算出入参再调函数」，绕过调用点，变异下全绿。改成走 `tiered_answer` 且**故意让两个入参不同**（相同时断言分不出用的是哪一个）之后才红。

### 4. `git checkout --` 抹掉未提交的改动 —— 一晚三次

两个 subagent 各一次、我自己一次（变异后还原，把没提交的编辑一起没了）。
⇒ **先提交再变异。** 后来照做就没再发生。

### 附：`vite preview` 的 SPA fallback 会给 HTTP 200

`followup-preview.html` 在项目根、**不在 `dist`** ⇒ `vite preview` 返回 index.html，**200 但没有 `window.__probe`**。起服务必须用 dev server：

```bash
cd mobile-rest-ai && npx vite --port 5411 --strictPort
FOLLOWUP_GATE_PORT=5411 node followup-gate.mjs        # rc=0/1/2 三态实测可达
```

---

## 五 · 计划 2：要做什么（零代码，计划已写好）

**文件**：`docs/superpowers/plans/2026-08-16-时间词语料与晋升.md`（4 个 task，含逐条测试代码）
**设计卡**：`docs/superpowers/specs/2026-08-16-时间词语料与澄清按钮-design.md`

### owner 裁定的三段式

```
① 确定性规则识别时间词  ── 识别到 ──► 直接进后续 LLM 完整句子理解
                        └ 没识别到
                             ② 交 LLM double check
                                ├ 认出来 → 规范化 → **写入时间词语料库** ──► 进后续理解
                                │                        └(人工晋升)──► 确定性规则
                                └ 也没认出 ⇒ 确实没有时间词
                                     ③ 给按钮（计划 1 已做完）
```

①②③ 的判别今天**都已经在跑**，缺的只有 ②的后半（语料库 + 晋升）。

### owner 已拍板的三件

- **晋升是人工**（⛔ 不自动）
- **晋升产出代码补丁**（选项 B），⛔ 不做运行时查库 —— 那会让 `_resolve_sales_date_range` 从纯函数变成「取决于哪台机器」，刚建的日历半年 / 今天到现在那几条闸当场失效
- **积压阈值 20 条**

选 B 的实测支撑：404 条真实问句里规则解得出时间 **75%**，解不出的 103 条**逐条读绝大多数根本没有时间词**（「哪个菜卖得好」「加权毛利率是多少」「员工排班情况」）⇒ 时间词是**封闭语法 + 少量格式变体，会饱和**，发版成本一次性。

### 🔴 写计划时实测到设计卡**四处错**（已回改，但你要知道它们错过）

1. **写入点 `:2261` 写不了库** —— `_build_spec` 是普通 `def`，**没有 pool**。
   真正的写入点是 **`parse_restaurant_query`（`:6354`，async，形参有 `pool` 和 `factory_id`）**，条件用产品**已经算好**的 `spec.window_from_llm_phrase`（字段在 `:379`，默认 `False`，只在 `:2266`「规则解不出 + LLM 给了短语 + 不是代码默认」时置 `True`）。
2. 设计卡说的 `scripts/restaurant-intent-promote.py` **不存在**（只有三个 `smartbi/data/promoted_*.json`）。
3. **晋升的目的地不是一张词表** —— `_resolve_sales_date_range` 是 **237 行 / 38 个 `if`** 的手写解析器，模块里没有可追加的时间词常量。
   ⇒ 「晋升」= 人手写一个分支 + 补测试 + 发版。CLI **只能**列出该处理哪些词、记下已处理。
   ⚠️ 这句话**必须写进 CLI 的 `--help`** —— 一个让人误以为跑一下就生效的工具，比没有工具更糟。
4. 跑批要把**「语料至今 0 条」与「无积压」分开说**。写入点附近**没有任何日志**（实测）⇒ 今天数不出这条路一天触发几次，**语料表自己就是那个仪器**，所以它的「0」必须能被读懂。

### 上线验收（阳性对照，⛔ 不是「部署成功」）

拿一句**确定性层解不出、而 LLM 认得出**的问句真跑一次，看语料**多一行**。
现成的：**`最近损耗怎么样`** —— 实测确定性层返回「全部历史」。
⚠️ 若没多一行，**先查 LLM 那一跳是不是根本没被调用**（本仓栽过：把降级正则的行为归因成「LLM 抽错了」，而 LLM 每次都 500）。

---

## 六 · 登记为「不做」的六条（都在设计卡里，⛔ 别当成漏了）

| # | 是什么 | 为什么现在不做 |
|---|---|---|
| 1 | `has_followup_signal` 的 **32 字**长度闸（`restaurant_intent.py:1751`） | 比已登记的 web-admin 48 字那个更紧；同一族 |
| 2 | **五处**产出裸 `clarification_options`（`:4987` `:5005` `:5018` `:6553` `:5724`） | 设计卡原本只登记了一处 |
| 3 | COMBINED 澄清仍发裸串（`全部门店 本月`） | 那一支不改的理由（「自撰选项是任意短语」）对**产品自生成**的这批**不成立**，代码注释在这批上是错的 |
| 4 | 窗口清单全仓还有 **6 份副本**（3 Python + 3 Java） | 新建的 AST 闸只扫得到 1 份；抽成一份要跨语言，需单独立项 |
| 5 | web-admin `AIQuery.vue:1249` 的 48 字闸会静默翻假 | web-admin 与 mobile-rest-ai 是两个载体，本轮只修后者 |
| 6 | NOTIF / 菜品级归因 / 套餐 | 更早就登记过 |

---

## 七 · 环境速查

```
生产 Python  47.100.235.168:8083   代码 /www/wwwroot/cretas/code/backend/python
             venv: source venv-current/bin/activate
             ⚠️ 查 PID 用端口反查, ⛔ 不用 pgrep -f(会抓到你自己的探针):
             PID=$(ss -lntp | grep :8083 | grep -oP 'pid=\K[0-9]+' | head -1)
部署         ./scripts/deploy/deploy-smartbi-python.sh --env prod
             ⛔ 纯 Python 改动不要用 release-cretas.sh —— 它只发 Java + web-admin,
                会给你 DEPLOY_EXIT=0 而东西根本没上线
提交         ./scripts/safe-commit.sh "<msg>" <file>...   ⛔ 不用裸 git commit
前端         mobile-rest-ai/  (真实载体, ⛔ 不是 frontend/CretasFoodTrace)
             npm run build = vue-tsc --noEmit && vite build
```

⚠️ **subagent ⛔ 不要让它跑全量**（3 分钟）—— 实测两次它都把命令转后台然后**丢掉整轮**。全量由控制端跑，subagent 只跑 focused。

---

## 八 · 下一步

`docs/superpowers/plans/2026-08-16-时间词语料与晋升.md` 的 Task 1 起，用 `superpowers:subagent-driven-development` 执行。计划里每个 task 都写了完整的测试代码和变异表。

⚠️ **Task 2 是这个计划里最容易出形态 B 的地方** —— 承重断言必须走 `parse_restaurant_query`，⛔ 不直接调 `record_time_phrase`。计划里已经把这条写进 task 的 Interfaces 段。
