# 交接：画布 BOM / 归属重锚 / 生产 E2E（2026-08-11）

**环境**：prod `cretas_prod_db` · 工厂 `F006`（六膳门）· 账号 `f006_admin`
**基线**：`origin/main` @ `f5be6c8efc`
**方式**：全程 Chrome 真实点击 + 每次改动落库核实，不看部署回执下结论

---

## 一、本轮合并并已部署 prod 的 PR

| PR | 内容 | 侧 |
|---|---|---|
| #2488 | 归属对象按研判**自动重锚** —— 单原料多成品自动归到原料 | Java |
| #2492 | 辅料/包材连线与主流程统一 + 辅料间距跟高度走 + 顶部提前说出归属搬迁 | Web |
| #2494 | 副产就地标记 + **修好一条走不通的指引**（`updateMaterialType` 漏 `setIsByproduct`） | Java+Web |
| #2497 | BOM 状态改成挂在成品 Cell 旁的气泡，顶部瘦身 | Web |
| #2499 | 气泡给显式宽度（只写 `max-width` 会塌成一字宽竖条） | Web |
| #2503 | 多选拖动整批提交 + 辅料间距用**辅料自己**的高度 + 联产不显示归属对象 | Web |
| #2504 | 清选区改用 vue-flow API（修 #2503 合进去的 CI 红） | Web |
| #2510 | 辅料留白加地板 + BOM 气泡改放 Cell 正上方 | Web |
| #2512 | 包材 Cell 挪到成品上方 + 成品气泡只说产品级的事 | Web |

另有两个更早的：**#2478 → #2479（撤回）**、**#2480**，见第三节。

---

## 二、E2E 全链路已跑通（F006 `wf=163`）

```
画布搭建(七要素) → 发布+BOM激活 → 计划匹配 → 补料链路 → 报工×2 → 生产小结 → 成品入库
```

**报工两道**，两级出成率串得上：

| 序 | 工序 | 领用 | 产出 | 剩余 | 对上工序 | 对原料累计 |
|---|---|---|---|---|---|---|
| 1 | 干式熟成鸡前处理 | 5.00 | 3.75 kg | **0** | 75.00% | 75.00% |
| 2 | 定量包装 | 3.75 | **14 盒** + 鸡油 0.25kg | 14 盒 | 93.33% | **70.00%** |

**生产小结**扣 4 条消耗共 27.75 = `胸肉5 + 姜粉5 + 半成品3.75 + 真空袋14`
—— **辅料和包材都在内**，画布上那两个 cell 是真生效的。

落库：

| 对象 | 结果 |
|---|---|
| 成品 | `FG-PLAN-…-S1-PT_F006_` 叮咚好食光红烧猪蹄 250g **14 box** AVAILABLE @WH-FG |
| 副产 | `CLK-SEMI-280cfd0d-RMT_74b0` 鸡油(`is_byproduct=t`) **0.25 kg** AVAILABLE |
| 半成品 | `WIP-CLK-W-20260811-88488` 3.75/3.75 耗尽 |
| 原料/包材 | 生产仓 `-5kg` / `-14个` |

⚠️ 成品 `unit_cost = 0` 是**诚实的**：原料走无订单入库没录单价，成本链算不出来。

---

## 三、🔴 最贵的一次：修错了、部署了 prod、4 分钟后撤回

多工序逐道报工在 prod 500：

```
ERROR: product <半成品> has no published/enabled canvas workflow;
       LEGACY production was retired 2026-08-09
Where: pin_production_batch_workflow_selection() line 97
```

**根因是两条各自合理的规则打架**：
- Java 故意不给 WIP 批次挂 planId（防成本重复计）
- 触发器把「不挂计划」当 LEGACY 老路的**唯一判据**

**方案 A（PR #2478）干跑 4 条断言含变异对照全绿**，部署 prod → **4 分钟后换了个错**：

```
IllegalArgumentException: No enum constant ...WorkflowSelectionMode.LEGACY
```

枚举里 `LEGACY` 早删了。放行后 INSERT 成功，但列默认给的 `'LEGACY'` **Java 读不回来**。
发 PR #2479 撤回。

> 🔑 **判据：断言 A 的输出里 `mode=LEGACY` 就写着答案，我没看出来。**
> 那条 `SELECT count(*)` 走 psql 不走 Hibernate —— **干跑的读取路径和生产的不是同一条**。
> **放行一类数据前先问「放行后它被打上什么值，读取侧认不认」。写入侧全绿 ≠ 这条路能走通。**
>
> ⚠️ **变异对照证明「信号是真的」，证明不了「信号够不够」** —— 这两件事别当成一件。

**正解（PR #2480）**：WIP 批次也挂 planId 走 WORKFLOW 分支；只改 `ProductionBatchRepository`
**3 个方法**加 `batch_type <> 'CLERK_WIP'` 就覆盖了全部 6 个调用点。

🔑 **「行为不变」可以构造性证明**：改前 CLERK_WIP 的 `production_plan_id` 恒为 NULL
（prod 全量 40 行历史数据带计划数 = 0），这些查询**本就返回不到它们**。
这比「跑测试没红」强 —— 前者是证明，后者是采样。

---

## 四、附带查明的两个真缺陷（都已修）

### 1. `V20261029_78` 删掉的 20 条「LEGACY 老路遗留」全是正常数据

```
legacy_retired.production_batches_20260809
  → workflow_selection_mode=LEGACY, batch_type=CLERK_WIP, 20 条, plan_null=20
    2026-08-02 19:52 → 2026-08-09 16:38:26
```

**100% 是正常逐道报工产生的中间半成品批次**（连同 reports/consumptions）。
整行备份在，**未回灌** —— 改历史生产与成本记录需业务侧拍板。

两天没人发现的原因：最后一条成功批次建于 `16:38:38`，闸落地于 `16:40:45`，差 **2 分钟**。

### 2. `updateMaterialType` 漏了 `setIsByproduct`

`setIsByproduct` 只写在**创建**路径，更新路径逐个赋值 20 多个字段却独独漏了它。
→ 物料档案那个「这是副产」开关**编辑已有物料时点了不写库**。

而画布副产 Cell 给的指引正是「去『仓库 → 物料档案』勾一下」——
**用户照做，回来下拉还是空的，因为那一勾从来没保存过。一条走不通的指引。**

prod F006 306 个原料里只有 2 个带标记，就是这个原因。已修 + 改成就地弹窗标记。

---

## 五、归属对象自动重锚（PR #2488）

**能力本来就有，缺的是最后一步** —— `requireWorkflowOwner()` 早就支持 owner = 原料
（为 `RawMaterialType` 懒建 id 等于原料 id 的 inactive 影子 ProductType `__RAW_WORKFLOW_OWNER__*`），
prod 库里已有 6 条影子记录。而 `V20261029_81` 摘掉三条复合外键的 `product_type_id`，
正是为了让归属能改而不打断已启用记录 / 在跑的计划 / 运行实例。

**规则**（`WorkflowAnchorPolicy`）：

| 研判 | 归属对象 |
|---|---|
| 单成品 | 那个成品 |
| **单原料多成品（原料分流）** | **那个共享原料** |
| 多原料多成品（联产） | **不动**，顶部直接显示「联产 · 不归属单一对象」 |

**触发点是「自动同步并发布」**，不是打开画布 —— 存量图不会被偷偷搬走。
顶部会提前显示「发布后移到「XX」」。

### ⚠️ 待验证：F006 拓扑成品C/D 那张图还没发布过

```
144/154/157/158/162 (v1~v5)  仍在 拓扑成品C 名下
预期发布后 → 拓扑原料R4 (RMT_1785475718109)
```

**下一步该做的**：真机点一次「自动同步并发布」，确认 5 个版本整条搬到 R4 名下，
日志出现 `归属对象按研判自动搬迁: … → …`。

---

## 六、画布 UI 本轮定稿

- **上方挂的都是这道工序/这个产出要消耗的配方项，左右才是物料流**
  （辅料挂工序上方、包材挂成品上方，两者读法统一）
- 浮层连线与主流程**同线型**（都走 vue-flow 默认曲线，不再是直角折线）
- 间距用 `max(实测高度, 下限)` —— 实测来自上一帧，内容展开后会变高，只信实测会贴上去
- 顶部只留**跨对象**信息（工艺版本 / 归属搬迁），逐产品的事挂到成品 Cell 气泡上
- 成品气泡**只说产品级的事**（还没建 BOM / 草稿未生效）；辅料/包材各自的 Cell 自己说

---

## 七、遗留

| 项 | 状态 |
|---|---|
| 拓扑成品C/D 发布后重锚 | ❌ **待真机验证**（见第五节） |
| 被误删的 20 条 CLERK_WIP 批次是否回灌 | ❌ 需业务拍板 |
| LIUSHANMEN `wf=151/153` 清理 | ❌ 需换 `liushanmen_admin` 登录；`wf=153` 里还有我早先调试留的两个测试节点（DRAFT 未启用，不影响生产） |
| 我建画布时把辅料设成 `1000 g/kg` | ⚠️ 1kg 肉配 1kg 姜粉，闸如实拦过；计划已钉 v1，要修得发 v2 |
| `mvn test` 基线 130 红 | ⚠️ 长期问题，导致大改动拿不到可信回归证据 |
| `WorkflowUnitReviewWritePathTest` 2 挂 | ⚠️ 既有基线红，已在未改动的 origin/main 上复现，与本轮无关 |

---

## 八、这一轮反复踩的坑（下一个人直接拿走）

1. **干跑只验了写入侧** —— 闸的两端要各验一次（第三节）
2. **我跑的和 CI 跑的不是同一条命令** —— 本地 `vue-tsc -p tsconfig.json` 绿、
   CI `vue-tsc -b --force` 红（走 project references，覆盖面不同）。**用 CI 的原命令**
3. **合并前看每个 job 的结论，不是我认得的步骤名** —— #2503 我盯着 vitest 那两行，
   没注意失败的是同一个 job 里的「Type check」步骤，把红着的 CI 合了进去
4. **注释声称的行为 ≠ 代码** —— 我写「依赖 `.material-node` 的 `position:relative`（已有）」，
   实际没有；写「需要动手时才展开」，代码却是写死 `ref(false)`。**两次都是自己埋的**
5. **按关键字切文件前先问它出现过几次** —— `split("CREATE OR REPLACE FUNCTION")` 命中了
   注释里那句同样的话；`indexOf('.material-node {')` 命中的是 transition 那条
6. **切片不够长会让 `indexOf` 拿到 -1，看着像「那行不存在」** —— 拖拽 spec 的 900 字符
7. **探针自己造假阴性** —— shell 转义写错报「没还原」；按 `updated_at desc limit 3` 排序
   没捞到副产库存就说「没入库」，直查主键那行一直都在；部署报「四方哈希不一致」实为
   `Connection closed by … port 22`，服务器文件哈希与三路完全一致
8. **`rg -rn` 的 `-r` 是 `--replace` 不是 recursive** —— 把匹配全替换成字面量 `n`，
   一轮踩两次。ripgrep 本来就递归
9. **「渲染出来了」和「渲染对了」是两件事** —— 查 `testid` 存在的测试对排版塌掉完全沉默
10. **一个字段盖住另一个字段的缺失** —— 辅料/包材共用「明细行数=0」判据，配了辅料
    就等于连包材也不提了（F006 拓扑成品C 实测：1 行辅料 / 0 行包材，气泡什么都不说）
11. **主工作目录落后 `origin/main` 1191 个 commit** —— 在它上面 grep 差点写成
    「这功能后端不存在」。动手前先 `git rev-list --count HEAD..origin/main`
12. **附带动作永远不该弄挂主动作** —— 重锚里直接调 `requireWorkflowOwner`，
    目标不存在时抛异常会把整个发布带挂（单测当场抓到）

---

## 九、下一步建议顺序

1. 真机点一次「自动同步并发布」，验证拓扑成品C/D 的重锚（第五节）
2. 决定被误删的 20 条 CLERK_WIP 批次是否回灌
3. 清 LIUSHANMEN `wf=151/153` + 我留的测试节点
4. 把辅料 `1000 g/kg` 修成合理值（发 v2 + 重建计划）
5. 清 `mvn test` 的 130 条基线红 —— 它是所有大改动拿不到回归证据的根
