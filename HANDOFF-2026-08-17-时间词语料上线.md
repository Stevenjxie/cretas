# 交接 · 2026-08-17 · 时间词语料 + 人工晋升通道已上线

**写给下一个 chat。** 计划 2（`docs/superpowers/plans/2026-08-16-时间词语料与晋升.md`）**四个 task 全部做完并已在生产验收通过**。

---

## 一 · 现在的状态

```
PR       #2726 已 squash 合并 = origin/main 3028c0199e
生产     Python 8083  PID 2727447  启动 Mon Aug 17 00:33:02 2026
迁移     V20261101_15 已 applied (108ms, 含 GRANT)
跑批     crontab 45 5 * * *  (crontab 备份 /root/crontab-backup-20260817-003700.txt)
分支     codex/claude-time-corpus-3 (off origin/main, 硬约束 7 重开的)
全量     8 failed / 6037 passed —— 那 8 条在 origin/main 上就有 (baseline 当场重跑 8/6007)
```

## 二 · 它做了什么

三段式时间词识别里 **② 的后半**：

```
① 确定性规则 _resolve_sales_date_range ─ 认出 ─► 进后续理解
                                    └ 没认出
                                         ② 交 LLM double check
                                            ├ 认出来 → 规范化 ──► 进后续理解
                                            │            └ **写入 ai_time_phrase_corpus** ← 本轮新增
                                            │                 └(人工晋升)──► ①
                                            └ 也没认出 ⇒ ③ 给澄清按钮 (#2723 已做完)
```

以前 LLM 认出来的知识**用完就扔** —— 同一个词每次重付一次 LLM。

**owner 拍板三件**：人工晋升 · 晋升产出**代码补丁**（⛔ 不做运行时查库）· 阈值 20。

⛔ **「晋升」不是往词表里 append。** `_resolve_sales_date_range` 是 **237 行 / 38 个 `if`** 的手写解析器，**没有可追加的词表** ⇒ 晋升 = 人手写一个分支 + 补测试 + 发版。CLI 只能告诉你该处理哪些词、记下你处理了哪些 —— 这句话钉在 `--help` 里并有测试守着（**一个让人误以为「跑一下就生效了」的工具，比没有工具更糟**）。

## 三 · 🔴 证明它真的生效的那条读数

⛔ `DEPLOY_EXIT=0` 不是判据，「md5 对上」也不是（见第五节）。prod 真跑：

```
[旁证①] 确定性层单独跑「最近损耗怎么样」 -> window_label='全部历史'   ⇒ 合格样本
[主读数] before = {'total': 0, 'unpromoted': 0}
[旁证②] source_tier='llm'  window_from_llm_phrase=True
        window_label='最近30天'  date_range=(2026-07-19, 2026-08-17)
[旁证③] _parse_t3_time_range 被调用 1 次: [({'relative','day',30}, '最近30天')]
[主读数] after  = {'total': 1, 'unpromoted': 1}
判据 after.total > before.total : True   (0 -> 1)
未晋升条目: '最近损耗怎么样'  llm_phrase='最近30天'  hit=1  factory=MOCK_REST
VERDICT=PASS
```

⚠️ **语料里那一行是探针写的，⛔ 不是真实用户问的。** 别把它当第一个真样本。

跑批三态在 prod 上逐个真跑过，三种措辞互不相同：

| 态 | 构造方式 | rc | 告警行 |
|---|---|---|---|
| 正常 | 阈值 20，unpromoted=1 | 0 | （静默，不写） |
| 有积压 | `TIME_PHRASE_THRESHOLD=0` | 1 | `XXX BACKLOG … 未晋升 1 条 (阈值 0)` |
| 没量到 | `TIME_PHRASE_CODE_DIR=/nonexistent` | 2 | `XXX INSTRUMENT DEAD … 语料表读不到` |

第四种：`total=0` 时写 `--- 时间词语料至今 0 条 …`，**与「无积压」措辞不同** —— 写入点附近**没有任何日志**，这张表自己就是唯一的仪器，它的「0」必须能被读懂。

## 四 · 明天最可能需要动的（都已登记，⛔ 不是遗漏）

**① 语料按整句入库，晋升按词发生 —— 回路还差一段。** 这是最值得先做的一条。

- 人手加完「最近」的分支发版后，含「最近」的**存量行仍是未晋升**（`--list` 没有子串筛选、`--mark-promoted` 一次一句）⇒ **告警会在活干完之后继续响** ⇒ 形态 E（闸被忽略）。
- **反向更糟**：`_UPSERT_SQL` 不重置 `promoted_at`，所以**晋升没生效**的行（分支没匹配上 / 被回滚）会一直被记、`hit_count` 一直涨，却**永远不再出现在 `--list` 里、永远不算积压** —— 没有任何读回说「那条规则还是接不住」。
- 修法（仍是人工驱动，⛔ 不违反「不自动晋升」）：`--list` 加第二段 `promoted_at IS NOT NULL AND last_seen_at > promoted_at`（「已登记晋升但仍在被记 —— 那个分支没生效」）；`--mark-promoted` 加 `--like <子串>`（先打印将命中什么）。

**② 探针污染。** `restaurant_capability_audit.py` / `restaurant_department_audit.py` / `t6_time_resolver_probe.py` / `local_d_diag.py` 都调**公共** `parse_restaurant_query`，一次 404 条问句的普查就能把 `unpromoted` 顶过 20，**告警全是探针流量**。已加 `TIME_PHRASE_CORPUS_OFF`（默认不设 = 照记），但**要操作者自己设**，⛔ 没有机械触发点。

**③ 没有清理程序。** `GRANT` 故意不含 `DELETE`（「语料清理不是运行时职责」）⇒ 全仓没有任何清理路径，而 `raw_query` 是用户输入文本、跨租户、无限期累积。要清理只能超级用户 SQL。

**④ 「至今 0 条」是心跳不是警报。** 语料为空期间跑批每天追加一行，⛔ 别训练出「忽略这个文件」的习惯。

**⑤ `--list` 打的 `[hit=N]` 旁边没写口径。** `hit_count` 只用于排优先级 —— `chat.py:2286` 先 peek 一次 `parse_restaurant_query`，随后还会再解析一次 ⇒ **一次提问可能记 2 次**，⛔ 不是频次指标。

**⑥ 第一周该看的读数**：语料里的 `raw_query` **是不是真的都含时间词**。如果大量根本没有时间词，说明模型过量产出 `time_range`，那时 ① 从「理论」变成「紧急」。

## 五 · 🔴 这一轮用代价换来的两条

### 1. 部署只做了一半，而「md5 全对」正是它的长相

第一次 `DEPLOY_EXIT=255`。逐行读日志（⛔ 没用 `| tail`）：

```
[3/5]   rsync                 ✅
[3.5/5] migration             ✅ BEGIN → CREATE TABLE → GRANT → COMMENT×6 → COMMIT
[4/5]   验证依赖              ⛔ Connection closed by 47.100.235.168 port 22
[5/5]   重启                  ⛔ 从来没跑
```

于是 prod 的状态是 **4 个文件 md5 逐条等于 main，而 PID / 启动时间一秒没动** —— 跑的还是内存里的旧代码。

▎**只核 md5 会得到一个全绿的假读数。「找到标记 ≠ 它能跑」。**
⇒ 重跑一次补上 [4/5][5/5] 即可（migration 幂等）。同一晚网络抖了两次（另一次是 `git fetch` 的 `SSL_ERROR_SYSCALL`），⛔ 别读成「脚本坏了」。

### 2. 探针第一次读出「闸没开」，而那是探针自己没 key

第一次跑验收，`window_from_llm_phrase=False`、`_parse_t3_time_range` 调用 **0 次**、router 打 `All providers exhausted: empty_api_key ×3`。

⛔ 那**不是**接线坏了 —— 是**我的探针进程没有服务的环境变量**。
⇒ 问活进程要环境时**只 export `LLM_` 前缀**（`LLM_AISTORE_API_KEY` / `LLM_DEEPSEEK_API_KEY` / `LLM_ZHIPU_API_KEY`），⛔ 不 eval 整份 environ（PATH 会被冲掉，`python` 掉回系统解释器）。pid 用**端口反查**，⛔ 不用 `pgrep -f`（会抓到探针自己）。

▎**救回这一次的是探针里那两条旁证**（LLM 调用次数 + 确定性层的独立读数）。
▎只看主读数「total 没涨」，三种完全不同的成因长得一模一样。

## 六 · 登记为「不做」的六条（⛔ 别当成漏了，理由在设计卡里）

`has_followup_signal` 的 32 字闸 · 五处产出裸 `clarification_options` · COMBINED 澄清仍发裸串 · 窗口清单全仓 6 份副本 · web-admin 48 字闸 · NOTIF/菜品级归因/套餐

## 七 · 环境速查

```
生产 Python  47.100.235.168:8083   /www/wwwroot/cretas/code/backend/python
             venv: venv-current/bin/activate
             PID=$(ss -lntp | grep :8083 | grep -oP 'pid=\K[0-9]+' | head -1)
CLI          cd backend/python && venv-current/bin/python -m smartbi.scripts.time_phrase_corpus_cli --list
跑批         /www/wwwroot/cretas/code/scripts/cron/time-phrase-backlog-daily.sh
             告警 /www/wwwroot/cretas/logs/time-phrase-backlog-alerts.log
部署         ./scripts/deploy/deploy-smartbi-python.sh --env prod [--migration-target VXXXXXXXX_NN]
             ⛔ 纯 Python 改动不要用 release-cretas.sh
提交         ./scripts/safe-commit.sh "<msg>" <file>...
方法学       .claude/rules/measurement-and-wiring.md（**origin/main 那份**，1246 行）
```
