# 交接：餐饮 AI 主动化 + 五部门定型（2026-08-06 晚）

> 接手方：继续做 Cretas 餐饮的 session。
> 本轮 16 个 PR 全部已合并 main 并部署 prod，逐条实测过。

---

## 一、这一切是为了什么：小蓝店长

**起因**（2026-08-05 记）：Steve 参加投资活动得知**扫呗投了 WEIBOT（AI Digital Employee）天使轮**，用于给自家「**小蓝店长**」横向扩 AI 员工能力。这会与 Cretas 餐饮 AI 正面竞争。

由此定的目标：**把 Cretas AI 做成小蓝店长形态** —— 不是"能问答的 BI"，是**像个员工一样主动说话**。

本轮把它落成一条可验收的判据：

> **店长打开系统，在没有提问的情况下，看到一条他本来不知道、且今天就能动手的事。**

### 🔴 为什么"主动"是这个目标的命门

2026-08-05 prod 实测（近 30 天）：

| | F006 | 六膳门 |
|---|---|---|
| 物料批次 | 243 | 54 |
| 生产批次 / 报工 | 12 / 3 | 21 / 16 |
| **AI 会话** | **0** | **0** |

`conversation_sessions` 近 30 天共 12 个，**全部来自演示租户，真实工厂 0**。

**不是"没有用户"，是"有每天在用的用户，而 AI 那一层对他们不存在"。**

所以：**只挂在 AI 对话里的功能，等于没做。** 这是本轮第 3 步（主动出口）存在的全部理由，也是验收必须落在"不提问就能看到"上的原因。

⚠️ 本轮就栽过一次：Step 1 我把发现层挂在 `RestaurantWastageAnomalyTool` 上，部署后实测**它日志 0 次调用**——餐饮提问在到达 Java Tool 之前就被 tiered 路由委派走了。**挂在没人走的路上，和没做一样。**

### 产品边界（Steve 已更正过两次，别再搞混）

- **Cretas 餐饮 和 Cretas 工厂是两个产品**，不是一个产品带模块
- **重点是算成本，不是溯源**（仓库名叫「食品溯源系统」会误导）

---

## 二、现在店长打开系统看到什么（prod 实测）

以 `mock_ops`（`restaurant_manager`）登录，**不提任何问题**：

```
变质损耗近7天 ¥287,658.64，占全店损耗 36.8%
ℹ️ 食材损耗离群：两期食材名单不可比: 近 7 天 25 种 / 基线 13 种 (重合度 52%)，暂不判断。
```

他本来不知道——唯一会告诉他损耗的两条既有链，一条说"损耗率 3.75% 偏低"（severity=info，告警桥明确不推），一条读空的主库表说"库存管理状态良好"。

今天能动手——备货量与 FIFO 在店长权限内。

**同时**：加权毛利率 **67.69%**，菜单工程四象限 **明星 4 / 主力 1 / 谜题 1 / 瘦狗 4**。

---

## 三、已上线（16 PR，全部 prod 实测过）

### 发现层（第 1–3 步）

| PR | 内容 |
|---|---|
| #2331 | `domain="restaurant"` 发现层 + 两条损耗规则 + **第三态 `skippedRules`** |
| #2333 | 顺带提示挂在 tiered 委派的**唯一汇合点**（7 个调用点全汇入 `tryRestaurantTieredDelegate`） |
| #2334 | 主动出口：`GET /api/mobile/{fid}/findings` + 店长落地页「今日营运台」卡片 |
| #2335 | 卡片改**服务端渲染**——前端自己拼会被价格脱敏打空金额 |

**三态**（`checkedRules` / `skippedRules` / `failedRules`）是本轮的核心纪律：
- 真的没有 → 「已检查 X，均正常」
- 数据没采集到 → 「两期食材名单不可比…暂不判断」
- 查询失败 → 「检查失败，暂无法判断」

⚠️ **规则 R1（食材损耗离群）今天在 MOCK_REST 上必然返回"判不了"**，这是**正确答案不是缺陷**：`fact_restaurant_wastage` 在 2026-07-29 一次性回填了 5,338 行（覆盖 06-29~07-29），07-30 起才是每日生成，**单笔均价从 ¥18–30 跳到 ¥200–228**——两个不同的数据生成过程。同质历史只有 08-01 起 6 天。朴素的"近7天 vs 前30天"在这份数据上会让 **25 个食材全部呈现 16–34 倍增长**。

### 部门与权限（五部门定型）

| PR | 内容 |
|---|---|
| #2336 / #2338 | WorkdeskRole 加店长；**厨师长退役**并钉断言闸 |
| #2340 / #2342 | 老板权限 + 采购归属（后被 #2351 推翻，见下） |
| #2347 / #2348 | 采购成为**第五个部门** + MOCK_REST 采购/老板账号 |
| #2350 | 五部门矩阵重述为 **L1 权威**（带 `L1-AUTHORITY` 标记，30 行） |
| #2351 | **老板 = 全局 RW**（可替其它角色做 OA） |
| #2352 | 部门数别再硬编码（main 曾因此变红） |

**餐饮五个部门**：运营(店长) / 市场 / 财务 / 人事 / 采购
→ `restaurant_manager` / `sales_manager` / `finance_manager` / `hr_admin` / `restaurant_purchaser`

**MOCK_REST 七个账号**（口令统一 `123456`，见 gitignored 的 `db-credentials.md`）：
`mock_rest`(超管) / `mock_ops`(店长) / `mock_market` / `mock_purchase` / `mock_finance` / `mock_hr` / `mock_owner`

### 成本与毛利

| PR | 内容 |
|---|---|
| #2353 | **毛利 resolver 漏传 role** —— 财务驾驶舱毛利对所有人恒为 0 |
| #2355 | 菜单工程四象限（销量 × **单份毛利贡献**，不是毛利率） |
| #2356 | 指标比标杆好时**不再挂反向建议** |

---

## 四、⛔ 还欠的

1. **`agg_supplier_price` 0 行** —— 采购看板的比价/采购价异常做不了。**这要客户真的录供应商报价，不是改代码能变出来的。**
2. **人事数据基本是空的** —— 员工表 0 行、排班表 4 行、`staff_id` 在 21.5 万笔账单里填了 **0 笔**。人效、员工业绩全部算不了。
3. **实收 `actual_receive` 全空** —— 收款差异、平台抽成算不了。
4. **`RESTAURANT_WASTAGE_ANOMALY` 意图指向不存在的 Skill** `restaurant-wastage-anomaly` —— 该意图永远落 no-tool 分支。本轮绕开了（改挂 tiered 汇合点），但意图本身没修。
5. **前一份交接的欠项**：RN #2322 真机验收未做；`ark/doubao-seed-2-1-turbo` 是 **429 SetLimitExceeded（自设上限，控制台可调，不花钱）**。
6. **main 上 4 条既有红**（`ToolRbacEnforcerWiringTest.siteD/siteE`、`WriteGuardWiringTest.siteB/writeIntent`）——与餐饮无关，一直亮着。

---

## 五、📌 本轮踩出来的判据（会反复踩）

### 关于"我以为发现了缺陷"

**本轮三次把不是缺陷的东西判成缺陷**，每次旁边确实藏着真缺陷，但都不是我指的那个：

1. **`ops-summary` 金额全 None** → 以为运营驾驶舱坏了。真因是**我自己同时发了 `Authorization` 和 `X-Internal-Secret`**——内部密钥那条路不设 `request.state.role` → 全员被脱敏。只带 Bearer 重测，全部正常。
2. **成本卡 `product_id` 全是 0** → 以为连不上菜品、加权毛利做不了。真相：`product_id=0` 是**故意的**（ETL 原话 `legacy column, kept for FK; real grain is product_source_pk`），桥本来就在（`normalized_name` 10/10 匹配），毛利本来就算得出来。
3. **损耗规则本身** → 见下"业务口径"。

**判据：看到反常读数先问「写它的人怎么说」，别直接判缺陷。** 三次里两次是我的探针造的假象。

### 关于并发（这片区域现在很挤）

- **`DEPLOY_EXIT=1` + 构建全绿 + 无 `RELEASE_FINAL_STATUS`** → 先 `git fetch` 比 `HEAD` vs `origin/main`。**构建期间 main 会动是常态**，本轮撞了 3 次。
- **Flyway 撞号：改号前查 prod 的 `flyway_schema_history`，光看仓库文件名不够**；而且**合并之后要再查一次**——并发的人可能在你合并前后挑到同一个号（本轮 61 号就是这么撞的，main 一度"后端启动即炸"）。
- **发布锁在本机 `/tmp/cretas-release.lock`**。脚本没自动清就说明持有者真活着（用 PowerShell `Get-Process` 查，`ps` 看不见 Windows 进程），**此时不能抢锁**。
- **蓝绿槽位会变**（本轮末尾活跃槽是 **green / 10020**，10010 返回 000 属正常）。

### 关于"一个东西有几处定义"

本轮反复撞到同一个形状，**修一处不等于修完**：

- 老板权限有**三处**（Java `PermissionServiceImpl` / L1 迁移 / 前端 fallback），三处互相矛盾
- 毛利 role 转发：`store-margin` 早修过，`summary` 和 `gross-margin` 两个调用点漏了
- 部门清单有**五处**（`departmentConfig`(权威) / `permission.ts` / `menuConfig` / `router` / `departmentSingleSource.spec`）——代码注释写着"四处"，实际五处，漏的那处把 main 弄红了

**判据：加维度前先数清有几处承载，并优先按「所有调用点」写断言，而不是测一个。**（#2353 的测试用 AST 扫全部调用点，就是为这个。）

### 关于"退役"

**「代码里有」≠「它还活着」。** 厨师长被反复当成活角色加回来，根因是**停用只做在数据层（账号 `is_active=f`），代码层从未清**——`FactoryUserRole` 里它是一等公民，还有 4 个测试套件**主动断言它的行为**。

任何 session 只要遵守"别信记忆、去读代码"，就会重新发现它。**写 memory 治不了**（纪律本身是代码优先于记忆）。根治是把决定钉在**声明处（`@Deprecated` + javadoc 写判据）+ 一个会红的断言闸**。

### 关于业务口径（Steve 拍板，别再自己发挥）

- **餐饮不该把损耗放在台面上。** 重点是加权毛利、总成本、总营收、总毛利、食品消耗量。工厂按批次追物料损耗是因为投入产出一一对应；餐饮浪费/多打勺/被拿走**最后都体现在毛利被吃掉**。把"变质 ¥26.8 万"单拎出来是工厂思路。
  - ⚠️ 本轮的损耗规则是在这条口径**确定之前**做的，**Steve 明确说过损耗意义不大**。它仍在线上（作为发现层的第一条规则跑通了形状），但**下一步应该把发现层的规则换成毛利/成本口径的**，而不是继续加损耗规则。
- **加权毛利按营收贡献加权**。
- **总成本分两档**：不含 optional 的（食材层，现在能算）/ 含 optional 的（+ 财务，缺数据）。**必须分开呈现**，不能把"食材毛利"说成"毛利"。
- **渠道要分开 + 也要总的**。渠道在 `order_type`（堂食/外卖/团购），**不在 `channel_origin`——那个字段全 NULL，是死字段**。
- **成本卡是 optional**：客户提供不了也不能堵塞计算。

---

## 六、可复用的 prod 查询

```bash
# 餐饮 POS / gold（smartbi_prod_db，RLS GUC 是 app.factory_id）
ssh root@47.100.235.168
cd /www/wwwroot/cretas
SP=$(grep -oP '(?<=^SMARTBI_DB_PASSWORD=).*' .env.prod)
PGPASSWORD=$SP psql -h localhost -U smartbi_user -d smartbi_prod_db -c "SET app.factory_id='MOCK_REST'; ..."

# 登录拿 token（活跃端口先看 ss -lntp | grep -E ':10010|:10020'）
curl -s -X POST "http://127.0.0.1:10020/api/mobile/auth/unified-login" \
  -H "Content-Type: application/json" \
  -d '{"username":"mock_ops","password":"123456","deviceInfo":{...}}'
```

⚠️ **打 Python 服务时只带 `Authorization`，别同时带 `X-Internal-Secret`** —— 后者不设 role，金额会被全员脱敏，看起来像"数据坏了"。

⚠️ **本机直连不到 47 的业务端口**（安全组只放行网关 139），健康检查要 **ssh 上去打 127.0.0.1**。

---

## 七、下一步建议

按小蓝店长的目标排，我认为优先级是：

1. **把发现层的规则换成毛利口径** —— 现在跑的是损耗，而 Steve 已明确损耗意义不大。地基（三态 / 单一挂载点 / 主动出口）是通用的，换规则不用重做。
2. **菜单工程接到前端** —— 四象限后端已出数（明星 4 / 主力 1 / 谜题 1 / 瘦狗 4），但还没有页面。「罗氏虾单份赚 ¥78.57 全店最高但销量垫底」这种结论，是店长真能动手的。
3. **采购部门的数据缺口** —— 要么推动客户录供应商报价，要么把采购看板的口径调成只用现有数据（领料/收货/盘点）。

不建议继续加损耗规则。
