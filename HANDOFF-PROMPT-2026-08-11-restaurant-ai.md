# 交接 Prompt — 餐饮 AI（2026-08-11 夜）

> 把这份整个贴进新 chat 作为开场。它假设接手方**没有任何上下文**。
> 前一份是 `HANDOFF-PROMPT-2026-08-10-restaurant-ai.md`，那份里有三处说法已被实测推翻（见 §2）。

---

## 0. 你接手的是什么

白垩纪食品溯源系统（Cretas）里的**餐饮经营问答 AI**。餐厅老板用自然语言问经营问题
（营收、菜品、门店、毛利、排班），系统解析成结构化计划 → 确定性代码查库 → 生成答案。

前端 RN App（`AIChatScreen`），后端 Java(10010/10020 蓝绿) 转发给 Python(8083)。

**主工作目录**：`C:\Users\Steve\my-prototype-logistics`
⛔ 别在主目录干活，开 worktree（见 §3）。

---

## 1. 目的与产品愿景（owner 原话，按提出时间）

### 长期目标（2026-08-10）

> 「把所有的东西跑完，有问题就去修复，以结果为难分析的用 LLM，以后全量跑下来没有问题，
> 而且**能力很强的餐饮 AI**。如果有任何**反复**的问题，才有长期的，并且采用**对抗性审计**
> 去盘查自己的问题和方案。」

**能力推进顺序**：归因 → 预测 → 建议 → 数据缺口。预测那段已做完（回测闸 + 周内因子）。

### 产品口径拍板（2026-08-11）

| 议题 | owner 的话 | 状态 |
|---|---|---|
| 门店范围 | 「账号是单店的还是总部的，**如果是总部的话就走乙**（默认全部门店 + 显式声明）」 | ✅ 已实现 |
| 模糊输入 | 打不全 / 打错字要能**准确找到** | ✅ 已实现 |
| 确认 | 「**唯一命中也要反问**用户是否要问的是那家，然后给到按钮」 | ✅ 已实现 |
| 技术内容 | 「**最重要的是不能在前端返回输出里面显示置信度这些技术内容**」 | ✅ 已加闸 |
| 输出形态 | 「回答用**表格 + 文字**的结构输出在前端给用户，这样更直观」 | ✅ 8 处已转 |

### 🔴 这个产品现在离目标有多远（今天实测，最重要的一节）

我以「餐厅老板」身份问了 **12 个电池里没有的问题**（口语、笼统、一句话两个问题）：

| 问 | 结果 |
|---|---|
| 这月流水多少 | ✅ 好答案 |
| 哪家店最近不行了 | ✅ 好答案 |
| 明天大概要备多少货 | ⚠️ 答的是**排班人数**不是备货量 |
| 这个月比上个月好了还是差了 | ⚠️ 诚实说数据不足，但**没直接回答好还是差** |
| **有没有菜在亏钱卖，要不要下架** | 🔴 **答错**（见 §5） |
| 这个月到底赚钱了没有 | ❌ 拒答 |
| 为什么毛利率下降了 | ❌ 供应商挂了 |
| 周末和平时哪个更赚钱 | ❌ 反问门店范围 |
| 损耗是不是有点不正常 | ❌ 拒答 |
| 人手够不够用 | ❌ 拒答（解释清楚） |
| 米饭卖得咋样，还有毛利多少 | ❌ 反问 |
| 最近生意怎么样 | ❌ 反问 |

**12 问 → 2 个好答案。**

**结论**：
- **强**：fail-closed 的纪律是这个系统最硬的资产。今天打了几十次，它**一次都没有**拿相邻
  指标顶替（「没有找到名为「长宁龙梦店」的门店，也不会退化为全店榜」）。核心是**确定性代码**
  不是 LLM：日期解析、门店匹配、口径计算全在代码里。
- **弱**：**覆盖面窄**。在电池 85 题覆盖到的问法上很扎实，**一旦出圈，一半以上要么拒答要么答偏**。
  拒答不丢人（比乱答强），但 12 问 6 拒的产品，老板问两次就不问了。

⚠️ **近 7 天落库 6169 条问句，6046 条来自 `MOCK_REST`（电池自己）。没有一个真实餐厅老板问过一句话。**
所以「能力很强吗」目前**没有证据基础** —— 85 题是我们自己写的，它同时是测试和规格。

---

## 2. ⛔ 上一份交接里被推翻的三处（引用前先看这里）

| 它说 | 实测 |
|---|---|
| 基线 84/85，三轮读数完全一致 | 三轮同版本读数是 **83/82/84**；且全部早于 23:35 部署，而 23:09 改的是**量尺本身** |
| [51] 是唯一稳定失败的题 | [51] 已通过 |
| 「还没改」表格化清单 4 处 | 实际还有 3 处同形状，已全部转完 |

---

## 3. ⛔ 硬约束（违反会出事，不是风格问题）

| 约束 | 说明 |
|---|---|
| **工厂端不碰** | owner 说过两次。`factory_ai_eval.py` 每天 404 崩一次 —— **已知，刻意不修** |
| **`_SAFE_MODELS` 一条都不许新增** | 计费闸。加模型需 owner 在控制台确认「免费额度用完即停」。**移除**只需探针证据 |
| **prod 只从 main 部署** | 绝不从 feature 分支部署（2026-05-30 事故：并发 session 互相覆盖 jar） |
| **worktree 隔离** | 任何代码任务开独立 worktree，off `origin/main` |
| **commit 锁范围** | `git commit -m "..." -- <paths>`，commit 前先 `git status`（husky 会顺走并发 session 的文件） |
| **禁止降级处理** | 不返回假数据。查不到就说查不到，绝不用相邻指标顶替 —— **这是系统的核心承诺，也是它现在最强的地方** |
| **migration 走 runner** | 禁手动 `psql -f` 跑 DDL，必须 `deploy-smartbi-python.sh` |
| **`MOCK_REST` 绝不进只读名单** | 它是电池租户，需要完整写能力 |

---

## 4. 现状（2026-08-11 22:00）

| 指标 | 值 |
|---|---|
| 今天合并部署 | **17 个 PR**（#2470→#2507） |
| 线上代码 vs main | md5 完全一致 ✅ |
| 电池读数 | 83 / 78 / 76 —— ⚠️ **互不可比**，见下 |
| 模型额度 | **8 个 403**（`qwen3.8-max` 三个账号全没，`qwen3.7-max` 各变体全没） |
| 今天 token | 4.77M，其中 **91% 是电池烧的**（真实用户流量 0） |

### 🔴 读任何电池分数前必须先看「取数电条件」

从 #2472 起每轮末尾会打印：

```
── 本轮取数条件 ──
计划缓存: 命中 19 / 真解析 176
服务模型: aliyun_b/qwen3.7-max-2026-05-17 ×73、aliyun_c/kimi-k2.7-code ×62、…
```

**电池分数是 `代码 × 今天哪个模型还活着 × 缓存冷热` 的函数，不是代码的函数。**
今天 83/78/76 三个数各自跑在不同模型组合上，**跨在链头崩塌过程中，不能用来判断代码好坏**。

---

## 5. 🔴 今天发现的最严重缺陷（**第 1 步已修，PR 见下**）

> **2026-08-11 夜更新（接手方）**：§5 第 1 步 + 判据修正**已实现并合并**，第 2/3 步仍未开工。
>
> **⚠️ 本节原文有一处推断被日志推翻**：③ 说「Java 的向量种子……→ 捞成 `RESTAURANT_DISH_SLOW`」。
> 实测 prod trace `268a6fdc-697c-4f86-9c57-35343cc1693e` 写的是
> `识别到意图: code=RESTAURANT_DISH_SLOW, matchMethod=**LLM**, confidence=0.8`
> —— 捞它的是**LLM 识别层**，不是 25 条向量种子（种子只进候选集）。①② 逐字属实。
>
> **已做**（两层，都装了会变红的闸）：
> - `RESTAURANT_DELIBERATIVE_MOOD`：**判语气不判说法**。征询（要不要/该不该/是否要/建议）
>   或假设（如果/假如）出现时，无论提到什么动词都是读 → 回到 Python 语义权威。
>   ⛔ 没有往「已下架/下架记录」那张过去时豁免表里加词条（那还是逐条枚举，见 §6 判据 10）。
> - `blockRestaurantWriteVerbReadFallback`：写动词压掉语义权威、而识别层给的是**读**意图时，
>   两个判据矛盾 → **fail closed**，不猜。装在 `/execute` 与 **SSE 两个入口**的意图解析点上。
>
> **爆炸半径（11 天 prod 日志实测，不是估计）**：7821 条餐饮请求 / 219 种问句里，
> 由写翻成读的**只有 3 种**，全是这个缺陷本身；`下架红糖糍粑` 这类祈使句**一条没动**；
> 85 题电池里带写动词的 2 题走的是 previewOnly + 具名下架语法 / 排行批量澄清，
> 两条路都排在本判据之前，**不受影响**。
>
> **还没做**：§5 第 2 步（删 8 条与 Python 重复的关键词规则）、第 3 步（4 个 Java 独有
> intent 迁进 Python 登记表 + 修慢销榜排序缺陷和过期时间窗）。
> ⚠️ **慢销榜自己排序是坏的（63,368 排第 1 而 77,046 排第 4）+ 时间窗停在两个多月前** ——
> 这两条**没修**，只是「要不要下架」不再会走到它了。它自己的入口（「哪些菜卖得差」）还在。

### 原文（下面是发现当天的记录，保留原样）

### 现象

问：「有没有哪个菜是**在亏钱卖**的，**要不要下架**」

答：一份「慢销/滞销菜品（销量垫底）」榜单 —— 而且那份榜单**自己排序是坏的**：

```
1. 酸梅汤 — 销量 63,368 份
4. 红糖糍粑 — 销量 77,046 份     ← 比第 1 名多 21%
```

时间窗还是 `2026-06-01 至 2026-08-01`（两个多月前）。

**问的是亏不亏钱（毛利），答的是销量** —— 老板照这个可能去下架一道赚钱的菜。

### 根因（三层，全部实测确认）

**① Java 的写动词判据分不清「提到」和「下令」**

```java
RESTAURANT_WRITE_VERB = "创建|新增|…|上架|下架|停售|…"
RESTAURANT_HISTORICAL_WRITE_READ = "已下架|下架记录|下架情况|…"   ← 豁免表
```

「要不要**下架**」命中写动词，而豁免表只有「已下架/下架记录/下架情况」这类**过去时**说法，
**漏了「要不要/该不该/需不需要」这类征询说法** → `isRestaurantWriteRequest = true`。

**② 于是 Python 语义权威被整个跳过**

```java
boolean requiresRestaurantSemanticPlan = tieredFirstEnabled && … && !isRestaurantWriteRequest(userInput);
```

⚠️ **Python 侧日志里完全没有这句话** —— `grep '亏钱卖' python-prod.log` 零命中。
它根本没到规划器。架构本身是**对的**（注释写明「餐饮读问题有唯一一个语义权威，
Python 跑在所有关键词捷径之前」），坏的是守门的判据。

**③ 落到 Java 的兜底答题器，那条路上没有任何契约**

Java 的向量种子里有「哪些菜卖得差」，句式上跟「有没有哪个菜…」高度相似 → 捞成 `RESTAURANT_DISH_SLOW`。
而 Python 那套 `request_coverage` 契约（答案必须覆盖用户问的指标）**只挂在 Python 路径上**。

### 量清楚的暴露面

| | 数量 |
|---|---|
| Java 能独立返回 intent 的关键词规则 | **19 条** |
| 覆盖 intent | **12 个** —— 其中 **8 个 `RESTAURANT_OPS_*` 与 Python 重复**，4 个 Java 独有 |
| 模糊匹配种子 | **25 条** |

Java 独有的 4 个：`DISH_SLOW` / `PEAK_MONTH` / `STORE_REVENUE_RANK` / `WEEKDAY_WEEKEND`
（Python 登记表 19 条里一个都没有）。

### 根治方案（三步，按风险从低到高；**未开工**）

⛔ **不是「Java 全删合到 Python」**。Java 该保留的东西代码注释写明了且是对的：
   explicit intents、写动词、**权限检查、preview、confirmation** —— 写操作要审批要审计。

问题只在于：**写路径上挂了一个「读」的兜底答题器。**

**第 1 步（最小、最该先做）：让兜底不许答业务问题**

被判成写请求但最终没走成写路径的问题 → **fail closed**（「我不确定你是想改还是想看」），
不许掉进关键词/向量匹配去猜。

**这一步单独就能消灭这个缺陷**，且与「禁止降级处理」完全一致。不删任何 Java 逻辑。

**第 2 步：删掉与 Python 重复的 8 个关键词规则**

⚠️ 删之前**逐条拿日志确认现在还会不会被走到** —— 别信这份文档的推断。

**第 3 步：4 个 Java 独有 intent 迁进 Python 登记表**

迁过去才能享受契约保护。顺带修慢销榜的排序缺陷和过期时间窗。

**判据本身也要修，但优先级在第 1 步之后**：
⛔ 修法**不是**往豁免表里加「要不要/该不该」—— 那还是手写清单，今天已因同一形状栽了四次。
正解是**征询词优先级高于动词**：出现「要不要/该不该/需不需要/建议/值不值得」时，
无论提到什么动词，它都是读。

---

## 6. 🔴 判据（今天全部有实测代价，按贵到便宜）

### 测量类

1. **两个读数之间有因果链，就不构成重复验证。**
   「两轮电池完全一致」是构造出来的：r1 缓存命中 2/真解析 147，r2 命中 59/真解析 24 ——
   r2 的 59 题吃的是 r1 刚写进 `_SEMANTIC_PLAN_CACHE`（进程内，TTL 6h）的计划，
   **结构上不可能与 r1 不同**。
   📌 可测做法：数**缓存命中 vs 真实计算**的比例。

2. **缓存把非确定性伪装成稳定。** [27] 连过 12 轮再连挂 2 轮，转折点压在部署重启上，
   看着像那次部署的回归 —— 落库显示**同代码同模型**下 21:16 通过、21:25 失败。
   **部署重启 = 重掷骰子。**

3. **一次冷复现区分不了「确定性坏」和「一枚偏心的硬币」。** 我为此把归因说反过。

4. **失败条数 ≠ 缺陷数。** 一条链断在第 3 轮，后面 6+ 轮连坐。

### 断言类

5. **改渲染就必须看渲染。** 8 张 markdown 表格上线后全被并成一坨（空行被下游一个
   **跟表格无关**的清洗函数整行删掉），四个 PR、两轮 85/85、CI 全绿，**没有一条断言看得见**。
   ⛔ 特别注意 `flat = " ".join(message.split())` 把空白压平 ——
   **基于 flat 的断言在结构上不可能发现排版问题**。

6. **载体要算出来，不能凭印象数。** 同一句 fail-closed 拒答有 **4 个载体**，
   我上午只装了 2 个留痕，下午 [40] 真的走到没装的那两处 —— 日志一片空白。
   **这是当天早上刚写进 memory 的判据，当天下午又犯。**

7. **变异要打在断言声称守着的那个东西上。** 我第一次变异改的是冗余守卫，12 条断言照样绿，
   换到承重处才红 —— 否则「变异不红」会被误读成「代码没问题」。

8. **写完一条断言先问它靠什么变红**，然后**真的去变异一次**。
   我写过一条完全空的闸（两种模型自述走的是同一条路，压根没触发分叉），是变异暴露的。

9. **断言不要挂在措辞/排版上。** 今天拆了好几条：「哪一组门店」字面量 / `单` 后缀 /
   `米饭 —` 分隔符 / 列表前导点。**行为对了断言反而红**。

### 结构类

10. **一张手写清单不全，而清单错了没有任何东西会红 —— 今天同形状四次**：
    `wants_margin` 被 planner 编出来没人核对 / `time_phrase` 同理 /
    内部黑话词表漏词 / **写动词豁免表漏说法**。
    ✅ 能推导的一定要推导（resolver 编码从登记表现算；载体用 AST 扫出来）。

11. **判据分不清「提到」和「下令」。** 见 §5。同族：「没解析出 X」不等于「用户没提 X」。

12. **守卫挡住正确能力时，要问它守的不变量能不能改成一条会变红的断言。**
    `--only` 那道 FATAL 是 07-28 加的，后来循环改成按单元跑却没跟着删，
    于是它挡住的是本来就正确的能力 —— 代价是**每次验证都跑全量 85 题**。

13. **两处期望互相矛盾时，先确认哪一处代表已拍板的产品意图。**
    我为此把一个正确的一行改动撤回过，绕一圈又用回了它。

14. **闸红了不许加豁免，要看它想逼出什么。** 一次大改被 11 条既有契约拦下，
    它们逼出的是「**按钮链是产品的一部分，不是待优化的摩擦**」—— 但只适用于**链内**，
    不适用于首轮。

### 工具类

15. ⚠️ **`pathlib.write_text` 在 Windows 上静默把整个文件转 CRLF** ——
    一次 commit 变成 979 insertions / 746 deletions（预期 ~110）。
    **改文件一律用二进制模式或 Edit 工具**；靠「diff 规模对不上预期」抓到。
16. **多租户表读数为 0 先想 RLS。**
17. **蓝绿部署后活跃 Java 槽位可能在 10020 不是 10010。**（今天实测就在 10020）

---

## 7. 关键文件

| 文件 | 作用 |
|---|---|
| `backend/python/smartbi/gold/restaurant/restaurant_intent.py` | T3 提示词、`_build_spec`、`_apply_store_scope_guard`、`scope_only_refinement`、计划缓存 |
| `backend/python/smartbi/gold/restaurant/restaurant_intent_service.py` | `tiered_answer` 主链路、`_drop_planner_invented_metrics`、披露 |
| `backend/python/smartbi/gold/restaurant/answer_contract.py` | 答案契约（`required_elements` / `request_coverage`） |
| `backend/python/smartbi/gold/restaurant/restaurant_ops_router.py` | 各 resolver + `_markdown_table`（全站唯一表格拼装点） |
| `backend/python/smartbi/gold/customer_text.py` | `sanitize_customer_ai_text` —— 面向店长的文本清洗 |
| `backend/python/smartbi/scripts/restaurant_ai_eval.py` | 85 题回归电池 + 取数条件 + `--only` |
| **`backend/java/.../IntentExecutionOrchestrator.java`** | **Java 侧路由、写动词判据、19 条关键词规则（§5 的问题所在）** |
| `backend/java/.../FuzzyExpressionSeeder.java` | 25 条向量匹配种子 |

---

## 8. 复测命令

```bash
# 电池全量（⚠️ 20-30 分钟，读分数前先看「本轮取数条件」）
ssh root@47.100.235.168 "bash /www/wwwroot/cretas/code/scripts/cron/restaurant-ai-eval.sh"
# 结果：/www/wwwroot/cretas/logs/restaurant-ai-eval.log

# 🔑 日常验证只跑受影响的链（#2506，85 题 → 4 题，省 89%）
ssh root@47.100.235.168 "cd /www/wwwroot/cretas/code/backend/python \
  && set -a && . /www/wwwroot/cretas/.env.prod && set +a \
  && ./venv-current/bin/python -m smartbi.scripts.restaurant_ai_eval \
     --base https://admin.cretaceousfuture.com --only '<问句片段>'"

# 模型探针
ssh root@47.100.235.168 "cd /www/wwwroot/cretas/code/backend/python \
  && set -a && . /www/wwwroot/cretas/.env.prod && set +a \
  && ./venv-current/bin/python -m scripts.probe_llm_registry 2>&1 | head -14"

# 部署（prod 只从 main）
cd <main worktree> && git pull --ff-only origin main
bash scripts/deploy/deploy-smartbi-python.sh --env prod
```

### 有个开关没启用（明天再定）

```
RESTAURANT_PLANNER_ACCOUNT_PINS="MOCK_REST=<账号>"   # 加进 .env.prod 再重启
```

把电池租户的规划流量限制到单一账号 —— 既省额度，**更重要的是让电池分数重新变成代码的函数**。
⚠️ 今天所有账号都在崩，**钉一个死的等于把电池打死**，明天看哪个活着再定。
⚠️ 代价：被钉的租户不再走用户实际的那条链。**只该钉电池租户。**

---

## 9. 建议的第一步

1. **跑探针**看链头恢复没有（免费额度按日重置）
2. **跑一轮电池**拿干净基线 —— 看取数条件确认它是不是全冷、模型是不是只有 1-2 个
3. ~~**做 §5 第 1 步**（兜底 fail closed）~~ ✅ **已做**（见 §5 顶部更新块）。
   部署后**必须实测这一句**：`有没有哪个菜是在亏钱卖的，要不要下架`
   —— 判据是日志里出现 `[Branch:TieredFirst]`（说明进了 Python 语义权威），
   而**不是**再看一遍答案顺不顺眼。⛔ 单测只证明路由改对了，**证明不了 Python 答得好**。
4. 之后再谈 §5 第 2/3 步（Java 收口）+ 慢销榜自身的排序缺陷和过期时间窗（**仍未修**）

### ⚠️ 如果要回答「这个 AI 到底行不行」

**最高信息量的动作不是再跑电池，是让真实老板问一批。** 哪怕 20 个老板各问 10 句，
信息量超过 12 轮电池 —— 因为今天的 12 问已经证明：**电池覆盖到的地方它很扎实，
出了圈一半以上不行**，而电池的 85 题是我们自己写的。
