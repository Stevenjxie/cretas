# HANDOFF 2026-08-10 夜 — 餐饮 AI（链排序 / 三条答非所问 / 演示重开 / 答案表格化）

**13 个 PR 全部已合并并部署 prod。** #2455 → #2468。

> ⛔ **边界（owner 明确）：工厂端不碰。** 本轮只做餐饮。`factory_ai_eval.py` 每天
> 404 崩一次（`DEMO_FACTORY2` 租户已不存在）——**已知，刻意不修**。

---

## 一、当前实测状态

| 指标 | 值 | 说明 |
|---|---|---|
| 回归电池 | **84/85** | 三轮读数完全一致（20:09 / 20:15 / 20:20） |
| 中位响应 | 1.6s | p95 2.8s |
| CI 闸 | 7414 passed / 10 failed | 10 条与 clean origin/main 逐条相同（本地无 PG / 无在跑服务），**新增 0** |
| 演示扫码 | 可用 | `tenant=rest` → DEMO_REST，只读闸生效 |

重跑电池：
```bash
ssh root@47.100.235.168 "bash /www/wwwroot/cretas/code/scripts/cron/restaurant-ai-eval.sh"
# 结果落在 /www/wwwroot/cretas/logs/restaurant-ai-eval.log
```

---

## 二、今晚修掉的

| PR | 内容 |
|---|---|
| #2455 | 能力表 + 死模型沉底 + 超龄告警 |
| #2456 | [66] 不变式补「必须出现」方向（原断言永远不会红） |
| #2458 | [03] 澄清延续轮不再剥掉继承来的指标 |
| **#2459** | **自我推翻**：撤掉延迟排序键（它造成 83→61 的真实回归） |
| #2460 | 淘汰 5 个实测 403 的模型 |
| #2461 | [66] 真因：换时间按钮对服务层派发那条路被误扣 |
| #2462 | 按「计划合法性」排链（第三把尺子）+ 单跳预算 10s→20s |
| #2463 | 重开餐饮扫码演示（身份与只读闸必须同时开） |
| #2464 | [45] 门店名匹配到多家时给候选，不给死胡同 |
| #2465 | 一行日志把请求变成 500（日志不该有能力让请求失败） |
| #2467 | 菜品排行改 markdown 表格 + 全站唯一一处表格拼装 |
| #2468 | 再改三处：某菜的门店排行 / 各店菜品排行 / 渠道构成 |

---

## 三、链排序的最终形态

```
排序键 = (地板置底, 存活档, 计划合法档, 延迟粗档, 到期日)
```

| 位 | 判据 | 产出方式 |
|---|---|---|
| 地板置底 | `_TEXT_TAIL` 永远最后 | **结构契约**：aliyun 全过期那天槽不能变空 |
| 存活档 | 能不能调通 | `python -m scripts.probe_llm_registry` |
| 计划合法档 | 有没有编造提示词里没有的枚举值 | `llm_capability_rank --schema`，合法值从 `_build_t3_prompt` **现解析** |
| 延迟粗档 | 快≤5s / 未测 / 可接受≤20s / 超预算 | `_CAPABILITY` 的 p50 |
| 到期日 | use-it-or-lose-it | `_SAFE_MODELS` |

「没测过」在每一档都单独占一位，排在「实测好」与「实测差」之间——缺席不构成任何一侧的证据。

### 三把尺子，前两把都不行

| 尝试 | 尺子 | 为什么废 |
|---|---|---|
| 一 | 契约合格率 | 19 选 **18 满分**，地板题。并列后退化成纯延迟升序＝「最小最快的排最前」，**电池 83→61** |
| 二 | 与参照模型一致率 | 参照自己会烧完额度而死（glm-4.6 当天 403），死了全 0 |
| **三** | **计划合法性** | 判「有没有编说明书里没有的值」——客观、有区分度、不需要人写标准答案 |

> 🔑 **一个量，你刚判定它区分不出 A，就不能拿它去给 A 排序。**
> 我在 `_CAPABILITY` 注释里亲手写了「契约区分不出强弱」，然后还是把它当了排序键。

### ⚠️ 免费额度一天烧掉六个链头

```
deepseek-v3.2 → glm-4.6 → qwen3-next-80b → qwen3.5-plus → qwen3.7-max-05-20 → qwen3.7-flash
```

全是 403 `AllocationQuota.FreeTierOnly`。**「先用快过期的」把「最快烧完」和「排最前」
绑成了同一件事**——链头位置本身就是最大消耗源。08-13 那批 aliyun_c 还剩几条，
未来三天还会有。当前有效链头是 `qwen3.8-max`（11-01，三账号）。

📌 **电池分数部分取决于「今天哪个模型还活着」**——[51] 就是随链头轮换而红绿的。

---

## 四、答案表格化（进行中）

owner：回答要以**表格 + 文字**的结构给用户。

**前端本来就就绪**：RN 端 `MarkdownRenderer` 用 `react-native-markdown-display`
（markdown-it），GFM 表格开箱即用，`table/thead/th/tr/td` 样式 2026-07-24 就写好了。
**前端零改动**，缺口全在后端。

**已改 4 处**（全部复用 `_markdown_table`，全站唯一一处拼装）：
- 菜品销量排行
- 某菜的各门店销量排行
- 所选门店的菜品排行（每店一张）
- 堂食 vs 外卖渠道构成

**还没改**（下次接手从这里开始）：
- 餐段（午市/晚市/夜宵）表现
- 损耗/报损排行
- 折扣构成（`- {name}：占折扣 X%`）
- 库存预警清单
- ⛔ **不要改**：门店名单（单列）、折扣汇总里的单条事实——做成表格反而更差

改的时候注意两点（都不报错）：
1. 排行循环常常**同时在建 `ranked_entities` / `chart_*`**，表格只接管文本那一半
2. 多个循环（有金额/无金额）要合成**一张**表，金额列留「—」，别另起一张

---

## 五、还开着的三件

| 问题 | 现状 | 诊断 · 要动什么 |
|---|---|---|
| **[51]「下周需要多少兼职」** | 答不出，「查询维度超出能力范围」 | 规划器标成 `dimensions=['staff']`（按员工个人分），而排班分析没这个粒度。提示词里**写明了**问岗位时不要这么标，是模型没照做。⛔ **不要放宽 resolver 维度声明**——那会放行错粒度回答。正解是给 `_drop_unanswerable_mislabeled_dimensions` 的 `namers` 补一个 staff 判定，且必须像 ingredient 那样**走真实员工名册**判「点没点名」，不能写关键词表 |
| **偶发失败丢上下文** | 一次瞬时「语义规划暂时不可用」后，下一轮当成全新问题 | 系统本有防这个的设计（供应商不可用时用密封原句重编），但没覆盖「合并反问只答了一半」这个形状。⚠️ 要碰澄清链，而那片区域有 **5 条踩过事故的 UX 契约**（见 `HANDOFF-2026-08-07-restaurant-ai-round.md` 的撤回记录） |
| **`POST /ai-intents/execute` 缺 `userInput`** | 已从 500 修成正常走意图识别 | 要不要改成 400 是**产品决定**，本轮只修了「日志能崩」这一半 |

---

## 六、这一轮踩的坑（判据都已进 memory）

1. **同一时刻的多个读数不构成重复验证** —— artifact 两个端点同时 403，我当成「两条独立证据」，其实是同一个瞬时窗口。重试一次就好了，而我用一句推断替掉了重试。
2. **「同版本复测」比的是两个读数各自的时间戳 vs 最后一次部署的时间戳** —— 我拿凌晨 cron 的 80/85 当基线，中间隔着自己的部署。
3. **断言不要挂在排版上** —— 一天抓到三条（列表前导点 / 破折号 / 整个解析器）。换个排版就静默失效。
4. **测了一个函数不等于测了它被调用** —— 删掉调用点，4 条行为断言全绿。
5. **变异不红先验变异有没有生效** —— 两次手拼转义写错导致变异根本没应用。
6. ⚠️ **heredoc 的 `\n` 一天写坏文件四次** —— memory 里早写着「多行 Python 用 Edit」。**判据写进 memory 不构成约束，换掉动作才构成。**

详见 `memory/feedback_failure_count_is_not_defect_count.md`。

---

## 七、接手第一步

```bash
# 1. 确认链头还活着（今天烧掉六个，先看现状）
ssh root@47.100.235.168 "cd /www/wwwroot/cretas/code/backend/python \
  && set -a && . /www/wwwroot/cretas/.env.prod && set +a \
  && ./venv-current/bin/python -m scripts.probe_llm_registry 2>&1 | head -12"

# 2. 跑一轮电池拿当前基线（⚠️ 至少跑两轮，且都要在最后一次部署之后）
ssh root@47.100.235.168 "bash /www/wwwroot/cretas/code/scripts/cron/restaurant-ai-eval.sh"

# 3. 有模型 403 就淘汰（移除只需探针证据，永远不制造计费风险）
```

⛔ **`_SAFE_MODELS` 一条都不许新增** —— 计费闸需 owner 在控制台确认「免费额度用完即停」。
