# 交接 2026-08-10b · 真值对账闸 —— 2074 个数第一次有了外部锚

分支 `codex/claude-restaurant-generic-executor`，5 个 commit 已推。

---

## 一句话

把聚合结果**还原成生成器的源码参数**逐条比。prod 实测 **23 条锚全部对上**，
零模型调用，几秒跑完，已接进每 15 分钟的监控。

那个把米饭营收从 ¥34,839 放大成 ¥2,001,255 的扇出缺陷——当时我手写 6 行 SQL
才发现——现在**自动、当场红**。

---

## 一、为什么需要它

登记表能算 3168 个格子，而逐字核对过的只有 3 个。**跑得通不等于算对。**

而且这类错在所有现有探针上都是**绿的**：服务 200、答得出来、耗时正常、日志干净。
监控里那四个探针全在问「活着吗、答得出来吗」，**没有一个在问「算得对不对」**。

---

## 二、锚长什么样（23 条，四类）

```
比值锚 ×20    菜单价     revenue×菜品 ÷ sales_qty×菜品  ==  seed.py 的 price_cents
              食材成本   food_cost×菜品 ÷ sales_qty×菜品 ==  配方推导值

绝对量锚 ×3   菜品数 10 / 门店数 10 / 食材数 25  ==  种子清单长度

跨粒度锚 ×3   Σ(各菜品营收) 23,258,368.00  ==  折前营收 23,258,368.00
              折前 − 折扣 22,270,118.53     ==  净额 22,270,118.53
              全店毛利率 67.71%             ==  各菜品加权 67.71%

分布锚 ×3     渠道单量占比 0.6229/0.2782/0.0988  ==  _CHANNEL_WEIGHTS (0.62/0.28/0.10)
```

---

## 三、⛔ 四条纪律，全部是被推翻出来的

**1. 左边从生成器源码取，绝不从数据库取。**
从库里读出 128 再拿去比 128 —— 恒真式。第 4 轮审计我就是这么写的，被自己抓到。

**2. 右边必须走 `execute_cell`。**
手写 `SUM(i.amount)/SUM(i.qty)` 也是恒真式：`amount` 本来就是采集时按
`qty × price` 算的，比值恒等于单价，只验了采集。**判别力来自分子由登记表自己
选表达式**——扇出时它会选错，比值当场从 3.00 变成 160.55。

**3. ⛔ 不用 `_DISHES` 的第 4 列 `cost_cents`**——源码注释写着已弃用。
拿它当锚会天天报假警（水煮牛肉：弃用列 ¥29.00 vs 真实食材成本 ¥28.15）。

**4. 分布锚必须按单量比，不能按营收。**
权重是**每张订单**抽渠道用的。实测：按单量差 0.3 个点 ✓，按营收差 1.7 个点 ✗，
而容差是 2 个点——**只差一点就会天天报一个不存在的问题，而且报得很像真的**。

---

## 四、副本会漂，所以有同步闸

`_ANCHORS` 是从 `mock-platform/.../seed.py` **抄**的（prod 上没部署 mock-platform）。
抄了就会漂，而漂的方向恰恰是「锚跟着被改坏的数据一起变」，那时对账**永远绿**。

`test_truth_anchors.py` 在能读到源码的地方重算逐条比。**6 个变异全红**：
副本被手改 / 副本漏一道菜 / 锚用了弃用列 / 门店数被改 / 渠道权重被改 / 分布改用营收口径。

⚠️ 读不到源码时 **skip 并打印理由**——⛔ 不许让「闸没跑」伪装成「闸过了」。

---

## 五、接进监控（三个分支都在 prod 验过）

```
OK     truth=OK      退出码 0
FAIL   truth=FAIL    退出码 1，日志逐条列出对不上的
NORUN  truth=NORUN   退出码 1   ← 「闸没跑」必须报警
```

监控行现在长这样：
```
OK health=ok cap=17f/10s lat=7ms gate=ok llm_pool=usable=5/31 errs=0/15min truth=OK
```

⚠️ 验退出码**别用管道**——`bash x.sh | tail` 之后 `$?` 是 tail 的码。我第一次就这么量的，
看到 0 差点以为脚本没报警。

---

## 六、三类锚不是冗余——变异实测证明的

注入扇出后：

| 锚 | 结果 |
|---|---|
| 跨粒度对账 | ✅ `Σ(各菜品营收) 1.001 亿 ≠ 折前营收 2,326 万` |
| 单价比值 | ✅ 米饭 ¥3.00 → **¥160.55**，10 条全红 |
| **毛利率一致性** | ❌ **92.50% == 92.50%，没红** |

毛利率那条**两边同倍膨胀**——这正是「比值锚对同倍缩放是瞎的」，现在有了实测证据。
**各条锚覆盖不同的失败方向，缺一不可。**

---

## 七、覆盖与盲区

✅ **抓得到**：扇出、粒度选错、join 错、成本桥接错、单位错、采集算错、丢行、
过滤写错（绝对量锚）

❌ **抓不到**：**参数本身合不合理**（罗氏虾该不该 ¥128）——那要真客户。
但这类是**人能判断**的量，而 ¥6,490,180.61 人判断不了。
**这正是这个方案比「人工核对黄金样本」好的地方：把不可判断的绝对额，
还原成了可判断的参数。**

---

## 八、还没锚的

实测 78% 的提问落在「全店汇总」类格子上——跨粒度锚补了营收和毛利率两条主线，
但这些仍无锚：

| 格子 | 提问次数 |
|---|---|
| `sales_qty × all × summary` | 2,298（最常问） |
| `orders × all × summary` | 288 |
| `wastage_cost × all` | 137 |
| `revenue × date × trend` | 117 |

⚠️ 销量和订单数的全店汇总，与菜品维度用的是**同一个表达式**（都在明细/交易粒度），
所以「加总 == 全店」对它们是**近似恒真**——要锚得另找路子（例如订单数对生成器的
日均单量参数）。**这条我没做，别以为已经覆盖了。**
