# 交接 Prompt — 餐饮 AI（2026-08-10 夜）

> 把这份整个贴进新 chat 作为开场。它假设接手方**没有任何上下文**。

---

## 0. 你接手的是什么

白垩纪食品溯源系统（Cretas）里的**餐饮经营问答 AI**。餐厅老板用自然语言问经营
问题（营收、菜品、门店、毛利、排班），系统解析成结构化计划 → 确定性代码查库 →
生成答案。前端是 RN App（`AIChatScreen`），后端 Java(10010) 转发给 Python(8083)。

**主工作目录**：`C:\Users\Steve\my-prototype-logistics`
**本轮实际在 worktree 里干活**：`C:\Users\Steve\cretas-forecast-dow`

---

## 1. 目标（owner 原话，按提出顺序）

1. **长期目标**：「把所有的东西跑完，有问题就去修复，以结果为难分析的用 LLM，
   以后全量跑下来没有问题，而且**能力很强的餐饮 AI**。如果有任何**反复**的问题，
   才有长期的，并且采用**对抗性审计**去盘查自己的问题和方案。」
2. **能力推进的顺序**（早先定的）：归因 → 预测 → 建议 → 数据缺口。
   预测那一段已做完（回测闸 + 周内因子 + 自测误差），当前停在「建议 / 数据缺口」前。
3. 「**按能力把**」——LLM 链要按实测能力排序，不要按免费额度到期日排。
4. 「修吧，**但是只需要做餐饮的**」——演示登录只重开餐饮。
5. 「回答……**用表格 + 文字的结构输出在前端给用户**，这样更直观。」

---

## 2. ⛔ 硬约束（违反会出事，不是风格问题）

| 约束 | 说明 |
|---|---|
| **工厂端不碰** | owner 明确说过两次。`factory_ai_eval.py` 每天 404 崩一次（`DEMO_FACTORY2` 租户已不存在）——**已知，刻意不修**。别再提它 |
| **`_SAFE_MODELS` 一条都不许新增** | 那是计费闸。加模型需 owner 在控制台确认「免费额度用完即停」。**移除**只需探针证据（移除永不制造计费风险） |
| **prod 只从 main 部署** | 绝不从 feature 分支部署 prod（2026-05-30 事故：并发 session 互相覆盖 jar） |
| **worktree 隔离** | 任何代码任务开独立 worktree，off `origin/main` |
| **commit 锁范围** | `git commit -m "..." -- <paths>`，commit 前先 `git status`（husky 会顺走并发 session 的文件） |
| **禁止降级处理** | 不返回假数据。查不到就说查不到，绝不用相邻指标顶替——这是系统的核心承诺，答案里到处都是它 |
| **演示租户只读** | `DEMO_REST` 在 `cretas.demo.factory-ids` 只读名单里。⛔ 配演示身份**必须同时**上只读闸，只做一半 = 公开扫码演示拿到 `factory_super_admin` 写权限 |
| **`MOCK_REST` 绝不进只读名单** | 它是电池租户，需要完整写能力 |

---

## 3. 现状（2026-08-10 23:00）

| 指标 | 值 |
|---|---|
| 回归电池 | **84/85**，三轮读数完全一致 |
| 中位响应 | 1.6s（p95 2.8s） |
| CI 闸 | 7414 passed / 10 failed（10 条为本地环境缺件，与 clean `origin/main` 逐条相同，新增 0） |
| 今晚上线 | 13 个 PR（#2455 → #2468），全部已部署 prod |

**复测命令**：
```bash
# 电池（⚠️ 至少两轮，且都要在最后一次部署之后）
ssh root@47.100.235.168 "bash /www/wwwroot/cretas/code/scripts/cron/restaurant-ai-eval.sh"
# 结果：/www/wwwroot/cretas/logs/restaurant-ai-eval.log

# 模型探针（今天一天烧掉六个链头，接手先看这个）
ssh root@47.100.235.168 "cd /www/wwwroot/cretas/code/backend/python \
  && set -a && . /www/wwwroot/cretas/.env.prod && set +a \
  && ./venv-current/bin/python -m scripts.probe_llm_registry 2>&1 | head -12"
```

---

## 4. 还开着的活（按优先级）

### A. 答案表格化 —— 进行中，直接接着做

owner 要「表格 + 文字」。**前端零改动**（`MarkdownRenderer` 用
`react-native-markdown-display`，GFM 表格 + 样式 2026-07-24 就就绪了），缺口全在后端。

**已改 4 处**，全部复用 `_markdown_table()`（`restaurant_ops_router.py`，全站唯一拼装点）：
菜品销量排行 / 某菜的门店排行 / 各店菜品排行 / 渠道构成。

**还没改**：餐段表现、损耗排行、折扣构成、库存预警清单。
⛔ **不要改**：门店名单（单列）、折扣汇总的单条事实——表格化反而更差。

改的时候两个**不报错**的坑：
1. 排行循环常常同时在建 `ranked_entities` / `chart_*`，表格只接管文本那一半；漏了
   答案照常显示，只是图表和结构化上下文空了
2. 多个循环（有金额 / 无金额）要合成**一张**表，缺的列留「—」，别另起一张

### B. [51]「下周需要多少兼职」

唯一稳定失败的电池题。诊断已明确：规划器标成 `dimensions=['staff']`（按员工**个人**
分），而排班分析没这个粒度。**提示词里写明了**问岗位/工种时不要这么标，是模型没照做。

⛔ **不要放宽 resolver 维度声明**（`_RESOLVER_DIMENSIONS`）——那会放行错粒度回答，
比反问更糟，代码注释里专门警告过。

正解：给 `restaurant_intent_service._drop_unanswerable_mislabeled_dimensions` 的
`namers` 补一个 staff 判定。**必须像 ingredient 那样走真实员工名册**判「有没有点到
某个具体员工的名字」，⛔ 不能写关键词表——那是本仓反复在拆的东西。

### C. 偶发失败丢上下文

一次瞬时「餐饮语义规划暂时不可用」后，下一轮被当成全新问题。系统本有防这个的设计
（供应商不可用时用密封原句重编），但没覆盖「合并反问只答了一半」这个形状。

⚠️ 要碰澄清链，而那片区域有 **5 条踩过事故的 UX 契约**。2026-08-07 有人为了让
`各门店对比如何` 少问一句，改完发现代价是那 5 条，**做完之后撤回了**——结论是
「按钮链是产品的一部分，不是待优化的摩擦」。动之前先读
`HANDOFF-2026-08-07-restaurant-ai-round.md` 那段撤回记录。

### D. 之前撤回 / 待立项的

- **门店槽默认值 + refinement context**（任务 #20）：已撤回，待独立立项
- **多指标根治**：[56] 现在是把销量补进最完整那条 if 分支；根治是走登记表的
  `spec_to_cells` 多指标路径。**互斥 if 链表达不了「多选」**，每加一个可问指标
  组合数翻倍
- **能力题**：现在的「计划合法性」尺子只能判「有没有编造枚举值」，判不了「谁答得更准」。
  要真正按能力排，需要一道会让好模型和更好的模型分开的题

---

## 5. LLM 链排序的最终形态（今天反复三次才定下来）

```
排序键 = (地板置底, 存活档, 计划合法档, 延迟粗档, 到期日)
```

- **地板置底**是**结构契约**不是偏好：`chain[-1]` 必须是永不过期的地板，否则
  aliyun 全过期那天该槽变空
- **计划合法档**：`llm_capability_rank --schema`，合法值从 `_build_t3_prompt`
  **现解析**（不另写枚举表）
- **延迟只按粗档**（快≤5s / 未测 / 可接受≤20s / 超预算），⛔ **不能当连续键**
- **到期日**降为末位平手键
- 「没测过」在每一档单独占一位，排在「实测好」与「实测差」之间

**三把尺子，前两把都不行**：
1. 契约合格率 → 19 选 18 满分，地板题。并列后退化成纯延迟升序 =「最小最快的排最前」，
   **电池 83→61**（#2459 已撤回）
2. 与参照模型一致率 → 参照自己会烧完额度而死，死了全 0
3. **计划合法性** → 客观、有区分度、不需要人写标准答案

⚠️ **免费额度一天烧掉六个链头**（deepseek-v3.2 → glm-4.6 → qwen3-next-80b →
qwen3.5-plus → qwen3.7-max-05-20 → qwen3.7-flash，全是 403）。
**「先用快过期的」把「最快烧完」和「排最前」绑成了同一件事。**
📌 电池分数部分取决于「今天哪个模型还活着」。

---

## 6. 踩过的坑（判据，全部有实测代价）

**测量类**
1. **同一时刻的多个读数不构成重复验证** —— artifact 两个端点同时 403，我当成「两条独立
   证据」，其实是同一个瞬时窗口。**重测成本远低于误判成本时，先重测再下结论。**
2. **「同版本复测」比的是两个读数各自的时间戳 vs 最后一次部署的时间戳** ——
   不是「我特意连着跑了两次」的感觉。cron 产出的读数最容易被误当成「刚才那次」。
3. **读数全 0 / 全空时，除了怀疑被测对象，还要怀疑参照物和探针本身。**
4. **失败条数 ≠ 缺陷数** —— 24 条失败是一条链断在第 3 轮、后面 6+ 轮连坐。
   看到一长串失败先问「这些失败之间有没有父子关系」。

**断言类**
5. **断言不要挂在排版上** —— 一天抓到三条（列表前导点 / 破折号 / 整个解析器）。
   换个排版就是静默失效：不报错、不变红，从此什么都不测。
6. **写完一条断言先问「它靠什么变红」** —— 答不上来就不是断言，是注释。
7. **测了一个函数不等于测了它被调用** —— 缺陷往往在接缝上，绕过接缝的测试对它沉默。
8. **变异不红先验变异有没有生效** —— 今天两次手拼转义写错导致变异根本没应用。
9. **「闸不可能变红」的四种形状**：闸没跑 / 左右同源恒真式 / 被测数据里没有可失败的
   东西 / **规则表达不了这条判据，于是被填成空操作**。

**推理类**
10. **一个量，你刚判定它区分不出 A，就不能拿它去给 A 排序。**
    「我知道」和「代码知道」是两件事——判据写进注释不等于写进排序键。
11. **「没解析出 X」不等于「用户没提 X」** —— 把前者折叠进后者就是拿缺席当证据。
12. **闸说「不支持」时，要去看被判定的对象到底支不支持**，而不是接受闸的说法
    （今天为此把一个结论说反过一次）。
13. **这个闸的载体比它查的那张表多** —— 只查一个派发表 = 对第二条派发路径完全沉默，
    而沉默的方向是「误拒」，能躺很久。
14. **测试夹具引用的东西如果会随运维动，就从权威表里现算，不要写死**
    （同一天为此交了三次维护税）。⛔ 例外：断言的**主语**不能换，夹具才能换。

**工具类**
15. ⚠️ **heredoc 里的 `\n` 会被展开成真换行，一天写坏文件四次** ——
    **含反斜杠转义的多行内容一律用 Edit 工具**；脚本改文件按行定位替换，
    不要手拼带转义的查找串。
16. **多租户表读数为 0 先想 RLS**（今天又救了一次）。
17. **蓝绿部署后活跃槽位可能在 10020 不是 10010**，curl 连不上先查端口。

详见 `memory/feedback_failure_count_is_not_defect_count.md`（本轮全部判据）
与 `MEMORY.md` 索引。

---

## 7. 关键文件

| 文件 | 作用 |
|---|---|
| `backend/python/common/llm_router.py` | 链排序、`_SAFE_MODELS`、`_CAPABILITY`、`_PLAN_SCHEMA_VIOLATIONS` |
| `backend/python/smartbi/scripts/llm_capability_rank.py` | 能力/合法性打分（`--schema` / `--compare`） |
| `backend/python/smartbi/gold/restaurant/restaurant_ops_router.py` | 各 resolver + `_markdown_table` |
| `backend/python/smartbi/gold/restaurant/restaurant_intent.py` | T3 提示词 `_build_t3_prompt`、契约 `_t3_contract_violation` |
| `backend/python/smartbi/gold/restaurant/restaurant_intent_service.py` | `tiered_answer` 主链路、执行前闸 |
| `backend/python/smartbi/scripts/restaurant_ai_eval.py` | 85 题回归电池 |
| `scripts/deploy/deploy-smartbi-python.sh` | Python 部署（`--env prod`） |
| `scripts/deploy/deploy-backend.sh` | Java 部署（`--env prod`） |

---

## 8. 建议的第一步

1. 跑探针看链头还活着没有（今天烧掉六个）
2. 跑**两轮**电池拿当前基线
3. 从「答案表格化」剩下的几处接着做——那是 owner 最近一次明确要的，且路径已经验证过
