# 交接 2026-08-17 —— 报工↔库存打通、双端 E2E、legacy 退役起步

**授权**: Steve 全权（部署／合并／F006 真实写入与冲销／自行做产品决策）
**生产写入**: 全部冲销，逐项核回基线；阴性对照其他租户零变动

---

## 一、🔴 工厂现在**还不能开工** —— 差两件，都只有现场能做

1. **OTA 发了，但没有任何一台设备被确认拉到。**
   包本身四步验收过了（见第四节），但「app 会去拉」那一步是**配置与制品的匹配**，
   不是「观察到某台手机真的下载并重载了」。OTA 服务端不记 manifest 请求日志。
   ⇒ 需要有人拿一台工人手机：**杀掉 app 重开两次**（第一次后台拉、第二次生效），
   然后点第②道工序，确认打开的是第②道。
2. **业务数据没铺。** F006 只有 **2 个在制批次 / 2 道待报工序**，
   而且 **计划量 0 / 2** —— 一道都没配。没有计划量就没有超收上限。

⚠️ **不发 OTA 就让工厂开工，会直接撞上今晚修的三个缺陷**（点错工序、单位提交不了、已完成工序可重报）。

---

## 二、今晚合并上线的 PR

| PR | 内容 | 生产验证 |
|---|---|---|
| **#2751** | RN：点第几道就报第几道 + `COMPLETED` 工序不再可重报 + 领半成品单位传错 | ✅ 真跑 |
| **#2752** | 半成品缺配告警 100% 误报 + 报工人结构性为空 | ✅ 真跑 |
| **#2759** | **驳回报工时回退库存** | ✅ 真跑 |
| **#2760** | 退役口径 + 实测底账 + 冻结棘轮 + `listReports`/`getReportDetail` | ⛔ **未部署验证** |
| **#2771** | web-admin 报工审批页轮询 + 越权角标请求 | ⛔ 未部署验证 |
| **#2770** | Fable 5 退出路由（模型轴收成 Sonnet→Opus 两档） | docs |

**部署判据（四条，缺一不可）**：`DEPLOY_EXIT=0`、`RELEASE_FINAL_STATUS` 恰好 1 次、
**进程启动时间晚于制品 mtime**、**并且真跑一次那条路径拿到预期产出**。
⛔ 在 jar 里 grep 到标记**不算**判据 —— 它证明「发上去了」，不证明「跑得起来」。

---

## 三、客户那句抱怨，现在的链是通的

> 「上工序不报工，下工序就没有库存，而且经常会漏报」

在 **F006 批次 10759** 的真实 RN 界面（Expo Web 打到 prod）走完，**全程零审批**：

| 步骤 | 界面 | 生产库 |
|---|---|---|
| 第②道卤制 投入 3kg | 「投入已提交」 | 报工落在 **task 1786**（修前会落到 1785）|
| 第②道 产出 2.5kg 完工 | 出成率 83.33%，**批次未被关掉** | 半成品建行 2.50 |
| 第③道拼装分装 | **「上道半成品有 1 笔 · 卤制 · 可领 2.5 kg」** | 第③道 `wipAvailable` null → 2.5 |
| 第③道 领用 2.5 | 进入过程报工 | `DEPLETED`，OUT 流水 `balance_after=0.000000` |
| 文员在网页驳回 | 报工 `REJECTED` | **半成品退回 `0.00/0.00`** + `REVERSE` 流水 |

**阻断路径**：投入量 0 → 弹窗拦住；超领 99 > 2.5 → 提示 + **提交按钮 disabled**（事先拦住，不是事后解释）。

---

## 四、OTA 状态（自建 OTA，不是 EAS）

发布入口 `scripts/ota/push-bundle.sh production android`，服务 `ota.cretaceousfuture.com`。

| 步 | 读数 |
|---|---|
| a 命令成功 | `PUSH_EXIT=0` |
| b manifest 发了新的 | id `7ff9f81f…` → `17bdd709…`；`createdAt` 08-16 → **08-17 07:59Z** |
| c app 会去拉 | `rv=1.0.4 / channel=production` 与 `app.json` 对上；工人装的 apk 与 `v1.0.4.apk` **字节相同** |
| d 包里有改动 | `resolveInitialStepIndex`/`resolveInputUnit`/`resolveStepPhase` **旧包 0 → 新包 1**；阳性对照两包都 >0 |

⚠️ **标记必须用 ASCII** —— Hermes 的 `.hbc` 不以 UTF-8 直存中文。
⚠️ `effectiveInputUnit` 在两包里都读 0 是**正常的**（局部 `const`，Hermes 不保留局部变量名），
⛔ 别把它误读成「改动没进包」。

回滚：`./scripts/ota/rollback.sh 1.0.4 production 1786953489878`

---

## 五、在飞 / 等决定

**在飞（subagent）**：
- 单位问题三连诊断（`case→盒` 换算失败 + 调拨单「必须选具体规格」）
- PR #2767 cherry-pick 重开（撞了 squash 陷阱，见第七节）
- subagent 车道验证与建设

**等 Steve 一句话**：

1. **`#2727 → #2766 → #2769` 这条 RN 测试链要不要合。**
   #2727 落后 main 47 个 commit。⚠️ 合并顺序必须是 2727 → 2766 → 2769 ——
   `integration/screens` 要到 #2727 那条链才**真的会被执行**，基在 main 上断言只编译不运行。
2. **web-admin 那 5 次请求，实际看到的是「同一秒 5 个」还是「隔一段打一次」**。
   两者读数一样、**修法相反**。#2771 按后者修的（keep-alive 下 `onUnmounted` 不触发，定时器永不停）。
   若是前者，归因错了，要回查重复挂载。
3. **驳回常用语**需要车间自己的说法（现在可选项是「同意 / 已核实 / 金额过高需总监审」，
   驳回一条报工却要从里面挑）。机制是通的，缺的是内容。
4. **RN 离线草稿要不要做** —— 取决于车间手机连 wifi 还是 4G、报工会不会断。
   ⛔ 没有这个读数不要动手，它不便宜。

---

## 六、生产状态（已全部冲销回基线）

```
reports_F006_live   = 0
SFI_F006            = 331 | 2.00 | 0.00 | 2.00 | AVAILABLE   (孤儿行, batch_id 为 NULL)
SFI_txn_F006        = 0
tasks               = 1785:COMPLETED:6.0000  1786:PENDING:0.0000  1787:PENDING:NULL
batch_10759         = IN_PROGRESS
lineage_F006        = 45      (删掉了 9 条今晚写的; 客户真实数据最新是 2026-06-12)
other_tenant        = 0
```

⚠️ **冲销的 FK 顺序**：先删 `semi_finished_inventory_transactions`，再删 `semi_finished_inventory`。
⚠️ **蓝绿会在 10010 / 10020 之间换边** —— 按端口找进程的脚本两个都要认。
⚠️ **查行数一律带 schema 限定**（本库 7 个 schema；`f006_clear_71` 里有 3 条报工，
`public` 里是 0 —— 挑错一行结论完全相反）。

---

## 七、⚠️ 已澄清的错误结论（别再照着做）

| 曾经的说法 | 真相 |
|---|---|
| 「42/186 工序没配半成品产出编号 ⇒ 产出静默不入库」 | **错**。legacy 路径没配会自己派生编号照样入库。⛔ 别派人去补那 144 道 |
| 「打卡两套并行在线上用着，迁移要先合数据」 | **错**。`batch_work_sessions` 生产上 **0 行**，那一步是删死代码 |
| 「`getSummary` 对应 `/reports/last`」 | **错**。`/reports/last` 对应 `getLastReport`，而它**零生产调用点** |
| 「`getReports` 有 9 处调用」 | **错**。那是把测试算进去了，生产只有 **2 处** |
| 「scanner 用 `effort: low`」 | **错**。Anthropic 的只读 Sonnet `explore` 是 `xhigh`；轴是**判断密度**不是只读与否 |

**这五条的共同形状**：从代码/命名/目录结构推出一个自洽的故事，
而**否定它的东西就在一条命令之外**（表行数、端点清单、调用点、官方样本）。

⇒ 判据：**说「两套并行」之前先数两张表的行数；说「有对应实现」之前先把端点清单贴出来逐条对。**

---

## 八、下一轮从哪开始

1. **先合并 / 处理第五节那四个待决项**（都需要你一句话）
2. **legacy 退役第 3 步起**：见 `docs/decisions/2026-08-17-legacy报工栈退役.md` 第六节
   （第 1 步冻结棘轮已合，第 2 步补端点已合一半，`getSummary`/`getHistoricalAverage` 在飞）
3. **#2760 / #2771 补部署验证** —— 它们合了但没在生产上真跑过

⛔ **不要先写代码** —— 先把上面这些账结掉。

---

## 九、今晚最该记住的一条

合并的东西里，**最有价值的不是写出来的代码，是几个独立 agent 推翻我的那五次**。
五次订正，全部来自「有人去量了我没量的东西」。

⇒ **对抗性核对的收益，高于把同样的时间花在多写一个修复上。**
