# 交接 — 餐饮 AI（2026-08-11）

> 假设接手方没有上下文。前一份是 `HANDOFF-PROMPT-2026-08-10-restaurant-ai.md`，
> 那份里有三处说法**已被实测推翻**，见 §1。

---

## 0. 今天合入并部署了什么（10 个 PR，全部已上 prod）

| PR | 内容 |
|---|---|
| #2470 | `wants_margin` 假拒修复 [27] + 四处答案表格化 + 修一条永不变红的断言 |
| #2472 | 电池每轮报「取数条件」（缓存冷热 + 服务模型） |
| #2473 | 表格化收口（盘亏/配方成本/领料），`_PENDING_TABLE` 清空 |
| #2475 | **空行被清洗函数吃掉——8 张表全被并成一坨** |
| #2476 | 表格结构闸（此前不存在的检查维度） |
| #2485 | 供应商不可用的 fail-closed 留痕 |
| #2487 | 门店范围 refinement：spec + 三块基建（零行为变更） |
| #2491 | 接上 refinement —— 收窄不再把原问题丢掉 |
| #2493 | 门店默认单源化 —— 延续轮不再由模型 `missing_fields` 说了算 |

---

## 1. ⛔ 上一份交接里被推翻的三处

| 它说 | 实测 |
|---|---|
| 基线 84/85，三轮读数完全一致 | 三轮同版本读数是 **83/82/84**；且全部早于 23:35 部署，而 23:09 改的是**量尺本身**（`restaurant_ai_eval.py`） |
| [51] 是唯一稳定失败的题 | [51] 已通过；当时稳定失败的是 [27] |
| 「还没改」清单 4 处 | 实际还有 3 处同形状（缺货/配方成本/领料），已在 #2473 转完 |

---

## 2. 🔴 今天最贵的两条判据

### ① 两个读数之间有因果链，就不构成重复验证

「两轮电池读数完全一致」是**构造出来的**：

    r1（冷）  计划缓存命中 2  / 真解析 147
    r2        计划缓存命中 59 / 真解析 24

r2 的 85 题里 59 题直接吃了 r1 刚写进 `_SEMANTIC_PLAN_CACHE`（进程内，TTL 6h）的
计划，**结构上不可能与 r1 不同**。

更贵的一半：**缓存把非确定性伪装成稳定**。[27] 连过 12 轮再连挂 2 轮，转折点正好
压在部署重启上，看着像那次部署的回归；落库记录显示**同代码同模型**下 21:16 通过、
21:25 失败 —— planner 在摇摆，部署重启 = 重掷骰子。

📌 电池分数是 `代码 × 今天哪个模型还活着 × 缓存冷热` 的函数。#2472 起每轮都会打印
这三项，**读任何分数前先看那一段**。

### ② 改渲染就必须看渲染

8 张 markdown 表格上线后**全被并成一坨**（表头前的空行被下游一个跟表格无关的清洗
函数 `sanitize_customer_ai_text` 整行丢掉）。四个 PR、两轮 85/85 电池、CI 全绿，
**没有一条断言看得见**——因为这一路的断言只有两种形态：

- 子串检查（`"销量" in answer`）→ 字还在
- 源码检查（`"_markdown_table(" in <该函数源码>`）→ 调用还在

⛔ 特别注意电池里的 `flat = " ".join(message.split())` 把空白压平 ——
**任何基于 flat 的断言在结构上就不可能发现排版问题**。

---

## 3. 🔴 还开着的三件（有实测证据，按优先级）

### ① 门店片段没进确定性匹配层就被拦了（根因，最该先修）

prod 实测（在「本月米饭的销量是多少」答完之后输入）：

| 输入 | 实际返回 | 判定 |
|---|---|---|
| 「模拟·长宁龙之梦店**店**」多打字 | 正确解析出该店数据 | ✅ |
| 「长宁龙梦店」**打错字** | 「没有找到名为「长宁龙梦店」的门店」+ 给出路 | ✅ |
| **「龙之梦」打不全** | 「我识别到的问题对象与准备执行的分析范围不一致…」 | ❌ |
| **「长宁」更短** | 同上 | ❌ |

⚠️ **「龙之梦」和「长宁」在 MOCK_REST 都唯一命中一家店**（`模拟·长宁龙之梦店`）——
信息完全够，且 `_canonicalize_store_mention` 里的包含匹配 SQL 早就写好了
（精确优先 → 包含 → 按名字长度排序 → 取前 6）。

**缺的不是匹配能力，是这个片段压根没被当成门店名交给匹配层**，上层先判定
「对象与范围不一致」把它拦了。**打不全比打错字更糟**，而打不全是最常见的输入方式。

⛔ **不要为此引语义置信度**。三个代价：在一条本来确定性的路上新增 LLM 调用
（架构红线，08-07 为此撤回过）／置信度随模型强弱漂（今晚整晚都在拆这个形状）／
多一个会漏的载体。确定性匹配 + 候选按钮已能覆盖打不全、多打字、歧义三种。

**owner 拍板**：唯一命中**也要**反问确认（给按钮），不许直接答 —— 唯一命中不等于
用户想的就是它。命中多家 → 候选按钮（#2464 已做）；0 家 → 现在的诚实拒答（已对）。

### ② 内部概念词漏进面向店长的答案

上面那句「我识别到的**问题对象**与准备执行的**分析范围**不一致」就是。
`sanitize_customer_ai_text` 有一张术语替换表，但它是**手写清单** ——
本仓判据：「判据里出现手写清单就问，这张表错了会怎样」。

**owner 拍板**：置信度这类技术内容绝不出前端。

正解不是往表里再补几个词，是**一道闸**：面向店长的答案里不许出现内部概念词，
而那张词表从代码里的常量名/枚举**现算**，不手写。

### ③ 电池两条断言挂在模型措辞上

[02]/[11]「本月」期望字面量「哪一组门店」，而 #2493 之后的实际返回是：

    你想查看哪家门店的米饭销量？
    按钮：全部门店 / 模拟·普陀真如社区店 / 模拟·杨浦五角场店 / …

**行为是对的**（带按钮的门店反问，正是 #2493 要的），断言过期。
改成按**按钮结构**判（含「全部门店」+ 至少一个门店名），与措辞无关。

---

## 4. ⚠️ 我自己今天犯的、下一个人容易重犯的

1. **方向搞反一次**：给门店默认块补 `and not is_continuation` 时，我先判断它
   「与 owner 拍板的乙相反」并撤回；后来发现它只与**链内**相反，与首轮的乙不冲突。
   最终 #2493 用的就是那一行。
   📌 判据：**两处期望互相矛盾时，先确认哪一处代表已拍板的产品意图。**

2. **一次大改被 11 条既有契约拦下**：把延续轮也改成走乙，
   `..._store_buttons_survive_t3_outage` 等 6 条当场红 —— 它们守的是
   「时间 → 门店按钮 → 答案」这条**零 LLM** 确定性链，其中两条守的是
   **T3 不可用时唯一还走得通的路径**。
   📌 08-07 撤回记录的结论在这里成立：**「按钮链是产品的一部分，不是待优化的摩擦」**
   —— 但它只适用于**链内**，不适用于首轮。

3. **载体漏了一半**：#2485 给「餐饮语义规划暂时不可用」加留痕，只覆盖了
   `restaurant_intent.py` 的 2 处；**`chat.py:1557` 和 `chat.py:2476` 还有两处没装**。
   今天 [40] 真的走到了没装的那两处，于是**没有任何痕迹**。
   📌 这是当天早上刚记过的判据（「这个闸的载体比它查的那张表多」）当天又犯。

4. **我写的一条闸是空的**：`..._outcome_does_not_depend_on_what_the_model_reported`
   第一版两种模型自述走的是同一条路（上游块只存在于 semantic-first 路径，而测试
   没传 `semantic_first=True`），压根没触发分叉。**是变异把它暴露出来的。**
   📌 **写完一条断言先问它靠什么变红**，然后真的去变异一次。

5. **`pathlib.write_text` 在 Windows 静默转 CRLF**：一次 commit 变成
   979 insertions / 746 deletions（预期 ~110 行）。靠「diff 规模对不上预期」抓到。
   改文件一律用二进制模式或 Edit 工具。

---

## 5. 复测命令

```bash
# 电池（⚠️ 读分数前先看它打印的「本轮取数条件」）
ssh root@47.100.235.168 "bash /www/wwwroot/cretas/code/scripts/cron/restaurant-ai-eval.sh"
# 结果：/www/wwwroot/cretas/logs/restaurant-ai-eval.log

# 模型探针
ssh root@47.100.235.168 "cd /www/wwwroot/cretas/code/backend/python \
  && set -a && . /www/wwwroot/cretas/.env.prod && set +a \
  && ./venv-current/bin/python -m scripts.probe_llm_registry 2>&1 | head -12"
```

⚠️ 今天服务链头已经全部落到 `qwen3.7-max-2026-05-17`（p50 10.7s），
一轮电池要 20+ 分钟，且 [39]/[74] 这类模型噪声会浮上来。**分数波动先看取数条件。**
