"""措辞轮换：同一份数据，换着说法讲。

🔴 Steve 2026-08-07 拍板：
> 「答案一样是指里面的数据是一样的，但是回答的方式可以不一样。这样子的话…
>   让她觉得我真的是在跟一个人说话。」
> 「我们还是用这种多套模板轮换吧…**不能说让 LLM 去做这种措辞的不确定性**，
>   这个有延迟，而且很没必要。」

⛔ 三条硬约束：
  1. **只换措辞，绝不碰数字。** 变体里不许出现任何数值占位以外的数字。
  2. **零 LLM、零延迟。** 纯查表 + 取模。
  3. **可复现。** 同一个问句在**同一天**必须给出同一句措辞 —— 否则用户刷新一次
     发现说法变了，会怀疑数据也变了。跨天才换，既有变化又不制造疑心。

判据：变化的价值在「读起来像人」，而不在「每次都不一样」。
把随机性引进来会让测试变脆、也会让用户困惑；用 (问句, 日期) 做种子两者都避开了。
"""
from __future__ import annotations

import datetime as _dt
import hashlib
from typing import Optional, Sequence


def pick_variant(
    variants: Sequence[str],
    key: str,
    *,
    today: Optional[_dt.date] = None,
) -> str:
    """从若干等价说法里挑一句。

    :param variants: 等价措辞。**必须语义相同**——这里不做校验，因为「意思一样」
                     没法用代码判；靠的是写变体的人和 review。
    :param key:      稳定种子（一般是归一化后的问句）。同 key 同日 → 同一句。
    :param today:    注入日期便于测试；默认当天。

    空列表返回空串（fail-open：宁可没有措辞，也不要抛异常打断问答）。
    """
    if not variants:
        return ""
    day = (today or _dt.date.today()).isoformat()
    digest = hashlib.sha256(f"{key}\x1f{day}".encode("utf-8")).digest()
    return variants[digest[0] % len(variants)]


#: 顺带提示的引子。⚠️ `{n}` 是条数占位，**唯一允许的数字来源**。
HINT_LEAD_IN = (
    "⚠️ 顺带 {n} 件事：",
    "⚠️ 另外有 {n} 件事想提醒你：",
    "⚠️ 还有 {n} 处值得看一眼：",
    "⚠️ 顺手帮你留意到 {n} 件事：",
)

#: 门店范围取了默认值时的披露。⛔ 说法可以变，「说了这件事」不可以省。
STORE_SCOPE_DISCLOSURE = (
    "（范围：{scope}。想看单店直接说门店名即可。）",
    "（以上是{scope}的合计。要看某一家，说门店名就行。）",
    "（这里按{scope}算的。想单独看一家店，报个店名给我。）",
)

#: 时间窗取了默认值时的披露。同上：措辞可变，**披露本身不可省**。
TIME_RANGE_DISCLOSURE = (
    "（你没有指定时间范围，以上按**{window}**计算；想看别的区间可以直接说，例如「最近7天」。）",
    "（时间范围你没说，我按**{window}**算的；要换区间直接讲，比如「最近7天」。）",
    "（默认取了**{window}**这个区间；想看别的时间段，说一声就行，例如「最近7天」。）",
)


# ─── 收尾建议句 ──────────────────────────────────────────────────────────────
#
# 🔴 这一批是「最像模板腔」的地方：数字每天变，收尾那句话一字不变。老板每天问
#    同一个问题，读到的是一模一样的一段话 —— 那正是 Steve 说的「让她觉得我真的
#    是在跟一个人说话」缺的东西。
#
# ⛔ 但有些收尾句**背着一条必须说的话**（折扣不承诺带来增量、比价不承诺能省钱）。
#    轮换措辞时**绝不能把该说的那句轮换掉** —— `REQUIRED_TOKENS` 登记每个池
#    「每一条变体都必须包含」的标记，由 `test_phrasing_rotation.py` 逐条强制。
#
# ⇒ 判据：**说法可以变，「说了这件事」不可以省。**

#: 菜品毛利收尾。纯建议，无免责义务。
DISH_MARGIN_CLOSING = (
    "建议：毛利低的菜先看是不是食材涨价，再看售价有没有跟上；两头都没问题就考虑调整分量或下架。",
    "接下来可以这样看：先查这几道菜的食材进价有没有涨，再对一下售价；都正常的话，就该想想分量或者要不要留着了。",
    "怎么用这份榜：低毛利的菜，先排除食材涨价，再排除售价没调；两条都排除了，剩下的就是分量和去留的问题。",
)

#: 堂食外卖收尾。纯建议。
CHANNEL_MIX_CLOSING = (
    "建议：分别看堂食和外卖的客单价与毛利；外卖占比高时先查包装、出餐时长和平台抽佣，堂食占比高时优化翻台与套餐引导。",
    "往下可以拆开看：堂食和外卖各自的客单价、毛利分别是多少。外卖偏重就盯包装成本、出餐速度和平台抽成；堂食偏重就盯翻台和套餐搭配。",
    "两个渠道要分开算账：外卖占比高，先查包装、出餐时长、平台抽佣这三项；堂食占比高，重点在翻台率和套餐引导。",
)

#: 折扣收尾。⛔ 每条**必须**否定因果（这个 schema 支撑不了「折扣带来增量」）。
DISCOUNT_CLOSING = (
    "说明：这里只描述让利规模，**不能**据此说折扣「带来了」多少额外营收 —— 现有数据无法支撑那个因果结论。要判断值不值，需要把同期的单量与毛利一起看。",
    "一句边界：以上只是让了多少利，**不能**推出折扣拉来了多少生意 —— 那需要「不打折会卖多少」这个反事实，库里没有。要看值不值，得同时看单量和毛利。",
    "读的时候注意：这是让利的规模，**不代表**折扣带来了同等的营收增长；库里没有反事实数据，做不了这个归因。判断划不划算要配合同期单量与毛利。",
)

#: 供应商比价收尾。⛔ 每条**必须**否定「价差 = 能省」。
SUPPLIER_PRICE_CLOSING = (
    "说明：只在**同一食材、同一单位**内比价 —— 不同食材的单价没有可比性，同一食材不同单位（kg / 箱）也不合并。价差大**不等于**换供应商就能省下这个比例，还要看用量、供货能力和品质，这些数据这里没有。",
    "口径提醒：比价只在同一食材、同一单位里做，跨食材或跨单位都不可比。另外价差大**不等于**换一家就能省这么多 —— 用量、供货能力、品质都会影响，那些数不在这张表里。",
    "怎么读这份价差：同一食材、同一单位才可比，跨品类的单价没有意义。还有一点，价差**不代表**可省金额，实际能不能换要一并看用量、供货稳定性和品质。",
)

#: 池 → 每条变体都必须包含的标记（任一即可）。⛔ 新增带免责义务的池必须登记。
REQUIRED_TOKENS = {
    "DISCOUNT_CLOSING": ("不能", "不代表"),
    "SUPPLIER_PRICE_CLOSING": ("不等于", "不代表"),
    "STORE_SCOPE_DISCLOSURE": ("{scope}",),
    "TIME_RANGE_DISCLOSURE": ("{window}",),
    "HINT_LEAD_IN": ("{n}",),
}
