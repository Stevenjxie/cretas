# 交接 2026-08-17 —— 工序报工实时入库：上线 + 生产 E2E + 两轮对抗审计

**授权**: Steve 全权（部署／合并／F006 真实写入与冲销／自行做产品决策）
**Goal/Working prompt**: `docs/dispatch/PROMPT-工序报工实时入库-goal-and-working.md`
**生产写入**: 全部冲销，逐项核回基线；阴性对照其他租户零变动

---

## 一、GOAL A 达成：生产上真的走通了

在 **F006 批次 `10759`** 按真实产品路径走完整链，**全程零审批、零小结**：

| 步骤 | 生产读数 |
|---|---|
| 第②道领用 2.0kg | `consumed 0→2.00`、`available 2.00→0.00`、`DEPLETED` |
| 第②道产出 1.5kg | **立刻**出现新行 `E2E-SEMI-LUZHI`，挂 `sourceWorkProcessTaskId=1786` |
| 🔴 超领 99kg | **409 `WIP_INSUFFICIENT`**「WIP 余额仅 1.5 kg，不能领 99 kg」 |
| 第③道领 1.5 / 产出 120 盒 | ✅ 逐道入账，新行挂 task 1787 |
| 关单 | `completed: true`，**所有工序完成**才关 |
| 代报工 | `worker_id=1311`（被代者）／`operator_id=1310`（操作人）—— 两者分开 |

⚠️ **这条链此前走不通的三个成因，现在三个都修了**（前两个昨晚，第三个今天）：
产出等审批 → 一报工关整批 → **工序没配半成品产出编号导致静默不入库**。

## 二、本轮合并上线的 PR

| PR | 内容 |
|---|---|
| **#2731** | 半成品领用补写 **OUT 流水**（此前只有进账没有出账）+ E2E 走通证明 + 两份 prompt |
| **#2734** | OUT 流水的 `balanceAfter` 算成负数 —— 构造点在字段改完之后 |
| **#2737** | **报工幂等**（生产实测重复提交双扣）+ 接上 `semiOutputNotConfigured` |
| **#2743** | 幂等键作用域加上工序 —— 同号会跨工序把报工吞掉 |

**部署判据全绿**：`RELEASE_FINAL_STATUS=deployed` 恰好 1 次、
进程 `10:23:06` 晚于制品 `10:22:42`、且**真跑一次拿到预期产出**。

## 三、E2E 与对抗审计抓到的 5 个真问题

单测全绿时**一个都看不见**：

| # | 问题 | 怎么发现的 |
|---|---|---|
| 1 | 半成品**只有进账没有出账**（两次领用，0 行 OUT） | 生产 E2E 读流水表 |
| 2 | F006 只有 **42/186** 工序配了半成品产出编号，客户在用的批次**一道都没配** ⇒ 产出**静默**不入库 | `output-options` 返回 `{"items":[]}` |
| 3 | 我修 #1 时引入 `balance_after=-2.000000` | 部署后真跑一遍读流水 |
| 4 | **重复提交双扣**（领 0.5 扣 1.0） | 对抗审计第 1 轮，生产上打两次 |
| 5 | **同号跨工序把报工吞掉**（工人报了、系统说成功、什么都没记） | 对抗审计第 2 轮 |

**并发超领是好的**（实测：两请求同时各领 1.5 > 余额 2.0 → 一个 200 一个 409，余额不变负）。

## 四、🔴 我自己踩的坑（每条都自己抓住了，写下来防复发）

| # | 坑 | 判据 |
|---|---|---|
| 1 | 探针打错端口 → `HTTP=000` 却读到**上一轮遗留的响应**，差点把昨晚的 409 当成今天的结论 | **每次请求前清场** + `HTTP=000` 直接中止 |
| 2 | `--base-sha` 传成 HEAD → `selection: none`，实质步骤全成功却 `exit=1` | `--base-sha` 传**起点**，看 `RELEASE_SELECTION` |
| 3 | **测了 helper 不是测接线** —— 把 `txnRepo.save` 包成 `if(false)`，纯函数测试依然全绿 | 每个修复配一条**走真实 service** 的断言 |
| 4 | 接线测试断言了显眼的 `quantity`，**漏了派生的 `balanceAfter`** —— 而错的正是它 | **按最不显眼的那个验收** |
| 5 | 后端发 `semiOutputNotConfigured`，RN **零消费端** | 「产出端有了、消费端收不到」——今晚**第四次** |
| 6 | 断言「应该 3 处调用点」当场失败，**实际 6 处**（`head -4` 骗了我） | **先数再改**；⛔ 不用 `head` 数东西 |
| 7 | 把「前端会重新生成号」当保证 → 同号跨工序吞报工 | **约定不是机制**，约定只有人记得才生效 |
| 8 | 想清孤儿 node 进程，查了才发现是 **Cursor + MCP server** | ⛔ **不许盲杀 node**，先看 command line |

⚠️ `safe-commit` 的「已 squash 分支」闸**三次**拦住我复用旧分支 —— 闸在替我干活。

## 五、产品决策（各留设计卡）

- `docs/decisions/2026-08-17-未配半成品产出编号必须出声.md`
  —— 做完但明说做了一半，⛔ 不拒绝（会挡死 144 道存量工序）、⛔ 不静默（用户以为成功了）。
  判据**必须窄**：只在「后面还有未完成工序」时出声，末道安静。
- `docs/decisions/2026-08-17-报工幂等用客户端请求号而非时间窗.md`
  —— ⛔ 不照抄 legacy 的 5 分钟窗：报工是**合法高频**动作，时间窗会误拦正当操作，
  而**会误拦的闸最后一定被绕开或关掉**。

## 六、仍然没做（诚实清单）

| # | 项 | 说明 |
|---|---|---|
| 1 | **RN Expo Web E2E**（GOAL B5） | 未跑。RN 侧只做到 tsc 0 error + 单测 113/1326 全绿 |
| 2 | **web-admin Playwright headed E2E**（GOAL B6） | 未跑 |
| 3 | **RN 离线草稿 + 客户端生成 `clientRequestId`** | 后端幂等已就绪，**前端还没开始传号** ⇒ 幂等目前只对显式传号的调用方生效 |
| 4 | **文员级联修改 + 小结转核对** | 设计定了，未实现 |
| 5 | **legacy 报工栈退役**（`WorkReportingServiceImpl`） | 它的 `workerId` 仍被忽略；yield 栈已支持代报工 |
| 6 | **42/186 工序配置缺口** | 已让它出声，但**配置本身要业务侧补** |
| 7 | RN 4 个集成测试剩 **35 条**内容断言 | PR #2727，待逐条判「测试过期 vs 屏幕回归」 |
| 8 | **台账 `ACTIVE.md` 未更新** | 按规矩只有协调者能改 |

⚠️ **第 3 条最要紧**：后端幂等已上线，但 RN 不传 `clientRequestId` ⇒
**工人在 App 上双击仍然会双扣**。这条不做，#2737/#2743 对真实用户等于没生效。

## 七、给下一个人的判据

- 生产 E2E 脚本在 `scratchpad`（走查 + 冲销 + 阴性对照全套），端口要先确认蓝绿在哪边
- **冲销 SQL 的 FK 顺序**：先删 `semi_finished_inventory_transactions`，再删 `semi_finished_inventory`
- 部署后**必须真跑一次那条路径**，⛔ `DEPLOY_EXIT=0` 和 grep 到字符串都不是判据
