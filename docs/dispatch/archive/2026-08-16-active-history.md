# Dispatch 完成记录 — 2026-08-16

## 已完成

- `ML-LIUSHANMEN-LABEL-ORACLE-MATCHING-20260816` — `review` — Owner: `/root` — Base SHA: `9af4cf0692cc39ca713fd47e629d14d193021684`。完成 tray→crop→label→assignment→verdict 分层诊断、真实人工托盘训练数据构建、多个 RTX 3060 候选训练/回放及 fail-closed 晋级；所有发生缺陷回退、正常误报回退或延迟超限的模型/切片候选均判退，生产 ONNX 未变。最终修复聚焦已证实的确定性缺陷：`min_crop_px` 默认值由 120 调至 150（可由 `LABEL_QC_MIN_CROP_PX` 覆盖），过小盒子改为 `UNJUDGEABLE` 并进入人工复核，不再在未运行标签推理时误报 `BOTH_MISSING`。最后 4 条人工复核均为 `reviewed=true, source=human`，但同属一个生产任务，正式缺陷门禁只追加 1 条且明确记录模型选择偏差；追加式真值侧车不改保护集 manifest/原图，使门禁恢复为 7 个有效缺陷+19 张正常图。正式保护回放中 120→150 的缺陷命中 5/7、目标盒覆盖 6/7、正常误报 0→0，CPU P95 4396.850→4437.071 ms，晋级 gate 通过；一次性 sealed final 为 20 张/5 任务/2 SKU，双方正常误报均为 0，CPU P95 4148.846→4418.319 ms（+6.50%），normal-specificity gate 通过并写入独占消费标记。sealed final 全为正常图，故不能充当缺标召回证明；新增缺陷又是模型选择样本，亦不得冒充无偏召回估计。VisionLab 229/229、Python label-QC 41/41、Java `LabelQcAnalysisClientTest` 3/3、Ruff 与 `git diff --check` 通过；提交时为 `NOT_DEPLOYED`，生产业务写入 0、原图修改 0，scope 锁已释放。
- `AUDIT-E2E-CREDENTIAL-DEFAULTS-20260816` — `review` — Owner: Codex automation `/root` — Base SHA: `96fd2ed028073b3eb05d9971b92474ee0fb7c7b3`。当天审计确认 `web-admin/e2e-auth-helper.ts` 虽已建立环境变量口令边界，三个公共 helper 默认参数仍保留 tracked `factory_admin1/123456`，且 `loginOrReuseSession` 在未配置口令时会先发必然失败的登录请求。现已改为环境变量默认值；直接登录 helper 对空口令在网络前 fail closed；会话复用 helper 在空口令时直接读取受控 storageState。新增 2 条行为测试证明零网络请求与 F006 token 复用；目标 Vitest 2/2、`vue-tsc --noEmit`、`git diff --check` 通过。未修改运行时前后端、数据库、生产配置或业务数据，严格 `NOT_DEPLOYED`。
- `SOP-RAG-SYNC-20260816` — `merged` — Owner: `/root` — Base SHA: `05541a86883273283e43dcc5320adf384f4cb695`。按上次基线 `9fbe5db7474d865a3ac059f789aeeedfd5b3d8de` 至当前 main 完成工厂/餐饮独立 SOP、AI Assist、既有 canonical KB source 与确定性回答合同同步；内容 PR #2684 合入 `195ea1a2a682b5398b7c62dbf39a0ab56c84385d`，真实餐饮 canary 发现宽泛 guard 抢占后由修复 PR #2685 合入 `c5e5ff638e9e0e3e3f43797c214bd196b91d36e0`。目标测试最终 `104 passed`，`py_compile`、HTML/内联 JavaScript parse、`git diff --check`、release preflight、两次 PR CI 均通过；生产 migration dry-run/正式 runner均为 `140 discovered / 0 pending / 140 skipped`，Python 从 clean exact main 发布并通过依赖、import、route、Torch、systemd 与 8083/PostgreSQL 健康检查。三页 repo/公网 SHA-256 一致：AI Assist `ed08ddf1a451ba7b93253a6de40a834cd6cad7c90d56181d2e95fcbd42234a2d`、工厂 SOP `fa09ca13d6843c88a248c1add574740b70afa2930b27478b7b76dd538ba26121`、餐饮 SOP `40c52f66b2cd52313e47e4ee94692e224a6e7a5dfd1e0c4278bd93598581fef8`；静态回滚点后缀 `20260816_092826`。四源仅按实际语义变化原子重建，正式块为 F006 `71`、餐饮 full/product/metrics `85/245/176`，`.NEW=0`；餐饮 docs 回滚点同后缀。最终真实回答工厂 `2/2`、餐饮 `4/4`，分别只引用 F006 source 与三个注册餐饮 source；固定 BOM/Workflow、单菜毛利、日结表、季度时间窗和导览零代算均通过。生产 ERP 业务写入 `0`，scope 已释放，无阻塞。
