# Dispatch 完成记录 — 2026-08-11

- `UX-LIUSHANMEN-LABEL-OPAQUE-OVERLAY-20260811` — `review` — Owner: `/root` — Base SHA `098b2e801b69e2d2925c41b0ed87ac91a4a22749`；复杂包装背景上的细线 label 框已替换为黑色外沿、类别色粗边、轻量区域着色及不透明类别标题（蓝色白标、绿色彩标），选中态使用黄色粗框和不透明手柄；canonical `label_annotator_local.py` 新增能力标志及页面渲染 marker 的 fail-closed 校验，旧细线页面无法再静默启动。外部运行脚本 SHA 从 `d050a62837ce6bfc8aa62b196df0844ab44d7574589efafdc0256da6620e8bac` 变为 `6d4d5f1e95f7d1a3340493f10885a4dcee051ae06526aaee6eb13c1e2663de2a`；隔离 8773 的真实 HTML/canvas 与浏览器截图通过，用户完成第 4 张后才安全切换 8772，队列重启前后均为 7/7 reviewed、0 remaining、36 框，未丢人工结果。VisionLab 49/49、Python 编译、`git diff --check` 通过；未改 manifest、annotation、模型、原图或生产，`NOT_DEPLOYED`，scope 随本归档释放。

- `BUG-LIUSHANMEN-LABEL-ANNOTATOR-ENTRY-20260811` — `review` — Owner: `/root` — Base SHA `7fdf6298e6f4ded28364d792167ea46f07a7f9aa`；根因是本机同时保留现代 label 标注器与昨日 `legacy_adapter`，后者在前端硬编码“白标和彩标必须同时存在”，手工启动选错后会阻止真实单类缺标保存。PR #2508 新增唯一 `label_annotator_local.py`：强制 loopback、显式缺标二次确认、legacy 文件名及旧两类硬阻塞 fail-closed，并让 tray/label 启动器按 manifest 双向拒绝错误队列；运维文档只保留 canonical 命令。用户明确授权后，外部旧 `label_hard_negative_annotator_legacy_adapter.py` 已精确删除，现代脚本、tray 工具、模型缓存和全部队列均保留。VisionLab 44/44、Python 编译、diff check、隔离端口真实启动及 tracked-secret-scan 通过；当前 8772 已由新入口运行并保留 9/9 reviewed（2 unjudgeable），生产零写、`NOT_DEPLOYED`，scope 随本归档释放。

- `ML-LIUSHANMEN-TRAY-ROUND2-20260811` — `review` — Owner: `/root` — Base SHA `2eb84fc6dcd9fe3bc50379237615882c164cbcb1`；新蓝筐/孤立 tray 队列 `tray-active-20260811T073242Z` 已服务器与落盘双重验证 8/8 `reviewed=true, source=human`、167 框，8 组 image/label/annotation 一一对应且图片 SHA 全匹配；8 张来自不同任务、全部顶部蓝筐风险、7 张生产/历史候选分歧、2 张孤立场景，距保护集最小 pHash 84，保护集/旧 6 个 tray manifest 均未混入。为阻止 Ultralytics AMP 自检再次联网，tray 训练固定 `YOLO_OFFLINE=true, pretrained=false, amp=false`；VisionLab 目标测试 38/38、Python 编译与 diff check 通过。代码合入后才以历史已审核 92 张加本批 8 张累计训练并运行 ONNX parity、受保护 7+20 完整 tray→当前生产 label 回放及核心蓝筐根因断言；任一门槛失败保持生产 tray 不变，scope 随本归档释放。

- `ML-LIUSHANMEN-LABEL-OPERATOR-PROMOTION-20260811` — `review` — Owner: `/root` — Base SHA `ff99732a97fc8f4539b9c116695d57e298819a9a`；用户基于受保护 7+20 的缺陷 3/7→4/7、正常误报 26→9 明确接受候选当前仍漏 3 张、`new_blind_defect` 仅 1/2 的权衡，并授权先替换生产 label、随后继续补数据。实现新增显式 `ACCEPT-INCOMPLETE-RECALL` 人工入口：只允许豁免 `required defect group did not reach full recall`，默认失败仍拒绝，哈希/模型回执绑定、总缺陷回退、误报、生产回放、parity、延迟及其他错误不可豁免；部署回执保留原始 `gate=false`、失败项、人工理由、旧/新 SHA 和回滚路径。VisionLab 目标测试 37/37、Python 编译与 diff check 通过；生产候选固定为 `label-eb43abf090d6-20260811-135305` / `bfd8a3adccbbe0bf5ccbe84eec205858204f05013c97e9e1cc6fb192a9b1f3f2`，旧 label `08aa70231a154fcbb55f4b2c9248fcfca5f3808ae13fe4ea423eaa98749b85e5` 为回滚点，tray 模型不变。代码随 PR #2495 合入后才执行已授权的生产原子替换与健康验证；scope 随本归档释放。

- `ML-LIUSHANMEN-LABEL-SIDE-VIEW-20260811` — `review` — Owner: `/root` — Base SHA `ef4cddbce691747ac39d48afedf14aebe6459b61`；完整 tray→label 回放确认侧视/倾斜/遮挡白标为独立瓶颈后，只读扫描 300 张人工 `NO_DEFECT` 新来源并建立 3 张独立 label-side-view 队列；服务器落盘 3/3 `reviewed=true, source=human`、6 框。3 个来源距保护集 pHash 86–94、距既有 240+83+57/旧 29 来源 86–88；新数据集 `label-eb43abf090d6` 为 train 310/val 69、白标 396/彩标 415 框，旧 29 与保护集 photo/task/SHA 重叠均 0。候选 `label-eb43abf090d6-20260811-135305` 的 ONNX SHA 为 `bfd8a3adccbbe0bf5ccbe84eec205858204f05013c97e9e1cc6fb192a9b1f3f2`，PT/ONNX parity 0；侧视训练样本白标匹配由 1/3 升至 3/3，但该结果不作为独立推广证据。受保护 7+20 中缺陷由生产 3/7 升至 4/7、正常误报 26→9、p95 4289→4479ms，仍因 `new_blind_defect` 仅 1/2 且核心蓝筐图 `df1f6029-389d-45b5-995e-be19b2f5b943` 无 tray suspect 而被硬门禁淘汰，未部署。首次训练因 Ultralytics AMP 自检擅自下载 `yolo26n.pt` 被立即终止，下载物可恢复归档；最终入口固定 `YOLO_OFFLINE=true, amp=false, pretrained=false` 后离线重跑，目标 VisionLab 测试 33/33、Python 编译与 diff check 通过。旧 29 MARK/manifest 与生产 tray/label SHA 前后不变，cloud calls=0、production writes=0、originals modified=false；Draft PR #2495，scope 随本归档释放。

- `AUDIT-WEB-WORKPROCESS-OUTPUT-KIND-COMPAT-20260811` — `review` — Owner: `/root` — Base SHA `67190968bec702c3c8e84de67a0cf066690adfac`；每日跨层审计确认 Workflow 节点快照修复后，tracked Web 生产代码对 `PATCH /work-processes/{id}/output-kind` 已零调用，但 `processProduction.ts` 仍保留死 helper，两个 Workflow 测试仍保留无效 mock。本次删除 helper/mock；后端 route 暂按已发布客户端兼容边界保留，并在最相关 controller 写明删除条件：完整支持窗口内访问日志为零后，与 request DTO/service shortcut 一并删除。当天全范围 diff check 还定位到两个纯 CRLF Python 模板缺 `cr-at-eol` 属性，已仅为精确路径补 `.gitattributes`，源码内容和行尾未改。目标 Vitest 2 文件 39/39、`vue-tsc -b --force`、任务 diff check、`bece147d..HEAD` 当天全范围 diff check、前端 helper/mock 零命中均通过；`npm ci` 报告既有 3 moderate/15 high/1 critical advisories，未修改依赖或 lockfile。未改接口行为、Python 运行代码、Entity/Repository/JPQL/Flyway/Security，JPA startup gate 不适用；生产零写、`NOT_DEPLOYED`，scope 随本归档释放。

- `AUTO-SOP-DUAL-LINE-20260811` — `merged` — Owner: `/root` — Base SHA `11f752800117f407aaa64b13cf615fa8e499b96f`；按 `a12acf9f..11f75280` 的当前行为独立同步工厂 Workflow 存放位置/本图产出、WIP 计划链，以及餐饮 GFM 表格、真实门店候选、澄清保持与费用分类口径。PR #2482 合入同步内容，生产 canary 暴露旧范围动作 guard 抢占餐饮等价问法后，由 PR #2483 调整确定性路由优先级并补回归。两次 PR/CI 均通过，目标 Python 测试首批 148 项及修复批 84 项通过；三个静态页从 exact main 原子替换并与仓库 SHA-256 一致，四个 canonical source 原子重建为 71/81/240/173 块且 `.NEW=0`。Python、Embedding、PostgreSQL 健康；工厂 4 问只引用 `f006-production-full-chain-sop.md`，餐饮 5 问只引用注册三源，固定 BOM/Workflow、单菜毛利和导览零代算口径均通过。生产 ERP 业务写入 0；备份保留为 `*.bak.sop-20260811T093204+0800`，scope 随本归档释放。

- `ML-LIUSHANMEN-TRAY-ACTIVE-20260811` — `merged` — Owner: `/root` — Base SHA `96ec6f318e2e2fc0e8c3b601285f3d41c4a0c4e7`；建立 tray 主导的本地主动学习、生产 ONNX 预标注、人工 reviewed 真值、蓝筐场景排序、多队列累计训练、保护集隔离及候选自动门禁，PR #2474 以 merge commit `7a6a03d43e3c86616c2197fe7ed7881a47199a87` 合入 `origin/main`。四个独立人工队列共 84/84 张、1758 框，保护集 exact/pHash≤10 交集 0；用户确认的第四轮蓝筐 MARK 完成 12/12、282 框。最佳候选 `tray-9b6bc32f302d-20260811-105913` 达到缺陷 6/7、新盲测 2/2、根因命中，但误报 25 未达 ≤24 且 PT/ONNX 不一致 5 次；最终保守候选误报 24 但召回回退至 3/7、根因漏检，全部被硬门禁淘汰。目标 tray 测试 18/18、Python 编译、diff check 及 PR Java/Python/Vitest/secret/Web artifact CI 全通过；生产 tray/label 与 label MARK 哈希未变，cloud calls/生产写入均 0，未部署模型。下一轮必须使用新的独立真实 tray 数据，或确认 `B:\AIModels\LocateAnything-3B` 固定 revision 完整可读后再启用 crop-level teacher，不得继续针对保护集调参；scope 随本归档释放。
- `ML-LIUSHANMEN-LABEL-HARD-NEGATIVE-20260811` — `review` — Owner: `/root` — Base SHA `9b47cf8ec159cd5ff9bfc3c1418f5c804ba9c2ea`；最新 label 候选在受保护 7+20 上召回 4/7→4/7、误报 9→9，且核心蓝筐白色干扰框置信度升高，因此新增 `white-confuser-disagreement` 只读挖掘：被拒绝候选仅按同 tray/white-box IoU 与置信度增量排序，固定候选 SHA、teacher 非真值，保护集不参与相似度、排序或调参。200 张有界预检得到 7 张/7 任务/4 SKU，2 个新增白标框、5 个放大框；距保护集最小 pHash 88、距 18 个既有 label/tray manifest 最小 78，424 个既有 source ID、旧 29 与保护集均排除。计划回执 `label-white-confuser-production-plan-20260811T134415Z.json` SHA `c6fd38bae5fa20296b0d14c401f7fb83b70ab46c03abc893eb3a8892a367c767`；独立队列 `label-side-view-active-20260811T141154Z` 已由 canonical 现代标注器在 `127.0.0.1:8772` 启动，等待 7/7 人工确认。VisionLab 48/48、Python 编译、diff check 通过；云调用 0、生产写入 0、原图未改、未训练、未部署，scope 随本归档释放。
