# Dispatch 完成记录 — 2026-08-02

- `FIX-LLM-TOKENHUB-ARK-CONFIG-20260802` — `review` — Owner: `/root` — Base SHA: `6343022bcfd2acd224efcffe501004240de4db5c` — 官方文档与生产只读实测确认两条故障都属于路由配置而非账号总额度耗尽：TokenHub 的通用餐饮链误纳翻译模型 `hy-mt2-pro`、已下线/预下线 DeepSeek 型号，且未按模型家族发送 `enable_thinking=false` 或 `thinking.type=disabled`，导致真实 T3 请求超时/空内容；火山旧五个端点被逐模型 `SetLimitExceeded` 暂停，但同账号当前 Turbo/Lite 端点 5/5 合同正常。候选按官方协议补齐 TokenHub 非思考参数，仅保留实测 5/5 的 DeepSeek V4 Pro、GLM 5.2、Qwen 3.5 Plus、MiniMax M3，并将实测 5/5 的火山 Turbo/Lite 与 TokenHub 交错纳入真实 LLM 尾链；未新增无模型规则兜底，未改后付费、安全体验、限额、密钥或账户设置。Router 61/61、Ruff、diff check 通过；餐饮精确集合 1904 passed，3 个 Windows `asyncio.get_event_loop()` 套件顺序失败，失败文件独立复跑 7/7，判定与本改动无关但不记作全绿。本归档 commit 尚未合并/发布，后续以 PR merge、exact-main Python 发布及生产强制 provider 真入口探针为最终真值。Scope 已释放，生产业务写入 0。
- `FIX-RESTAURANT-AI-PROVIDER-OUTAGE-20260802` — `review` — Owner: `/root` — Base SHA: `7c2972eec76f6e153eddb33887849270636ec090` — 用户明确餐饮自然语言必须由 LLM 规划，不接受无模型规则兜底。Claude Code 旧会话与生产日志复核表明 TokenHub/火山已接入且真实调用，但当日 TokenHub 主力返回 402、火山触发账户 `SetLimitExceeded` 暂停；生产真实餐饮 T3 逐模型探针发现 A/B/C 三个 `qwen3.7-plus-2026-05-26` 仍为 200、合同完整、置信度 0.95，耗时 4.57/4.16/3.90 秒，却只在 INSIGHTS 而遗漏于餐饮 REVIEW 链。候选按 C/B/A 实测速率将三只既有 billing-safe dated Plus 接到两只 Max 后、裸名 Plus 前；五类餐饮语义（客单价、趋势、损耗、具名菜品、门店目录）最终执行合同均通过，router 70/70、餐饮全量 1885/1885、Ruff 与 diff check 通过。未新增规则执行兜底，未改凭证、账号额度、付费开关或生产数据；本归档 commit 尚未合并/发布，后续以 PR merge、exact-main Python 发布、真实 `_run_case` 和线上 provider 日志为最终真值。Scope 已释放，生产业务写入 0。
- `FIX-RESTAURANT-AI-PROFIT-COMPARE-HINT-20260802` — `review` — Owner: `/root` — Base SHA: `19675c9d015cb0c2274fc9bde85726a4e863af48` — PR #2201 exact-main Python 发布后的生产全量真实入口为 80/81，唯一失败“两菜哪个赚钱”经独立复跑 2 次稳定重现。根因是 sealed plan 为利润判断追加内部规范提示“赚钱了吗”，单菜提取器会剥离，多菜比较提取器却未剥，锚定比较式因此失配、两菜均丢失，合同层正确 fail-closed 而未展示全菜单答案。PR #2202 仅补多菜提取器的对称 hint 规范化；目标 3/3、resolver 378/378、餐饮全量 1514/1514、fatal Ruff、diff check 与 PR 两组 pytest/secret scan 全绿。本归档 commit 尚未合并/发布，后续以 PR merge、exact-main Python 发布、失败项与 81 条生产真实入口复跑为最终真值。Scope 已释放；生产业务写入 0，LLM 账户/额度/计费/凭证未改。
- `FIX-RESTAURANT-AI-FINAL-EVAL-20260802` — `review` — Owner: `/root` — Base SHA: `db1fdbf5cc949ef6a7312d26297562a9c0006179` — 生产 81 条真实入口首轮 78/81：日月光门店澄清本身正确，仅验收短语过严；三菜销量在 DEMO_REST 规划后映射 RES_3101_009 取数时，目标租户菜单闸静默删掉无数据的第三菜；清晰复合问被拆解后第二问漏继承已给出的“全部门店”。PR #2201 保留 sealed plan 的多菜候选并对目标租户缺数明确披露，同时对“另外/顺便/以及/还有/同时”做本地确定性拆分、只继承原句显式时间/全部门店且不覆盖具名店；目标 7/7、核心 471/471、全量餐饮 1512/1512、fatal Ruff、diff check 与 PR 两组 pytest/secret scan 全绿。本归档 commit 尚未合并/发布，后续以 PR merge、exact-main Python 发布、81 条生产回归和真实 capability audit 为最终真值。Scope 已释放；生产业务写入 0，LLM 账户/额度/计费/凭证未改。
- `FIX-RESTAURANT-AI-RUNTIME-IMPORT-20260802` — `review` — Owner: `/root` — Base SHA: `39114b1a9250aa7b9aeb2f53f1612b60a917ec4e` — PR #2198 exact-main Python 发布后，13:54 手工运行生产 cron 真实入口，失败隔离正确继续了后续步骤，但两次 `refresh_demo_rest_dish_facts` 都因导入 `smartbi.services.materialized_analytics` 时执行旧包初始化而报 `ModuleNotFoundError: services`，因此渠道 marker 尚未回填。候选去掉该运行时包导入，把同一 channel Gold UPSERT 保留在 cron 脚本自身事务内，并新增从 `backend/python` 执行 `python -m smartbi.scripts.refresh_demo_rest_dish_facts --help` 的生产同形子进程回归。目标 pytest 20/20、真实模块入口、compileall、Ruff fatal、diff check 通过；本归档 commit 尚未合入/重发，后续以 PR merge、exact-main Python 部署、6/6 cron 回执、marker/Gold 回读为最终真值。Scope 已释放；渠道算法/租户范围不变，其他生产业务写入 0。
- `FIX-RESTAURANT-AI-LLM-CLOSEOUT-20260802` — `review` — Owner: `/root` — Base SHA: `5bd9622b6ae9eb389b51e097e03500256a076108` — 生产 81 条真实入口基线为 71/81；失败集合拆解后修复多菜品单槽合同、菜品排名口语/错字、年度同比零 token 路由、部分门店别名澄清和具名菜品合成追问中的无关门店，同时把“挣了多少”明确归入当前不可可靠计算的净利润缺口。数据侧确认 DEMO_REST 8 月新交易 3,111 笔及 RES_3101_009 既有 `DEMO_ROLL_TX` 缺渠道字段，候选仅对两个演示租户自有 marker 行做 null-only 幂等 55/35/10 渠道补齐并刷新渠道 Gold。LLM 侧确认 TokenHub 与火山已在线真实调用；候选只把火山精确错误码 `SetLimitExceeded` 纳入持久额度退避，普通 429 仍短冷却，计费 allowlist/到期保护/凭证均未改。餐饮扩大回归 1,511/1,511、router 70/70、compileall、Ruff fatal 与 diff check 通过；本归档 commit 尚未合并、未发布、演示 marker 尚未更新，后续以 PR merge、exact-main Python 发布、marker/Gold 回读、真实 `_run_case` 与 81 条生产回归为最终真值。Scope 已释放；其他生产业务写入 0，R_GML_DEMO / R_XMX_CHAIN 不在范围。
- `LLM-ABC-FREE-FALLBACK-20260726-002` — `merged` — Owner: `/root` — Base SHA: `a39e25717059b742a56a1b8e01286addbd95e33f` — 原候选实际已由 PR #1800 于 2026-07-26 合入为 `a4cac2402d`，A/B/C 已验证免费模型进入 MAPPER/INSIGHTS/REVIEW，后续 PR #2036 又把可用 TokenHub 模型与 5 个实测火山方舟模型接入非阿里云地板，PR #2117 增加额度耗尽跨进程持久退避。2026-08-02 核验生产 `llm_router.py` Git blob 与 `origin/main` 完全一致，日志同时出现 TokenHub 200 与火山成功/额度上限 fallback，证明已部署且被真实调用；旧 review 状态为台账滞后，现归档并释放 scope。计费 allowlist、到期 fail-safe 和生产业务数据均未改动。
- `LLM-MAPPER-FALLBACK-20260726-001` — `merged` — Owner: `/root` — Base SHA: `28b415b5ddecb071fa047b7afe33583e42c1a6d4` — PR #1795 已合入为 `66629eb34b` 并完成 exact-main Python 发布；B/C versioned Flash/alias 已进入 MAPPER/CHAT/CHART 快速主链，深度经营分析继续走 INSIGHTS/REASONING，Max/DeepSeek/Kimi 未被塞入短 JSON MAPPER。2026-08-02 再次核验线上代码 blob 与 `origin/main` 一致，旧 review 状态无剩余实现或部署动作，现归档并释放 scope；生产业务写入 0。
- `FIX-DEMO-REST-DUAL-TRACK-20260802-001` — `review` — Owner: `/root` — Base SHA: `87830eeb67f8a9f266ed1c2f515d1b6326dfe4c3` — Steve 拍板同时维护 DEMO_REST 数据闭环、主演示继续只用 MOCK_REST + RES_3101_009。候选把固定模板聚合器硬限制为 DEMO_REST/RES_3101_009 两个租户，DEMO 使用独立 2026-08-01 cutover 与 `9000728` marker，cron 按“自身模板聚合→POS/菜品合成→自身 POS 覆盖核验”执行，并保留逐步失败隔离；审计默认 MOCK、公开 DEMO Gold 映射 RES 的真实主演示契约有测试锁定。生产只读预览：两个模板均 81/81 天连续、缺口 0，DEMO 8/1 目标/marker 均 0，首次预计 22 条聚合与约 3,111 笔合成交易。Ruff、`bash -n`、Python 编译、目标 pytest 27/27、`git diff --check`、PR #2192 首轮两个 attached pytest 与 secret scan 均通过。用户已继续授权合并、exact-main Python 发布及仅 DEMO_REST 的受控 apply；本归档 commit 仍未合并/未部署/生产业务写入 0，后续以 GitHub merge commit、部署回执与 DEMO marker/覆盖回读为真值。Scope 已释放；不涉及 R_GML_DEMO、R_XMX_CHAIN、鉴权/RLS、LLM router、凭证或其他租户。
- `AUDIT-SUPPLY-CHAIN-UNIT-CONTRACT-20260802` — `review` — Owner: `/root` — Base SHA: `e7157ff1991701a7c8ac213119e61af8c6dbe522` — 当天跨层审计发现进销存总览把 `ProductionBatch.plannedQuantity/actualQuantity` 跨单位相加后硬贴 `kg`。现改为读取后端 `unit` 真值：同单位才保留小数汇总，多单位、缺单位或无效值明确拒绝伪汇总；生产批次明细同时展示原数量与单位。目标 Vitest 2 文件 10 测试、`vue-tsc -b --force`、Vite build（4544 modules）与 `git diff --check` 均通过。`npm ci` 报既有 3 moderate / 14 high / 1 critical advisories，未改依赖或 lockfile。Scope 已释放；仅纳入每日集成 PR，不合并、不部署、生产零写。
- `AUDIT-PROCESSING-SOURCE-COMPAT-COMMENT-20260802` — `review` — Owner: `/root` — Base SHA: `e7157ff1991701a7c8ac213119e61af8c6dbe522` — comment-only 收口：把 `createBatchFromPlan` 中已过时的“startProduction 待另轮修复”改为当前已收敛事实，并明确存量 fail-closed 分支须等 V20261029_46 全环境应用且连续一个发布周期零命中后才能删除；给 `ProcessingServiceImpl` optional source-name repository 注入补“测试迁共享 builder 后改回 final 构造器依赖”的删除条件。`git diff --check` 与 comment-only diff 审查通过，运行逻辑零变化。Scope 已释放；只更新当天 daily PR，不合并、不部署、生产零写。
