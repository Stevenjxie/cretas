# Dispatch 完成记录 — 2026-08-17

## 已完成

- `AUDIT-PURCHASE-FINANCE-OA-BOUNDARY-20260817` — `review` — Owner: Codex automation `/root` — Base SHA: `23c38804b478cdac7922d6f49ca9c9975d0002a2`。当天跨层审计确认 Web/RN 已删除采购财审死入口并统一改走 OA，但后端 `purchase_finance_approve` 和通用 `purchase_order_approve` 的旧 finance 动作仍直接调用 `PurchaseService.financeApproveOrder/financeRejectOrder`，可绕过 controller 的 OA-only 410 边界。现将独立 legacy Tool 收敛为零参数兼容 sentinel，只返回“工作台 → 待我审批”指引且业务 service 零注入；通用 Tool 的 schema 仅保留 submit/approve/cancel，并对旧模型或缓存发来的 finance 动作二次 fail closed。保留 legacy tool name 仅为避免生产既有 intent 配置变成 tool-not-found；等运行时 intent、descriptor 和历史迁移均不再引用后整体删除。新增合同测试证明两个 finance 动作零 service 调用、三个非财务动作继续委托；`PurchaseApprovalToolOaBoundaryTest` + `PurchaseControllerOaOnlyTest` 7/7、Tool access-mode 目标方法 1/1、`ToolDescriptorInventoryDriftTest` 4/4、`ToolRegistryStartupGateTest` 4/4、完整 Java 主/测试编译与 `git diff --check` 通过。全量 `ToolAccessModeDeclarationTest` 另有当天 diff 之外的既有 `RestaurantPeakHoursGoldTool` 缺显式声明，未跨 scope 修改。无 Repository/Entity/JPQL/`@Query`/Flyway 改动，JPA startup gate 不适用；未部署、生产业务写入 0，scope 锁已释放。
- `SOP-RAG-SYNC-20260817` — `merged` — Owner: Codex automation `/root` — Base SHA: `c5219802a80815fa617b415f9aa43c0c65462c85`。按 `9310a52cc556cdd8374ec732f7e9e79584da0b9e` 至当日 main 完整核对后，工厂与餐饮均判定有用户可见变化并独立同步；PR #2736（`df52eee79f73148b5649bc93396bd9dad76a2609`）更新共享 AI Assist、两条在线 SOP、四个实际变化的 KB source 与确定性回答合同，PR #2739（`38a11c8df5d888d4e5f49837491f8f972f5220e5`）移除 4 个已被 F006 单一知识源取代的旧工厂注册。目标合同测试 115/115、合同检查、`py_compile`、HTML/内联脚本解析、`git diff --check` 及两批 CI 全部通过。生产从 exact main 发布：AI Assist / 工厂 SOP / 餐饮 SOP 仓库、服务器与公网 SHA-256 分别为 `64fa81f34764c7a036baaaa5fae36878a05eb8f9ef698f680f0b12b7922e41f2`、`c4e07791683772d7d24076229f5d53f6d6a85cec4c1c8986f90b51bd6c514834`、`f7385d46dea43842204249556b15a2afba403e04879cf7e633a63343472ac11d`；Python/Embedding/PostgreSQL 健康。四个变化 source 原子重建后正式块为 F006 71、餐饮全链路 86、指数字典 176、产品手册 249，`.NEW=0`；旧工厂块 688 条已在完整 CSV.gz 备份后按精确 source 名事务删除，最终为 0。工厂 BOM/Workflow、采购/OA/收货、实时 WIP、标签小图与等价问法共 5 个真实 canary 只引用 F006；餐饮单菜毛利、营收归因、追问时间窗、维度缺口、420 px 日结表共 5 个真实 canary 只引用 3 个餐饮注册 source，均通过。回滚点：`web-admin.bak.20260817_094034`、两条 SOP `index.html.bak.20260817_094115`、餐饮 docs `.bak.20260817_094347`、RAG 备份 `/www/wwwroot/cretas/backups/food-kb-legacy-factory-sources-20260817_1003.csv.gz`（SHA-256 `240b624a94f667bf6f1baf9399cb5aaffe372ab56619715255b2a2d07ea23bd1`）。生产 ERP 业务写入 0；scope 锁已释放。
