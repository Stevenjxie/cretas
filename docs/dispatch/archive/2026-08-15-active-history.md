# Dispatch 完成归档 — 2026-08-15

- `AUDIT-DAILY-CRLF-20260815` — `review` — Owner: `/root` — Base SHA: `df7c45f05e7e80b8368ceafbc618c804c5e1960f` — 为 14 个已确认 CRLF blob 增加精确 `whitespace=trailing-space,cr-at-eol` 规则，保留真实尾随空格检查，并在 `.gitattributes` 写明逐文件 LF 归一化后的删除条件。运行时代码未改；精确整日区间 `git diff --check` 与 tracked encoding hook 通过；严格 `NOT_DEPLOYED`、生产业务写入为 0。
- `SOP-RAG-SYNC-20260815` — `merged` — Owner: `/root` — Base SHA: `f7de1aa2086d6ff30b739c021cbf82ed7fd7dc24` — 工厂与餐饮双线 SOP、共享 AI Assist、4 个 canonical KB source 和确定性回答合同已按当前 `origin/main` 同步；PR #2646 合入内容，PR #2647 修复“只有 5”被误判为计数单位“只”的生产 canary 路由。目标 pytest 101/101、两组 CI pytest、secret gate、`py_compile`、HTML/内联 JavaScript 解析与 `git diff --check` 通过；Python exact-main 发布、Web Admin 原子发布、三页原子替换及 4 source `.NEW` 原子交换完成。公网/仓库/服务器三页 SHA-256 一致；正式块为工厂 71、餐饮 85/176/245，`.NEW=0`；工厂和餐饮各至少 3 个真实等价问法、source 隔离及固定毛利/BOM-Workflow 基准全部通过。生产 ERP 业务写入为 0，回滚点已保留，scope 锁已释放。
