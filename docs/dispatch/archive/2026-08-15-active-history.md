# Dispatch 完成归档 — 2026-08-15

- `AUDIT-DAILY-CRLF-20260815` — `review` — Owner: `/root` — Base SHA: `df7c45f05e7e80b8368ceafbc618c804c5e1960f` — 为 14 个已确认 CRLF blob 增加精确 `whitespace=trailing-space,cr-at-eol` 规则，保留真实尾随空格检查，并在 `.gitattributes` 写明逐文件 LF 归一化后的删除条件。运行时代码未改；精确整日区间 `git diff --check` 与 tracked encoding hook 通过；严格 `NOT_DEPLOYED`、生产业务写入为 0。
- `SOP-RAG-SYNC-20260815` — `merged` — Owner: `/root` — Base SHA: `f7de1aa2086d6ff30b739c021cbf82ed7fd7dc24` — 工厂与餐饮双线 SOP、共享 AI Assist、4 个 canonical KB source 和确定性回答合同已按当前 `origin/main` 同步；PR #2646 合入内容，PR #2647 修复“只有 5”被误判为计数单位“只”的生产 canary 路由。目标 pytest 101/101、两组 CI pytest、secret gate、`py_compile`、HTML/内联 JavaScript 解析与 `git diff --check` 通过；Python exact-main 发布、Web Admin 原子发布、三页原子替换及 4 source `.NEW` 原子交换完成。公网/仓库/服务器三页 SHA-256 一致；正式块为工厂 71、餐饮 85/176/245，`.NEW=0`；工厂和餐饮各至少 3 个真实等价问法、source 隔离及固定毛利/BOM-Workflow 基准全部通过。生产 ERP 业务写入为 0，回滚点已保留，scope 锁已释放。
- `FEATURE-LABEL-QC-OBJECT-REVIEW-20260815-001` — `merged` — Owner: `/root` — Base SHA: `281fafda5ea02d391866c2c2b4e7a22926ff906b` — PR #2672 已合入 `main`，merge commit `aea51efa8519e1523241be71aebf53c985669ff8`。工厂 Web/RN 已实现逐盒确认、补画、移动/缩放、删错与存在性核对；AI 原始提议和人工最终快照分离，训练导出包含对象真值。Java Service/API/真实 JPA 目标测试 23/23、Web label-QC 30/30 + `vue-tsc -b` + build、RN label-QC 18/18 通过；未部署生产、生产业务写入为 0，旧 scope 锁已释放。
- `FEATURE-LABEL-QC-CROP-REFINE-20260815-001` — `merged` — Owner: `/root` — Base SHA: `aea51efa8519e1523241be71aebf53c985669ff8` — PR #2675 已合入 `main`，merge commit `41b394739d189edb30ac0c8c214f4ac37998abe6`。工厂对象复核改为 AI 默认接受、切换照片/盒子即保存确认、矛盾状态阻塞切换且最终提交自动校验；平台端新增只使用人工最终认可盒子框的幂等虚拟裁切、SHA/坐标变换谱系和单盒标签精修入口，无法判断/拒绝盒子不裁切，工厂盒子真值与平台标签真值隔离。Java Service/API/真实 JPA 目标测试 25/25、RN label-QC 18/18、Web label-QC/路由 31/31、`vue-tsc -b`、Web build、Flyway 版本唯一性和 GitHub 门禁通过；生产按 Flyway 风险串行 backend-first 发布，Java 从 green/10020 切至 blue/10010，Web 四方哈希一致，统一回执 `deployed`。生产业务数据写入为 0，scope 锁已释放。
