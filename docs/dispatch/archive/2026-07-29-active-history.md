# 2026-07-29 Dispatch Archive

- `BUG-WORKFLOW-BOM-UNIT-ALIAS-UPGRADE-20260729-001` — `review` — Owner: `/root` — Base SHA: `eec67b79c8b360677740282f6d94d09e4fedbff0` — 生产只读核验确认 `LIUSHANMEN` 的“温氏黄油鸡”在旧 Workflow revision 122 与当前 revision 164 中物料主键均为 `RMT_1784271051888`，目标 SKU 反向 DAG 也仍包含该原料；409 实因画布重建后稳定节点/端口/连线 ID 变化，迁移兜底又把 BOM 规范单位 `pcs` 与历史画布显示单位“只”误判为不兼容。修复对不可变 Workflow 快照应用与单位契约一致的内建别名后再迁移稳定槽位，不按名称猜测、不改变 DAG；并确定性归档早于当前 ACTIVE 生效的旧草稿 Family（保留历史、不删除），再从当前 ACTIVE 克隆下一版本，避免 v5 已生效却复用旧 v1。统一候选 Java 构建只执行一次，`BomWorkflowRevisionServiceTest` 23/23、真实 JPA `BomRecipeDraftLifecycleJpaTest` 21/21，JAR SHA-256 `34f16e6120e23635fbe680339f9535cc9785898b108af34995d2d6ed7f0bad5c`，候选预上传成功；无 Entity、Repository、Flyway、Web 变更，生产业务写入 0，scope 锁已释放，待 PR 合并与 clean exact-main Java 蓝绿发布。
- `BUG-WORKFLOW-REVISION-HASH-ROUNDTRIP-20260729-001` — `review` — Owner: `/root` — Base SHA: `49403dd8a2c669ab1bf5e45dac75baf28075007d` — PR [#1937](https://github.com/Stevenjxie/cretas/pull/1937). 生产只读排查确认 `LIUSHANMEN` 的 Workflow 120 当前 DRAFT revision 164 未被任何 BOM 固定，但其节点坐标在 JSONB 往返后由 `32.0` 规范成 `32`，旧哈希把数值表现差异误判为内容变化，导致 BOM 同步预检返回 409；ACTIVE BOM v5 仍合法固定旧 revision 122，未改写历史。修复将对象键和数字表现一起语义规范化，继续接受 legacy raw/key-only hash，只对未被 BOM 固定且已漂移的当前草稿修订安全重建；发布前端在预检后重新读取权威 revision/hash/lock，图内容并发变化时停止而不误发。验证：Java `WorkflowRevisionSnapshotServiceTest,BomWorkflowRevisionServiceTest,ProductProcessWorkflowServiceImplTest` 60/60，Web Workflow 发布目标 Vitest 16/16，`vue-tsc -b` 与 `git diff --check` 通过；无 Entity、Repository、Flyway 变更。候选已无冲突合入最新 `origin/main` `468e6670d`，保留其匿名 AI 鉴权修复。生产只读核验完成，业务写入 0；本任务 scope 锁已释放，合并与 clean exact-main 统一生产发布由本次发布流程继续完成。
- `BUG-LIUSHANMEN-WORKFLOW-AUTOSAVE-PUBLISH-20260729-001` — `review` — Owner: `/root` — Base SHA: `ba86edd551735c45ca68880ae860dacf41410c50` — 生产只读证据确认 ACTIVE BOM v6 与已启用 Workflow 120/v1 均固定 revision 164，而 Workflow 124/v2 在发布后约 8 秒由迟到自动保存误建；页面又把 ACTIVE BOM 与编辑草稿比较，因而同时出现“仍关联旧工艺”和“BOM 与 Workflow 已一致”。修复增加发布期自动保存 barrier、等待在途保存及后端已发布同图保存 no-op；真实修改仍创建新草稿。生产横幅改为比较 BOM `workflowId/definitionVersion` 与 activation，草稿兼容性单独展示。验证：Web Vitest 13/13、`vue-tsc -b`、Java `ProductProcessWorkflowServiceImplTest` 31/31、`WorkflowRevisionSnapshotServiceTest` 9/9 与 `git diff --check` 通过；可信 JAR SHA-256 `81557fb3e3181d8c101688b6c8dfe2ce0c3e5a7b40a11c1252987b4792fc6d42`、Web archive SHA-256 `0804dba076cba4af9d890ec97d8b07a46c3a62cf86ff991e45772a8d507d11d6`，后端候选已预上传但未部署。无 Entity、Repository、Flyway 变更，scope 锁已释放；待单 PR 合并、clean exact-main Java/Web 发布，并仅在图等价证据成立时把 LIUSHANMEN Workflow 124/v2 归档为快照，保留 BOM v6 与 Workflow 120/v1。
- `BUG-LIUSHANMEN-WORKFLOW-AUTOSAVE-VIEWPORT-20260729-002` — `review` — Owner: `/root` — Base SHA: `0a86a38e0cfb96f36d3755c83c387fb4a91b6f22` — 首次部署后的生产只读收尾发现 Workflow 124/v2 与已启用 120/v1 的 schema、nodes、edges 完全相同，仅 viewport 不同；这确认 v2 没有业务改动，同时证明后端首版同图门禁仍把缩放/平移视角当成业务版本。修正将候选 definitionVersion 与 viewport 固定到已发布行后再做规范哈希比较，只忽略非业务相机视角，不忽略 schema、nodes 或 edges；回归显式把 viewport 从 `(0,0,1)` 改为 `(480,-320,0.65)` 并断言返回 PUBLISHED/零草稿写，真实节点变化仍创建 v2。`ProductProcessWorkflowServiceImplTest` 31/31、统一 Java 候选构建和 `git diff --check` 通过，JAR SHA-256 `f8cd54475d7eff66ab37aafdd5aa07dfebc5f95f377ea57643a9d6e383ae16b5` 已预上传；无 Web、Entity、Repository、Flyway 变更，scope 锁已释放。待单 PR、exact-main Java 蓝绿发布及零引用条件下归档 LIUSHANMEN Workflow 124/v2。
- `AUDIT-DAILY-ARCH-RETENTION-20260729-001` — `review` — Owner: `/root` — Base SHA: `c83742db3b47c6129c9ee605f442450517b39c07` — 当日前后端审计确认没有 frontend-only 合入，但发现四条尚未跨端收口的保留路径：RN Form Assistant `context` 仍在传但后端未采信任意上下文、factory-less Generic AI Chat 仍有三个 RN 语音调用方、`outputPreference` 已由 Python/Java 权威转发但三端尚未消费、Web 发布屏障落地后后端仍保留迟到 autosave 防线。基于调用证据与防御价值暂不删除或扩张行为，只在最相关 Java 代码写明保留理由和可验证删除条件；改动仅为注释/台账，`git diff --check` 与四处源码断言通过，无 Repository/Entity/JPQL/`@Query`、运行时或生产变更，生产业务写入 0，scope 锁已释放，待每日集成 PR。
- `BUG-LIUSHANMEN-SAFETY-STOCK-PRODUCTION-ENTRY-20260729` — `review` — Owner: `/root` — Base SHA: `e9cd476c3b99bbaa3fb0b22fba1207e47de4915f` — 生产只读定位确认 `LIUSHANMEN` 库存生产计划 `PLAN-1785287175966-68B703EA` 的 `plannedQuantity=0` 是合法开放数量，点击“生产录入”失败并非 Workflow 未准备，而是自动物化批次时 `ProductionBatch.quantity @Positive` 拒绝 0，页面 catch 又额外显示了笼统 Workflow 错误。修复允许批次目标量 0、普通非库存计划仍要求正数、实际报工继续要求正数，并补零计划量完成率除零保护；Web 只保留后端具体错误/纯网络 fallback、按钮防连点和“按实际报工”展示。真实 JPA Context 1/1、Java Service 10/10、Web 目标 11/11、`vue-tsc + Vite build` 与 `git diff --check` 均通过；现有 SKU/BOM/Workflow/计划无需重建，生产业务写入 0，scope 锁已释放，准备走受控 direct-main 与 clean exact-main Java/Web 发布。
- `SOP-DUAL-LINE-SYNC-20260729` — `merged` — Owner: `/root` — Base SHA: `eaba38707f6f86860d3964ef6a08403ba3e11379` — 自上次基线 `c8983b4e06e2bef3f4661f9e17c122bb8add23c2` 完整审计 main，并按工厂/餐饮独立同步 AI Assist、在线 SOP 与 Food KB。PR [#1948](https://github.com/Stevenjxie/cretas/pull/1948) 合并为 `80132be1c083b464dfbbbdff35e4eb65ac6922a9`；随后 main 新增餐饮跨轮摘要、月报预览/XLSX/PDF 与声明式读写门禁，PR [#1951](https://github.com/Stevenjxie/cretas/pull/1951) 补齐被 `temp*` 忽略而漏合并的 `smartbi/reporting/template.py`，合并为 `0144b85d5b82ffa9bc362935ed67063afbb8df97`。Python exact-main preflight、迁移 dry-run与正式发布通过，仅应用 `V20261031_01`，8083/Embedding/PostgreSQL 健康且 `NRestarts=0`。三页仓库/服务器/公网 SHA-256：AI Assist `5b783620911d476fd5555194c09c43cc8e5233edb3a741d1dce386affeb0d3e0`、工厂 SOP `072a3ee00f3f9d727c9cb63198d446f21fc2c0da31b7767ff79a4fe64944be82`、餐饮 SOP `842924df5682dfad9103dfe8155d83b453e8ed48805f77702eecb980358e55f2`；回滚点分别为 `/www/wwwroot/web-admin/aiassist.html.bak-sop-20260729T1000-0144b85d5`、`/www/wwwroot/lsmsop/index.html.bak-sop-20260729T1000-0144b85d5`、`/www/wwwroot/cysop/index.html.bak-sop-20260729T1000-0144b85d5`。仅原子重建变化 source：F006 `59`、restaurant full `69`、restaurant product `233`；metrics 严格 no-op `169`，`.NEW=0`。月报 + Food KB `56/56`、会话 `38/38`、原有 Java 目标测试、`py_compile`、AI Assist JS、secret scan 与 diff check 通过；真实回答工厂 `3/3`、餐饮 `7/7`，source 完全隔离，固定 BOM/Workflow 与单菜毛利基准均通过。生产 ERP 业务写入 `0`，无阻塞，scope 锁已释放；worktree/branch 按任务约定保留。
