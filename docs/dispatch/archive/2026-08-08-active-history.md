# Dispatch 完成记录 — 2026-08-08

- `BUG-OFFLINE-RELEASE-MANIFEST-HEAD-20260808` — `merged` — Owner: `/root` — Base SHA `3f51f1f59a4886d6ef169d83d9e571d1b586b1d4`；修复 GitHub 封控窗口中组件发布脚本虽接受 `SKIP_GIT_CHECK=1`，可信 Java manifest 却仍硬编码锚定旧 `origin/main` 的合同缺口。默认在线模式继续只认 clean exact origin/main；离线模式只允许 clean detached HEAD 且必须精确等于本地 `refs/heads/main`，feature 分支、脏树和其他 detached commit 均拒绝，JAR SHA-256、归档完整性、build commit 与 backend tree 校验不变。manifest 合同测试、后端制品缓存集成测试、脚本语法及 diff gate 通过；scope 随本归档释放。
- `BUG-F006-CUSTOMER-STOCK-REQUISITION-OWNERSHIP-20260808` — `merged` — Owner: `/root` — Base SHA `6b089678a806d2073d35db5ce60b95bff8918e78`；修复客户专属库存生产沿正式领料单调拨时误走公司库存 FEFO，以及迁入生产仓后丢失客户归属的问题。客户库存路径现按同客户、同销售订单归属（库存生产为未绑定订单）使用加锁 FEFO，显式批次同时校验仓库、物料和客户归属，生产仓新批次继承 ownership/customer/order/item；普通公司库存路径不变。目标领料测试 20/20 两次通过，候选正式门禁合计 46/46 通过，候选 JAR 已完成不可变预上传；离线合入最新本地 `main` 后继续从精确 main 重建并发布，F006 Playwright 写入验收独立记录。scope 随本归档释放。
- `BUG-F006-PRODUCTION-PLAN-CUSTOMER-DISPLAY-20260808` — `merged` — Owner: `/root` — Base SHA `eec9ef1fde969c99c23c32474700ec00530d1ddb`；F006 写入型 Playwright 已成功创建客户专属库存生产计划并确认请求/响应中的客户、归属和租户均正确，但刷新后的 RN 计划卡片只读取旧嵌套 `customer.name`，误显示“未指定”；按后端 DTO 真值优先展示 `sourceCustomerName`，兼容 `customerName` 和旧嵌套字段，并补目标合同测试。scope：生产计划 RN 卡片、目标测试、dispatch；不修改生产计划、客户资料、后端或数据库。
- `BUG-F006-CUSTOMER-SELECTOR-NULL-SAFE-20260808` — `merged` — Owner: `/root` — Base SHA `00ed1b5220761bddf583770cb60cddc6635259f5`；F006 写入型 Playwright 在客户专属库存生产计划表单输入客户搜索词时复现 `CustomerSelector` 对缺失 `code` 无条件调用 `toLowerCase()` 导致组件崩溃，且生产计划业务写入为 0；将名称/编码/联系人搜索统一为空值安全、大小写不敏感的纯函数并补 4 个目标用例。scope：公共客户选择器、纯搜索 helper、目标测试、dispatch；不修改客户资料、后端或数据库。
- `BUG-F006-FA-PRODUCTION-PLAN-NAV-20260808` — `merged` — Owner: `/root` — Base SHA `92c0ce5aaf9442f381469fe85c55e3e0e8abb7c9`；F006 写入型 Playwright 验收确认管理员首页“待生产任务”触发 `ProductionPlanManagement` 导航但当前 FA Home stack 未注册目标页面，导致动作无人处理且停留首页；在所属 stack 注册既有生产计划管理页并新增导航合同测试。scope：`FAHomeStackNavigator.tsx`、目标导航合同测试、dispatch；不修改后端、数据库、其他角色导航或业务数据。
- `BUG-F006-CUSTOMER-ARRIVAL-IDEMPOTENCY-20260808` — `merged` — Owner: `/root` — Base SHA `3453db7f8d4eb650ce527ff9abeced6b6293ad61`；F006 生产写入型 Playwright 复现运营来料预告首笔仓储收货被 68 字符幂等键稳定拦为 400，前端改为保留任务 UUID 与 base36 时间的 54 字符键；既有客供收货 source test 扩展为两条路径且 3/3 通过。scope：`PendingPurchaseReceivingPanel.vue`、既有 source test、dispatch；生产预告与库存数据由同一 E2E 任务在部署后续测。
- `FEATURE-F006-CUSTOMER-OWNED-STOCK-PRODUCTION-001` — `merged` — Owner: `/root` — Base SHA: `b1a9c2b465d62cc905634495ab455298b182b916` — 功能提交 `4d840fdfa3`、离线规则对齐 `f4493227c5`、迁移撞号修复 `47337db3cd`；离线合入本地 `main` 后的发布 SHA 为 `13063cf90df748f01c1672dc6064fb04030ba77a`。实现运营非订单来料预告、仓储据实收货为客户专属原料、现有库存生产内可选客户归属、客户专属原料/成品隔离、正式销售订单按客户+SKU认领已有成品，以及 `operations_coordinator` 最小读写权限；来料收货不引入生产前质检。Java 目标测试、真实 JPA Context、Web 构建与 53 项目标测试通过；RN 改动文件语法验证通过，因工作区依赖缺失未完成全项目类型检查。合并后首次迁移门禁发现 `V20261029_69` 撞号并在生产写入前停止，改为 `V20261029_74` 后 Flyway + 全部 Repository 启动门禁 62/62 通过。生产 Java 通过蓝绿从 `10010` 切换到 `10020`，JAR MD5 `4be9ee122a0c45da69f428548c8df480`；Flyway `20261029.74` 成功，新表、销售订单字段及运营角色 18 项权限只读回验通过。Web 原子发布后四方 SHA-256 均为 `6d685be056b661e1b4b9723492330b9067f4124ac5ac1a595c550ea02aafb39e`；生产业务数据写入为 0。测试环境 `10011` 当时异常但不在本批授权范围内，未重启。发布完成后本地 `main` 被其他已合并批次继续推进，本批未代其部署；GitHub 恢复后使用干净分支 `codex/customer-stock-production-pr`（`47337db3cd`）单独走 PR/CI，scope 随本归档释放。
