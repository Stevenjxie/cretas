# Dispatch 完成记录 — 2026-08-08

- `BUG-F006-CUSTOMER-ARRIVAL-IDEMPOTENCY-20260808` — `merged` — Owner: `/root` — Base SHA `3453db7f8d4eb650ce527ff9abeced6b6293ad61`；F006 生产写入型 Playwright 复现运营来料预告首笔仓储收货被 68 字符幂等键稳定拦为 400，前端改为保留任务 UUID 与 base36 时间的 54 字符键；既有客供收货 source test 扩展为两条路径且 3/3 通过。scope：`PendingPurchaseReceivingPanel.vue`、既有 source test、dispatch；生产预告与库存数据由同一 E2E 任务在部署后续测。
- `FEATURE-F006-CUSTOMER-OWNED-STOCK-PRODUCTION-001` — `merged` — Owner: `/root` — Base SHA: `b1a9c2b465d62cc905634495ab455298b182b916` — 功能提交 `4d840fdfa3`、离线规则对齐 `f4493227c5`、迁移撞号修复 `47337db3cd`；离线合入本地 `main` 后的发布 SHA 为 `13063cf90df748f01c1672dc6064fb04030ba77a`。实现运营非订单来料预告、仓储据实收货为客户专属原料、现有库存生产内可选客户归属、客户专属原料/成品隔离、正式销售订单按客户+SKU认领已有成品，以及 `operations_coordinator` 最小读写权限；来料收货不引入生产前质检。Java 目标测试、真实 JPA Context、Web 构建与 53 项目标测试通过；RN 改动文件语法验证通过，因工作区依赖缺失未完成全项目类型检查。合并后首次迁移门禁发现 `V20261029_69` 撞号并在生产写入前停止，改为 `V20261029_74` 后 Flyway + 全部 Repository 启动门禁 62/62 通过。生产 Java 通过蓝绿从 `10010` 切换到 `10020`，JAR MD5 `4be9ee122a0c45da69f428548c8df480`；Flyway `20261029.74` 成功，新表、销售订单字段及运营角色 18 项权限只读回验通过。Web 原子发布后四方 SHA-256 均为 `6d685be056b661e1b4b9723492330b9067f4124ac5ac1a595c550ea02aafb39e`；生产业务数据写入为 0。测试环境 `10011` 当时异常但不在本批授权范围内，未重启。发布完成后本地 `main` 被其他已合并批次继续推进，本批未代其部署；GitHub 恢复后使用干净分支 `codex/customer-stock-production-pr`（`47337db3cd`）单独走 PR/CI，scope 随本归档释放。
