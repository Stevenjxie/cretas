# Dispatch 完成记录 — 2026-08-03

- `ENH-LABEL-QC-BULK-PHOTO-ARCHIVE-20260803-001` — `review` — Owner: `/root` — Base SHA: `ebed6f81850e7cfbccc826a8c7422b9fb891ae40` — Web 质检“已审核整理/归档记录”已增加当前页批量勾选与一次下载照片 ZIP：仅 `REVIEWED` 行可选，每批使用 `SKU_提交时间_批次_序号` 独立目录，目录内照片继续按 `SKU_yyyyMMdd_HHmmss_序号.jpg` 命名，避免跨批同名覆盖；翻页、筛选、模式切换或刷新会清空选择。任一详情、照片下载或 JPG 转换失败时整批不下载且不写备份记录；完整包生成后逐批调用既有记录接口，个别记录失败会明确告警但不扣留完整归档。目标 Vitest 3 files / 21 tests、`npm run build:check` 与 `git diff --check` 通过；实现 commit `e2fb21cebf7038ced696f772b392b75d02243d3b`，统一候选构建回执 `cretas-1785733892-18766.json` 确认 Java 未变、Web 单次构建成功，Web tree `8772aba0a1b862d831360d3bfae9773fe8ede516`、archive SHA-256 `557314c3ad9cd8b803f067e1693817eac4f6d7c97322009eee1ae34ffcd3d1c8`、index SHA-256 `419670c0ac88efe3da2662f13fb6c92151a14c2459d206b75fb6b3eb9423cc83`。Steve 对同一质检照片归档目标已授权 commit、push、PR、合并及生产 Web 发布；本归档随最终 PR commit 释放 scope，合并后仅从 clean exact `origin/main` 发布并以统一回执、四方哈希及“批量下载照片”线上 bundle 命中为真值，生产业务写入为 0。
- `OPS-RELEASE-ENTRYPOINT-CONSOLIDATION-20260803` — `review` — Owner: `/root` — Base SHA: `ebed6f81850e7cfbccc826a8c7422b9fb891ae40` — 依据 2026-07-30 Claude Code 发布提速会话、交接文档、Git 历史与当前调用图，确认现行流程为 CI 构建/签名 Web 与 Java 制品、东京节点取回/预热可信缓存、clean exact-main 的 `release-cretas.sh --phase deploy` 统一编排，底层 `deploy-web-admin.sh` 仍负责把本机制品原子安装到 139 网关。已删除服务器拉任意分支现场编译的 `scripts/deploy.sh`、旧 JAR/固定 10010/kill-9 的 `backend/java/cretas-api/deploy.sh`、MySQL `ddl-auto=update` 且含历史硬编码口令的 `deploy-first-time.sh`，并从 `deploy-backend.sh` 删除 legacy `--git` 模式且让未知 flag fail-closed；本地 `build.sh` 明确降级为非生产开发 helper，README/两套项目 deploy skill 已指向统一可信制品入口。新增静态门禁通过；发布链路、CI Web artifact、rsync、缓存、互斥、探针、时序、secret scanner 等 13 个目标测试及全部变更 shell 语法、`git diff --check` 通过。既有 `test-deploy-backend-optimization.sh` 在 untouched exact-main 同样因仍要求 5 轮而失败；post-switch probe 首轮计数抖动后本分支与基线均通过。严格 `NOT_DEPLOYED`、生产零写，scope 已释放。
- `ENH-LABEL-QC-PHOTO-ARCHIVE-20260803-001` — `review` — Owner: `/root` — Base SHA: `f1a0d1e5569bed03c72552c92a912e8382b79323` — Web 端已审核质检任务的客户备份已从 JSON 改为一次下载 ZIP，包内仅含按 `SKU名称_yyyyMMdd_HHmmss_序号.jpg` 命名的全部照片；保留技术训练集 JSON，原图不是 JPEG 时在浏览器转为 JPG，缺 URL/下载或转换失败时不生成残缺包，且完整打包后才记录备份。目标 Vitest 3 files / 18 tests、`npm run build:check` 与 `git diff --check` 通过；rebase 后实现 commit `087db36f3c3e6b8421f4c88a1f67c52f74aaa6ba`，统一候选构建回执 `cretas-1785731790-8640.json` 确认 Java 未变、Web tree cache 复用成功，Web tree `858f207853abae894cd3b65dd09ae1d9b6d8166f`、archive SHA-256 `20522d38d28870d5b9d39c1695c51eac54283413904bf29c8543c7687ca5b094`。Steve 已授权 commit、push、PR、合并及生产 Web 发布；本归档随最终 PR commit 释放 scope，合并后仅从 clean exact `origin/main` 发布并以统一回执和四方哈希为上线真值，生产业务写入为 0。
- `AUDIT-RN-WEBONLY-ORPHAN-20260803` — `review` — Owner: `/root` — Base SHA: `fa5068521f2ef3e8a3801c6f46c3729dbc54fa76` — #2205 新增的根级非仓管改道已由 #2210 完整撤销；仓库检索证明 `WebOnlyRoleScreen.tsx` 只剩文件自身与“不得再引用”的负向导航合同，因此删除这个零调用前端孤儿，不改导航、API 或后端契约。目标 Jest 1 suite / 3 tests 与 `git diff --check` 通过；独立安装报告既有 34 个依赖漏洞，未改 lockfile/依赖。严格 `NOT_DEPLOYED`、生产零写，scope 已释放。
- `UX-RESTAURANT-WEB-ROLE-EXPERIENCE-20260802` — `review` — Owner: `/root` — Base SHA: `0503a120831f6f007ac9e5ae05579f02b490f2b0` — 餐饮 Web 四角色首页/菜单、四部门驾驶舱、真实 LLM 问答与数据分析入口首轮打磨完成：老板、店长、采购、厨师长按职责获得不同工作台、快捷问题与交接边界；运营、市场、人事、财务页面统一为真实数据、诚实空态、紧凑 KPI 和行动导向布局；菜单隐藏与直达路由使用同一餐饮角色约束，并对餐饮租户隐藏工厂生产异常页。目标 Vitest 81/81、`vue-tsc -b --force`、Vite build、1536/1024/768 三档视觉验收、diff check 与生产只读基线通过，AI 调用及业务写入均为 0。实现 commit `2a3a818a48` 已推送并创建 PR #2215；Steve 已授权合并及生产 Web 发布，本归档 commit 合入后从 clean exact `origin/main` 发布并以统一回执和 Web 四方哈希为真值，scope 已释放。
- `FIX-RESTAURANT-LLM-ROUTER-20260803` — `review` — Owner: `/root` — Base SHA: `5fee4b515e6c7c83fab269f83ec76da53180b6a1` — 已从全部 LLM slot 物理移除 13 个在 2026-08-03 已过期的 Aliyun 账号+模型对（46 个可达位置），并将 2026-08-02 餐饮 T3 真实契约 5/5 的 TokenHub/火山方舟按两家交错排在 REVIEW 前四位，保证在 12 秒语义规划预算内可达；免费 allowlist、未来到期 fail-closed、持久配额退避和餐饮内容契约均保留。LLM 路由/配额目标测试 76/76、餐饮 intent/service/audit 回归 479/479、compileall、Ruff 与 diff check 通过。只读核对同时确认当前排班建议仅基于近 30 天 POS 日均与静态在岗/目标人效，未接入预订数据，也未按 7/30/365 天趋势产生明天/下周/下月排班；该缺口已与本次 LLM 热修隔离，本批 scope 已释放。
- `FIX-RESTAURANT-LLM-EXHAUSTED-20260803` — `review` — Owner: `/root` — Base SHA: `ecc6840eeda95a802ca628260525d41abcbc7bb0` — 部署后用生产服务完整环境逐模型最小探针复核：TokenHub `deepseek-v4-pro-202606` / `glm-5.2` 与火山方舟 `doubao-seed-2-1-turbo-260628` / `doubao-seed-2-0-lite-260428` 均 200 且有有效 content；Aliyun A/B/C bare `qwen3.7-plus` 与 C `qwen3.7-plus-2026-05-26` 均稳定返回 `403 AllocationQuota.FreeTierOnly`，从免费 allowlist 与全部 slot 物理剔除，仍 200 的 B/A 日期版继续保留。路由/配额目标测试 77/77、餐饮 intent/service/audit 回归 479/479、compileall、Ruff 与 diff check 通过；生产业务写入为 0，scope 已释放。
- `ROLLBACK-RESTAURANT-STAFFING-TARGET-GROUNDING-20260803` — `review` — Owner: `/root` — Base SHA: `304e0a469199c3a9f7794636124eac3055354933` — PR #2218 曾窄放行 FactBook 已配置排班目标，生产真入口随即产出方向错误的未来动作：把实际 13.8 单/人低于目标 15 解释成“人手不够、继续补人”。这证明仅有历史日均订单、静态在岗人数和目标人效不足以排未来班；在预订量与 7/30/365 趋势预测接入前，宁可保留接地闸拒绝，也不能展示方向错误的排班建议。完整回滚 #2218，不影响已验证的 TokenHub/火山路由及 Aliyun 失效项剔除；生产业务写入为 0，scope 已释放。
- `FEATURE-RESTAURANT-FORECAST-STAFFING-20260803` — `review` — Owner: `/root` — Base SHA: `33fc652a5deb892325dd34bfe589162692ff0c7e` — 已实现餐饮预订平台幂等导入与 `MOCK_REST`/`RES_3101_009` 每日可审计模拟滚动，以当前预订和独立 7/30/365 天 POS 客流/订单/历史人效趋势生成明天、下周、下月 FactBook，并按岗位技能、班次工时、周工时上限和目标人效生成排班建议；历史 actual/target 仅作证据，LLM 为必经解释层且模型不得新增数字，失败时明确不可用而不退回旧算法。Web 已提供老板/店长/人事角色化预测排班工作台、来源标签、逐日班次与指纹绑定的确认审计。Python 核心 49/49、餐饮意图路由 799/799、Web 目标 50/50、`vue-tsc` 与生产 Vite build 全部通过；TokenHub/火山/Aliyun 路由配置保持未改。待 PR 合并后从 clean exact `origin/main` 发布 Python/Web、仅为两租户滚动模拟预订/策略并逐表记录写入，随后完成真实餐饮 AI 与页面只读验收；scope 已释放。
- `FIX-RESTAURANT-STAFFING-STORE-DIMENSION-20260803` — `review` — Owner: `/root` — Base SHA: `724793acbc1176f68cef60ec24d4dd13d721850b` — 首次生产 `_run_case` 验收证明三种时间跨度均已识别为预测排班，但 `RESTAURANT_OPS_STAFFING_ADVICE` 的执行能力表漏声明 `store`，使“各门店”问法在 FactBook/LLM 前被拒绝。现已把真实可执行粒度改为 `store + time`，同时保留排班不生成单店切换按钮的限制，因为自然语言 resolver 当前返回全店 FactBook、尚不消费单店筛选；直接相关 159/159、餐饮全域 1523/1523 通过。待小 PR 合并后仅部署 Python 并用原四条生产真入口复验；未改 LLM 路由、FactBook、数据库、Java/Web，生产业务写入为 0，scope 已释放。
- `FIX-RESTAURANT-STAFFING-OPTIMIZE-PLAN-20260803` — `review` — Owner: `/root` — Base SHA: `7faadbdd4d6f62a2d38716513be1fe56ff8ca411` — 第二租户生产 `_run_case` 证明同一句各门店排班问法会因 LLM 将 `analysis_action` 标为 `optimize`，在 planner 已选 `STAFFING_ADVICE` 后仍被通用分支覆盖成 `BUSINESS_OPTIMIZATION`，再由接地闸拒绝。现已把预测排班列为完整能力：其自身用预订、7/30/365 趋势、技能与工时完成优化，不能改走通用综合；确定性复现 2/2、餐饮全域 1524/1524 通过。待小 PR 合并后仅部署 Python 并复验两租户真入口；未改 provider/model、FactBook、数据库、Java/Web，生产业务写入为 0，scope 已释放。
- `BUG-RESTAURANT-STAFFING-LLM-GROUNDING-20260803` — `review` — Owner: `/root` — Base SHA: `c1adb55376de84dcdc49d3f97c143c1dd37d767d` — 生产真实问答发现模型会复述带数字 FactBook 或使用列表序号，导致餐饮专属数字作者校验耗尽既有路由并显式失败；现改为只向 LLM 发送由同一 FactBook 程序派生的定性标签和有限重点门店，完整数字继续由确定性代码渲染，并修正门店名称脱敏注册参数。纯行首列表序号可被剥离，任何其他阿拉伯数字、中文数量或无依据因果仍失败关闭；`common/llm_router.py`、TokenHub/火山/Aliyun provider/account/model/slot 均未修改。餐饮预测/意图/SOP 目标回归 113/113、Ruff、py_compile、diff check 通过；生产未部署 canary 使用现有 `SLOT.INSIGHTS` 在 7.8 秒内返回 333 字定性解读且数字作者校验通过，生产业务调整写入为 0，scope 已释放。
- `SOP-DUAL-LINE-SYNC-20260802` — `merged` — Owner: `/root` — Base SHA: `a74dfdf9bfdef031f429f19bb5657882a0c9f6a8` — 审计 `0e09c525eff19273149358d22ee5f24ecb214ff5..a74dfdf9bfdef031f429f19bb5657882a0c9f6a8` 共 76 个 commit 后，工厂同步会计期间 OA 关账、RN 仓储/Web 角色边界、库存单位、冻结 Workflow 多批、过期库存、终端成品调料 BOM 与结单规则；餐饮同步门店/时段未来预测排班与预览精确确认边界，并修复“预测排班”问法未命中。PR #2227 合入 `ecfc6d79df809f746d3fde57713e8893b56edf26`；release-preflight、migration dry-run pending=0、Python 生产发布、三页带备份原子替换、四个 changed source `.NEW → 正式块` 均成功。最终页面 repo/server/public SHA-256 一致，RAG 为工厂 65、餐饮 75/236/170 块且 `.NEW=0`；工厂 4/4 只引用 F006 source，餐饮 4/4 只引用注册餐饮 source，Python/Embedding/PostgreSQL 健康，生产 ERP 业务写入 0，scope 已释放。
- `UX-RESTAURANT-STAFFING-LIST-20260803` — `review` — Owner: `/root` — Base SHA: `effef09e453d4861d69533374ee2596248036594` — 餐饮预测排班全门店明细已增加可搜索门店、营业时段与人力状态筛选，以及急缺/客流/置信度/门店排序；表格改为 10/20/50 受控分页并限制 560px 内部滚动，每页最多渲染 50 行，保留固定列、缺口标签和班次明细入口。筛选与排序会回到第一页，越界页码由纯函数归一；移动端筛选器纵向铺满、分页区可滚动，补齐控件 aria-label、键盘焦点与 reduced-motion。餐饮 Web 目标 17 files / 118 tests、`vue-tsc -b --force` 与 `git diff --check` 通过；待 clean exact-main Web 发布及生产只读验收，业务写入为 0，scope 已释放。
- `TEST-RESTAURANT-STAFFING-READONLY-20260803` — `review` — Owner: `/root` — Base SHA: `dc597286235b88b8bdb9fdd6d251ebd2a87622a7` — 共享生产只读 MCP harness 已正式注册 `/restaurant/staffing`，并修复 MCP 隔离运行时缺少全局 `URL`/timer 导致 filename entry 无法执行的问题；首次真实 F006 运行在发送前 mutation guard 下登录成功，但 `factory_super_admin` 因前端权限矩阵漏掉四个餐饮部门键被确定性路由到 `/403`，业务写入 0、blocked mutation 0。最小修复仅为最高权限角色补齐四部门 `rw` 并加回归测试；Web 权限目标 26/26、harness 单元 18/18、本地共享 fixture 4/4、bundle 漂移和 diff check 均通过。合并后需从 clean exact `origin/main` 重建并发布 Web，再只重跑 staffing 生产场景；scope 随本归档释放。
