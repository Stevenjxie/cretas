# Dispatch 完成记录 — 2026-08-03

- `UX-RESTAURANT-WEB-ROLE-EXPERIENCE-20260802` — `review` — Owner: `/root` — Base SHA: `0503a120831f6f007ac9e5ae05579f02b490f2b0` — 餐饮 Web 四角色首页/菜单、四部门驾驶舱、真实 LLM 问答与数据分析入口首轮打磨完成：老板、店长、采购、厨师长按职责获得不同工作台、快捷问题与交接边界；运营、市场、人事、财务页面统一为真实数据、诚实空态、紧凑 KPI 和行动导向布局；菜单隐藏与直达路由使用同一餐饮角色约束，并对餐饮租户隐藏工厂生产异常页。目标 Vitest 81/81、`vue-tsc -b --force`、Vite build、1536/1024/768 三档视觉验收、diff check 与生产只读基线通过，AI 调用及业务写入均为 0。实现 commit `2a3a818a48` 已推送并创建 PR #2215；Steve 已授权合并及生产 Web 发布，本归档 commit 合入后从 clean exact `origin/main` 发布并以统一回执和 Web 四方哈希为真值，scope 已释放。
- `FIX-RESTAURANT-LLM-ROUTER-20260803` — `review` — Owner: `/root` — Base SHA: `5fee4b515e6c7c83fab269f83ec76da53180b6a1` — 已从全部 LLM slot 物理移除 13 个在 2026-08-03 已过期的 Aliyun 账号+模型对（46 个可达位置），并将 2026-08-02 餐饮 T3 真实契约 5/5 的 TokenHub/火山方舟按两家交错排在 REVIEW 前四位，保证在 12 秒语义规划预算内可达；免费 allowlist、未来到期 fail-closed、持久配额退避和餐饮内容契约均保留。LLM 路由/配额目标测试 76/76、餐饮 intent/service/audit 回归 479/479、compileall、Ruff 与 diff check 通过。只读核对同时确认当前排班建议仅基于近 30 天 POS 日均与静态在岗/目标人效，未接入预订数据，也未按 7/30/365 天趋势产生明天/下周/下月排班；该缺口已与本次 LLM 热修隔离，本批 scope 已释放。
- `FIX-RESTAURANT-LLM-EXHAUSTED-20260803` — `review` — Owner: `/root` — Base SHA: `ecc6840eeda95a802ca628260525d41abcbc7bb0` — 部署后用生产服务完整环境逐模型最小探针复核：TokenHub `deepseek-v4-pro-202606` / `glm-5.2` 与火山方舟 `doubao-seed-2-1-turbo-260628` / `doubao-seed-2-0-lite-260428` 均 200 且有有效 content；Aliyun A/B/C bare `qwen3.7-plus` 与 C `qwen3.7-plus-2026-05-26` 均稳定返回 `403 AllocationQuota.FreeTierOnly`，从免费 allowlist 与全部 slot 物理剔除，仍 200 的 B/A 日期版继续保留。路由/配额目标测试 77/77、餐饮 intent/service/audit 回归 479/479、compileall、Ruff 与 diff check 通过；生产业务写入为 0，scope 已释放。
- `ROLLBACK-RESTAURANT-STAFFING-TARGET-GROUNDING-20260803` — `review` — Owner: `/root` — Base SHA: `304e0a469199c3a9f7794636124eac3055354933` — PR #2218 曾窄放行 FactBook 已配置排班目标，生产真入口随即产出方向错误的未来动作：把实际 13.8 单/人低于目标 15 解释成“人手不够、继续补人”。这证明仅有历史日均订单、静态在岗人数和目标人效不足以排未来班；在预订量与 7/30/365 趋势预测接入前，宁可保留接地闸拒绝，也不能展示方向错误的排班建议。完整回滚 #2218，不影响已验证的 TokenHub/火山路由及 Aliyun 失效项剔除；生产业务写入为 0，scope 已释放。
- `FEATURE-RESTAURANT-FORECAST-STAFFING-20260803` — `review` — Owner: `/root` — Base SHA: `33fc652a5deb892325dd34bfe589162692ff0c7e` — 已实现餐饮预订平台幂等导入与 `MOCK_REST`/`RES_3101_009` 每日可审计模拟滚动，以当前预订和独立 7/30/365 天 POS 客流/订单/历史人效趋势生成明天、下周、下月 FactBook，并按岗位技能、班次工时、周工时上限和目标人效生成排班建议；历史 actual/target 仅作证据，LLM 为必经解释层且模型不得新增数字，失败时明确不可用而不退回旧算法。Web 已提供老板/店长/人事角色化预测排班工作台、来源标签、逐日班次与指纹绑定的确认审计。Python 核心 49/49、餐饮意图路由 799/799、Web 目标 50/50、`vue-tsc` 与生产 Vite build 全部通过；TokenHub/火山/Aliyun 路由配置保持未改。待 PR 合并后从 clean exact `origin/main` 发布 Python/Web、仅为两租户滚动模拟预订/策略并逐表记录写入，随后完成真实餐饮 AI 与页面只读验收；scope 已释放。
- `FIX-RESTAURANT-STAFFING-STORE-DIMENSION-20260803` — `review` — Owner: `/root` — Base SHA: `724793acbc1176f68cef60ec24d4dd13d721850b` — 首次生产 `_run_case` 验收证明三种时间跨度均已识别为预测排班，但 `RESTAURANT_OPS_STAFFING_ADVICE` 的执行能力表漏声明 `store`，使“各门店”问法在 FactBook/LLM 前被拒绝。现已把真实可执行粒度改为 `store + time`，同时保留排班不生成单店切换按钮的限制，因为自然语言 resolver 当前返回全店 FactBook、尚不消费单店筛选；直接相关 159/159、餐饮全域 1523/1523 通过。待小 PR 合并后仅部署 Python 并用原四条生产真入口复验；未改 LLM 路由、FactBook、数据库、Java/Web，生产业务写入为 0，scope 已释放。
- `FIX-RESTAURANT-STAFFING-OPTIMIZE-PLAN-20260803` — `review` — Owner: `/root` — Base SHA: `7faadbdd4d6f62a2d38716513be1fe56ff8ca411` — 第二租户生产 `_run_case` 证明同一句各门店排班问法会因 LLM 将 `analysis_action` 标为 `optimize`，在 planner 已选 `STAFFING_ADVICE` 后仍被通用分支覆盖成 `BUSINESS_OPTIMIZATION`，再由接地闸拒绝。现已把预测排班列为完整能力：其自身用预订、7/30/365 趋势、技能与工时完成优化，不能改走通用综合；确定性复现 2/2、餐饮全域 1524/1524 通过。待小 PR 合并后仅部署 Python 并复验两租户真入口；未改 provider/model、FactBook、数据库、Java/Web，生产业务写入为 0，scope 已释放。
