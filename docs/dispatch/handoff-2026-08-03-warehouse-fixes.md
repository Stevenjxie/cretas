# 交接 — 仓储/结单/单位六处修复 + F006 最小闭环

**日期**: 2026-08-03
**分支**: `codex/claude-warehouse-fixes` → **PR #2220 已合并** (`b66f8810c4`)
**工作目录**: `C:\Users\Steve\cretas-multibatch`

---

## 🚀 已上线并在 prod 实测通过（2026-08-03 03:33 部署）

`RELEASE_FINAL_STATUS=deployed`（Java + Web，356s；因 Flyway 迁移变更走 backend-first 顺序部署，
后端 green 槽 health pass，Web 四路 hash 一致）。**三条判据都取了部署前后对照：**

| 判据 | 部署前 | 部署后 |
|---|---|---|
| **修复 3+4** 结单 BOM 允许集 | `materialTypeIds` **1 个** | **3 个**（原料 + 2 包材）|
| **修复 3+4** 结单本身 | `409 所选原料批次不属于该生产计划 BOM` | **`COMPLETED`**，`settlementId 8510f91e…`，实际产出 50 box，`postingStatus=PENDING_WAREHOUSE_RECEIPT` |
| **修复 5** 报工过期库存 | 界面只有「可用 0kg」 | 后端 `"available":0,"expired":300.0`；界面「0kg **过期 300kg，不可投料，请联系仓管处理**」|
| **修复 6** 分批到货第二批 | `409 数据已存在，请勿重复提交` | **`200 收货确认成功，库存批次已生成`**，新批次 `MT-20260803-3730`，PO 从待收货任务消失 |

> F006 猪蹄线最小闭环（`PLAN-1785685243382-18046415`）现已从「报原料 → 报半成品 → 报成品」
> 一路走通到**结单**。结单对话框显示「审计通过」并自动带出 3 个原料批次
> （冻猪蹄 20kg + 成品盒 50 盒 + 封膜 50 片）—— 正是新允许集里的 3 个 SKU。

> ⚠️ 实测修正：交接原文说羊排「原料仓 100kg 全过期」，prod 实际是 **300kg**。

### 🔴 回归比对的一个方法教训：只比「失败类集合」不够

第一轮比对得出「40 = 40，集合一致」，看着就是零新增。但再往下比**每个失败类的
Failures/Errors 计数**，发现 `TransferShipBatchSelectionTest` 在 main 上 2 个 error、
分支上 3 个 —— 多的那条是随修复 #1 失效的 stub（`UnnecessaryStubbing`）。
**这个类本来就在失败集合里，集合比对永远看不见它变差。**
判据补成三层：失败类集合 → 每类 Failures/Errors 计数 → 差异类的失败方法集合。

---

## ✅ 已跑完（2026-08-03 续）：回归比对出 3 个新增失败，已逐条处理

全量回归跑完并比对了失败**类集合**（不是计数）：相对 `origin/main` **新增 3 个失败类**，
三条都落在本 PR 改过的面上。判据取法：这 3 个类在 `origin/main`(33fc652a5d) 上单独实跑
**31 tests 全绿 / BUILD SUCCESS**，在本分支上红 —— 所以是本 PR 引入的，不是既有失败。

| 新增失败类 | 处理 |
|---|---|
| `TransferUnitCanonicalizationTest`（3 条红） | **撤回本 PR 的修复 #2**，见下 |
| `BomRecipeDraftLifecycleJpaTest#workflowUpgradeAppendsNewInputSkeleton` | 修**假件**（生产代码没问题） |
| `TransferMaterialPackagingConversionTest` | 移除随修复 #1 失效的 stub |

### 🔴 修复 #2（单位归一覆盖自定义单位）已撤回 —— 它会把一起线上事故放回来

去掉 `filter(MASS|VOLUME)` 不只是放开「盒→box」，**同时放开了权威别名表里的
`alias("pcs","pcs","件","个","只")`** —— 把用户选的「只」写回 `pcs`，正是 #1976 钉住的
LIUSHANMEN 2026-07-30 事故本身（生产仓 501 只，报工按字面比较看不见）。
`TransferUnitCanonicalizationTest` 就是那次的回归测试，它红得完全正确。

第二个理由更要紧：**报工侧 `canonicalNativeUnit` 的同款 filter 没有一起改**。两侧口径必须一致，
只改调拨一侧是把一处口径打架换成另一处 —— 与本 PR 自己的主题相反。

> ⚠️ 一并删掉了新加的 `TransferCustomUnitCanonicalTest`：它自己 `mock` 一份 catalog
> （`catalog("盒","box",COUNT)`），**从不碰真别名表**，所以对「只/件/个 塌成 pcs」完全无感。
> 老测试特意用真实 `UnitContractServiceImpl` 并写明「测的是真的契约怎么归一，而不是我编的
> 一份假别名表」—— 正是为了防这个。

**⏸ 于是 P0-1（包材有 10000 盒却报「可用 0」）在代码层仍未修**，只有 F006 那批数据被改过。
真根因在**存储**：同一物料档案存 `box` 而批次存「盒」。该修的是**写入路径归一 + 一次性数据迁移**，
而这依赖 Steve 尚未拍板的「单位存以码为准还是以中文为准」（memory 2026-08-01 记为未决；
本文档下方「待你拍板」里的「单位创建允许纯中文、英文码自动生成」是同一件事的另一半）。
**定了口径再动，两侧一起改。**

---

## ⚠️ 原「下一个 session 第一件事」（已完成，保留判据）

**跑完整回归并比对失败集合。** 提 PR 时 `mvn -o clean test` 仍在跑，结果未比对。

```bash
cd C:\Users\Steve\cretas-multibatch\backend\java\cretas-api
mvn -o clean test
# 然后按「失败类集合」比对，不是看计数
```

基线（`origin/main`）**40 个失败类**，清单在
`D:\Temp\claude\...\scratchpad\after.txt`（若已清理，就在 main 上重跑一次取基线）。
**判据：新增失败 = 0。**

> 🔴 **跑全量期间绝对不要碰 `target/`。** 本次踩过：后台 `mvn test` 跑着的时候我又跑
> `mvn compile`，两者共用 `target/`，把正在被加载的 class 目录重写了 →
> `ClassNotFoundException`，失败面横跨物流/MCP 等完全无关模块（191 vs 基线 40），
> 整轮结果作废。

> 🔴 **自查语句要确认是有条件的。** 我写过 `comm -13 a b; echo "(空=零新增)"` ——
> `echo` 是无条件打印的，于是上面明明列了 191 个新增，我却贴了「零新增」。
> 这个坑本仓已踩第二次（上次是 `git status; echo "(空=干净)"`）。

---

## 本次改了什么（PR #2220，15 个文件）

六条修复，共同形状：**两处规则各自看着合理，合起来把路堵死，报错文案指不到真因**。

| # | 修复 | 核心判据 |
|---|---|---|
| 1 | 调拨去掉重复提交拦截 | 去重键无内容维度；兄弟实现都有，唯独调拨没有 |
| ~~2~~ | ~~单位归一覆盖自定义单位（盒↔box）~~ | **已撤回**，见上方 —— 会放回 #1976 的线上事故 |
| 3 | 结单允许集不消灭无槽行（**保住辅料/包材**） | 闸门 `anyMatch(node!=null)` 对全部行生效 |
| 4 | BOM 激活补孤儿槽反向断言 | 只有「槽→行」，缺「行→槽」 |
| 5 | 报工显示过期库存 | `status='AVAILABLE'` 静默滤掉过期批次 |
| 6 | 应付按收货单唯一（`V20261029_49`） | 代码要多条 AP/PO，约束只允许一条 |

每条都做了变异实证（红在对应回归条、其余绿）。**3/4 的语义用例是同形函数、不调用生产代码**，
所以另补了两条源码级契约钉住生产实现 —— 变异时正是这两条变红。

### 已对 prod 做的数据改动

| 改动 | 说明 |
|---|---|
| F006 BOM 4 条包材行清空工艺槽 | 干跑核对后提交；原料行原样保留 |
| F006「成品盒」批次 `quantity_unit` 「盒」→`box` | 修单位不匹配，改完同一调拨请求立即通过（P0-1 根因实证） |
| F006 两个半成品 + 成品配净含量 | 半成品 1000g，成品按 SKU 名的克数 |
| F006 新建 3 个半成品 SKU | 猪蹄净肉/碎肉/卤制猪蹄（`PTSEMI-F006-200x`） |

**LIUSHANMEN 那份 BOM 不动** —— 它的辅料槽本就是 NULL，属正确形态，代码修复后就不会被误杀。

---

## F006 现状（可直接接着用）

| 项 | 值 |
|---|---|
| 账号 | `f006_admin` / `123456` |
| workflow 138 | SHH0713香辣孜然羊排（**多入单出**，2 工序），PUBLISHED + BOM ACTIVE |
| workflow 140 | 叮咚好食光卤猪蹄200g（**单入多出 + 3 工序**），PUBLISHED + BOM ACTIVE |
| 已跑通的计划 | `2b848faf-6891-4b7a-b3b3-1c30881c6dd0`（三道全部报完） |

### 闭环验证结果（猪蹄线）

```
冻猪蹄 20kg →〔拆骨〕→ 净肉14 + 碎肉4 →〔卤制〕→ 卤制猪蹄15kg →〔拼装分装〕→ 成品50盒
```

**已验证通过**：单入多出 UI 正确、多入单出自动带出来源批次、多工序串联、单位全链取自
SKU 档案、包材来自 BOM 且按用量校验库存并计入成本、成本按重量在多产出间分摊
（440 ÷ 18kg = 24.44/kg）、成本按批次实际单价计。

**卡住的最后一步**：结单 `409 所选原料批次不属于该生产计划 BOM` —— 本次修复 3+4 就是治它，
**但改完还没重新验过**。

---

## 下一个 session 的待办（按优先级）

1. **跑全量回归比对失败集合**（见开头）
2. **合并 #2220 → 部署 → 重跑闭环验证**
   - 部署只认 `RELEASE_FINAL_STATUS=deployed`（`DEPLOY_EXIT` 不是脚本输出，是包装脚本自己 echo 的）
   - 重跑结单，确认 3+4 真的治好了 409
   - 重跑分批收货（`PO-20260707-0002` 还卡着待收 6.544kg），确认 6 生效
   - 报工页确认过期提示显示（F006 羊排原料仓 100kg 全过期）
3. **羊排线闭环还没跑通** —— 原料全部过期（07-15~07-18），需要一批新货。
   入库路径见下方「已知的入库限制」。
4. **P0-4 之外的入库问题**：缺凭证的校验太晚（按钮全程可点，二次确认后才报 409）

### 已知的入库限制（走过一遍，有坑）

- `POST /material-batches` 已 `@Deprecated` 并直接拒绝，提示只能走
  「仓储来源任务 / 受控调整 / 期初建账」
- `POST /material-batches/opening` 期初建账**同一工厂当天只能一次**（凭证号按工厂+日生成，
  冲突时报 `409 数据已存在`，`hintTarget: factory_id, voucher_number`，调用方给不了凭证号）
- 采购收货流程完整可用：待收货任务 → 收货弹窗（含**实际到货包装/折合基本量**）→
  创建草稿 → **必须上传凭证** → 确认入库

---

## 待你拍板/未做的

| 项 | 状态 |
|---|---|
| BOM 成本卡上传识别 | **Steve 已说不做** |
| 单位创建允许纯中文（英文码自动生成，不好翻译用拼音） | Steve 已拍板规则，**代码未做** —— `unit_code` 目前 `nullable=false` 必填 |
| F006 其余产品线重建 | 未做 |

---

## 我在本轮判断错、已撤回的（避免下一个 session 重蹈）

| 我原来的说法 | 实际 | 判据 |
|---|---|---|
| 「多产出第 2 个产出永远扣不掉、97.78 元凭空消失」 | **设计行为** —— SFI 投料按设计在**结单**才扣，我只跑到报工 | 说「某数据没被更新」前，先确认它的更新时点是不是在我没走到的后续环节 |
| 「结单被半成品卡住」 | **根因不是半成品** —— 半成品那类根本不查 BOM；真因是包材行的工艺槽指向已删除节点 | 先调只读端点看系统自己怎么分类，别从错误文案猜 |
| 「羊排 200kg 有货」 | **0 可用** —— 批次全 DEPLETED/EXPIRED | 算可用量必须带 `status='AVAILABLE'` + 未过期 + 排除 `WIP-%` |
| 「工序投入单位不一致是 P0 缺陷」 | **我建模错了** —— 包材不该做成 workflow 输入端口 | 报缺陷前先看真实数据里同类场景怎么建的（真实 SOP 线的定量包装只有 1 个半成品输入端口） |
| 「包材行的工艺槽是 NULL」 | 对 F006 那份**是错的** —— 槽非空但指向已删除节点 | 子代理用只读端点 `settlement-bom-eligibility` 实测推翻（BOM 3 条，闸只认 1 条） |

---

## 参考

- 问题清单（12 条，含 UI 交互记录）：`docs/dispatch/2026-08-02-f006-minloop-issues.md`
- 前一轮的两个 PR：#2208（多批次投料）、#2213（调料配方按成品查），均已上线并验证
- 相关设计文档：`docs/superpowers/specs/2026-08-02-seasoning-recipe-owner-design.md`
