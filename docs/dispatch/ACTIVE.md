# Dispatch 台账 — ACTIVE
**最后更新**：2026-07-24

**当前真值**：`origin/main` 为代码与合并真值；本文件为当前任务状态真值。完成记录见 [2026-07-24-active-history.md](archive/2026-07-24-active-history.md)，此前历史见 [2026-07-23-active-history.md](archive/2026-07-23-active-history.md)。

## 在飞任务

- `RELEASE-CRETAS-ANDROID-1.0.2-20260724` — `review` — Owner: `/root` — Base SHA: `6514af9ed352125e9b12ba6aec960f9d74f2b084` — Worktree: `C:\Users\Steve\cretas-apk-release-1.0.2` — Scope lock: `frontend/CretasFoodTrace/{app.json,app.config.js,android/app/build.gradle}`、`download-page/index.html`、`docs/dispatch/{ACTIVE.md,archive/2026-07-24-active-history.md}`；候选 APK 已通过 production 配置、`1.0.2` / versionCode `13` 元数据、release keystore 指纹和 SHA-256 本地硬闸，严格 `NOT_DEPLOYED`；合入后必须证明 RN tree 等价，并通过 OSS/CDN 上传与公网下载回验后才可归档。
- `SEC-CREDENTIAL-ROTATION-20260719` — `blocked` — Owner: Codex coordinator — Base SHA: `1ba9a241a77144a80851051efbac584abf4db69d` — tracked tree、47/139 非受控副本、数据库/JWT/internal/BaoTa 轮换和生产验收已完成；仍需各供应商控制台吊销无法由当前 API 权限完成的旧长期凭证，详情见 [凭证轮换收尾记录](../security/credential-rotation-closeout-2026-07-20.md)。
- `ENH-F006-SUPPLIER-IMPORT-001`（含 `REQ-F006-SUPPLIER-REQUIRED-CONTACT-001`、`ENH-F006-SUPPLIER-DETAIL-STATUS-001`）— `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 复用现有供应链 Excel 共享能力，统一供应商必填资料、导入预览/幂等确认、状态生命周期、采购门禁与详情信息架构；只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `BUG-F006-MATERIAL-CATEGORY-CASCADE-001`（含 `BUG-F006-MATERIAL-AUTOFILL-UNIT-001`）— `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 修复原料类型字典 category/L1/L2/L3 单一真值、异步级联与单位建议来源/手动覆盖语义，后端 fail-closed 校验；只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `FIX-F006-R2-TAXONOMY-MIGRATION-CONFLICT-001` — `review` — Owner: `/root/taxonomy_migration_guard` — Base SHA: `9318305f4c146f5b253f3e920a7d8978868a33f2` — 收口分类规范化迁移对历史同父同名/空白/大小写冲突的启动风险：不得自动合并或改写生产分类；迁移必须可解释 fail-closed/延后唯一索引，服务层继续并发门禁，并补迁移 fixture 与真实 JPA Context；严格 `NOT_DEPLOYED`、生产零写。
- `BUG-F006-UNIT-DIMENSION-SCOPE-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 以共享 UnitContractService 为唯一真值补齐量纲/使用场景/物料白名单，统一原料、采购、入库、BOM 的单位门禁与 Web 选项；canonical/display 分离，只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `ARCH-F006-MATERIAL-TAXONOMY-DUPLICATION-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 明确 L1/L2/L3 共享分类与 MaterialType 具体物料边界，移除同名 L3 自动创建，补分类独立权限、父级内规范化去重/并发门禁与只读旧数据兼容；只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `ENH-F006-MATERIAL-TAXONOMY-SUGGESTION-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 基于现有分类字典/同义词/同工厂已确认物料提供可解释的完整 L1-L3 候选与置信度，支持人工覆盖和 Excel 预览复用，绝不自动建类；只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `BUG-F006-PURCHASE-ORDER-CREATE-ATTACHMENTS-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 复用现有采购附件链，在普通新建表单提供多文件暂存、一次创建受控绑定、失败清理、幂等、权限与审计一致性；不得修改现有 `PO-20260720-0001`，只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-TAX-RATE-INHERIT-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 采购行默认继承 MaterialType 采购税率，持久化订单行不可变税率快照并由后端权威重算未税/税/含税金额；不桥接历史订单，只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-ACTION-IA-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 精简采购订单列表操作区，外层仅保留对外供货单、详情、状态允许时提交及去重后的更多菜单，移除无价值审计入口但保留底层审计能力；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-APPROVAL-ROUTING-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 提交前只读解析真实采购审批路由，确认后事务内唯一启动工作流实例与待办；无有效流程/明确默认负责人时 fail-closed，补职责分离、幂等和财审阶段区分；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-WAREHOUSE-RECEIVING-BOUNDARY-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 审计并收敛采购 receive 与仓储采购入库为唯一仓储收货任务/库存写路径；采购详情移除收货写动作，仅追踪到货、实收、质检、批次并跳转仓储工作台；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-RECEIPT-DUPLICATE-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 修复已创建收货单后采购页仍显示已收0并可重复创建的问题；OA完成自动唯一仓储任务，仓储统一处理附件/批次/质检/入库/打印，支持部分到货和守恒幂等；禁止重放或桥接 `PO-20260720-0001`，严格 `NOT_DEPLOYED`。
- `ARCH-F006-UNIFIED-MATERIAL-RECEIPT-SOURCE-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 将“采购入库”统一为仓储原料/物料入库，批次/任务保存 PURCHASE、CUSTOMER_SUPPLIED、TRANSFER、退货/退料、期初/调整等结构化来源与库存所有权；客供料按客户/订单隔离成本和领用；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 收敛采购到应付/付款职责：采购侧移除付款申请写入口并只读展示采购、收货、发票、付款四维状态；复用现有 AP、PaymentRequest、财务工作台与 OA，按付款条款和收货/发票里程碑幂等生成应付与付款计划，财务侧完成三单匹配、审批、支付和核销；保留历史数据且零桥接，只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001-CORE` — `in-progress` — Owner: `/root/final_backend_review` — Base SHA: `ff7ae8784` — 实现 AP payable 未清余额/状态、payment allocation、财务执行 fail-closed 与真实 JPA Context 门禁；收口旧 `PaymentRequest` 采购/OA 绕过写入口，并确保历史无 allocation 应付仅进入待核对而不被迁移重开；scope 限 finance/payment controller、migration 与对应测试，不修改采购 UI、复盘或 ACTIVE；严格 `NOT_DEPLOYED`、生产零写。
- `ARCH-F006-DEMAND-DRIVEN-MATERIAL-RECEIPT-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 统一销售净需求、采购建议、采购订单、仓储待收、客供料所有权与批次来源链；关闭日常无来源“入库登记”和批次“续入”，有来源合并入既有批次必须执行完整身份/单位/日期/QC/成本匹配并写独立库存交易；历史记录只读兼容，只合入 `main`，严格 `NOT_DEPLOYED`。
- `ENH-F006-CUSTOMER-CREATE-PROGRESSIVE-DISCLOSURE-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 客户新增/编辑表单采用渐进披露，客户名称/联系人/联系电话/收货地址/状态统一为前后端必填，其余 CRM、开票、信用与行业信息进入默认折叠高级区；历史缺项只读兼容但新销售订单使用前要求补齐；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-CUSTOMER-NAV-001` — `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `667954797` — 客户详情补统一返回按钮与可点击面包屑，保留客户列表搜索/筛选/分页/滚动状态，并覆盖直接 URL 与浏览器历史兜底；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-SKU-UNIT-001`（含 `UX-F006-R2-SKU-SPEC-001`）— `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `667954797` — SKU 新建/编辑统一以 displayUnit 展示销售/包装单位，并将基本单位与净含量合并为紧凑规格行；净含量单选范围为 g/kg/ml/L，新建默认 g、编辑不覆盖，同维度 canonical 化且跨质量/容量禁止隐式换算，历史回填与规格预览不泄漏工程码；只合入 `main`，严格 `NOT_DEPLOYED`。
- `UX-F006-R2-SKU-PROCESS-001` — `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `667954797` — 保留 SKU 行“工序/快捷工序配置”及右侧抽屉，并与版本化 Workflow 入口清楚并存；快捷抽屉补名称/编码/标签搜索、真实类别/产出单位/关联状态筛选、已关联置顶去重和大列表性能，添加后保持检索与滚动；只合入 `main`，严格 `NOT_DEPLOYED`。
- `FEATURE-F006-R2-WORKPROCESS-DEDUP-001`（含 `BUG-F006-R2-WORKPROCESS-SORT-001`、`BUG-F006-R2-WORKPROCESS-UNIT-MODEL-001`）— `in-progress` — Owner: `/root/workprocess_governance` — Base SHA: `0f2b8329c` — 重复治理仅建立 canonical/mergedInto 并关闭未来可选性，绝不迁移任何既有 SKU/Workflow/计划/报工等引用；同时从工序主数据业务语义移除全局排序与单位，顺序及节点输入/输出单位只属于 pinned Workflow 节点/计划快照；必须真实 JPA Context，禁止对生产重复组执行治理，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-VIEWPORT-001` — `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `0f2b8329c` — Workflow 编辑器占满全局导航后的剩余 viewport，body 不随画布撑高；工具栏/画布控件/AI 输入保持可见，兼容列表改画布内可折叠面板，并覆盖主流桌面视口、浏览器缩放与键盘可访问性；只合入 `main`，严格 `NOT_DEPLOYED`。
- `PRODUCT_DEFECT-F006-R2-BOM-OUTPUT-001`（含 `UX-F006-R2-BOM-DRAFT-ACTION-001`、`BUG-F006-R2-BOM-I18N-DELETE-001`、`FINAL-MODEL-F006-R2-BOM-MATERIAL-USAGE-001`）— `queued` — Owner: `/root` — Base SHA: `457b3b5e9` — 与 `FEATURE-WORKFLOW-BOM-AUTO-BINDING-DAG-001` 重叠，已串行吸收并暂停独立写入；保留原验收要求与 `NOT_DEPLOYED` 边界。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001`（含 `FEATURE-F006-R2-BOM-MULTIPACK-001`）— `queued` — Owner: `/root` — Base SHA: `457b3b5e9` — 与 `FEATURE-WORKFLOW-BOM-AUTO-BINDING-DAG-001` 重叠，已串行吸收并暂停独立写入；保留替代集合、成本不双算、计划快照和生产零写验收。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001-CORE` — `queued` — Owner: `/root/bom_substitute_core` — Base SHA: `a362fac0c` — 未发现 live thread、worktree 或开放 PR；由统一任务串行实现并保留真实 JPA/Service 门禁，暂停并发写入。
- `F006-R2-WORKFLOW-FIRST-BOM-GATE`（含 `F006-R2-BOM-COMPLETENESS-GATE`）— `queued` — Owner: `/root` — Base SHA: `457b3b5e9` — 与统一任务共用 BOM/Workflow 状态机，已串行吸收并暂停独立写入；运行态继续 ACTIVE-only，历史快照零改写，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INLINE-PROCESS-CATEGORY` — `in-progress` — Owner: `/root/workprocess_governance` — Base SHA: `0f2b8329c` — Workflow 现场创建工序补充与独立工序管理同源的必填类别选择，payload/主数据/节点快照保持同一 category identity，后端拒绝缺失、非法及跨工厂类别；旧数据只读兼容，生产零写入，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-CELL-ACTIONS-UX` — `in-progress` — Owner: `/root/workprocess_governance` — Base SHA: `0f2b8329c` — 统一原料/半成品/成品/工序 Cell 在 hover、focus 与触屏下的编辑删除入口，删除仅作用当前草稿且中文预览节点/连线影响，移除工序右上角重复“+”，历史快照零改写，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INPUT-MODE-DERIVATION` — `queued` — Owner: `/root` — Base SHA: `457b3b5e9` — 与统一任务的 Workflow 投入槽、BOM 主料/替代料和报工候选交集重叠，已串行吸收并暂停独立写入；发布与报工继续共用同一固定快照，严格 `NOT_DEPLOYED`。

## Scope 锁地图
- `FEATURE-LABEL-QC-COLD-START-001`：`backend/python/label_qc/**`、`backend/python/main.py`、`backend/python/auth_middleware.py`、对应 Python 测试；Java label-QC Entity/Enum/DTO/Repository/Service/Controller/Python client/Flyway 与真实 JPA/API 测试；`frontend/CretasFoodTrace` 质检拍检 screens/navigation/types/api/home 入口；`web-admin` label-QC API/types/route/审核页面及目标测试；`docs/specs/label-qc-cold-start.md`、`docs/dispatch/ACTIVE.md`。验收：Python tile/解析/聚合及内部鉴权测试，真实 JPA Context、Java Service/API 目标测试，RN TypeScript/目标测试，Web typecheck/目标测试/build，`git diff --check`；只创建 PR，严格 `NOT_DEPLOYED`、生产业务写入为 0。
- `FEATURE-WORKFLOW-BOM-AUTO-BINDING-DAG-001`：Java Workflow revision snapshot/兼容性/激活门禁、BOM Recipe/Family/Output Recipe/原料辅料包材与替代关系 Entity/DTO/Repository/Service/Controller/Flyway、DAG slice/升级映射、标准成本分摊、生产计划 resolve-by-outputs 与报工物料批次候选交集，以及对应真实 JPA/Service/合同测试；Web Admin BOM 配方/版本/辅料工作区、工艺来源卡片、升级冲突与 API/types/tests；`docs/dispatch/ACTIVE.md`。验收：首次 BOM 同事务自动固定唯一完整 DRAFT revision，BOM-only 克隆继承精确 snapshot，历史不漂移，稳定 node/port 升级 fail-closed，多投入全祖先回溯，多产出共享规则不复制且共享成本按比例/专属成本直归，角色与 100% 门禁，缺价不伪造 0，候选严格三重交集，真实 JPA startup、后端目标测试、Web typecheck/目标测试/build、UI 截图与只读审查、`git diff --check`，只创建 PR，`NOT_DEPLOYED`、生产零写。
- `SEC-CREDENTIAL-ROTATION-20260719`：`scripts/systemd/` 中遗留明文启动脚本、现有/新增 secret 扫描配置与测试、`.gitignore` / 凭证模板、`docs/dispatch/ACTIVE.md`、`docs/dispatch/archive/2026-07-19-active-history.md`；外部状态仅限已授权的 Cretas 47/139 服务器配置、PostgreSQL 角色密码、相关阿里云/API 凭证与必要服务重启。验收：tracked tree 与完整 Git 历史脱敏盘点、scanner gate、exact-main 发布门禁、Java/Python/网关健康、登录与 Restaurant Agent 只读 smoke、核心 ERP 零写入。
- `ENH-F006-SUPPLIER-IMPORT-001`：供应商 Java Entity/DTO/Controller/Service/Repository、采购下单与供应关系门禁、共享 Excel 导入基础设施及目标测试；Web Admin 供应商列表/统一详情/新增编辑/导入与 API/types/tests；`docs/qa/F006-MVP-E2E-bug-review-2026-07-20.md`、`docs/dispatch/ACTIVE.md` 与当日归档。验收：真实 JPA Context（如触及 Entity/Repository）、Java API/Service、Web 组件/payload/build、预览零业务写、幂等与跨工厂隔离、历史只读兼容、`NOT_DEPLOYED`。
- `BUG-F006-MATERIAL-CATEGORY-CASCADE-001`：Web Admin 原料类型字典创建/编辑级联与单位建议组件/helper/tests；Java 原料类型 DTO/Service/Controller 层级和单位兼容校验及目标测试；共享文档仅限复盘、ACTIVE 与当日归档。验收：L1/L2/L3 异步防 stale、单一 category 真值、manual/auto 来源、后端错配 4xx、kg/g 与 box/case/slice 合法生产形态、`NOT_DEPLOYED`。
- `BUG-F006-UNIT-DIMENSION-SCOPE-001`：Java 单位 Entity/DTO/Repository/UnitContractService、原料类型/采购/入库/BOM 使用场景校验及真实 JPA/Service 测试；Web Admin UnitSelect/API/types/display helper 和原料/采购/入库/BOM 相关单位选项目标测试；复盘/ACTIVE/当日归档。验收：INVENTORY_QUANTITY 排除 TIME/TEMPERATURE/RATIO 与未授权 LENGTH，物料/L3 白名单、非法 API 4xx 零部分写、显式包装换算、历史异常只读、canonical `case`/display“箱”、`NOT_DEPLOYED`。
- `ARCH-F006-MATERIAL-TAXONOMY-DUPLICATION-001`：Web Admin 原料类型分类帮助、快捷分类创建/申请与权限 UX、目标测试；Java MaterialCodeSegment Controller/Service/Repository/Entity（若必要）及权限、父级内规范化唯一门禁/JPA 测试；复盘/ACTIVE/当日归档。验收：物料创建不复制名称建 L3、多个物料共享分类、改物料名不改分类、分类权限/重复/并发、编码稳定、BOM/采购/入库继续引用 MaterialType、旧数据零迁移、`NOT_DEPLOYED`。
- `ENH-F006-MATERIAL-TAXONOMY-SUGGESTION-001`：Java 分类建议 DTO/Service/API、字典别名/关键词与同工厂历史匹配、原料 Excel 预览复用及测试；Web Admin 候选/置信度/理由/手动覆盖/重新匹配 UX 与目标测试；复盘/ACTIVE/当日归档。验收：高/中/低置信、规格噪声/同义词/多候选、无匹配、手动覆盖、名称变化、零自动建类、跨工厂隔离、Excel 未确认行禁提交、保存父链 4xx、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-CREATE-ATTACHMENTS-001`：现有订单附件实体/API/存储服务审计；Java 临时上传/绑定/过期清理/工厂权限/幂等及目标测试；Web Admin 普通新建附件区、进度/删除/重试/失败回滚与目标测试；复盘/ACTIVE/当日归档。验收：含附件创建、创建失败无孤儿、部分上传失败不静默、重复提交不重复绑定、编辑/复制/下载/删除权限一致、生产订单零修改、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-TAX-RATE-INHERIT-001`：RawMaterialType采购税率读取、PurchaseOrderItem税率/金额快照、采购Service后端权威计算、编辑/复制/审批/入库一致性；Web Admin物料选择默认税率与金额核对；Java/Web目标测试及复盘/ACTIVE/当日归档。验收：13%、免税、显式有权限覆盖、历史快照不漂移、非法金额不被信任、旧订单零桥接、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-ACTION-IA-001`：Web Admin采购订单列表状态/权限驱动操作、去重后的更多菜单、危险取消确认、响应式/键盘可用性及目标测试；复盘/ACTIVE/当日归档。验收：外层严格四类入口、不可提交状态无提交、菜单无重复、审计UI移除但日志保留、窄屏不溢出、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-APPROVAL-ROUTING-001`：现有 Canvas PURCHASE_ORDER workflow/rule/task/notification 审计；Java resolve/submit DTO、事务启动、唯一实例/待办、默认路由/自批/幂等门禁与目标测试；Web Admin 提交确认、当前审批人与业务审批/财审区分及目标测试；复盘/ACTIVE/当日归档。验收：有流程角色/人员、自动通过理由、无路由4xx、重复提交不重建、自批拒绝、财审衔接、生产PO零修改、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-WAREHOUSE-RECEIVING-BOUNDARY-001`：采购详情 receive/openReceiveDialog、后端 receive API 与现有仓储采购入库/任务/质检/批次/库存服务审计；Java 仓储任务、分批/容差/欠收/拒收/质检/单位规格快照/幂等回写及测试；Web Admin 采购只读追踪与仓储唯一写入口及测试；复盘/ACTIVE/当日归档。验收：采购权限不可收货、仓储权限与跨厂跨仓隔离、部分/超收/重复点击、质检不绕过、库存仅仓储动作增加、生产PO/库存零修改、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-RECEIPT-DUPLICATE-001`：现有收货记录聚合/任务唯一键/确认幂等、仓储任务ETA排序筛选、全量快捷/部分到货/QC/附件/正式与待核对打印、PO-receipt-batch-inventory守恒及 Java/Web/E2E 目标测试；复盘/ACTIVE/当日归档。验收：OA完成仅一任务、已有未完成单不重复、真实已收聚合、收齐无写入口、凭证可配置必填、重复确认不双入库、生产记录零写、`NOT_DEPLOYED`。
- `ARCH-F006-UNIFIED-MATERIAL-RECEIPT-SOURCE-001`：现有 MaterialBatch/采购入库/客供料/调拨/销售退回/生产退料消费者审计；Java sourceType/sourceId/sourceNo、ownershipType/ownerCustomerId、统一待入库任务、来源身份/数量/单位/幂等/跨订单领料门禁及真实JPA测试；Web Admin页面命名、来源筛选徽标/批次列/手工入口门禁及测试；复盘/ACTIVE/当日归档。验收：采购、客供、调拨、销售退回、生产退料来源可追溯，客供不进应付且跨客户领料拒绝，库存写仅仓储动作，生产零写、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001`：现有 PaymentRequest/AP/付款计划/应付账款/财务工作台/OA/凭证与采购详情 `goRequestPayment` 只读审计；Java 付款条款里程碑、AP 唯一事件键、部分收货累计、三单匹配/调整、客供排除、财务权限/OA节点门禁及测试；Web Admin 删除采购管理付款申请入口、采购列表/详情四维状态与只读财务进度、财务工作台入口及测试；复盘/ACTIVE/当日归档。验收：预付/现结/票到/月结、部分收货与重复事件幂等、金额后端真值、价税退货调整不覆盖历史、采购零付款写权限、历史 PaymentRequest 只读兼容、跨厂隔离、生产 PO/RCV/财务零修改、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001-CORE`：仅 `backend/java/cretas-api` 内 finance AP entity/DTO/repository/service/controller/Flyway `V20261028_94*` 与对应真实 JPA/Service/Controller 测试；不得修改 `PurchaseServiceImpl`、PaymentRequest legacy 数据、Web、复盘/ACTIVE。验收：一付多应付/多付一应付、部分/全额核销、未清上限、supplier/currency/source identity、幂等/CAS/跨厂、自批自付门禁接口、历史孤立付款只读异常标记，JPA Context 通过，`NOT_DEPLOYED`。
- `ARCH-F006-DEMAND-DRIVEN-MATERIAL-RECEIPT-001`：现有销售订单确认/生产需求/BOM净需求/采购申请/采购建议/入库登记/批次续入消费者审计；Java 销售需求版本与幂等键、需求到PO反向追溯、CUSTOMER_SUPPLIED任务与CUSTOMER_OWNED门禁、有来源批次合并匹配/库存交易/CAS及测试；Web Admin 销售缺料进度、采购“销售需求驱动”队列、仓储来源任务、隐藏普通入库登记和批次续入及测试；复盘/ACTIVE/当日归档。验收：足料不采购、部分不足净需求、重复确认不重复建议、多订单合并保留分配、客供不采购不应付、普通手工写入口关闭、严格批次合并/冲突/并发/交易追溯、生产记录零写、`NOT_DEPLOYED`。
- `ENH-F006-CUSTOMER-CREATE-PROGRESSIVE-DISCLOSURE-001`：Web Admin 客户新增/编辑/详情共享表单、校验定位、响应式与组件测试；Java Customer DTO/Service/Repository/API/import 与销售订单使用门禁及真实 JPA 测试（如触及查询）；复盘/ACTIVE/当日归档。验收：五项必填trim/电话多形态/地址可读/status显式、工厂内规范化名称去重、基础快速创建、高级折叠与已填计数、高级校验自动展开定位、编辑完整回显、历史缺项只读但下单拒绝、默认值零污染、Tab顺序/小屏滚动/重复提交、生产客户零修改、`NOT_DEPLOYED`。
- `BUG-F006-R2-CUSTOMER-NAV-001`：仅 Web Admin 客户详情导航、客户列表状态保存 helper 与目标测试；不得触碰后端、生产客户或其他模块。验收：列表进入/直接 URL/浏览器历史、搜索/筛选/分页/滚动恢复、无重复 history、`NOT_DEPLOYED`。
- `BUG-F006-R2-SKU-UNIT-001`：仅 Web Admin SKU 新建/编辑基础规格、共享 displayUnit/净含量单位 helper 与目标测试；Java 净含量契约由协调者另行审计后在不重叠 scope 落地。验收：box/case/slice 仅显示盒/箱/片，g/kg 与 ml/L 各自同维度等值归一化、跨维度拒绝、历史回填、单位切换不改真实量、规格预览中文化、`NOT_DEPLOYED`。
- `UX-F006-R2-SKU-PROCESS-001`：仅 Web Admin SKU 列表工序/Workflow 入口、关联工序抽屉、筛选/分页或虚拟滚动、Workflow 摘要与目标测试；不得改 Workflow 后端模型或生产数据。验收：入口语义清楚、167项可搜索筛选、已关联置顶且不重复、显示类型/单位/使用情况、绑定 Workflow 时展示名称/版本/状态并只读遵循 pinned workflow，`NOT_DEPLOYED`。
- `FEATURE-F006-R2-WORKPROCESS-DEDUP-001`：Java WorkProcess/Workflow/SKU 关联实体、DTO、Repository/Service/Controller、Flyway/JPA tests；Web Admin 工序重复治理与所有工序字典入口/导入导出/AI 的排序、单位字段移除及目标测试；复盘由协调者统一写。验收：用户显式选主、推荐理由不含排序/单位、只建 canonical/mergedInto 且仅未来候选排除，任何状态 Workflow、SKU、计划、报工、成本、质检和历史外键原样不迁移；已有 pinned 实例即使 master 停用仍可报工/结单，编辑可保留旧项但删除后不可选回；新 Workflow/新关联不出现治理项；冲突阻止、跨厂/幂等/CAS/回滚、无物理删除；工序 API 安全忽略旧排序/单位，Workflow 节点顺序及输入/输出 identity+canonical unit 快照稳定，错误报工单位拒绝；默认产出类型/出成率仅可作为模板建议且不得覆盖节点快照；生产零写、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-VIEWPORT-001`：仅 Web Admin Workflow 编辑器布局/CSS、兼容列表折叠面板、画布控件可见性与目标组件/E2E；不得改 Workflow 业务数据或后端。验收：1366x768、1440x900、1920x1080 及 80/100/125% 缩放时 document 不显著超过 viewport，画布 flex:1 内部交互、工具栏 sticky、缩放/模式/AI 控件不遮挡，侧栏独立滚动，Tab/Esc/焦点可见，`NOT_DEPLOYED`。
- `PRODUCT_DEFECT-F006-R2-BOM-OUTPUT-001`：Java BOM recipe create/ensure-draft/new-version、SKU 基本单位与净含量 DTO/Entity/Service 校验、唯一草稿/幂等/并发约束及目标测试；Web Admin BOM 版本表和零版本/DRAFT/ACTIVE 状态动作、displayUnit/净含量副文案与目标测试；复盘由协调者统一写。验收：box+800g、bottle+500ml 与 kg-base 均为 outputQuantity=1 且 unit 继承 SKU，净含量独立；禁止 productWeight/netContent/standardWeight 写入 output quantity；直接添加明细原子创建唯一 v1 DRAFT，重复点击/多 Tab/刷新不增版本；ACTIVE 且无 DRAFT 才完整克隆 next version；版本行无重复编辑动作/重复 toast；生产 `BOM-20260720-004`、SKU、库存和历史零写入，`NOT_DEPLOYED`。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001`：Java BOM item substitute 关系 Entity/DTO/Repository/Service、版本克隆/激活校验、计划物料需求/领料/报工/成本/追溯快照及真实 JPA/合同测试；Web Admin BOM 主料表单与树形明细、Workflow BOM 需求项归组、领料报工替代提示及目标测试；复盘由协调者统一写。验收：主料父项可维护多个替代料且无自由文本组码，自替代/同父重复/非法单位拒绝，明确换算，总等价需求守恒且成本不双算；Workflow 不复制第二套真值，计划固定 pinned BOM 替代集合，执行只允许快照授权物料并记录 original required material、真实 material/batch 与换算快照；当前不支持拆分时 fail-closed 限制一次只选一种；既有历史只读兼容、版本克隆持久、生产零写、`NOT_DEPLOYED`。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001-CORE`：仅 Java 结构化替代关系 Entity/DTO/Repository/new Service/Flyway `V20261028_99*` 与真实 JPA/Service 测试；不得修改 `BomRecipeServiceImpl`、计划/报工、Web、ACTIVE/复盘。验收：RAW/AUXILIARY/PACKAGING 共用 parent→substitutes，工序/包装层级作用域、同父唯一、自替代/跨维度/跨作用域拒绝、显式换算、历史 substituteGroup 零迁移、`NOT_DEPLOYED`。
- `F006-R2-WORKFLOW-FIRST-BOM-GATE`：Java Workflow 草稿结构判定、BOM 全部写入口前置门禁、BOM 完整性 DTO/Service/API、激活与 Workflow 发布/启用、生产计划准入及测试；Web Admin 阶段状态/缺失项/下一步引导与禁用测试；复盘由协调者统一写。验收：越级调用 4xx 且零部分写、空工序拒绝、缺辅料/包材列出明确缺项、同一 Workflow/BOM 草稿幂等、ACTIVE-only 运行契约不变、历史快照零改写、生产零写、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INLINE-PROCESS-CATEGORY`：仅 Java WorkProcess 创建 DTO/Service/类别校验与目标测试，以及 Web Admin Workflow 现场创建工序表单、共享类别数据源、payload/回填/筛选目标测试；与 `/root` 的 BOM readiness 代码不重叠。验收：类别必填可搜索、包装/前处理可选、保存与重开一致、缺失/非法/跨厂 4xx、旧 null 只读、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-CELL-ACTIONS-UX`：仅 Web Admin Workflow Material/Process Cell 组件、草稿图删除 helper/确认文案与组件/图结构测试；与 `/root` 的 BOM readiness 后端不重叠。验收：hover/focus/触屏操作一致、无选中前置、无布局跳动、工序重复“+”消失、删除显示节点与连线影响且不留孤儿连线、历史零写、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INPUT-MODE-DERIVATION`：Java BOM 替代关系到 Workflow 端口组的派生/快照/发布校验与计划报工准入，Web Admin Workflow 投入规则只读展示及目标测试；不得把派生规则写回 WorkProcess 主数据。验收：无完整 BOM=待配置、A/B 两组均必需、主项+替代项组选一、可选项可省略、多组不合并、非法/空组 fail-closed、历史快照不漂移、`NOT_DEPLOYED`。
## 阻塞项

- Aliyun 主账号旧 AccessKey：官方 RAM API 返回 `Forbidden` 并提示泄露风险，旧 key 的 STS 仍有效；必须由阿里云控制台主账号删除。
- 旧 Model Studio/DashScope、Zhipu、DeepSeek key：生产消费者已切换或禁用旧值，但需要在各供应商控制台执行禁用/删除；当前 replacement RAM key 无法通过已安装 CLI 调用 ModelStudio key-management OpenAPI。
- Mall 微信长期凭证：已从 tracked/0644 YAML 外置到 `0600` root-only EnvironmentFile，运行态验证通过；MP secret/token/AES key、Mini App secret、商户 key 仍需在微信公众平台/商户平台协调重置后同步更新受控环境文件。

## 维护规则

- 只记录当前在飞、阻塞和下一动作；已完成事项移入带日期的 `archive/`。
- 创建 worktree、派发或合并前，先核对本表与 `git worktree list`。
- 本文件只有当前协调者可修改；子代理只能按 `AGENTS.md` 的结构化回执格式报告状态。
- 状态只允许：`queued`、`claimed`、`in-progress`、`review`、`merged`、`blocked`。
- 合并完成时必须同时归档任务、释放 scope 锁，不能留下已经结束的在飞行。
- 同一仓库默认 WIP 上限：2 个代码执行任务 + 1 个只读/测试任务。
