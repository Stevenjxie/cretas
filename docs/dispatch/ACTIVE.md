# Dispatch 台账 — ACTIVE
**最后更新**：2026-08-16

**当前真值**：`origin/main` 为代码与合并真值；本文件为当前任务状态真值。完成记录见 [2026-08-16-active-history.md](archive/2026-08-16-active-history.md)、[2026-08-15-active-history.md](archive/2026-08-15-active-history.md)、[2026-08-14-active-history.md](archive/2026-08-14-active-history.md)、[2026-08-13-active-history.md](archive/2026-08-13-active-history.md)、[2026-08-12-active-history.md](archive/2026-08-12-active-history.md)、[2026-08-11-active-history.md](archive/2026-08-11-active-history.md)、[2026-08-10-active-history.md](archive/2026-08-10-active-history.md)、[2026-08-09-active-history.md](archive/2026-08-09-active-history.md)、[2026-08-07-active-history.md](archive/2026-08-07-active-history.md)、[2026-08-04-active-history.md](archive/2026-08-04-active-history.md)、[2026-08-03-active-history.md](archive/2026-08-03-active-history.md)、[2026-08-02-active-history.md](archive/2026-08-02-active-history.md)、[2026-08-01-active-history.md](archive/2026-08-01-active-history.md)、[2026-07-31-active-history.md](archive/2026-07-31-active-history.md)、[2026-07-30-active-history.md](archive/2026-07-30-active-history.md)、[2026-07-29-active-history.md](archive/2026-07-29-active-history.md)、[2026-07-28-active-history.md](archive/2026-07-28-active-history.md)、[2026-07-27-active-history.md](archive/2026-07-27-active-history.md) 与 [2026-07-26-active-history.md](archive/2026-07-26-active-history.md)，此前历史见 [2026-07-25-active-history.md](archive/2026-07-25-active-history.md)。

## 在飞任务

- `ML-LIUSHANMEN-WORK-AREA-ROI-20260811` — `in-progress` — Owner: `/root` — Base SHA: `52db837ec0b83c8ddb6d5ac50e5f17d4175ce2ba` — 独立四点 ROI 标注器、SHA 绑定队列、7+20 evaluator/workflow 分层门禁及 fail-closed `unknown_work_area` 已实现；台外真实缺标保留且不得回退，`df1f6029-389d-45b5-995e-be19b2f5b943` 必须由独立 sidecar 证明台外并单列。四轮人工 ROI 已服务端核验 8/8、24/24、16/16、18/18，累计 66 个独立任务、台内 1107、台外 253、unknown 0；第四轮审计回执为 `work-area-audit-20260811T190400752959Z.json`。66 张四角热图容量拟合为平均/最差 IoU 0.9973/0.9835、中心零错分，但任务级 8 折仍只有平均/最差 IoU 0.8134/0、98 个中心错分、3 个非法多边形且最差台内/台外召回均为 0，回执 `work-area-roi-corner-experiment-20260811T194100822073Z.json`，继续为 `insufficient_image_conditioned_roi_evidence`，不得保存模型或授权部署。为补齐失败更明显的 SKU 0013/0014，只读原始库计划 `work-area-raw-tray-plan-20260811T191537291550Z.json`（SHA `2dd4887df39c34073ad1f292a6650aeda80abe860b7e6c33a1ed629d92fc505e`）选定 30 个独立任务（0013/0014 各 15）；托盘队列 `tray-active-20260811T194228Z` 已服务端和逐文件核验 30/30、人工框 515、`reviewed=true/source=human`，源图/打包图/SHA/pHash/保护集与计划绑定全部通过。提交 `b4dc3f7447` 增加原始计划同批转 ROI 的受控桥接且 VisionLab 93/93 通过；独立队列 `work-area-active-20260811T205254283935Z`（manifest SHA `c7c4a5d3b8889908870b79b549c2831cb3dd11fdeddb76f88b7557caf502a306`，build receipt SHA `78d27ce57fcae2a740a342ef2c556554bbdeeacd1cdc835d32b15be795175cdd`）已人工完成 30/30，其中 29 个合法四点多边形、`trayal_0e5a259f_030` 因画面不含完整工作台边界正确标为 unjudgeable；台内 471、台外 43、unknown 1，ROI annotation-set SHA `2e24c48a3efc8dc4f8195d1eba9844da675b6b50a612662373b284c2d0618a9c`。正式 audit 按预期以 `unjudgeable=['trayal_0e5a259f_030']` fail closed，未写审计回执、未启动累计拟合。旧训练仍停在约 epoch 16；7+20、原 label MARK、保护集记录、数据集/检查点、生产模型与原图均未改写，严格 `NOT_DEPLOYED`。
- `LLM-PROVIDER-CLIFF-20260815` — `in-progress` — Owner: 另一 Claude session — Base SHA: `281fafda5e` — 🔴 **本线是阻塞项，优先级高于 `REST-AI-READ-PATH`（有倒计时，后者没有）**。背景：**aistore 额度 2026-09-13 硬到期**（owner 已确认不可续；`llm_router.py:139-141` 三条同一天），而它是 CHAT/INSIGHTS/CHART/MAPPER/REVIEW **五个槽的链首**（109 个产品调用点）。**已完成并部署 `281fafda5e`**（PR #2663/#2666/#2668 已合并）：DeepSeek 官方接回（`_provider_config` + `_SAFE_MODELS` 复审日 `2026-11-15` + `_MINIMAL_SAFE_SET` + `thinking:{"type":"disabled"}` 注入 + `record_local` 记账 + `$1/天` 花费告警）。**部署后实测核对**：五文件 md5 与 origin/main 逐条一致；进程 20:54:13 晚于制品 20:52:00；deepseek 在五槽链上第 2 位（chart 第 3）；把 today 推到 `2026-09-14` 时 review 链首 = deepseek ⇒ **9-13 自动接手，当天无需人工操作**。**剩余（A1–A6）**：① 七槽统一成 aistore→deepseek→zhipu（`SLOT.REASONING` 池加 aistore+deepseek，⚠️ 它 profile 是 `{}` 思考开着、预算 **30s**（`detector.py:1496` 是唯一调用点、不传 timeout），⛔ 不许拿 6s 判它）· ② 删 aliyun×3/`aliyun_a_deepseek`/tencent/ark 的**条目**，⛔ **不删机制**（`enable_thinking` 双开关 / `_TOKENHUB_*` / `_ARK_*` / 三个 thinking 分类集 —— owner 说「暂时」，删机制的账是重新要用时才结的，参照 #580）· ③ 补两格未量（deepseek profile B 严格 JSON / aistore profile C **真实 T3 prompt**）· ④ `2026-09-04`（registry 超龄）与 `2026-11-15`（复审日）挂**定时提醒** · ⑤ `deepseek_spend_alert.py` 与 `minimal_safe_set_liveness.py` 挂定时跑批（rc=2 须与 rc=1 不同措辞）· ⑥ 验收：七槽链贴原文 + **推 today** 的悬崖模拟（⛔ 不是 monkeypatch 到期日）+ 断言链首**真能应答**（⛔「可用 N/M」≠「N 个能答」，计费闸看不见配额状态）+ 变异 + 付费调用记账。⚠️ **模型选型已测维度零区分度**（契约 8×6 全过、延迟全在预算内、成本差是噪声、日期不由模型算）⇒ **不换模型**。⛔ 不手工重排链（`_build_chain` 按到期日算）；⛔ 不接 `deepseek_over_budget`（拒绝调用 = 9-13 后五槽全黑）。详见 memory `project_deepseek_official_requires_three_configs.md`。严格 `NOT_DEPLOYED`。
- `REST-AI-READ-PATH-20260812` — `in-progress` — Owner: **本 chat（唯一，全权：自裁自审自合并自部署）** — Base SHA: `281fafda5e` — 🔴 **2026-08-16 方向变更（推翻此前十几轮）**：目标不再是「数准」，是 **让老板打开之后能自己做出一个决定**；取舍排序 **能不能决定 > 数字准不准 > 覆盖得全不全**（owner 口述转录，⛔ 非逐字稿）。**B-1 日结改表格 已完成待合并（PR #2680）**：性质是**接线**——`output_preference` 一直被算出来放进响应但**零消费端**；接线前先修了它的潜伏缺陷（实测「别用表格」→ `('text','table')` 反而**锁死**表格；成因是没有 TEXT 档 + `return (TEXT,*forms)` 只加不减）。表格三列 营收/成本/毛利，⛔ 无毛利率列（`marginRate` 对缺卡菜恒为 `1−0.32=0.68` 是常数回声）；缺卡行写「缺成本卡」⛔ 不是 0/空/`-`；差额拆「缺卡 + 截断」且**恰好**成立；覆盖率限定语与套餐披露不许丢。判据：偏好闸 8/8（2 变异恰好红 {2,3,4,8}/{8}）· 呈现闸 9/9 + 2 阳性对照（5 变异恰好红 2a/1/3a/6/7）· 接线闸 9 passed（AST 守调用点，变异下**只有那一条红**）· **照抄 CI 的 pytest 命令 8142 passed / 0 failed / rc=0**。🔴 **我自己错过三次**：① 表格挂错意图（从意图描述字面推，而日结产出者是 `SALES_SUMMARY` 的 resolver）⇒ 接线接了个寂寞 · ② 四条披露之间没空行 ⇒ markdown 并成一坨（本仓同形前科，CI 全绿没发现）· ③ 限定语丢了 ⇒ 退化成普通 BI 报表。✅ **已上线并核对**：#2680(`317ecba78d`) + #2681(`cd52f5e987`) 两批均已合并、`deploy-smartbi-python.sh --env prod` 部署、md5 逐条一致、进程重启且启动晚于制品、**真跑一次那条路径通过**。真跑读数撞出并已修的一条：MOCK_REST 被截断的 4 道菜毛利 −¥115,674 而营收仅 ¥77,147（那张 ¥81.00 米饭坏卡）⇒ 已加「成本>营收点名 + 下一步动作」，prod 实测输出「米饭（成本 ¥120,123.00 > 营收 ¥4,449.00）…先核这几张成本卡」—— **产品第一次自己指着坏数据说话并给出动作**。✅ **表格已在真实载体上人眼看过（B-1 至此闭合）**。🔴 **载体此前挑错了**：真载体是 `mobile-rest-ai`（Vue + `marked`，餐厅老板开的），⛔ 不是 `CretasFoodTrace`（工厂溯源 RN app）。链路已核：`answer_text` → `GoldBackedRestaurantTool.java:287` → `data.message` → `App.vue:308 marked.parse({gfm:true})` → `style.css:411 .markdown-body table`。改动在服务端所以三个前端都吃得到，但**验收得去对的地方**。🔴 **看一眼抓到一个五道闸都没抓到的缺陷**：420px 手机宽下菜品列 212px（全表 55%）、表格实际宽 485px > 可见 385px ⇒ **最右的「毛利」列被挤出屏外**，而毛利是这张表的全部意义。文本闸 10/10、DOM 里 th 确实 4 个、CI 全绿、md5 一致、prod 真跑通过 —— 五样全绿唯独看不见。成因是我自己把「（份数最多，1774 份）」塞进了菜名格；挪进披露后 **212px → 73px**、表格宽 **485 → 386 == 可见宽**、四列全进屏。已上线（#2687 `f329ef3639`，md5 `307d3a7e…` 一致 / PID `2347850→2493998` 启动晚于制品 / 真跑通过）。新增 **`mobile-rest-ai/layout-gate.mjs`**：⛔ 不量「有没有 4 个 th」，量**最后一列右边界有没有超出可视区**；阳性对照把容器压到 200px 断言它报出超出 148px。复现：`cd mobile-rest-ai && npx vite --port 5199` → `node layout-gate.mjs`；肉眼看 `/mobile-ai/rest/preview-answer.html`（真 marked + 真 style.css）。⚠️ 另记一条我的错：第一次量溢出**量错了元素**（读 `.markdown-body` 的 overflow，而滚动容器是 `table` 自己），据此报过「老板永远看不到毛利」—— 错的，真相是「默认看不到、横滑能看到」，已用真的 `scrollLeft` 滚一次订正。✅ **B-3 / B-0 / B-4 / B-5 / A4 / A5 全部完成并上线**（#2689 `36dd78b6b6` · #2690 `0ddf57e78b` · #2691 `2ed3aa37bd`；部署后 md5 三条逐一一致 / PID `2493998→2512176` 启动 12:00:42 晚于制品 11:59:55 / **prod 三条路径各真跑一次**）。· **B-3** 开价加后果表述（后果用金额不用百分比；⛔ 底层两个百分数保留 —— 「不直观」最容易的误读是「别给数了」，变异 P4 钉住它）· **B-0** 时间词不再被当菜名，⛔ **没加一个黑名单词**（棘轮问得对：目录能拦住全部）——改成对时间语法整体匹配；中途两次误伤「半年陈花雕」都被对照抓到 · **B-4** 「今天到现在」不再等于开业至今；判别是「今天」在「到」之前，⛔「含今天就当今天」会把真累计一起改坏（变异 Q1，**在正向档上全绿**）· **推翻裁定③** 上半年/下半年=日历半年，设计卡 `docs/decisions/2026-08-16-上半年下半年-推翻裁定3.md`；顺带修掉更糟的一个：改前「今年上半年」→ `(1-1,8-16)` 标「今年」，比问的更宽还标错 · **B-5** 「接下来可以问什么」接到 `mobile-rest-ai`（⛔ 卡里说它零消费端是错的：web-admin 一直在消费，是**老板那条路**不显示）· **A4/A5** 每日体检跑批已挂 crontab `25 5 * * *`，端到端真跑过一次，台账/告警都写出来了，并**抓到一个真阳性**：`alive 4 / checked 5`，兜底最小集里有一个答不了。🧊 **NOTIF 挂账**（`docs/decisions/2026-08-16-NOTIF-挂账.md`）：实测三处断口，第三处让前两处失去意义 —— **老板用的 `mobile-rest-ai` 里 notification/通知/bell 命中 0**（阳性对照：同目录 chart 38 / feedback 19）⇒ 修 RN 的 `numberOfLines` 受益的不是他。⚠️ 这是本线**第二次载体误判**。🔴 **本轮最重要的发现：9-13 悬崖的接班计划实测没有装弹**（#2693 `7d953c3d02`）。跑批报 `liveness_rc=1` 顺下去查到：**`LLM_DEEPSEEK_API_KEY` 在活进程 environ 和 `.env.prod` 里都是长度 0**（其它 8 个 key 都是 35–51 字符 —— 阳性对照）。推时钟到 9-14 逐条问：`aistore refuse='expired'` ⇒ 调用时正确跳过 ✅；`deepseek refuse=None 但 key 长度 0` ⇒ 调用即 `NO_API_KEY`；`zhipu` 是**唯一还能答的**。⇒ 台账原先写的「9-13 自动接手，当天无需人工操作」**只有前半句是真的** —— 拒绝机制работает，接班人本身答不了。28 天后七个槽全部退化成 zhipu 单点、无兜底。已加「接班人体检」闸（带倒计时，阳性对照：给 deepseek 一个 key 后 THIN 从 7 降到 1；阴性对照：连 zhipu 也拿掉 → 7×MISSING 全黑）。⚠️ 顺带真发现：`SIMPLE_TEXT` 的池是 `['aistore','zhipu']`，**没有 deepseek** —— A1「七槽统一」没覆盖它，到期后它无论如何都是单点。🧊 **归因（层 2 反推）挂账**（`docs/decisions/2026-08-16-归因层2-挂账与解冻条件.md`）：与别的几件**不同形态** —— 不是零消费端。`diagnose` 被正确识别且有 12 个消费点（阳性对照：optimize 15 / compare 9 / lookup 23），窗口也对；但 `generic_answer.py:539` 对 `diagnose` **提前退出返回 None**（结构化侧不出归因），而 `answer_contract.py:279` 只查答案里**有没有**「原因拆解/驱动因素」这些**措辞**。⇒ **它会说得像在归因，而没有任何东西保证它归对了。** ⛔ 不半建：只算出来塞进 `structured_context` 就是知情地制造下一个形态 B。解冻要三样：① 基线由谁定（基线选错方向就是反的，需裁定）② 正文那条路的验收方式（LLM 输出不能靠「跑一次看着对」）③ 拆解不出来时的拒答形态。✅ **归因层 2 解冻并上线**（#2696 `ca5402bf6d`；md5 两条一致 / PID `2521899→2538366` 启动 13:14:22 晚于制品 13:13:39 / prod 真跑出正确文案）。挂账的三个条件**由本 chat 自己裁**：① 基线 = **上周同一天**⛔ 不是昨天（餐饮周内效应，跟昨天比会把「周末结束了」报成「生意变差了」，方向就错）② 文案**由代码渲染**⛔ 不交给 LLM —— 挂账时卡在「LLM 输出没法验收」，解法不是想办法验它，是**不让它干这件事** ③ 拆不出来**明说拆不出来**，⛔ 不许退回「今天营收 X 元」。承重墙是恒等式 `ΔR = ΔQ·P₀ + Q₀·ΔP + ΔQ·ΔP`，交叉项**单列不摊**（摊了就没法说主因），占比 >30% 时**不许编主因**。判据：拆解闸 12/12 + 两条阳性对照；四个变异各自恰好命中 —— 其中 **S2「总是挑一个主因」在恒等式档上完全绿**，只有第 3 档抓得住它（没有那一档，我会在大促那天告诉老板「主要是客流」而真相是他自己打的折）；**S4 期望 rc=2**（主因判反同时打坏了阳性对照本身，闸正确宣告读数作废 —— 三态不许压成两态）。接线闸 8 passed 含 AST 守调用点，三个接线变异全红。全量 **8158 passed / 0 failed**。✅ **`SIMPLE_TEXT` 补上 deepseek**（A1「七槽统一」漏了它）。⚠️ 它**仍然报 THIN，这是对的** —— 池里有了但 key 是空的，闸如实反映。✅ **归因已在真实数据上跑通**（#2698 `95a470b112`；md5 一致 / PID `2538366→2549953` 启动 13:52:39 晚于制品 13:51:56 / prod 两支各真跑一次）。MOCK_REST 真库实测：「跟上周同一天比，今天营收少了 ¥18,826.69；客流少 20 单、客单价低 ¥5.81；⇒ **主要是客单价**」，恒等式在真数据上成立。「没有可比基线」那一支也验了 —— ⚠️ 它只报缺**营收**不报缺单量，正是「`COUNT` 空集给 0 / `SUM` 空集给 NULL」，而营收**没被兜底成 0**，否则会编出一条「从 0 涨到 X」的假归因。🔴 **真跑之后发现一个设计时没想到的缺陷**：基线是把窗口整体前挪 7 天，对单日窗口没问题，窗口一长就**与主窗口重叠** —— 本月 9/16 天，**上半年 174/181 天**。那种「对比」两边几乎是同一批数据，拆出来的主因是噪音假装成洞察**而且带着一个精确的数字** ⇒ 比不归因更糟。已改成窗口 >7 天**明说跳过并留痕**；阳性对照钉住「7 天窗口仍然归因」（变异 X3 把上限收到 0 时红 5 条，含那条对照）。全量 8160 passed / 0 failed。⛔ **菜品级归因（「哪道菜拖了后腿」）决定不建**：客户原话是「为什么今天卖的这么少」，门店级已经答了它；那句话是**我自己在预览页里编的**，生产也不发它（真实追问走 `_topic_followups` 的指代式「这个菜的销量呢？」）。按「他哪句话要求了这个」——答不上来就是给自己造活。✅ **DeepSeek 已真正接进生产（2026-08-16）**。🔴 **成因**：`llm_router` 读环境变量 `LLM_DEEPSEEK_API_KEY`，而生产进程里它**存在但值长度 0** ⇒ `call_chain` 的 `if not api_key: continue` **完全静默**地跳过 deepseek（不报错、不留日志，`_refuse_reason` 也看不见 —— 它查白名单和到期日，⛔ 不查 key）。key 本身在 `/root/.deepseek_probe_key`（0600，35 字节，指纹 `9303ea56aa75`）；已填进 `.env.prod` 的空占位（备份 `.env.prod.bak.pre-deepseek-key-20260816_142520`）并重启。**验收三条 + 阴性对照，全过**：① 进程环境值长度 deepseek **35** / zhipu 49 / aistore 51（并排贴出来当对照，⛔ 不查「有没有这一行」）② **走路由器**真发，`chain=['deepseek']` → `served='deepseek-v4-flash'`，⛔ 不直打 api.deepseek.com③ 悬崖模拟推 `_today` 到 2026-09-14，CHAT/INSIGHTS/CHART/MAPPER/REVIEW **五个槽全部真能应答**且都是 deepseek。阴性对照：临时清空该变量重跑②，`All providers exhausted` ⇒ 断言有区分力。跑批复验：`liveness_rc 1→0`、`alive 4/5→**5/5**`、`single_point_after_cliff [7 个槽]→**[]**`。🔴 **本轮最该记住的教训**：这个洞活到今天，是因为上一次部署核对的四样**全是纯函数检查**（md5 一致 / 进程重启 / 链里有它 / 推 today 后链首变了）——**一样都不碰 key**。▎**「找到标记 ≠ 它能跑」** ⇒ **部署核对里必须有一条真实调用**。已把 `deepseek_wired_acceptance.py` 落进仓库。⚠️ 另记两条我自己的错：(a) 先前报「服务器上哪儿都没有 key」是错的 —— 我搜的是 `DEEPSEEK[A-Za-z_]*=` 这种**变量赋值**，而它是个**裸 key 文件**，任何这类正则都匹配不到；(b) 备份里那把 ...3892（指纹 `2647e1d43a16`）是**旧的且已吊销**（curl 直连官方回 `is invalid`），⛔ 别再拿它试。**下一动作（按序）**：① 🔴 **去 DeepSeek 控制台重新签发一把 key**（⛔ 别用备份里那把，已验证 401；旧 key 尾号 `3892`）。填进配置后跑 `llm-provider-health-daily.sh`，判据是 liveness `deepseek 1/1 活` 且 SUCCESSOR THIN 归零。倒计时 28 天。 ② 长窗口的归因基线（上月同期 / 去年同期）是另一条规则，需要时再建 ——现在它明说跳过，⛔ 不会给错答案 ③ 归因目前只挂门店级概览意图。**阻塞项**：`LLM_DEEPSEEK_API_KEY` 为空 —— 倒计时 28 天。**红线**：已被 owner 全部撤销；仅保留提醒——`MOCK_REST` ¥81.00 米饭卡与青花椒 ¥167.20 卡是唯二能验「产品自己指着坏数据说话」的样本，动它们前先确认还有别的样本。原始上下文：餐饮 AI **读路径**。完整方案见 [2026-08-12-restaurant-ai-architecture-plan.md](2026-08-12-restaurant-ai-architecture-plan.md)。⛔ **排在 `LLM-PROVIDER-CLIFF` 之后**（那条有倒计时）。日结九节链条已上线，三租户两条路 `diff=0.0`；T1–T6 已合并并部署（`281fafda5e`），生产实测「上个季度」→ `2026-04-01~06-30`（部署前落「全部历史」）。**剩余，按序**：**B0** 修 `dish` 抽取把「上半年」误当菜品 —— 实测「上半年营业额怎么样」被「菜品范围不能由全店汇总 resolver 代答」拦成反问；⚠️ 它同时是 **B2 的前置**（那条问句走不到答案分支，披露就永远触发不了），并且**推翻了裁定 ③ 的前提**：organizer 依据 `restaurant_ops_router.py:2714` 的**注释**保留「上半年→全部历史 是诚实回退」，而那条路**实测走不到**（形态 B，且是第二次「照注释讲的历史下结论」）⇒ 修 B0 时须一并确认它真的走得到 · **B1** T7-d：把「今天是 X」移出 system prompt 吃满 DeepSeek 前缀缓存（命中 vs 未命中差 **50 倍**，新前缀要第三次调用才命中；⚠️ 语义不许变） · **B2** 用**真实产品路径**验 T6② 的披露真的出现在正文里（⛔ 不注入构造 spec —— 本仓反复栽在「探针构造了生产上不出现的形状」）。⚠️ 记下不做：正文「上个季度（2026-06-29 至 2026-06-30）覆盖 2 天」括号里是**实际覆盖**不是**请求窗口**，读起来像系统认为季度就是那两天，措辞待改。⚠️ **两处订正，不照做会重走弯路**：(a) 「日期交给代码算」**已经是现状**（`restaurant_intent.py:5399` 明令禁止模型算日期，T6-f 实测 5/5 只吐结构），organizer 那轮「8 模型×6 题日期错 6/8」量的是**生产不存在的行为**，⛔ 不要据此改模型或重测 · (b) 真缺陷是模型在结构化词表里表达不了「季度」而**降级成「最近3个月」**，代码算得对、错在输入的词被换过。⛔ 不许跑全量 85 题电池，用 `--only`。上下文串发生率**冻结**（见 [2026-08-15-上下文串-挂账与解冻条件.md](../decisions/2026-08-15-上下文串-挂账与解冻条件.md)），⛔ 旧的 3/19 / 15.8% / 0/12 / 0/2 一律不作数。严格 `NOT_DEPLOYED`。
- `UX-RN-RESTAURANT-PROCUREMENT-DELIVERY-20260802` — `review` — Owner: `/root` — Base SHA: `579f648ed61c87e89f74065169a2a24bd72cff8c` — 餐饮 RN“采购确认送货”已完成：补齐餐饮管理员实际使用的管理栈路由，供应商与食材只使用业务名称选择/展示，移除供应商/档口/食材内部 ID 泄漏，修复搜索改字后旧食材 ID 残留，并用提交前阻塞原因和 52px 候选卡降低误操作；共享给工厂仓管的送货单页面未修改。目标 Jest 11/11；TypeScript 三次均精确只剩既有 6 个基线错误且对应文件未改；Expo Web 餐饮管理员真实路径可达，完整态启用、改字后重新禁用、残影为 0，允许 2 次已知 query-only list-summary POST，blocked mutation 0、实际业务写入 0。Scope lock：餐饮 `ProcurementDeliveryConfirmScreen`、餐饮模式条件路由及导航类型、对应纯函数/helper 与目标测试、`docs/specs/rn-restaurant-procurement-delivery-polish-2026-08-02.md`、`docs/dispatch/ACTIVE.md` 及当日归档。待 commit/PR/合并/OTA 发布。
- `FIX-F006-PRESTOCKED-SHIPMENT-E2E-20260809` — `review` — Owner: `/root` — Base SHA: `73848929c39be0ebaf33e029918ea0beeb425994` — 已收口客户已有成品库存的正式发货链：销售只能创建发货单、仓管确认实际数量并执行唯一库存扣减；销售订单显示精确预留批次，仓库确认时自动匹配 PRESTOCKED 预留，销售侧旧直接发货 API 固定 410；零单价在创建/编辑提交前拒绝，PRESTOCKED 不再显示错误的客供原料需求。Java 目标测试 18/18（含真实 JPA Context）、Web 目标测试 9/9、`vue-tsc -b`、`git diff --check` 已通过；未改 Entity/Repository/Flyway/Security。待唯一 Java/Web 发布构建、GitHub 恢复后的远端核销/PR 门禁、生产发布，以及 F006 既有预留订单从建发货单到仓库正式出库的受控写入 E2E。
- `AUDIT-RESTAURANT-SYNTHESIS-RETENTION-20260727` — `review` — Owner: `/root` — Base SHA: `c1e4ed173eb8487e1af7d3b5cb3617c5e638e806` — 审计 2026-07-27 Web 统一 intent 路由后是否遗留无消费者的综合 synthesis 适配器；已确认 Java Gold、`mobile-rest-ai` 与 Web `ChartInsights` 仍直接消费 comprehensive synthesis，故仅补兼容层保留价值与删除条件注释，不改变运行行为。目标 Vitest 18/18 与 `git diff --check` 已通过；只创建 review marker 分支，严格 `NOT_DEPLOYED`、生产零写。
- `SEC-CREDENTIAL-ROTATION-20260719` — `blocked` — Owner: Codex coordinator — Base SHA: `1ba9a241a77144a80851051efbac584abf4db69d` — tracked tree、47/139 非受控副本、数据库/JWT/internal/BaoTa 轮换和生产验收已完成；仍需各供应商控制台吊销无法由当前 API 权限完成的旧长期凭证，详情见 [凭证轮换收尾记录](../security/credential-rotation-closeout-2026-07-20.md)。
- `ENH-F006-SUPPLIER-IMPORT-001`（含 `REQ-F006-SUPPLIER-REQUIRED-CONTACT-001`、`ENH-F006-SUPPLIER-DETAIL-STATUS-001`）— `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 复用现有供应链 Excel 共享能力，统一供应商必填资料、导入预览/幂等确认、状态生命周期、采购门禁与详情信息架构；只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `BUG-F006-UNIT-DIMENSION-SCOPE-001`（含 `BUG-F006-COUNT-UNIT-LITERAL-20260810`、`UX-F006-WORKFLOW-INPUT-COPY-20260810`）— `in-progress` — Owner: `/root` — Base SHA: `9aaecef811e5159cde424f8e670a2c93400adeee` — 以共享 UnitContractService 为唯一真值补齐量纲/使用场景/物料白名单，统一原料、采购、入库、BOM 的单位门禁与 Web 选项；本轮收口“件/个/只”库存字面单位选择及工序投入量文案；canonical/display 分离，只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `POLICY-CODEX-MODEL-ADVISORY-20260810` — `in-progress` — Owner: `/root` — Base SHA: `9aaecef811e5159cde424f8e670a2c93400adeee` — 删除遗留的强制模型/effort 切换确认门禁，保留至多一次非阻塞建议；不得因模型不可观察或未切换暂停已授权工作，不影响其他生产、JPA、dispatch 与发布门禁。
- `BUG-F006-WORKFLOW-OUTPUT-KIND-SNAPSHOT-20260810` — `review` — Owner: `/root` — Base SHA: `a4cfe02d941b6fdf7758c40b712c062d20b2972f` — Workflow 选择成品/半成品 SKU 时已不再误写全局工序默认产出类型；工序快捷编辑、工序出品选择及产出 Cell 选择三条路径统一修改当前草稿节点快照，仅主数据字段真实变化时才调用受限接口。目标 Vitest 35/35、`vue-tsc -b --force`、`git diff --check` 已通过；不放宽 WorkProcess 权限、不改历史版本，严格 `NOT_DEPLOYED`。
- `ARCH-F006-MATERIAL-TAXONOMY-DUPLICATION-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 明确 L1/L2/L3 共享分类与 MaterialType 具体物料边界，移除同名 L3 自动创建，补分类独立权限、父级内规范化去重/并发门禁与只读旧数据兼容；只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `ENH-F006-MATERIAL-TAXONOMY-SUGGESTION-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 基于现有分类字典/同义词/同工厂已确认物料提供可解释的完整 L1-L3 候选与置信度，支持人工覆盖和 Excel 预览复用，绝不自动建类；只合入 `main`，严格 `NOT_DEPLOYED`，生产业务写入为 0。
- `BUG-F006-PURCHASE-ORDER-CREATE-ATTACHMENTS-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 复用现有采购附件链，在普通新建表单提供多文件暂存、一次创建受控绑定、失败清理、幂等、权限与审计一致性；不得修改现有 `PO-20260720-0001`，只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-TAX-RATE-INHERIT-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 采购行默认继承 MaterialType 采购税率，持久化订单行不可变税率快照并由后端权威重算未税/税/含税金额；不桥接历史订单，只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-ACTION-IA-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 精简采购订单列表操作区，外层仅保留对外供货单、详情、状态允许时提交及去重后的更多菜单，移除无价值审计入口但保留底层审计能力；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-WAREHOUSE-RECEIVING-BOUNDARY-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 审计并收敛采购 receive 与仓储采购入库为唯一仓储收货任务/库存写路径；采购详情移除收货写动作，仅追踪到货、实收、质检、批次并跳转仓储工作台；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-RECEIPT-DUPLICATE-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 修复已创建收货单后采购页仍显示已收0并可重复创建的问题；OA完成自动唯一仓储任务，仓储统一处理附件/批次/质检/入库/打印，支持部分到货和守恒幂等；禁止重放或桥接 `PO-20260720-0001`，严格 `NOT_DEPLOYED`。
- `ARCH-F006-UNIFIED-MATERIAL-RECEIPT-SOURCE-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 将“采购入库”统一为仓储原料/物料入库，批次/任务保存 PURCHASE、CUSTOMER_SUPPLIED、TRANSFER、退货/退料、期初/调整等结构化来源与库存所有权；客供料按客户/订单隔离成本和领用；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 收敛采购到应付/付款职责：采购侧移除付款申请写入口并只读展示采购、收货、发票、付款四维状态；复用现有 AP、PaymentRequest、财务工作台与 OA，按付款条款和收货/发票里程碑幂等生成应付与付款计划，财务侧完成三单匹配、审批、支付和核销；保留历史数据且零桥接，只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001-CORE` — `in-progress` — Owner: `/root/final_backend_review` — Base SHA: `ff7ae8784` — 实现 AP payable 未清余额/状态、payment allocation、财务执行 fail-closed 与真实 JPA Context 门禁；收口旧 `PaymentRequest` 采购/OA 绕过写入口，并确保历史无 allocation 应付仅进入待核对而不被迁移重开；scope 限 finance/payment controller、migration 与对应测试，不修改采购 UI、复盘或 ACTIVE；严格 `NOT_DEPLOYED`、生产零写。
- `ARCH-F006-DEMAND-DRIVEN-MATERIAL-RECEIPT-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 统一销售净需求、采购建议、采购订单、仓储待收、客供料所有权与批次来源链；关闭日常无来源“入库登记”和批次“续入”，有来源合并入既有批次必须执行完整身份/单位/日期/QC/成本匹配并写独立库存交易；历史记录只读兼容，只合入 `main`，严格 `NOT_DEPLOYED`。
- `ENH-F006-CUSTOMER-CREATE-PROGRESSIVE-DISCLOSURE-001` — `in-progress` — Owner: `/root` — Base SHA: `3ddf6751603476205457fcf8aa0ee9f1adbea792` — 客户新增/编辑表单采用渐进披露，客户名称/联系人/联系电话/收货地址/状态统一为前后端必填，其余 CRM、开票、信用与行业信息进入默认折叠高级区；历史缺项只读兼容但新销售订单使用前要求补齐；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-CUSTOMER-NAV-001` — `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `667954797` — 客户详情补统一返回按钮与可点击面包屑，保留客户列表搜索/筛选/分页/滚动状态，并覆盖直接 URL 与浏览器历史兜底；只合入 `main`，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-SKU-UNIT-001`（含 `UX-F006-R2-SKU-SPEC-001`）— `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `667954797` — SKU 新建/编辑统一以 displayUnit 展示销售/包装单位，并将基本单位与净含量合并为紧凑规格行；净含量单选范围为 g/kg/ml/L，新建默认 g、编辑不覆盖，同维度 canonical 化且跨质量/容量禁止隐式换算，历史回填与规格预览不泄漏工程码；只合入 `main`，严格 `NOT_DEPLOYED`。
- `UX-F006-R2-SKU-PROCESS-001` — `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `667954797` — 保留 SKU 行“工序/快捷工序配置”及右侧抽屉，并与版本化 Workflow 入口清楚并存；快捷抽屉补名称/编码/标签搜索、真实类别/产出单位/关联状态筛选、已关联置顶去重和大列表性能，添加后保持检索与滚动；只合入 `main`，严格 `NOT_DEPLOYED`。
- `FEATURE-F006-R2-WORKPROCESS-DEDUP-001`（含 `BUG-F006-R2-WORKPROCESS-SORT-001`、`BUG-F006-R2-WORKPROCESS-UNIT-MODEL-001`）— `in-progress` — Owner: `/root/workprocess_governance` — Base SHA: `0f2b8329c` — 重复治理仅建立 canonical/mergedInto 并关闭未来可选性，绝不迁移任何既有 SKU/Workflow/计划/报工等引用；同时从工序主数据业务语义移除全局排序与单位，顺序及节点输入/输出单位只属于 pinned Workflow 节点/计划快照；必须真实 JPA Context，禁止对生产重复组执行治理，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-VIEWPORT-001` — `in-progress` — Owner: `/root/supplier_frontend` — Base SHA: `0f2b8329c` — Workflow 编辑器占满全局导航后的剩余 viewport，body 不随画布撑高；工具栏/画布控件/AI 输入保持可见，兼容列表改画布内可折叠面板，并覆盖主流桌面视口、浏览器缩放与键盘可访问性；只合入 `main`，严格 `NOT_DEPLOYED`。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001`（含 `FEATURE-F006-R2-BOM-MULTIPACK-001`）— `queued` — Owner: `/root` — Base SHA: `457b3b5e9` — 与 `FEATURE-WORKFLOW-BOM-AUTO-BINDING-DAG-001` 重叠，已串行吸收并暂停独立写入；保留替代集合、成本不双算、计划快照和生产零写验收。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001-CORE` — `queued` — Owner: `/root/bom_substitute_core` — Base SHA: `a362fac0c` — 未发现 live thread、worktree 或开放 PR；由统一任务串行实现并保留真实 JPA/Service 门禁，暂停并发写入。
- `F006-R2-WORKFLOW-FIRST-BOM-GATE`（含 `F006-R2-BOM-COMPLETENESS-GATE`）— `queued` — Owner: `/root` — Base SHA: `457b3b5e9` — 与统一任务共用 BOM/Workflow 状态机，已串行吸收并暂停独立写入；运行态继续 ACTIVE-only，历史快照零改写，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INLINE-PROCESS-CATEGORY` — `in-progress` — Owner: `/root/workprocess_governance` — Base SHA: `0f2b8329c` — Workflow 现场创建工序补充与独立工序管理同源的必填类别选择，payload/主数据/节点快照保持同一 category identity，后端拒绝缺失、非法及跨工厂类别；旧数据只读兼容，生产零写入，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-CELL-ACTIONS-UX` — `in-progress` — Owner: `/root/workprocess_governance` — Base SHA: `0f2b8329c` — 统一原料/半成品/成品/工序 Cell 在 hover、focus 与触屏下的编辑删除入口，删除仅作用当前草稿且中文预览节点/连线影响，移除工序右上角重复“+”，历史快照零改写，严格 `NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INPUT-MODE-DERIVATION` — `queued` — Owner: `/root` — Base SHA: `457b3b5e9` — 与统一任务的 Workflow 投入槽、BOM 主料/替代料和报工候选交集重叠，已串行吸收并暂停独立写入；发布与报工继续共用同一固定快照，严格 `NOT_DEPLOYED`。

## Scope 锁地图

- `ML-LIUSHANMEN-WORK-AREA-ROI-20260811`：仅 `tools/vision-lab/work_area*.py`、`tools/vision-lab/mine_tray_queue.py`、`tools/vision-lab/tray_workflow.py`、`tools/vision-lab/evaluate_tray_candidate.py`、对应 `tools/vision-lab/tests/`、`docs/ops/liushanmen-vision-lab.md`、`docs/dispatch/ACTIVE.md` 与当日归档；外部仅可新增独立 tray/`work-area-human/` 队列与回执，不得改写既有 tray/label 人工标注、保护集原始记录、原图、现有模型或生产。验收：原始库补样必须先生成绑定 task/SKU/SHA/ID/pHash 的只读计划，模型托盘预框只能作为待人工完整复核的 proposal；四点简单多边形/面积/坐标服务端校验；台内外以托盘中心点确定且分别报告；无 ROI 或低置信不得静默降级为台内；目标 pytest、`git diff --check`、浏览器本机验收，严格 `NOT_DEPLOYED`。
- `REST-AI-READ-PATH-20260812`：`backend/python/smartbi/scripts/restaurant_ai_eval.py`、`backend/python/smartbi/gold/restaurant/**`（answer_contract / generic_answer / generic_executor / metric_registry / restaurant_intent* / restaurant_ops_router）、`backend/java/cretas-api/src/main/java/com/cretas/aims/config/IntentKnowledgeBase.java`、`.../service/execution/IntentExecutionOrchestrator.java`、`.../service/intent/seed/FuzzyExpressionSeeder.java` 及对应测试、`docs/dispatch/ACTIVE.md`。⛔ 不得碰写路径执行（GoldFinanceClient 写入口、权限/preview/confirmation）、`_SAFE_MODELS`、Flyway 迁移。验收：每步有会红的断言且**变异实测过一次**（红必须红在被测行为上，不是 NPE）；触及面全量跑并与 origin/main 基线逐条对比；部署后 `--only` 实测受影响链并确认日志走 `[Branch:TieredFirst]`。⚠️ 基线**当场重跑**，⛔ 不许引用交接里记的数。
- `LLM-PROVIDER-CLIFF-20260815`：仅 `backend/python/common/llm_router.py`（`_provider_config` / `_SAFE_MODELS` / `_apply_slot_params` 的 deepseek 分支）、`backend/python/tests/test_llm_router_*.py`、`docs/dispatch/ACTIVE.md` 与当日归档。⛔ 不得改链顺序（`_build_chain` 按到期日升序算，`_SLOT_POOLS` 上方注释明写「人不要在这里排」；且有 golden 快照 `llm_router_chains.txt`）、⛔ 不得动任何 aistore 配置、⛔ 不得抬 `_SEMANTIC_TOTAL_TIMEOUT_SECONDS`（`tests/test_tiered_budget_alignment.py` 守着交互延迟契约）。验收：新增闸**钉耦合不钉字面量**（遍历 `SLOT_MODELS` 里 account=="deepseek" 的条目并 `assert 总数>0`）且变异实测红过一次；五个槽实际构建出的链逐条贴原文；付费调用次数逐次记账并贴出总数（仓里有 `llm_router.py:1605` 记的 $19.49/12 天成本前科）。🔒 `_SAFE_MODELS` 是计费闸，需 owner 控制台确认后才可合并。
- 🔗 **跨任务重叠（必须串行）**：`LLM-PROVIDER-CLIFF` 的 **T7-d**（把「今天是 X」移出 system prompt 以吃满 DeepSeek 前缀缓存）与 `REST-AI-READ-PATH` 的 **T6②**（静默换窗口必须披露）改的是 `restaurant_intent.py` **同一段 prompt** ⇒ ⛔ 不许并发，合进同一个 PR 由同一人做。
- `UX-RN-RESTAURANT-PROCUREMENT-DELIVERY-20260802`：仅 `frontend/CretasFoodTrace/src/screens/restaurant/procurement/ProcurementDeliveryConfirmScreen.tsx`、同目录采购送货纯函数/helper、`frontend/CretasFoodTrace/src/navigation/factory-admin/FAManagementStackNavigator.tsx` 中 `isRestaurantMode` 条件路由、对应 `frontend/CretasFoodTrace/src/types/navigation.ts` 类型与 `src/__tests__` 目标测试、`docs/specs/rn-restaurant-procurement-delivery-polish-2026-08-02.md`、`docs/dispatch/ACTIVE.md` 与当日归档；不得修改工厂仓管复用的 supplier-delivery 页面、后端、权限或生产数据。验收：餐饮管理员真实可达、工厂模式不注册、名称化选择与展示、编辑搜索清除旧选择、提交前阻塞提示、44pt 候选卡、目标 Jest、TypeScript 基线、Expo Web 餐饮只读零写验收。
- `FIX-F006-PRESTOCKED-SHIPMENT-E2E-20260809`：仅销售订单详情/列表与对应 Web 测试、仓库出货列表与对应 Web 测试；Java `SalesController`、`SalesService`/`SalesServiceImpl` 中发货与 PRESTOCKED 预留只读方法、`FgReservationLedgerService` 的只读映射、新增只读 DTO、对应 Controller/Service 测试，`docs/specs/f006-prestocked-warehouse-shipment-2026-08-09.md`，以及 `docs/dispatch/ACTIVE.md`/当日归档。不得修改 `production/plans/list.vue`（由 `CX-SHEET-VISUAL-20260731-001` 锁定）、Entity、Repository、Flyway、Security/Auth、客户资料或 F006 以外数据。验收：销售无直接库存扣减 API/按钮；PRESTOCKED 精确批次可见且仓库确认自动完整分配、失败时零扣库；零单价前端拒绝；Java 目标测试、Web 目标测试、类型检查、单生命周期发布构建、`git diff --check`；F006 登录租户证明后仅对指定订单创建一个发货单并由仓库确认，记录 delivery/batch/ledger/库存写后回读，其他租户业务写入为 0。
- `AUDIT-RESTAURANT-SYNTHESIS-RETENTION-20260727`：仅 `web-admin/src/api/smartbi/restaurant-synthesis.ts`、`docs/dispatch/ACTIVE.md` 与 `docs/dispatch/archive/2026-07-27-active-history.md`；不得修改 API、后端 synthesis 行为、QC/SOP/LLM 范围或生产状态。验收：现有多生产者兼容语义保持，删除条件明确，目标 Vitest 与 `git diff --check` 通过，生产零写。
- `SEC-CREDENTIAL-ROTATION-20260719`：`scripts/systemd/` 中遗留明文启动脚本、现有/新增 secret 扫描配置与测试、`.gitignore` / 凭证模板、`docs/dispatch/ACTIVE.md`、`docs/dispatch/archive/2026-07-19-active-history.md`；外部状态仅限已授权的 Cretas 47/139 服务器配置、PostgreSQL 角色密码、相关阿里云/API 凭证与必要服务重启。验收：tracked tree 与完整 Git 历史脱敏盘点、scanner gate、exact-main 发布门禁、Java/Python/网关健康、登录与 Restaurant Agent 只读 smoke、核心 ERP 零写入。
- `ENH-F006-SUPPLIER-IMPORT-001`：供应商 Java Entity/DTO/Controller/Service/Repository、采购下单与供应关系门禁、共享 Excel 导入基础设施及目标测试；Web Admin 供应商列表/统一详情/新增编辑/导入与 API/types/tests；`docs/qa/F006-MVP-E2E-bug-review-2026-07-20.md`、`docs/dispatch/ACTIVE.md` 与当日归档。验收：真实 JPA Context（如触及 Entity/Repository）、Java API/Service、Web 组件/payload/build、预览零业务写、幂等与跨工厂隔离、历史只读兼容、`NOT_DEPLOYED`。
- `BUG-F006-UNIT-DIMENSION-SCOPE-001`：Java 单位 Entity/DTO/Repository/UnitContractService、原料类型/采购/入库/BOM 使用场景校验及真实 JPA/Service 测试；Web Admin UnitSelect/API/types/display helper、原料/采购/入库/BOM 相关单位选项、`SeasoningBindingDialog.vue` 与对应目标测试；复盘/ACTIVE/当日归档。验收：INVENTORY_QUANTITY 排除 TIME/TEMPERATURE/RATIO 与未授权 LENGTH，物料/L3 白名单、非法 API 4xx 零部分写、显式包装换算、历史异常只读、canonical `case`/display“箱”、“件/个/只”分别可选且保留字面，投入量基准文案与真实公式一致，`NOT_DEPLOYED`。
- `POLICY-CODEX-MODEL-ADVISORY-20260810`：仅 `AGENTS.md`、`.agents/skills/rules-maintenance/SKILL.md`、`docs/dispatch/ACTIVE.md` 与当日归档；`model-effort-router` 保持现有 advisory-only 语义，不修改 Claude/Cursor 规则。验收：强制切换/等待确认文案零命中，建议明确非阻塞，`git diff --check` 通过。
- `BUG-F006-WORKFLOW-OUTPUT-KIND-SNAPSHOT-20260810`：仅 `web-admin/src/views/system/product-processes/workflow/ProductProcessWorkflowEditor.vue`、对应 `__tests__/ProductProcessWorkflowEditor.spec.ts`、`docs/dispatch/ACTIVE.md` 与当日归档；不得修改 WorkProcess Controller/Service/Security、权限角色或生产数据。验收：快捷编辑从当前节点读取并写回产出类型且不改主数据默认值；从工序出品或半成品/成品 Cell 选择另一分类 SKU 时仅确认并原子更新当前端口/Cell/SKU；不再调用 `/work-processes/{id}/output-kind`，取消选择零改动；目标 Vitest、`vue-tsc -b --force`、`git diff --check` 通过，`NOT_DEPLOYED`。
- `ARCH-F006-MATERIAL-TAXONOMY-DUPLICATION-001`：Web Admin 原料类型分类帮助、快捷分类创建/申请与权限 UX、目标测试；Java MaterialCodeSegment Controller/Service/Repository/Entity（若必要）及权限、父级内规范化唯一门禁/JPA 测试；复盘/ACTIVE/当日归档。验收：物料创建不复制名称建 L3、多个物料共享分类、改物料名不改分类、分类权限/重复/并发、编码稳定、BOM/采购/入库继续引用 MaterialType、旧数据零迁移、`NOT_DEPLOYED`。
- `ENH-F006-MATERIAL-TAXONOMY-SUGGESTION-001`：Java 分类建议 DTO/Service/API、字典别名/关键词与同工厂历史匹配、原料 Excel 预览复用及测试；Web Admin 候选/置信度/理由/手动覆盖/重新匹配 UX 与目标测试；复盘/ACTIVE/当日归档。验收：高/中/低置信、规格噪声/同义词/多候选、无匹配、手动覆盖、名称变化、零自动建类、跨工厂隔离、Excel 未确认行禁提交、保存父链 4xx、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-CREATE-ATTACHMENTS-001`：现有订单附件实体/API/存储服务审计；Java 临时上传/绑定/过期清理/工厂权限/幂等及目标测试；Web Admin 普通新建附件区、进度/删除/重试/失败回滚与目标测试；复盘/ACTIVE/当日归档。验收：含附件创建、创建失败无孤儿、部分上传失败不静默、重复提交不重复绑定、编辑/复制/下载/删除权限一致、生产订单零修改、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-TAX-RATE-INHERIT-001`：RawMaterialType采购税率读取、PurchaseOrderItem税率/金额快照、采购Service后端权威计算、编辑/复制/审批/入库一致性；Web Admin物料选择默认税率与金额核对；Java/Web目标测试及复盘/ACTIVE/当日归档。验收：13%、免税、显式有权限覆盖、历史快照不漂移、非法金额不被信任、旧订单零桥接、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-ORDER-ACTION-IA-001`：Web Admin采购订单列表状态/权限驱动操作、去重后的更多菜单、危险取消确认、响应式/键盘可用性及目标测试；复盘/ACTIVE/当日归档。验收：外层严格四类入口、不可提交状态无提交、菜单无重复、审计UI移除但日志保留、窄屏不溢出、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-WAREHOUSE-RECEIVING-BOUNDARY-001`：采购详情 receive/openReceiveDialog、后端 receive API 与现有仓储采购入库/任务/质检/批次/库存服务审计；Java 仓储任务、分批/容差/欠收/拒收/质检/单位规格快照/幂等回写及测试；Web Admin 采购只读追踪与仓储唯一写入口及测试；复盘/ACTIVE/当日归档。验收：采购权限不可收货、仓储权限与跨厂跨仓隔离、部分/超收/重复点击、质检不绕过、库存仅仓储动作增加、生产PO/库存零修改、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-RECEIPT-DUPLICATE-001`：现有收货记录聚合/任务唯一键/确认幂等、仓储任务ETA排序筛选、全量快捷/部分到货/QC/附件/正式与待核对打印、PO-receipt-batch-inventory守恒及 Java/Web/E2E 目标测试；复盘/ACTIVE/当日归档。验收：OA完成仅一任务、已有未完成单不重复、真实已收聚合、收齐无写入口、凭证可配置必填、重复确认不双入库、生产记录零写、`NOT_DEPLOYED`。
- `ARCH-F006-UNIFIED-MATERIAL-RECEIPT-SOURCE-001`：现有 MaterialBatch/采购入库/客供料/调拨/销售退回/生产退料消费者审计；Java sourceType/sourceId/sourceNo、ownershipType/ownerCustomerId、统一待入库任务、来源身份/数量/单位/幂等/跨订单领料门禁及真实JPA测试；Web Admin页面命名、来源筛选徽标/批次列/手工入口门禁及测试；复盘/ACTIVE/当日归档。验收：采购、客供、调拨、销售退回、生产退料来源可追溯，客供不进应付且跨客户领料拒绝，库存写仅仓储动作，生产零写、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001`：现有 PaymentRequest/AP/付款计划/应付账款/财务工作台/OA/凭证与采购详情 `goRequestPayment` 只读审计；Java 付款条款里程碑、AP 唯一事件键、部分收货累计、三单匹配/调整、客供排除、财务权限/OA节点门禁及测试；Web Admin 删除采购管理付款申请入口、采购列表/详情四维状态与只读财务进度、财务工作台入口及测试；复盘/ACTIVE/当日归档。验收：预付/现结/票到/月结、部分收货与重复事件幂等、金额后端真值、价税退货调整不覆盖历史、采购零付款写权限、历史 PaymentRequest 只读兼容、跨厂隔离、生产 PO/RCV/财务零修改、`NOT_DEPLOYED`。
- `BUG-F006-PURCHASE-AP-PAYMENT-BOUNDARY-001-CORE`：仅 `backend/java/cretas-api` 内 finance AP entity/DTO/repository/service/controller/Flyway `V20261028_94*` 与对应真实 JPA/Service/Controller 测试；不得修改 `PurchaseServiceImpl`、PaymentRequest legacy 数据、Web、复盘/ACTIVE。验收：一付多应付/多付一应付、部分/全额核销、未清上限、supplier/currency/source identity、幂等/CAS/跨厂、自批自付门禁接口、历史孤立付款只读异常标记，JPA Context 通过，`NOT_DEPLOYED`。
- `ARCH-F006-DEMAND-DRIVEN-MATERIAL-RECEIPT-001`：现有销售订单确认/生产需求/BOM净需求/采购申请/采购建议/入库登记/批次续入消费者审计；Java 销售需求版本与幂等键、需求到PO反向追溯、CUSTOMER_SUPPLIED任务与CUSTOMER_OWNED门禁、有来源批次合并匹配/库存交易/CAS及测试；Web Admin 销售缺料进度、采购“销售需求驱动”队列、仓储来源任务、隐藏普通入库登记和批次续入及测试；复盘/ACTIVE/当日归档。验收：足料不采购、部分不足净需求、重复确认不重复建议、多订单合并保留分配、客供不采购不应付、普通手工写入口关闭、严格批次合并/冲突/并发/交易追溯、生产记录零写、`NOT_DEPLOYED`。
- `ENH-F006-CUSTOMER-CREATE-PROGRESSIVE-DISCLOSURE-001`：Web Admin 客户新增/编辑/详情共享表单、校验定位、响应式与组件测试；Java Customer DTO/Service/Repository/API/import 与销售订单使用门禁及真实 JPA 测试（如触及查询）；复盘/ACTIVE/当日归档。验收：五项必填trim/电话多形态/地址可读/status显式、工厂内规范化名称去重、基础快速创建、高级折叠与已填计数、高级校验自动展开定位、编辑完整回显、历史缺项只读但下单拒绝、默认值零污染、Tab顺序/小屏滚动/重复提交、生产客户零修改、`NOT_DEPLOYED`。
- `BUG-F006-R2-CUSTOMER-NAV-001`：仅 Web Admin 客户详情导航、客户列表状态保存 helper 与目标测试；不得触碰后端、生产客户或其他模块。验收：列表进入/直接 URL/浏览器历史、搜索/筛选/分页/滚动恢复、无重复 history、`NOT_DEPLOYED`。
- `BUG-F006-R2-SKU-UNIT-001`：仅 Web Admin SKU 新建/编辑基础规格、共享 displayUnit/净含量单位 helper 与目标测试；Java 净含量契约由协调者另行审计后在不重叠 scope 落地。验收：box/case/slice 仅显示盒/箱/片，g/kg 与 ml/L 各自同维度等值归一化、跨维度拒绝、历史回填、单位切换不改真实量、规格预览中文化、`NOT_DEPLOYED`。
- `UX-F006-R2-SKU-PROCESS-001`：仅 Web Admin SKU 列表工序/Workflow 入口、关联工序抽屉、筛选/分页或虚拟滚动、Workflow 摘要与目标测试；不得改 Workflow 后端模型或生产数据。验收：入口语义清楚、167项可搜索筛选、已关联置顶且不重复、显示类型/单位/使用情况、绑定 Workflow 时展示名称/版本/状态并只读遵循 pinned workflow，`NOT_DEPLOYED`。
- `FEATURE-F006-R2-WORKPROCESS-DEDUP-001`：Java WorkProcess/Workflow/SKU 关联实体、DTO、Repository/Service/Controller、Flyway/JPA tests；Web Admin 工序重复治理与所有工序字典入口/导入导出/AI 的排序、单位字段移除及目标测试；复盘由协调者统一写。验收：用户显式选主、推荐理由不含排序/单位、只建 canonical/mergedInto 且仅未来候选排除，任何状态 Workflow、SKU、计划、报工、成本、质检和历史外键原样不迁移；已有 pinned 实例即使 master 停用仍可报工/结单，编辑可保留旧项但删除后不可选回；新 Workflow/新关联不出现治理项；冲突阻止、跨厂/幂等/CAS/回滚、无物理删除；工序 API 安全忽略旧排序/单位，Workflow 节点顺序及输入/输出 identity+canonical unit 快照稳定，错误报工单位拒绝；默认产出类型/出成率仅可作为模板建议且不得覆盖节点快照；生产零写、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-VIEWPORT-001`：仅 Web Admin Workflow 编辑器布局/CSS、兼容列表折叠面板、画布控件可见性与目标组件/E2E；不得改 Workflow 业务数据或后端。验收：1366x768、1440x900、1920x1080 及 80/100/125% 缩放时 document 不显著超过 viewport，画布 flex:1 内部交互、工具栏 sticky、缩放/模式/AI 控件不遮挡，侧栏独立滚动，Tab/Esc/焦点可见，`NOT_DEPLOYED`。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001`：Java BOM item substitute 关系 Entity/DTO/Repository/Service、版本克隆/激活校验、计划物料需求/领料/报工/成本/追溯快照及真实 JPA/合同测试；Web Admin BOM 主料表单与树形明细、Workflow BOM 需求项归组、领料报工替代提示及目标测试；复盘由协调者统一写。验收：主料父项可维护多个替代料且无自由文本组码，自替代/同父重复/非法单位拒绝，明确换算，总等价需求守恒且成本不双算；Workflow 不复制第二套真值，计划固定 pinned BOM 替代集合，执行只允许快照授权物料并记录 original required material、真实 material/batch 与换算快照；当前不支持拆分时 fail-closed 限制一次只选一种；既有历史只读兼容、版本克隆持久、生产零写、`NOT_DEPLOYED`。
- `FEATURE-F006-R2-BOM-SUBSTITUTE-001-CORE`：仅 Java 结构化替代关系 Entity/DTO/Repository/new Service/Flyway `V20261028_99*` 与真实 JPA/Service 测试；不得修改 `BomRecipeServiceImpl`、计划/报工、Web、ACTIVE/复盘。验收：RAW/AUXILIARY/PACKAGING 共用 parent→substitutes，工序/包装层级作用域、同父唯一、自替代/跨维度/跨作用域拒绝、显式换算、历史 substituteGroup 零迁移、`NOT_DEPLOYED`。
- `F006-R2-WORKFLOW-FIRST-BOM-GATE`：Java Workflow 草稿结构判定、BOM 全部写入口前置门禁、BOM 完整性 DTO/Service/API、激活与 Workflow 发布/启用、生产计划准入及测试；Web Admin 阶段状态/缺失项/下一步引导与禁用测试；复盘由协调者统一写。验收：越级调用 4xx 且零部分写、空工序拒绝、缺辅料/包材列出明确缺项、同一 Workflow/BOM 草稿幂等、ACTIVE-only 运行契约不变、历史快照零改写、生产零写、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INLINE-PROCESS-CATEGORY`：仅 Java WorkProcess 创建 DTO/Service/类别校验与目标测试，以及 Web Admin Workflow 现场创建工序表单、共享类别数据源、payload/回填/筛选目标测试；与 `/root` 的 BOM readiness 代码不重叠。验收：类别必填可搜索、包装/前处理可选、保存与重开一致、缺失/非法/跨厂 4xx、旧 null 只读、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-CELL-ACTIONS-UX`：仅 Web Admin Workflow Material/Process Cell 组件、草稿图删除 helper/确认文案与组件/图结构测试；与 `/root` 的 BOM readiness 后端不重叠。验收：hover/focus/触屏操作一致、无选中前置、无布局跳动、工序重复“+”消失、删除显示节点与连线影响且不留孤儿连线、历史零写、`NOT_DEPLOYED`。
- `BUG-F006-R2-WORKFLOW-INPUT-MODE-DERIVATION`：Java BOM 替代关系到 Workflow 端口组的派生/快照/发布校验与计划报工准入，Web Admin Workflow 投入规则只读展示及目标测试；不得把派生规则写回 WorkProcess 主数据。验收：无完整 BOM=待配置、A/B 两组均必需、主项+替代项组选一、可选项可省略、多组不合并、非法/空组 fail-closed、历史快照不漂移、`NOT_DEPLOYED`。
## 阻塞项

- 🔴 **aistore 额度 `2026-09-13` 的性质未确认**（可续期 / 硬到期 / 免费额度期限）—— 只有 owner 控制台看得到。硬到期则 `LLM-PROVIDER-CLIFF` 变成倒计时且优先级压过 `REST-AI-READ-PATH`，因为质量池另两条已死（402/429）。⛔ 在确认之前不要按「不紧急」安排。
- **`_SAFE_MODELS` 里 DeepSeek 官方该填什么到期日** —— 按量付费没有到期日，而该字段语义是「这天之后拒用」。organizer 建议填**三个月后的复审日**，把它变成「定期回来看一眼成本」的强制触发点；待 owner 定。
- **「打烊看一眼」这个形态尚未经真人验证** —— 日结九节链条全部建在它上面，而它是纯内部推断，一个真实餐厅老板都没说过。演示卡与「他说什么就做什么」对照表已备好（对照表是聊之前定的，⛔ 不许聊完再改）。这条不解，上面所有产品裁定的地基都是悬的。

- Aliyun 主账号旧 AccessKey：官方 RAM API 返回 `Forbidden` 并提示泄露风险，旧 key 的 STS 仍有效；必须由阿里云控制台主账号删除。
- 旧 Model Studio/DashScope、Zhipu、DeepSeek key：生产消费者已切换或禁用旧值，但需要在各供应商控制台执行禁用/删除；当前 replacement RAM key 无法通过已安装 CLI 调用 ModelStudio key-management OpenAPI。
- Mall 微信长期凭证：已从 tracked/0644 YAML 外置到 `0600` root-only EnvironmentFile，运行态验证通过；MP secret/token/AES key、Mini App secret、商户 key 仍需在微信公众平台/商户平台协调重置后同步更新受控环境文件。

## 维护规则

- 只记录当前在飞、阻塞和下一动作；已完成事项移入带日期的 `archive/`。
- 创建 worktree、派发或合并前，先核对本表与 `git worktree list`。
- 本文件只有当前协调者可修改；子代理只能按 `AGENTS.md` 的结构化回执格式报告状态。
- 状态只允许：`queued`、`claimed`、`in-progress`、`review`、`merged`、`blocked`。
- 合并完成时必须同时归档任务、释放 scope 锁，不能留下已经结束的在飞行。
- 同一仓库默认 WIP 上限：2 个代码执行任务 + 1 个只读/测试任务。
