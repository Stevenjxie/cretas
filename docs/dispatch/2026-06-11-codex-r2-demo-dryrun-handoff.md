# Codex 第二轮 — 周五演示路径预跑 + 防呆/UX 验证（自包含卡）

**日期**: 2026-06-11 晚
**背景**: 第一轮 §1-6 流程审计已完成并通过 organizer gate（audit: `docs/audits/liushanmen/2026-06-11-codex-e2e-fullflow-audit.md`）。**明天 2026-06-12 周五 = 客户演示日**（演示全部 → 没问题就上线）。本轮两个任务，**Part 1 优先**。
**环境/账号/铁律**: 同第一轮（`docs/dispatch/2026-06-11-codex-e2e-fullflow-handoff.md` §0），headed Playwright 1920×1080 zh-CN，截图全留档。**不改任何代码**。

## ⚠️ 并发注意
另一个 agent 正在 prod F006 写 `DEMO-771-VERIFY` 标记的验证数据（订单 remark 可识别）。**看到这批数据别动、别认领、别清理**，也别把它们当 bug 报（如"为什么有 DEMO 订单"）。

---

## Part 1（优先）: 周五演示路径预跑 — 发现演示阻断项立即报

以 f006_admin 把**明天会当着客户走的链**完整点一遍（headed，每步截图）：

1. 销售下单（含税客户，单价可空+毛利红线红字预警）→ 提交财审 → 财审通过 → **凭证三行**（借应收含税/贷收入未税/贷销项税）
2. SO 列表「开始采购」一键 → BOM 净需求预填 PO → 提交/审批 → 收货 → 入库
3. 付款申请 → 双审 → 出纳付款（f006_cashier 登录看待付款屏）
4. 生产计划（以销定产+财审门拦截）→ 多SO合并「加号追加」→ 转批次
5. 两点报工（任务列表只 2 个报工点）→ 报工审批 → 撤回 + 快速通道演示点
6. 成本拆分（材料 2.58/4.86/5.35 应显示，人工 null=「登下一期」诚实显示）+ 多段成本页 + 三价对比
7. 报表：进销存四时点 / 盐化报表 / 金蝶凭证导出
8. 盘点发起 → 财务审批门（演示到审批即可，不必生效）
9. 16 位编码预览（`/material-segments/generate-code`）+ 研发「价位选料」入口

**每步判定**: ✅秒开有数据 / ⚠️慢或空数据（写明缺什么数据，演示前可补）/ 🔴 报错或断链（**演示阻断项，发现即在报告顶部标出，不等跑完**）。
写操作可以真做（演示数据本来就要有），新建实体 remark 标 `DEMO-FRIDAY`。

**产出**: `docs/audits/liushanmen/2026-06-12-demo-dryrun.md` — 顶部=演示阻断项清单（若有），正文=逐步 ✅/⚠️/🔴 + 截图引用 + 「演示前要补的数据/配置」清单。

## Part 2: §7 防呆/快速关联/UX 验证（第一轮 handoff §7 原文执行）

按第一轮 handoff `docs/dispatch/2026-06-11-codex-e2e-fullflow-handoff.md` **§7 全节**执行，要点：
- **用真实低权角色**走（操作员/仓管报工领料入库、财务财审付款、销售下单开始采购），f006_admin 只看全局
- **防呆 5 条逐写操作 dialog 验**: Rule1 预显边界+:max / Rule2 上下文带品名单号 / Rule3 原因 dropdown 非 textarea / Rule4 重复提交幂等 409 / Rule5 dead-end 带导航按钮 + 错误 toast 4位一体（具体文案/原文/sticky/下一步）
- **快速关联/一键**: 关联客户下拉自动带 / 开始采购预填 / 快速出库幂等 / 价位选料 / 多SO加号 / 报工 input max+品名批次
- **每屏 UX 五问**（一眼知道填什么？多余步骤？该有一键的地方有没有？防呆拦住了吗？错误提示告诉怎么修了吗？）→ 优化清单

**一个已知纠偏**（第一轮假阴性，本轮顺手复验）: BOM 添加原辅料弹窗把**物料类别切到「包材」**后应出现「每产品用量」(packQtyPerProduct) 字段 —— 第一轮没切类别所以没看到。截图坐实它真出现。

**产出**: `docs/audits/liushanmen/2026-06-12-foolproof-ux-audit.md` — 防呆 5 条逐条 ✅/🔴 矩阵（按屏）+ UX 优化清单（分「周五前该修」/「演示后 backlog」两档）+ Headed Mode Verification block。

---

## 交付检查清单
- [ ] Part 1 报告（演示阻断项置顶）
- [ ] Part 2 报告（防呆矩阵 + 两档 UX 清单）
- [ ] 截图 → `docs/audits/liushanmen/2026-06-12-demo-dryrun-screenshots/` 和 `2026-06-12-foolproof-screenshots/`
- [ ] 新建数据 remark=DEMO-FRIDAY + ID 清单
- [ ] 诚实结论：明天这条演示链能不能从头走到尾不翻车
