# 餐饮 AI 推到底 · 执行台账

**目标** 见 `docs/dispatch/2026-08-17-餐饮AI推到底-工作prompt.md`。
北极星：让老板打开就能自己做一个决定。取舍 **能不能决定 > 数字准不准 > 覆盖得全不全**。

**分支** `codex/claude-restai-boss-loop`（off origin/main `cb329cc14c`）

---

## 阶段 0 · 地形（当场量的，⛔ 未引用任何文档里的旧表）

### 租户全景（2026-08-17 实测，逐租户独立 RLS 上下文）

| 租户 | 订单 | 有交易门店 | dim_store | 成本卡 | 营业日范围 |
|---|---|---|---|---|---|
| **RES_3101_009** | 625,764 | 33 | **38** | 136 | 2025-01-01 .. **2026-08-16** |
| **DEMO_REST** | 544,303 | 27 | 27 | 136 | 2025-01-01 .. 2026-08-16 |
| MOCK_REST | 97,812 | 10 | 10 | 7 | 2026-06-29 .. 2026-08-16 |
| R_GML_DEMO | 16,213 | — | 132 | 0 | 仅 2026-01-15 一天 |
| **R_XMX_CHAIN** | 141 | — | **1** | 9 | 仅 2026-02-15 一天 |
| 14 个 `R_*_REAL` | 0 | 0 | 0 | 0 | 目录里注册了，**零数据** |

阳性对照（跨样本）：5/20 个租户读出非 0 订单 ⇒ 仪器活着。

### 🔴 量这张表时踩的两个仪器缺陷（都被自己的输出抓住了）

**① `__internal__` 不是万能钥匙 —— 在 RLS 表上它就是「零行」。**
第一版拿它做跨租户普查，三张 RLS=True 的表**全部返回 0 个租户**，
RLS=False 的那张返回 14 个。**0 与 RLS 完全对齐。**
成因：我把 `ai_promoted_routes` 当通例，而它的 migration 自己写着
「THIS IS THE REVERSE OF THE PLATFORM CONVENTION」。
⚠️ 救下这次的是探针里那句 `relrowsecurity` —— 没有它，「0 个租户」会被写成
「事实表是空的」，方向判断整个反掉。

**② 日期列选错。** `fact_pos_transaction` 同时有 `date`(营业日) 与 `created_at`(入库时刻)，
优先级表把 `created_at` 排前面 ⇒ 量的会是 ETL 时刻。

### 老板那条路（实测）

```
登录  POST /api/mobile/auth/demo-login?tenant=rest
      -> factoryId=DEMO_REST  user=demo_rest  role=factory_super_admin
提问  POST /api/mobile/{factoryId}/ai-intents/execute
      body {"userInput","sessionId"} -> data.message / data.resultData.suggestedFollowups
```
⚠️ **蓝绿活跃槽是 `:10020`**（目标文件写的 10010 已过期 —— 它自己那条「槽位会变」是对的）。

---

## 阶段 1 · 冷启动读数（DEMO_REST 身份，31 独立问句 + 3 条追问链共 8 轮）

⚠️ 全程 `TIME_PHRASE_CORPUS_OFF=1` —— 我是探针不是用户（昨天登记的 I4）。

### 总分

| | 能决定 | 答不上/答错 |
|---|---|---|
| 独立问句 31 条 | **13** | **18** |
| 追问链 8 轮 | 3（各链第 1 轮） | **5（第 2、3 轮全挂）** |

▎**追问 0/5 —— 定义三完全不成立。**

### 做得好的（要保住，⛔ 别在修别的时候弄坏）

- **定义 2「说一件他没想到的事」达标**：谜题象限点名到具体菜 ——
  「招牌青花椒鱼(2-3人份) 每份赚 ¥179.71，是 117 道菜里的高位（中位 ¥44.02）；
  近30天卖 39 份，低于中位 294 份 —— 最赚钱的菜没卖动」。**可点名、可核对。**
- **定义 4 表格达标**（能答的路径上）：菜品销量表、门店毛利排行、营收/成本/毛利三列。
- **定义 5 有一个满分样本**：「今天到现在卖了多少」→「今天没有可用数据。
  **没有用全部历史或其他日期替代**。请先确认营业流水已经同步，再按同一时间范围重试。」
- **毛利口径披露非常扎实**：覆盖率 48.4%、缺卡菜排除不按零成本算、
  13 个异常成本值已排除、计算过程逐项列出、「要更准得盘一次库」。
- **域外问题正确拒答**：「今天天气怎么样」→ 明说不在数据范围内 + 给了能问什么。

### 🔴 已验证的缺陷（每条都复现过，⛔ 不是一次读数）

#### D1 · 追问全断（0/5）—— 对「能不能决定」伤害最大
三条链第 2、3 轮全挂，两种长相：
- **(a) 归因/建议类**：「为什么」「它的问题出在哪」「那这个菜该怎么办」「那哪家店影响最大」
  → 一律回 *「这次没算出本轮要求的原因分析或优化动作…我不会拿别的数据凑」*（62 字）
- **(b) 指代被当实体名**：「别的店也有这个问题吗」→ *「**没有找到名为「别的店」的门店**」*

#### D2 · 疑问词／指代被当成门店名
「哪几家店在拖后腿」→ *「**没有找到名为「哪几家店」的门店**，不能计算该店的毛利…」*
措辞非常肯定，**老板会以为自己打错字了**。
⚠️ 阳性对照（真门店名「青花椒徐汇日月光店这个月生意怎么样」）**也失败了**
（落到维度兜底反问）⇒ ⛔ 不能断言「只是疑问词的问题」，**带门店名的问句本身也走不通**，
这一条要在修之前单独再验一次。

#### D3 · 同一句问题，两个结果（非确定性）
「哪家店最差」× 5，各自新 session：**4 次作答（1382 字，字字相同）+ 1 次反问（22 字）**。
两侧 `source` 都是 `restaurant_ops_gold`。
看 elapsed：0.4s 那几次是缓存命中，2.6/3.5s 是真编译，
而**反问那次也是 2.71s 真编译** ⇒ **非确定性在 LLM 编译那一步，不在缓存**。
⚠️ 我另一组「先问一句别的再问同一句」的读数**全部无效**（3 次都是 0.4s 缓存命中，
同一条目 replay）—— 我没清缓存，硬约束 3，我自己刚写进 prompt 又犯。

#### D4 · 该默认的不默认 —— 同一个产品两套时间策略
「哪家店卖得最好」「哪个菜不赚钱」**默认最近30天并披露**；
而「成本主要花在哪」「是客流少了还是客单价低了」「供应商价格有没有涨」
→ 一律反问 *「你想看哪个时间范围？」*（10~18 字）。
⚠️ 与昨天上线那条同族（交接里「默认窗口没接到回放路径」）。

#### D5 · 决策类问题被退回成看数请求
「明天我该干什么」「你有什么建议」「客户满意度怎么样」「帮我把米饭价格改成12块」
—— 四个**完全不同**的问题落到**同一句** *「你这次最想先看哪件事？」*（11 字）。
▎北极星是「让他自己做一个决定」，而他问「我该干什么」时产品反问「你想先看哪件事」。
⚠️「帮我改价格」是**写操作**被吞进看数兜底；而「哪些菜应该下架」却正确声明了
咨询模式/操作模式 —— **同一族两种处理**。

#### D6 · 维度兜底话术是开发者视角，且吃掉了「缺数据」这一类
*「你是想看某道菜、某家门店，还是全店合计？说一个就行，我不会拿别的数据凑」*
出现 5 次：员工排班 / 外卖堂食 / 最近损耗 / 有几家店亏钱 / 哪家店成本控制最差。
- 老板不知道「哪一层的数」是什么意思
- 「员工排班合理吗」**根本没有数据**（员工表 0 行），却按「维度不明」反问
  ⇒ **定义 5 失败**：没说缺什么数据、怎么拿到、他自己要干什么

#### D7 · 「一共有多少家店」把闭店算进去了
| 问法 | 产品答 | 真值 |
|---|---|---|
| 一共有多少家店 / 今天到现在 | **38** | `dim_store` 38（**含 5 家名字里写着「（闭店）」**） |
| 昨天 / 本月 / 最近30天 / 上半年 | **30** | 该窗口有交易 = 30 ✅ |
| （全历史有交易） | — | 33 |

⇒ **30 是对的，38 是档案全量。** 不是算错，是**口径没说清**。
但老板问「我有多少家店」得到 38，他会**按 38 家去分摊成本、排人力** ⇒ 实际伤害。

### 🟡 一条差点被我写成 Critical 的（记下来当教训）

我一度认定这是**跨租户泄漏**：以 `DEMO_REST` 登录，答案里 8 个门店名有 **7 个属于
`RES_3101_009`**（鲜行者/兄弟土菜馆/有滋有味/龍燕楼…），而 DEMO_REST 自己的 27 家
全叫「示范门店01-19」+「青花椒*」。

**真相**：`backend/python/smartbi/api/gold_reads.py:157` 有白名单
`DEMO_GOLD_TENANT_ALIASES = {"DEMO_REST": "RES_3101_009"}` ——
「公开免登录 demo 账号在 Java auth 里故意做得很轻，但 AI demo 需要完整的餐饮 Gold 数据集」。
**是有意的演示别名，不是泄漏。**

⚠️ 本仓反模式清单第 6 条原话：**「看到反常读数直接判缺陷。先问『写它的人怎么说』」**。

**但同一处代码露出一个真问题**：
`_resolve_tenant` 做别名重映射，`_resolve_tiered_tenant` **明确不做**，
其注释写着「tiered 路径直接读 DEMO_REST 自己 seed 的 restaurant-ops Gold 数据」。
而我的探针**就是走 tiered 路**（`ai-intents/execute`），回来的却是 RES_3101_009 的门店。
⇒ **设计意图与实际行为不一致**，且 Java 侧 `resolveGoldFactoryId` 的注释说
2026-08-05 删掉 Java 那侧的别名正是为了消除「divergent data visibility」。
⛔ 机制未验证（不从代码推），登记为 **D8**，修之前要单独跑一次定位。

---

---

## 阶段 3-4 · 第一个修复：D6（维度对不上时说清差在哪一层）

### 🔴 诊断被实测推翻过一次 —— 这一步是关键

我最初的假设：LLM 规划出了**全产品都不支持**的维度（员工/供应商/满意度），
维度闸把它兜底成了通用反问。

**真跑一次（`parse_restaurant_query`，逐句清缓存）后推翻**：

| 问句 | intent | dimensions | clarify |
|---|---|---|---|
| 员工排班合理吗 | `RESTAURANT_OPS_STAFFING_ADVICE` | () | **False** |
| 外卖和堂食哪个更赚钱 | `RESTAURANT_OPS_CHANNEL_MIX` | (channel, dish) | **False** |
| 最近损耗怎么样 | `RESTAURANT_OPS_WASTAGE_TOP` | (ingredient) | **False** |
| 有几家店是亏钱的 | `RESTAURANT_OPS_GROSS_MARGIN` | () | **False** |

**规划器是对的**，四条 `clarification_needed=False`，
而且**没有一条**用了「全产品都不支持」的维度（并集 7 个维度全覆盖）。
拒答发生在**执行期**，日志里写得明明白白：

```
contract-repair SKIPPED RESTAURANT_OPS_CHANNEL_MIX:
  目标 resolver 只服务 ['channel'], 服务不了本计划的 ['channel','dish']
```

⇒ 真因：**规划器多要了一层，产品就整句拒答**，而它**已经算出差在哪一层**，
只是没投影到用户侧 —— **形态 B 第 7 例（投影丢失）：产出端有了，消费端收不到**。

### 修法（⛔ 不降级硬答）

⛔ 不去掉那一层硬答 —— 那是「拿别的数据凑」，本仓明令禁止。
只把**拒答**说清楚：哪一层能算 / 哪一层不能 / 换什么问法能拿到。

```
改前：我不确定你要看的是哪一层的数，所以这次我没敢算。
      你是想看某道菜、某家门店，还是全店合计？说一个就行，我不会拿别的数据凑。

改后：…所以这次我没敢算。按渠道我能算，但你这句还要求再按菜品拆一层，
      这两层的数不在同一张表上，拆不出来。想看的话分开问，
      例如先问「按渠道怎么样」。我不会拿别的数据凑。
```

配套：抽出 `_supported_dimensions` 作**唯一定义**（判据与文案必须读同一集合）。

### 两个被自己的测试当场抓住的实现缺陷

1. **中文名查不到，黑话差点漏给店长。** 第一版直接 `DIMENSIONS[k].label` ——
   而 spec 里是**管线名**(`dish`)、登记表是**登记键**(`product`)，
   查不到就原样吐出「还要再按 **dish** 拆一层」。
   ⇒ 改成从 `DIMENSIONS.label` + `canonical_dimensions` **推导**反向表，⛔ 不手写第二张。
2. **「只有一处推导」这个断言当场红**：实测 `_RESOLVER_DIMENSIONS.get(...)` 有 **4 处**。
   另外两处算的是**未归一化**的并集（不是同一个问题），替换会改行为 ⇒ 本轮不动，
   做成**棘轮**冻结在 4、只禁增长。⛔ 不装作不存在。

### 变异对照

| # | 变异 | 预期红的用例 | 实测 |
|---|---|---|---|
| M1 | 拒答处不再调 `_dimension_gap_advice` | 接线闸（AST） | ✅ 红 |
| M2 | `_dimension_gap_advice` 永远返回空串 | 说得出层名 | ✅ 红 |
| M3 | 反向中文名表退回登记键 | 登记表全覆盖 | ✅ 红 |

还原后 6 passed、工作树干净。

⚠️ **诚实记一条**：变异脚本里那个「行为变了吗」的探针**自己是坏的**
（四次全 `SyntaxError` —— `python -c` 里塞类定义没解析）。那一列没有信息量。
有效证据是「每次红的都是**点名预期的那一条**」—— 结构/行为断言变红本身
就证明变异到达了被测代码。⛔ 不拿坏探针的输出当证据。

### 🔴 上线验收：1/5，而这个读数推翻了阶段 1 的一个归类

PR #2730 → `9085915407`，已部署 prod（md5 `55b94c43…` 一致 ·
PID `2727447→2750815` · 启动 01:44:00 晚于制品 01:32:35）。
把那 5 句原样再问一遍：

| 问句 | 结果 | 真相 |
|---|---|---|
| 外卖和堂食哪个更赚钱 | ✅ | **完全按设计工作**：「按渠道我能算，但你这句还要求再按菜品拆一层，这两层的数不在同一张表上，拆不出来。想看的话分开问，例如先问「按渠道怎么样」。」 |
| 最近损耗怎么样 | 脚本判 ❌ | **实际 1114 字完整作答**（损耗类型分布 + top-10 表格）—— **我的验收判据写错了**（要求出现「分开问」），不是产品失败。⚠️ 但它为什么变好**不能算在我头上**：D3 的非确定性没排除 |
| 员工排班合理吗 | ❌ | 走**另一条**拒答路径（「没算出原因分析或优化动作」= D1(a) 族），我的修复不覆盖 |
| 有几家店是亏钱的 | ❌ | mismatch 是 `_STORE_SCOPE_MISMATCH`，**没有多余维度** ⇒ 我的函数正确返回空串、退回原文案 |
| 哪家店成本控制得最差 | ❌ | 第三种措辞（「全店汇总」而非「全店合计」），来自又一处 |

阳性对照：`哪个菜卖得最好` 仍 1719 字正常作答 ⇒ **没弄坏能答的路径**。

▎**结论：我在阶段 1 把这 5 句归成「都撞维度闸」是错的。**
▎它们长相相似（都是短反问 + 「我不会拿别的数据凑」），
▎实际来自**至少 4 个不同机制**。**相似的长相不等于同一个成因。**

⇒ 这条要写进方法学：**按「回答长什么样」分组会把不同成因并成一类**，
   归类必须落到**产生它的那处代码**（本轮是靠逐句读 prod 实际文案 + 日志才分开的）。
⚠️ 也说明我的修复**覆盖面比预期小**：只覆盖「维度有交集但多要了一层」这一种。
   另外三种（原因分析缺口 / 门店范围 mismatch / 第三处措辞）**仍然是死胡同**。

---

## D1 取证：诊断被推翻了两次，第二次踩的是本仓自己记过的坑

### 第一次推翻：不是维度闸（见上）

### 🔴 第二次推翻：**也不是 Answer Contract 拒答**

我从文案里的「本轮要求的**原因分析或优化动作**」推出这是契约否决
（`answer_contract.py:33` 确实有这个标签）。**跑一次就推翻了**：

| `resultData` 的键 | 第 1 轮 | 第 2 轮（追问） |
|---|---|---|
| `contractPass` | `true` | **不存在** |
| `executedResolvers` | `["RESTAURANT_OPS_STORE_MARGIN"]` | **不存在** |
| `conversationContext` | 有 | **不存在** |
| `queryPlanHash` | 有 | **不存在** |

⇒ **追问轮根本没走到契约那一步。** 它是一个**执行前的 clarification**，
只是措辞里含契约的词。

⚠️ **而这个坑本仓白纸黑字记过**（`restaurant_intent.py:7848`）：

> 更隐蔽的是：拒绝语里含「原因分析或优化动作」这几个字，于是按关键词统计会看到
> 「138/141 含契约认的词」——**一个完全是假象的读数**。⛔ 判据：统计一段文本里
> 有没有某个词之前，先确认这段文本是不是你以为的那一段。

我从文案推成因，正是它说的那件事。**读文案 ≠ 读机制。**

### 🔴 「追问 0/5」本身经受住了检验（差点是我仪器造的）

第 1 轮返回了 `conversationContext`，而我的探针**没有回传**它 ——
如果真实前端会回传，那「追问全断」就是**我自己造的假象**。

核了真实前端（`mobile-rest-ai/src/api.ts:257`），它发的是：

```js
body: JSON.stringify({ userInput: question, sessionId, mode: 'READ' })
```

**它也不回传 `conversationContext`。** 唯一差别是 `mode:'READ'`。
拿完整 payload（含 `mode`）复测三条链 ⇒ **追问仍然全断，读数不变**。

⇒ **「追问 0/5」是真的产品发现，不是仪器artifact。** 这一条现在是核过的。

### 下一步该查什么（⛔ 别再从文案推）

追问轮丢的是 `conversationContext`：**产出端每轮都给，没有任何人回传，
前端也不传**。而 `parse_restaurant_query` 的多轮能力挂在 `session_key`
（`_pending_pop`）和 `history`（要求 `semantic_first=True`）上。
⇒ 要查的是：Java 编排层在 `sessionId` 相同时，**到底有没有把上一轮的
上下文喂给 Python**。这是形态 B 的问法：**生产上谁保证它被调用？**

---

## D1 续查：三次推翻之后，真相比「没接上」窄得多

### 第三次推翻（这次推翻的是我自己刚下的结论）

**中间结论（后被推翻）**：A/B 隔离显示「不给上下文解不出、喂了就解出」
⇒ 我写下「能力在、没接上（形态 B）」。

**推翻它的读数**（全新会话，同一进程里按序做，时间差几秒）：

| 步骤 | 读数 |
|---|---|
| 第1轮「这个月生意怎么样」 | 1529 字，`contractPass=true` |
| **紧接着 lookup** | **命中**，`turn_count=1`，`turns_history=1 条` |
| 第2轮「为什么」 | 30 字反问：**「你想诊断*全部门店*哪方面的问题？」** |

▎**它说了「全部门店」** —— 上下文**确实被用上了**。
⇒ **持久化正常、lookup 正常、history 喂到了。断的不是接线。**

真实情况更窄：**产品拿到了上下文，仍然选择反问而不是执行归因。**
而同一句在直调 `parse_restaurant_query(history=真实形状)` 时解出了
`RESTAURANT_OPS_BUSINESS_OPTIMIZATION` / `clarify=False`。
⇒ 差异**可能是 D3 非确定性**，也可能是生产路多传的
`session_key`（`_pending_pop`）/ `session_summary` 造成的。**未定，需要重复采样。**

### 沿途三个作废的读数（都记下来，⛔ 不装作没发生）

1. **B 的桩形状错了**（形态 B‴）：第一版喂 `{role, content}`，
   而生产喂的是 `turns_history` 的 `{q, a_summary, context, ts}`。
   ⇒ 用**库里那条真实的**重跑，结论才站得住。
2. **lookup 测在错误的时刻**：拿 1 小时前那条会话行查，而 TTL 正好 1 小时
   ⇒ 三条 lookup **连阴性对照一起**全 MISS。过期解释掉了全部读数 ⇒ 没有区分力。
3. **蓝绿槽位在我跑到一半时翻了**：**02:07:37 从 10020 → 10010**，
   探针写死 10020 ⇒ `Connection refused`。
   ⚠️ 这也是 **D3 非确定性的一个混淆源** —— 我早期的读数跨在这次翻转两侧。
   ⇒ 探针一律**从监听端口反查**，⛔ 不写死。

### ✅ 已分开：D1 与 D3 **不是**同一件事

8 个互不相干的新会话，每个走「第1轮 → 两句追问」，阳性对照要求第1轮必须答出来：

```
有效样本 8/8（第1轮每次都答出 1529 字）
「为什么」          执行 0/8   反问 8/8   ⇒ 确定性地反问
「那哪家店影响最大」  执行 0/8   反问 8/8   ⇒ 确定性地反问
```

⇒ **追问拒答是确定的，不是抖到了反问那一侧。D1 ≠ D3。**

### ✅ 再收窄一层：两句追问不是同一回事

固定追问句、只换 history 来源，各跑 3 次、每次清缓存：

```
追问「它的问题出在哪」直调(history)
  A·history 来自「哪家店最差」       3/3 执行:RESTAURANT_OPS_BUSINESS_OPTIMIZATION ✅稳定
  B·history 来自「这个月生意怎么样」  3/3 执行:RESTAURANT_OPS_BUSINESS_OPTIMIZATION ✅稳定

追问「为什么」（四个参数变体 × 3 次）
  ①history / ②+session_key / ③+session_summary / ④三个都给
  → 12/12 全部反问 ✅稳定
```

⇒ **两句追问要分开看，⛔ 不能并成「追问全断」：**

| 追问 | 直调 | 生产路 | 定性 |
|---|---|---|---|
| **「为什么」** | 稳定反问 | 稳定反问 | **大概率是正确行为** —— 问句本身太含糊，反问合理。⛔ 不算缺陷 |
| **「它的问题出在哪」** | **稳定执行** | **稳定反问** | **真缺口**：两侧都确定，差异必然来自生产路多做的事 |

⚠️ 我早先把这两句并成「追问全断 0/5」是**粗了** —— 同一个统计里混进了
「产品做对了」的样本。⇒ 分类必须落到**每一句**，⛔ 不按「回答长得像不像」归堆。

### 🔴 落定：真因是**叙事接地闸把算出来的答案整份毙掉了**

逐层排除（每层都跑过，⛔ 无推断）：

| 层 | 读数 | 结论 |
|---|---|---|
| `parse_restaurant_query` | 12/12 解出 `BUSINESS_OPTIMIZATION` / `clarify=False`，**含全套生产参数** | 不是解析 |
| resolver 登记表 | 5 个被拒意图**全都有** resolver（`BUSINESS_OPTIMIZATION` 支持 6 个维度） | 不是能力缺口 |
| `should_delegate` | `True`（阳性对照也 True） | Python 接了这个请求 |
| **`tiered_answer` 内部** | stderr: **`synthesis rejected by narrative grounding gate`** | **就是它** |

五条 violations 全文：

```
'无保留因果断言：它靠的是高客单价（每单花得多）撑起营业额'
'未标注为假设的预算或目标：…VIP评价平均分能否从4.504星提升到4.7星以上'
'未标注为假设的预算或目标：并确认营销预算（比如每张券成本20元'
'未经验证或确认的高影响动作：可验收结果：** 下架这5个菜后'
'未经验证或确认的高影响动作：验收方式：对比下架前后一个月的菜单结构和替换菜品的销售额'
```

▎**产品把归因和行动方案完整算出来了**（具体门店名 / 4.504→4.7 星 / 下架 5 个菜 /
▎验收方式），**然后因为措辞纪律被整份毙掉**。五条**没有一条是「数字错了」**。

▎而给老板的那句「这次**没算出**本轮要求的原因分析」—— **是假的**。

⇒ 这违反反目标里最重的一条：**敢说不准的全部本钱，就是每一句都站得住。**
设计卡：`docs/decisions/2026-08-17-追问被叙事接地闸毙掉-设计卡.md`
（四个选项 / 利弊 / 裁定 **A 治本 + C 兜底，⛔ 不做 D 降级**）。

### 已排除的下一个实验（原计划，现已不需要）

同一句追问：
- **直调** `parse_restaurant_query(query, pool, factory_id, history=真实turns_history,
  semantic_first=True)` ⇒ 解出 `RESTAURANT_OPS_BUSINESS_OPTIMIZATION` / `clarify=False`
- **生产路**（多传 `session_key` + `session_summary`）⇒ 确定性反问

两者**都是确定的** ⇒ 差异必然来自那两个多传的参数。
⇒ 下一步：逐个加回去（先只加 `session_key`，再只加 `session_summary`），
看哪一个把「解得出」翻成「反问」。**二分定位，⛔ 不要一次加两个。**

---

## 下一步（按对「能不能决定」的伤害排序）

1. **D1 追问全断** —— 0/5，最伤
2. **D5 决策类被退回成看数** —— 直接违背北极星
3. **D4 该默认的不默认 / D2 指代被当门店名** —— 同一族，都是反问该不该发生
4. **D6 兜底话术吃掉「缺数据」** —— 定义 5
5. **D7 闭店算进门店数** —— 定义 1，修法明确
6. **D3 非确定性** —— 需要先定位在 LLM 编译的哪一步
7. **D8 tiered 路是否越过了别名边界** —— 先定位再判

⛔ 单店老板视角（R_XMX_CHAIN，1 店 141 单）尚未跑 —— 登记，不是漏了。

---

## ✅ C（止血）已上线并验收 —— PR #2740 → `308ebd1323`

部署三样全过：md5 `be07faa3…` 一致 · PID `2750815→2894377` ·
启动 10:02:39 **晚于**制品 09:47:53 · **真跑了那条路径**。

```
追问「它的问题出在哪」  62字 → 134字
  本轮要求的原因分析或优化动作我这次跑出来了，但里面有几句话没达到我们
  「不把推测说成结论」的要求，所以我没发给你 —— 我不会拿别的数据凑，
  也不给你一个我不敢担保的因果结论。你可以问得更具体一点，例如把「为什么」
  换成「哪家店/哪道菜拉低了毛利」，那条路我能给你确定的数。
```

**两支各走各的，设计生效**：「为什么」仍然是 26 字反问 —— 那一支**没有产出**，
走的是诚实的「真没算出来」分支。3/4 条追问换成了诚实文案。

### 这一轮又抓到两个我自己的仪器缺陷

1. **变异检测器把 `4 passed` 判成了「红了」** —— 我用「stdout 里出现测试名」当判据，
   而 **pytest 的警告汇总块里也会打印测试名**。又一次「闸把自己的输出数了进去」。
2. **M1 第一轮没红**，暴露断言只钉结构不钉来源：把 `produced_but_rejected`
   换成常量 `False`，AST 结构一模一样、分支已经死了，而断言纹丝不动。
   ⇒ 补钉「必须从 `answer_text` 推出来」。**变异抓出来的漏，不是我想到的。**

### 基线当场重跑挣回了自己

上一轮量的基线 6043，这一轮实测 **6054** —— 另一条 session 改了 3 个
`backend/python` 文件。⛔ 引用上一轮的数会得出「我引入了 11 个新通过」这种荒谬结论。

| | 结果 | 新增失败 |
|---|---|---|
| 基线 `origin/main` cf4fd165b9 | 8 failed / 6054 passed | — |
| 本分支 | 8 failed / **6058** passed | **0**（消失也 0）|

### 下一步：A（治本）

让生成侧按闸的要求措辞（因果加限定词 / 预算目标标注为假设 / 高影响动作标明待确认）。
⚠️ 设计卡里登记的盲区**先做**：① 跨问句采一批 violations 按类型排序
（⛔ 不针对这一条调 prompt）② 量一次闸在**合规**文案上的误报率 ——
若它对合规文案也开火，A 永远做不完。

---

## 架构自审（2026-08-17）—— 我口述的那张图，逐条验

owner：「你审计一下吧」。把上一轮口述架构时断言的五条**逐个量**，
⛔ 不引用台账里的转述。

| # | 断言 | 判定 | 实测 |
|---|---|---|---|
| 1 | 意图是闭集，模型 19 选 1 | ✅ 成立 | prompt 19 == `_RESOLVER_DIMENSIONS` 19，**差集两边全空** |
| 2 | prompt 明令禁止模型算日期 | ✅ 成立，**证据是假的** | 原文比转述更硬（「你绝对不能计算或输出具体日期」「真实日期一律由确定性代码计算，不是你的工作」）；我引的 `:5399` 是**从台账搬的**，本轮才第一次真读到 |
| 3 | LLM 一个数字都不许产 | ⚠️ 机制名错，力度说轻了 | 是**两个**机制：`FactReconciler.reconcile`（数字层 backfill + flag 编造名字）+ `_narrative_grounding_violations`（叙事因果层）。违规**不是打标记**，是 `return _deterministic_fallback_response(reason="叙述未通过数据因果门禁")` —— 整段 LLM 叙事丢弃换确定性兜底。生产调用点仅 2 处（另 14 处在测试里） |
| 4 | 至少 5 处各自拼老板可见拒答文案 | ❌ **低估 3 倍** | AST 数字典字面量 `"answer_text"`：**15 处**（`restaurant_intent_service.py` 独占 11） |
| 5 | 契约不过 → contract-repair 改道 | ❌ **混层** | `*_contract_repair` 是**规划期**的 plan authority 标签（用用户措辞修模型的 resolver 标签）；作答期是 `answer_contract.validate`（单一调用点、每个答案都过），不过走 `describe_missing` 拼拒答。两个同名不同阶段的东西被我说成一件事 |

### 🔴 方法学发现（比上面五条都值钱）

断言 1 我第一次用**源码 grep** 数意图码，数出 **7**，与登记表的 19 对不上 ——
差一步就报出「**12 个 resolver 模型根本看不到**」这个头条级**假发现**。

真因：`_build_t3_prompt` **动态拼**意图清单，源码里那 7 个只是硬编码示例。
渲染一份真 prompt（9710 字）才数到 19。

▎**今天第三次源码 grep 造出假读数。** 判据：**问「模型看到了什么」只能渲染，
▎⛔ 不能 grep 源码** —— 与「AST 而不是字符串计数」同族，但更进一层：
▎AST 量的是结构，**渲染量的才是运行时真值**。

### 对下一步的两条实际影响

- **接缝 ①（意图闭集）⛔ 不用做** —— 两边一个都不差，是这套架构里最干净的一块。
  原计划里「让模型只从闭集选」的工作量归零。
- **接缝 ③（收敛老板可见文案）比原 scope 大 3 倍** —— 15 处而非 5 处。
  重新估工作量再动手，⛔ 不按 5 处的估算开工。

### 自查

5 条里 **2 条（断言 2 的行号、断言 5 的机制）是从台账/交接搬的，没验就画进架构图**。
这正是本仓 B⁷「从一句注释推出行为，要先验它」——
而这两条**跑一次/读一次的成本几乎为零**。
⇒ 口述架构时，**每条断言当场标注「量来的 / 读来的」**，读来的不进图。
