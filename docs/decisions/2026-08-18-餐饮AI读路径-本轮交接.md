# 餐饮 AI 读路径 · 本轮交接（2026-08-18）

**租户** MOCK_REST（prod 活代码）· **每条读数都带口径**（问句集 / 命令 / 轮数）
**状态** 10 个 PR 全部合并并部署，逐条 prod 验过（#2805 #2806 #2807 #2809 #2811
#2812 #2813 #2814 #2816 #2818）

---

## 〇 · 基线（⛔ 与上一轮交接件的 41/48 不可比）

问句集已**落进仓库**：`backend/python/smartbi/scripts/restaurant_panorama_probe.py`

🔴 上一轮那份**没落盘**，接手时只能从文档里逐条摘一份自己的，
于是 41/48 与我量到的 40/48 是**两个集合**，两个数看起来只差 1。

▎**口径就是仪器的一部分。** 问句集不落盘，下一个人的「基线」永远是重新造的。

| 口径（24 问句 × 2 轮） | 起点 `7ed9877dba` | 本轮结束 |
|---|---|---|
| 独立 `session_key` | 40/48，契约缺口 2 | 40/48，缺口 2 |
| **共用 `session_key`（= 生产形态）** | **33/48，缺口 1** | **40/48，缺口 0** |

⚠️ 生产入口 `gold_reads` 对同一个 sessionId **全程共用一个 key**，
所以「共用」那一行才是老板真实会话的样子。两种口径量的是两件事，⛔ 别混着比。

回归（`python-gate.yml` 同一条命令，本机）：`8517 / 0 failed` → `8574 / 0 failed`

⚠️ 上一轮交接件说的「2544 passed / 1 failed」是**更窄的调用**；
那条 `WinError 64`（本地 postgres 断连）**在基线树上单跑同样红**，不是任何改动引入的。

---

## 一 · 已上线并逐条 prod 验过的（每项 2~3 轮稳定，判据三条齐）

| PR | 改动 | prod 前 → 后 |
|---|---|---|
| #2805 | `dimensions` 传进执行器 | 追问「哪家店最多」`bd1d6675` 1249 字（与上一问**逐字相同**）→ **`e9467c3e` 1367 字**，首段点名「模拟·长宁龙之梦店 损耗最多 ¥31,436.37…最高比最低高 7.8%」 |
| #2805 | 能力拒答不挂会话 | 「翻台率→营收趋势」`316dc92b` 129 字**复读** → **`db9d5b1b` 511 字 answer**；四个按钮文案独立成句全部答上 |
| #2806 | 计划赢**还要求指标兼容** | 60 字拒答 →（我先改坏成 2479 字答非所问）→ **回到 60 字拒答** |
| #2807 | 门店极差按全部门店算 | 正文 **0.7% → 2.7%**（¥2,100,493 vs ¥2,044,560） |
| #2807 | 喂给模型的门店名单 | **5 行 → 10 行**；「我要不要关掉最差的那家店」绕缓存后 1811 字、无坏措辞、**点名垫底候选** |
| #2809 | `dims=∅` 时的拒答措辞 | 「我准备算的东西跟你问的对不上」→ **「我不确定你要看的是哪一层的数」**；`dims` 非空时**保持原句**（阴性对照也验到） |
| #2811 | 叙述闸反向规则：**有数据的维度不许说成「你没提供」** | 6 个走 synthesis 的问句：新规则命中 **0**、既有规则命中 18；**prod 侧阳性对照**：已知违规串 → 命中 1（`guest_traffic`），已知正当串 → 0 ⇒ 那一支活着且不误报。全景仍 40/48、缺口 0 ⇒ 没把任何答案推进兜底 |
| #2812 | 门店首段在**覆盖不全**时带限定语 | ⚠️ MOCK_REST 覆盖度 100% ⇒ **prod 验不到**，对照全部是单测（桩取自 `_store_breakdown_block` docstring 记的那次 2% 事故）。判据③ 因此是**阴性对照**：预测「零可见变化」写在测量之前，实测 `e9467c3e` 1367 字**逐字节相同** |
| #2813 | 探针自举第五件套：**LLM key 缺失要被认出来** | 缺 key 时产品**正确地**回一句 40 字 fail-closed 拒答，而它和「产品被问倒了」长得一模一样 —— 我当天就拿它误诊过一次 #2812。判据取 `planner_authority` 结构标记（⛔ 不取文案）。prod 实测自检行：`chart 3/3 … vl 0/0(未配账号)` |
| #2814 | **拒答不许编「为什么算不出」** + 门店 × 时段 | 「哪家店晚市最好」：`1654dd87` 95 字拒答（含**假话**「这两层的数不在同一张表上」）→ **`6dd5bfe0` 913 字 answer**，首段「按门店看**晚市**：陆家嘴正大店最强 ¥1,480,432…相差 16.1%」+ 门店 × 时段交叉表 |
| #2816 | 表格按**首段用的那个口径**排 + 写出排序口径 | 首段点名陆家嘴（晚市最强），而表格第一行是徐汇（**总计**最强）—— 两个数各自都对，但让老板去核对就是在花「说的话能信」那笔本钱。→ `fa226b8f` 935 字，陆家嘴**成为第一行**，并加一行「各门店分时段营收（按晚市营收从高到低）」 |

---

## 〇bis · 🔑 本轮做的是**可信度**，不是覆盖 —— 而现有仪器量不到可信度

全景探针的判据是「答上 / 没答上」。本轮 9 个 PR 里有 5 个改的是
**答得可不可信**（不编机制、不误报、能算的别说算不出、口径别漂）——
▎**这些改动一条都不会让 40/48 变成 41/48。**

实测两次（同一命令、同一天、部署后）：`39/48 缺口 1` 与 `40/48 缺口 0`。
逐条比对，**只有「员工排班合理吗」一条在抖**，其余 47 条一致 ⇒ 39 是噪声。
⛔ 单次读数拿去说「退步了」就是错的（纪律 12 当天再次生效）。

⇒ **下一轮若要量可信度，得先造仪器。** 候选判据（都还没做）：
  - 答案里出现的**每一句机制性断言**能不能由 spec/能力表算出来
  - 首段点名的对象与表格第一行是不是同一个
  - 拒答里给的「换个问法」建议，照做一遍是不是真能答上

---

## 二 · 🔴 本轮订正掉的**六个**判断（含我自己的和上一轮交接件的）

### 1. 上一轮交接件：「缺口1 = 菜单目录接进 dish 槽位」——**前提不成立**

📏 `_verbatim_entity` 在生产路径上只有 `restaurant_intent.py:6244` 一个调用点执行，
**6/6 次 `value=None`** ⇒ LLM 从没往 dish 槽位放过东西。
「那最差的呢」拒答的 reason 串是 `_execution_mismatch:609`（`spec.intent not in plan`），
而 dish 分支在 613 **根本没被求值到**。日志里的 `dish='最差'` 只是同一行 warning
顺手打印的字段。

▎**从日志一行 warning 读出的字段，不能当作那个字段被求值过的证据。**

### 2. 我自己：「[1a] 消灭 3 条拒答」——**是错的**

收紧（指标必须兼容）之后，实测到的形状② 3 条**全部回到拒答**。
[1a] 在当前样本上**净效果为零**：它守的是「维度和指标都对」那个格子，
并且不再制造答非所问；正收益要等一个两边都对的样本。

### 3. 我自己：「4 failed 是变异与全量回归并发污染」——**归因错了**

干净重跑仍然红。真因是我的镜像分支漏了三行善后
（既有 repair 修好 `code` 之后还要抹掉
`clarification_needed / clarification_question / missing_slot`）。
▎我一边写着「⛔ 不把矛盾留给下游当拒答理由」，一边留了另一个。

### 4. 我自己：「[C2] 那句假话是当前代码的缺陷」——**被 A/B 证伪**

📏 绕过 `narrative_cache` 重新生成：A 组（现状）**无坏措辞**。
查缓存表坐实：那句话来自 8-17 16:04 UTC 写入、TTL 24h 的**一条**记录，
`window_key` 含当天日期 ⇒ 明天不会再命中。

▎**我差点为一条缓存里的历史答案去改现行代码，并声称修好了它。**

⚠️ 救我的是阳性对照：第一次 A/B 两组 `md5` 完全相同且「喂给模型的门店行=0」——
那说明答案根本没重新生成（我清了 `restaurant_intent` 的 4 个缓存，
**没清 synthesis 的叙述缓存**）。没有它我会得出「修法无效」的**反向**结论。

⚠️ 但修法的**独立理由**成立：抬头说「门店 10 家」而 Top 门店段只列 5 行、
且不说明这是前 5 —— **自相矛盾的输入**，每次 `BUSINESS_OPTIMIZATION` 都在发生。

### 5. 我自己：「17 个 resolver 收不到 dimensions ⇒ 追问只对损耗一条链成立」——**实测 0/10**

见下一节。

### 6. 我自己：探针读了不存在的键

读 `stores`（真名 `top_stores`），`.get()` 把「读错字段」兜底成「列表是空的」；
而我配的阳性对照 `store_count > len(stores)` **在仪器坏掉时照样成立**（0 恒 < 10）
⇒ 它不是阳性对照。换成 `n50 > n5`（只有 LIMIT 不同，条数必须跟着变）才真能红。

---

## 三 · 📏 追问接得住到什么程度（10 对「基础问句 + 换维度追问」× 2 轮）

```
✅ 真接住      5 / 10
🟠 换成了拒答  5 / 10
🔴 收不到维度  0 / 10      ← 我原先登记的那一类, 实测一条都没有
```

⚠️ **判据订正过一次**：第一版只比 `md5`，于是「答案变成拒答」也被算成「已接住」，
读出 9/10。加一层「第二轮必须仍是 `answer`」后真值 5/10。
▎我想知道的是「追问接得住」，实际量的是「两轮答案不一样」（形态 A）。

5 条拒答：哪家店晚市最好(95字) / 哪家店折扣最多(68字) / 哪家店最缺货(95字，已知真边界)
/ 哪个食材涨得最多(17字) / 按门店看趋势(60字，fp `41747685` = `code∉plan` 那句)

🔑 **真正的形态是「这些维度组合根本没有 resolver 服务」—— 能力表的事，
不是给 resolver 加 `dimensions` 形参。**
与「『哪个卖得最多』（菜品 × 销量）没有任何 resolver 服务这个组合」同源。

---

## 四 · ⛔ 登记为「不修」或「未做」的

| # | 内容 | 为什么 |
|---|---|---|
| 缺口 2 | 绝对日期核对「原话里有没有」 | 📏 3 轮 × 2 问句**未复现**（6/6 全部正确，无 `2026-06-03`）。⛔ 不修一个测不到的缺陷。再现条件：需要当初那次的原始 `spec.time_range` 结构 |
| 形状① | `dims=∅` 的 9 条**不消解矛盾** | 两边都 serves ⇒ 维度判据没有区分力；按架构 §反问⑥ **反问本身是对的**，本轮只改措辞（#2809） |
| ~~[A2]~~ | ~~损耗首段在门店覆盖不全时会说谎~~ | ✅ **已做（#2812）**。⚠️ MOCK_REST 覆盖度 100% ⇒ 本租户仍然**验不到**，对照全部是单测 |
| ~~[C3]~~ | ~~叙述缓存把一次生成冻结 24h，写入前无闸检查内容~~ | ✅ **已做（#2811）**，见下节。⛔ 那条 8-17 的缓存条目**没有主动清**（只读查过）——它自然过期且 `window_key` 含当天日期不会再命中；要不要主动清是产品决定 |
| — | `_execution_mismatch` 返回值结构化成 `(log_reason, user_text)` | 正解。日志要精确术语、用户要人话，两个受众要求相反而现在是同一个串；要一次改 6 个 return + 换掉那处身份比较，**独立一轮** |
| — | 按架构 §上下文把前文作为**建议**给出 | 需要 `history` 到达 `_execution_mismatch` 那一层，属架构点 25 |
| — | 「哪个卖得最多」（菜品 × 销量）没有 resolver | 能力表的事。⚠️ **我的能力表模型比实际保守**：`_served()` 用 `_default_metrics_for_code` 判「谁服务」，而实际执行会做 **contract-repair**（实测 `SALES_SUMMARY → GROSS_MARGIN`）。实测「哪家店卖得最好」`kind=answer` —— 别拿静态查表当端到端结论 |

---

## 五 · [C3] 已量出的规模与设计约束

📏 `narrative_cache` 本租户 28 条，**8 条**含「让用户去弄数据」的措辞。逐条读：

| 类 | 条数 | 判断 |
|---|---|---|
| 导出预算表 / 租金 / 会员名单 / 领料明细 | 6 | **多半正当** —— 那些确实可能不在库里 |
| 「需要你提供后5家店的营业额…」（8-17 16:04） | 1 | **确证冤枉** —— 门店数据就在库里 |
| 「目前没有菜品销售明细和成本数据」（8-17 20:52） | 1 | **同源可疑** —— 它的 `plan=finance,period_comparison` **没拉** sales；对那次 plan 忠实，对老板是假话 |

🔑 两条冤枉的共同形状：**「这次没拉」被说成「没有数据」**。

⇒ 设计约束（形态 E）：**按措辞抓的闸误报率 6/8，必然被关掉。**
判据必须是「那一维**有没有数据**」——**查**不是**猜**，与架构一致。
`dimension_coverage` / `available_dimensions` 已经在算这件事。

---

## 六 · 机制化：`scripts/mutation-guard.sh`

变异对照的标准套路里那个 `git checkout -- <file>`
**一天冲掉我三次未提交的改动**：一次整个实现重做、一次导致我误判归因、一次冲掉一段注释。
规则早就写在硬约束里（「变异对照前先 commit」），我照样犯了三次。

▎**决心不构成约束力** ⇒ 做成会拒绝执行的闸。
脏树 `rc=1` 并列出会被冲掉的文件，干净树 `rc=0`，两侧都实测过。

---

## 六bis · 📏 能力表：三条缺口逐条定性（本轮主线，已做完）

⚠️ 第一版我拿「我以为的指标名 × 维度」去查能力表，读出
`inventory × store 没人服务` —— 而 `inventory` **根本不是登记表里的指标名**。
⇒ 那批读数作废，改用**产品自己产出的 spec** 当输入（形态 A）。

| # | 组合 | 定性 | 依据 |
|---|---|---|---|
| ① | 门店 × 餐段 | 🔴 **产品说假话 + 能算而没算** | `fact_pos_transaction` 99792 行、`store_id` 与 `meal_period` **两者零 NULL**、门店×时段 **20 个非空组合** ⇒ 就在同一张表上。**已修（#2814）** |
| ② | 折扣 × 门店 | ✅ 说法准确，能力可加 | 产品说「这次选中的**算法**不按维度拆」——说的是算法不是数据。`agg_daily` **有** `store_id`（`SELECT DISTINCT store_id FROM agg_daily`），加维度可行。**登记不做** |
| ③ | 缺货 × 门店 | ✅ **不是缺陷** | `resolve_inventory_warning(smartbi_pool, factory_id, top_n)` 无门店维度，输出是全租户汇总，正文里「门店」零次 ⇒ 「按门店拿不到」准确 |

🔴 **③ 我一度误判成假话**，理由是 `STOCK_SHORTAGE` 能按门店（实测确实出 10 家店的表）。
但那是**盘亏**（盘点差异）不是**缺货**（库存预警），两个不同的量 ——
改路由过去就是产品自己明说不做的「拿别的数据凑」。
⇒ 判据：**两个 resolver 都能按某维度，不代表它们回答的是同一个问题。**

📎 顺带实测的路由分裂（2 轮稳定）：
「哪家店**盘亏**最多」→ `STOCK_SHORTAGE` 不拦截能答 ✅；
「哪家店**最缺货**」→ `INVENTORY_WARNING` 被拦拒答 ✅（正确）。

---

## 六ter · 🔁 三次「闸 / 变异抓出我自己」（都不是我想到的）

| 抓我的东西 | 抓到什么 |
|---|---|
| `test_no_unregistered_store_capable_resolver` | 给 daypart 加门店表 ⇒ 它进了「门店宇宙必须一致」那一族，而我**没主动想起**。已补进 `_DEMO_GOLD_MAPPED_CODES`（⛔ 不加进那张「刻意不映射」表 —— 它已棘轮到 0，只许变短） |
| 变异 M4（表格无条件读 `revenue`） | **全绿** —— `can_see_money` 仍是 False，格式化照样走「不带 ¥」那一支，营收数字原样印出来只是没有货币符号。断言守的是**符号**，被守的行为是**金额数值**。顺带发现那个决定写了**三处** ⇒ 收敛成 `money_key`/`_fmt` 一处 |
| 变异 M2 / M4b / M4′ | 我的断言分别守成了「出现过」（而不是「被点名为最强」）、「非价格角色看不到」（缺阳性对照「价格角色看到的是不是钱」）、「碰巧相同」（桩里首位时段本来就是晚市） |

▎三次的共同形状：**闸和变异抓到的，从来不是我预想的那一条。**

---

## 六quater · 🔬 可信度仪器：造出来当天就抓到 4/4 兑现不了（#2818）

`smartbi/scripts/restaurant_advice_is_actionable_probe.py`

判据：拒答里那句「想看的话分开问，例如先问「按门店怎么样」」是一句**承诺**，
把它**原样问一遍**，看是不是真答得上。

📏 第一次有效读数（2 轮，阳性对照 2/2）：**拒答 16 条 / 给出建议 4 条 / 兑现 0 条**。
老板照做会扑空，然后被再反问一次。

🔴 **第一次跑是 rc=2，拦的是我自己的设计缺陷**：阳性对照写的是
「被测问句里至少一条答上」，而被测问句是**特意挑的会撞维度闸的**
⇒ 必然 0 条答上、`rc=2` 恒成立。
▎**一道恒红的闸，和一条恒真的断言一样没用。**

### ⛔ 修法不是「建议带上话题」—— 逐条实测否掉了它

| 候选 | 答上 |
|---|---|
| 按食材看库存预警 / 盘点差异 / 食材损耗 | 2/2 ✅ |
| **最近30天**按门店看时段表现 | 2/2 ✅（还要补时间窗） |
| **全部门店 最近30天** 按门店看营收与订单 | **0/2 🔴（补全两个槽仍然不行）** |

▎**没有一个构造规则能保证建议兑现得了。** 按模板发 = 换一批兑现不了的。

⇒ 改成描述**动作**（「换成按食材问同一件事」），⛔ 不给可照抄的字面问句。
老板照做时用的是**他自己的话**，话题天然在句子里 ——
📏「哪些食材最缺货」「按食材最缺货」都 2/2 ✅。
他若组出来的话仍含糊（原问句本来就缺话题，如「趋势」），那次反问是**正确的**：
▎**是他没说全，⛔ 不是我们给了句假话。**

📏 部署后重跑：**拒答 16 / 给出建议 0 / rc=0**，抽取器活性 True。

### ⚠️ 仪器与产品必须**同时**改

阳性对照第一版是「实际读数里至少有一条建议」。而
「抽取器坏了」和「产品不再给可照抄的建议」会读成**同一个数** ——
后者正是修复方向。⇒ 活性改用**内置 fixture** 验；「实际有几条建议」降级成读数。

---

## 六quinquies · 📏 全景探针的**噪声底**是被一个机制驱动的

三次读数：**40/48、39/48、38/48**（同一命令、同一天、生产形态）。
逐条 diff：差异**全是同一问句在两轮之间翻转**，成因是

```
[restaurant-intent] 判定「没答到」, 已逐出计划缓存:
  query=哪家店成本最高 missing=回答给出的是毛利排名和毛利率，没有直接给出成本最高的门店。
```

轮1 `answer` 1227 字 → 被自评判「没答到」→ 逐出缓存 → **轮2 clarification 10 字**。

▎**老板重复问一次，得到的更少。** 而共用 `session_key` 正是他的真实体验。

⚠️ 该机制在 pan1(3 次)/pan2(2 次)/pan3(5 次) **都出现**，早于本轮改动 ⇒ 已排除。

⇒ 两条结论：
1. **拿全景做前后对比时，要把这些条目单列** —— 否则噪声掩盖信号。
2. 自评判定**本身很准**（那两句 `missing` 说得很好），问题在**处置**：
   逐出后重新编译走了更差的路。⇒ 方向 (a) 保底给回第一轮那份 /
   (b) **把 `missing` 那句话告诉老板**（它很准），⛔ 不换成通用反问。
   (b) 更贴交付定义⑤ 且不用改缓存机制。

---

## 六sexies · ✅ 逐条读完之后**撤回**的一个判断

`SAMPLE_QUERIES` 62 条示范问句，实测 **9 条两轮都答不上**（0 条不稳定）。
我先后写过两个定性，**都被自己的读数否掉**：

| 我写的 | 读数说的 |
|---|---|
| 「它在毒化 RAG 路由」 | 那 9 条**路由全对**，每条都到了自己的 code |
| 「resolver 答不了自己的示范问句」 | 其中 **4 条是它正确地不答** |

逐条读老板看到的原文：

- **正确拒答 4 条**：「**净利润**（缺少费用、税费及其他收支）算不出来」
  （老板说「净赚」= 净利润，我们只有毛利）；
  「不能把它偷换成明天的预测排班」（架构明说的边界）
- 维度边界 3 条 · 缺时间窗 1 条 · #2811 那道叙述闸拦下 1 条 ✅

⇒ **没有需要立刻修的缺陷。**
▎**从计数下结论，和从原文下结论，是两件事。**

---

## 七 · 下一轮建议顺序

1. ~~量可信度的仪器~~ ✅ **已做（#2818）**，见 六quater —— 造出来当天就抓到 4/4 兑现不了。
   ⇒ 下一件该量的：**答案被自评判「没答到」之后的处置**（见 六quinquies）。
2. **折扣 × 门店**（上表 ②）—— 唯一登记「可行但没做」的能力缺口。
   ⛔ 依据必须是「它真能出」：先让 `resolve_discount_summary` 真出门店表，
   再补登 `_RESOLVER_DIMENSIONS`，⛔ 顺序不能反。
3. **架构点 25：上下文存 spec 栈，⛔ 不存对话文字** —— 它同时解决两件登记未做的事：
   ① 真反问之后换话题仍会拼接（#2805 的 [C1] 只拦住了能力拒答那一类）；
   ② `_execution_mismatch` 那句话按前文给建议（#2809 只换了措辞）。
4. **`_execution_mismatch` 返回值结构化成 `(log_reason, user_text)`** ——
   日志要精确术语、用户要人话，两个受众要求相反而现在是同一个串；
   一次改 6 个 return + 换掉那处身份比较。

### 🔵 已挂账（写死解冻条件，⛔ 在那之前不再投入）

**「哪家店晚市最好」曾有一轮 `dims` 只带 `store`，于是答了全天数据而不声明。**
三个假设逐个被自己的读数证伪：

| 假设 | 读数 |
|---|---|
| 抽取非确定性 | 8 问句 × 3 轮 = 24 次，`spec.dimensions` **0 条不稳定**，全部 `tier=llm` |
| history 丢维 | 6 对 × 2 轮 A/B，**12/12 有效样本**（两侧都 `tier=llm` 真编译），丢维 **0** |
| contract-repair 丢维 | 日志「服务不了本次的 `['store']`」打的是 `extra` 不是完整 dims —— **我读错了那一行** |

⚠️ 每换一个探针就换了一次被测对象（session_key 共用与否、history 形状）——
那是**结构问题**的 signature，不是样本量问题（形态 A¹⁴′）。
症状在 #2814 之后**已不复现**（同问句 3 轮全对，`fa226b8f` 一致）。

**解冻条件**：出现一条**带来源标记**的读数同时满足
(a) `dims` 比同问句无 history 时少一维、(b) 两侧 `source_tier` 都是 `llm`、
(c) `kind == "answer"`。

⚠️ 那个 A/B 上次做废过一次：**计划缓存键不含 history**，不清缓存时两侧是
同一个条目，而读数长得像「完全一致」。⇒ 每次调用前都清，并逐条贴 `tier`。

### ⚠️ 两条**跨轮**的提醒

- **MOCK_REST 只做冒烟**：本轮有两处风险在它上面**验不到**
  （门店覆盖不全、上千家店的 prompt 膨胀）。真要验收得挑脏样本，
  而环境约定只保留 F006 / LIUSHANMEN / MOCK_REST —— 这是个已知冲突，⛔ 别忘了它。
- **叙述缓存 TTL 24h**：prod 上看到的文本可能来自任意历史版本。
  判「这是不是当前代码的缺陷」之前，**先绕过 `narrative_cache` 重新生成一次**。

⛔ 已裁定不要重开：有默认值就压掉澄清（2026-08-07 撤回过，代价 5 条 UX 契约）、
复活关键词路由、自动晋升、向量做执行许可。
