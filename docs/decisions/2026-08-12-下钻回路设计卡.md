# 设计卡：下钻回路（缺口 → 追问 → 归因 → 事件）

**2026-08-12** · 状态：待实施（本轮架构收口之后）
来源：`docs/dispatch/2026-08-12-内部方向讨论-日结主线-转录.md`
⚠️ 该转录三人全是内部人员，**不是需求验证**。本卡的取舍依据是「与已有实现的接口」，
不是「客户要求」。

---

## 一句话

把「缺口声明」「追问按钮」「归因止步」三件现在各自为政的事，合成一个能自我推进的回路。

转录里这三件事是**同一句话的三面**：

> `[32:38]`「先按照现在有的一些算一笔出来，但是会跟他提醒说现在还缺少哪些数据」
> `[36:37]`「你要不然你戳他就跳，你戳了他就跳，它有下钻引导」
> `[23:0x]`「原来这个菜今天卖的特别好，为什么卖的好，有降价吗，没降价吗，还是说他们推了，还是没推」

---

## 现状（实据，非推断）

| 环节 | 现在是什么 | 位置 |
|---|---|---|
| 缺口 | 「我这儿有的是 / 缺的是」—— **清单，不映射影响** | `capability_answer.py` |
| 追问 | **三个硬编码问句**，每个 scenario 都一样 | `GoldBackedRestaurantTool:457` `decisionFollowups()` |
| 归因 | 链条止于**数据维度**（客数/时段/客群） | `.ladder` 那套 |
| 事件标注 | **没有载体** | — |

现有的三个硬编码问句：

```java
followup("老板今天怎么用这张报表做决定？", …)
followup("哪些动作今天先不要做？", …)
followup("明天看哪三个数判断有没有效果？", …)
```

它们是**决策导向**的，与转录说的三种（汇整 / 偏重性 / 深究）不是同一套；
更关键的是它们**不可能知道这次答案缺了什么** —— 它只拿到一个 `scenario` 字符串。

---

## 目标形态

```
答案
 ├── 数字（带 provenance）
 ├── 限定语（由 provenance 生成，不手写）
 └── 追问按钮（有类型，不是自由文本）
      ├─ T1 下钻   → 沿当前指标往下一个维度
      ├─ T2 补数据 → 「补 X，能把 Y 从估变实 / 解锁 Z」    ← 缺口映射到影响
      ├─ T3 标事件 → 「这天有没有做活动？」                ← 归因止步时才出
      └─ T4 深究   → 预置分析框架（优先级最低，先不做）
```

---

## 四个类型的定义与生成规则

### T1 下钻 DRILL
- **何时出**：答案里有 ≥1 个还没用过的可细分维度
- **生成**：registry 反查「当前 metric × 未使用的 dimension」
- ⛔ 不手写。手写 = 每个 tool 各写各的，就是现在的样子

### T2 补数据 FILL —— 这条是「缺口映射到影响」的落点
- **何时出**：`CellResult.missing_columns` 非空，或 `provenance != MEASURED`
- **生成**：**registry 反查**。`Metric.requires` 形如
  `("fact_pos_transaction.net_amount", "fact_pos_item.amount")`，
  反转成 `column → [metric_key]` 即可知道「补这一列能解锁哪些指标」
- **两类文案，对应两种影响**：
  - 完全算不出 → 「补 {列的人话名}，能算出 {那些 metric 的 label}」
  - 精度降级（`provenance=ESTIMATED`）→ ⚠️ **这一类要先分 (a)/(b)，见下**

#### 🔴 2026-08-14：ESTIMATED 有两种，我们一直当成一种

| | 含义 | T2 该说什么 |
|---|---|---|
| **(a) 可补的估算** | 缺某列数据，补了就实 | 「补 X，Y 从估变实」 ✅ |
| **(b) 结构性估算** | 数据齐了也只能是估的 | 上面那句 **永远兑现不了** ⛔ |

起因是一个读数：`_provenance_of` 是**指标键的纯函数** —— 只要依赖里有成本卡
那一列就恒为 ESTIMATED。**把全店的卡补齐，它还是 ESTIMATED。**
而 T2 当时的开价正是「补齐对应的成本卡，毛利就能从估变实 —— 那时这个数就是
账上的了」。**那句话有一整个分支是空头支票，而且已经上线过。**

日毛利是 (b)：成本卡给的是**理论用量**，实际耗用要盘库才知道。
客户侧的约束正好钉死它 —— 邓总店里 **10 天盘一次**。

🔑 **判据：在承诺「补 X 就变准」之前，先问「补齐之后它会不会仍然是估的」——
如果会，那不是数据缺口，是方法本身的下限。**

对 (b)，诚实的说法不是「补数据」，是「这个数只能是估的，因为盘点周期是 N 天」。
要变实得改**经营动作**（天天盘库），那是个有代价的选择。

⏸ **挂账**：要不要把「盘点频率」这个取舍摆给店长看 —— 那是产品判断，owner 定。
说清代价让他选，⛔ 不是推着他去做。

##### 怎么判 (a)/(b)：从 `requires` 推，⛔ 不查清单

标注挂在**列**上（`metric_registry.IRREDUCIBLE_ESTIMATE_COLUMNS`），指标的
(a)/(b) 由 `effective_requires` 推出来。于是将来任何新指标只要依赖那一列，
自动就是 (b) —— ⛔ 没有一张「哪些指标是 (b)」的手写清单。

⚠️⚠️ **但那张列标注本身是靠人标的** —— 「理论用量 ≠ 实际耗用」是业务语义，
推不出来。这是本登记表里少数几个**非推导**的地方之一，写在这里留痕。
闸能守的只有两件：标了的列必须真的被指标依赖，且必须写清楚为什么。
- ⛔ 「列的人话名」也必须在 registry 上，**不能手写映射表** ——
  与 `category` 同一条判据：手写映射一旦落地，新登记的列会悄悄落在表外而不报错

**为什么这条能自动产出而不用写文案**：只要反查做出来，
「补了能到什么程度」是算出来的，不是人编的。这也是它必须做成反查的全部理由。

### T3 标事件 ANNOTATE —— 归因止步时的出口
- **何时出**：归因链走到**无痕层**
- **判据**：当前归因维度的下一层在 registry 里**没有对应的 metric/dimension** → 无痕
- **有痕 / 无痕的实际分界**（已核）：

  | 要素 | 数据里有痕 | 能不能答 |
  |---|---|---|
  | 有降价吗 | ✅ 折扣额、折扣单数在 registry 里 | 能答 |
  | 做活动了吗 | ✅ 部分（促销/套餐能从订单看出） | 部分能 |
  | 他们推了没推 | ❌ 口头推荐不进 POS | 答不了 |
  | 天气 / 隔壁开店 | ❌ | 答不了 |

- **文案**：「{时间}{对象}{现象}，这天有没有做什么活动 / 是不是有人在推？」
- **用户答了** → 写入事件标注，`provenance = REPORTED_BY_USER`
- ⛔ **事件标注是主观数据，不能和实测数据混算**。它有自己的 provenance，
  否则下次归因会把「老板说那天做了活动」当成事实去推。
  这与 C 项那张 provenance 卡是同一个机制，不是两套。

### T4 深究 ANALYZE
- **何时出**：用户已看过基础答案（第二轮及以后）
- **生成**：预置分析框架（转录里「框死掉，不然他有发散性思维就无法无天了」那个思路）
- 优先级最低，**先不做**

---

## 为什么必须是类型而不是自由文本

现在 `decisionFollowups` 是三个硬编码问句，对每个 scenario 都一样。后果：

1. 追问质量取决于写它的人当时想到什么
2. 新增一个 tool 就要重新想一遍三句话
3. **它不可能知道这次答案缺了什么** —— 只拿到 `scenario` 字符串

类型化之后，追问是**从这次答案的结构里算出来的**，不是预先写好的。

---

## 回路怎么自我推进

```
第 1 轮：给数 + 限定语 + T2「补 X 能算 Y」
   ↓ 他补了 X
第 2 轮：Y 能算了，provenance 从 ESTIMATED 变 MEASURED，限定语自动消失
   ↓ 归因往下走
第 3 轮：走到无痕层 → T3「这天有没有做活动」
   ↓ 他答了
第 4 轮：事件入库，下次同类归因可以引用
```

每一轮的产出变成下一轮的输入。这是转录里那句
「就是推导让他去做…**不好意思不是我不好是你不给**」的机制化。

---

## 边界与优先级

- 按钮数量有上限：前端 `normalizeFollowUpActions` 拼完四路来源后 `.slice(0, 4)`
- **类型间优先级：T2 > T3 > T1 > T4**
  理由：缺数据是阻塞项、无痕是阻塞项，下钻只是锦上添花
- ⛔ 转录里的「让你修改一下提示词」**不做**。
  暴露 prompt = 暴露内部实现（违反「维度这些东西他看不着」）+ 绕开路由与权限。
  替代做法是**改问法**：把 plan spec 的槽位翻译成人话让他确认，
  例如「我理解成：全部门店 · 最近 30 天 · 毛利 [改成某一家][改成本月][改成营收]」——
  改的是已有的结构化槽位，输出契约不变，且顺带能让用户自己纠正上下文继承错误。

---

## 与已有东西的接口（都不用新建）

| 需要 | 已有 | 位置 |
|---|---|---|
| 按钮渲染 | `{label, question}` 已保留 label，**前端不用改** | `restaurant-synthesis.ts` `normalizeFollowUpActions` |
| 按钮抑制 | 系统故障时不出按钮 | #2536 的 `isClarification` + `unavailable` |
| 缺口来源 | `missing_columns` + `provenance` | `CellResult`（provenance 见架构收口 C 项） |
| 反查数据 | `Metric.requires` / `category` | `metric_registry.py` |

---

## 🔴 T1 的前置：resolver 词汇与 registry 词汇是两份，已经漂了（2026-08-13 实测）

T1 的生成规则是「registry 反查 **当前 metric × 未使用的 dimension**」，
所以它要能从答案里读出「已经用了哪个维度」。`rows` 投影（PR#2548）让答案有了
结构化数据，但**有结构不等于有共同语言**：

| 投影里的值（prod 实测） | registry 里 | 结果 |
|---|---|---|
| `type: "dish"` | `DIMENSIONS` 有 `product`（label=菜品），**没有 `dish`** | ❌ |
| `sales_volume` | `METRICS` 是 `sales_qty` | ❌ |
| `bill_count` | `METRICS` 里没有这个概念 | ❌ |
| `revenue` | ✅ 在 `METRICS` 里 | ✅ |

**四个字段只有一个对得上。**

⚠️ 这不是意外漂移，是**两代实现并存**：registry 是新的声明式抽象，
resolver 是 601 条那个年代的手写世界。形态 D（同一个东西有两份，它一定会漂）。

### 三条路，选第三条（**本卡不做，做 T1 时再动**）

1. **改 resolver 字段名对齐 registry** —— ⛔ 不行。`bill_count` 在 registry 里
   **根本没有对应键**，不能改名成一个不存在的东西；而且改名会破坏现有消费者，
   要先做一次「601 条」那样的消费者清单才敢动。
2. **投影层由 registry 反向推导归一** —— ⛔ 做不到。registry 里 `label`/`asks`
   都是中文，resolver 字段名是英文键，中间**没有可推导的桥**，最后一定退化成
   手写表 —— 而手写表正是本卡明令禁止的。
3. ✅ **resolver 自声明 + 闸钉住完整性**。每个 resolver 声明自己的输出字段对应
   哪些 registry 键，声明写在 **resolver 自己身上（一处）**，不是中央映射表。
   闸：任何 resolver 的输出字段**没有声明就红**。

   它与手写映射表的区别**不在映射写在哪，在于漏了能不能被发现** ——
   中央表漏一条是静默的，自声明漏一条被闸抓住。
   而且它天然处理 `bill_count` 那种情况：声明可以显式写
   「此字段不入 registry 域，不参与下钻」，改字段名做不到这个。

⛔ **这是 T1 的前置，不是 P0 的前置。** T2（补数据开价）全程在 registry 内部，
   不碰这个岔口 —— 所以 P0 先做 T2。

---

## 📌 挂账：粒度（grain）没有默认机制（**通用问答的改进，不是 P0 前置**）

⚠️ **不要因为 `restaurant_intent_service:322` 那句文案改漂亮了，就以为这块处理好了。**
文案改的是「怎么问」，没改「该不该问」。

2026-08-13 prod 实测：「今天赚多少」「今天毛利多少」「今天生意怎么样」
「打烊了，今天怎么样」**四句全部 `kind=clarification`**，回的是
「你是想看某道菜、某家门店，还是全店合计？」

而店长问「今天赚多少」**显然就是全店合计**，不需要问。

### 三件事写清楚

1. **判据与时间窗口那条完全一样**：默认 + 明示 + 可改。
   owner 2026-08-13 裁定时间窗口的三条理由（禁止降级处理防的是编数据不是防选默认 /
   反问把负担推回给不知道怎么问的人 / 人审同向）**对 grain 一字不改地成立**。
   落地形状：
   ```
   今天全店毛利 X 元
   （默认按全店合计；想看单店或单菜可以直接说）
   按钮：看各门店 / 看各菜品
   ```

2. **卡点在 `_execution_mismatch`（`restaurant_intent_service.py:284`），承重不能碰**。
   它的语义是「不许在执行期重新解释一个不可变的 v2 计划」。
   ⇒ 粒度默认**必须发生在计划生成时**（与时间/门店同一个位置），
   不能靠在这道校验上开口子。

3. **是新建，不是接线**。实测：
   ```
   time_range_defaulted   : 15 处
   store_scope_defaulted  :  9 处
   grain_defaulted        :  0
   aggregation_defaulted  :  0
   dimension_defaulted    :  0
   ```
   `RestaurantQuerySpec` 上只有两个 `*_defaulted` 字段，粒度这一层一个都没有。
   **工作量与「照抄时间那条」差一个量级** —— 别默认是后者。

### ⛔ 为什么它不是 P0 的前置

P0 的主体是**一个固定形态的日结报表**：粒度写死全店、时间写死当日，
**根本不经过 planner**，也就不会撞上那道一致性校验。
所以 grain 默认是通用问答的改进，P0 不等它。

---

## 📌 挂账：面向用户的串收敛到一处定义（P0 之后做，**现在不动**）

黑话闸目前用「有没有中文标点」当「面向用户」的**代理判据**，2026-08-13 实测
它两个方向同时失效：

- **假阳性**：扩面后报出 9 条，6 条是给模型的 prompt / SQL / 正则 / 开发者断言消息
- **假阴性**：prod 上**天天在发**的那句「查询维度超出计划 resolver 的能力范围」
  **不在清单里** —— 它没有中文标点

⛔ **不要靠不断加启发式去逼近**。静态分析判不出「这个串会走到用户面前」，
   加下去只会做出一个更复杂、误报更多、仍然漏 f-string 的东西。

**唯一的修法**：把面向用户的串**收敛到一处定义**，闸扫那一处。
`customer_text.py` 已有先例（`NO_SUBSTITUTION` / `PLANNER_UNAVAILABLE` 那批常量）。

⚠️ 这是个真重构，规模与「601 条」同级 —— 要先做一次消费者清单才敢动。
   **排在 P0 之后。** 在那之前闸保持 `ENFORCE_JARGON_GATE = False`（只报不拦）。

---

## 顺带发现的两处手写清单（不在本卡范围，记一笔）

- `containsActionAdvice()` 用关键词表判断有没有给建议
  （建议/优先/复盘/关注/排查/试点/控制/调整）
- `defaultAdviceForTool()` 每个 tool 一段硬编码建议文案

两者都是「**词表单独承接一个决策方向**」的形态，与本卡要消除的是同一类问题。
做 T1~T3 时会碰到它们，届时一并处理。

---

## 📌 挂账：成本覆盖率没接进限定语（**机制已经在，只是没接上**）

**2026-08-13 实测**：`provenance.qualifier()` 已经接受 `coverage_ratio`，覆盖不足时
会在限定语前面加一句「其中 X% 的营收没有配方成本，」；`provenance.coverage_ratio()`
也写好了。**而 `generic_answer.render()` 传的是 `coverage_ratio=None`** —— 那两样
今天是死代码。

**为什么这件事有实害**：`food_cost` 的表达式是

```
SUM(i.qty * COALESCE(c.food_cost, 0))
```

⛔ **没有成本卡的菜按 0 成本计入**。覆盖率 40% 的租户，毛利会被抬高一大截，
而限定语只说「按成本卡的理论用量估算」，**不说这个估只覆盖了四成**。
店长看到的是一个偏高的毛利 + 一句听起来已经解释过了的限定语。

**为什么今天没做**：
1. 接它要在执行器里多跑一次「有成本的营收 ÷ 全部营收」，属 P0 之外的改动；
2. **今天在 prod 上量不出来** —— MOCK_REST 的覆盖率是 **100.0%**
   （10 个在售菜品全部有成本卡，2026-08-12 营收 ¥749,009 全额覆盖）。
   这是「平租户让闸恒绿」的又一例：在这个租户上接不接，读数完全一样。

**做的时候的判据**：先找一个覆盖率明显不足的租户（或构造），
证明限定语里那句百分比**跟着覆盖率变**；再补一条变异对照——
把覆盖率强改成 100%，那句话必须消失。

---

## 📌 挂账：「没营业」和「数据没落库」在事实表上是同一个长相

**2026-08-13 实测**：打烊日结要判「今天有没有东西可推」。我原来的判据是
「`orders` 分得清（`0` vs `None`）」—— **错的**。`orders` 是 `COUNT(...)`，
空集上返回 **0 而不是 NULL**。拿一个根本不存在的租户跑，得到的是 `no_business`。

⛔ 更根本的一层：**没有行**这件事，既可能是今天没营业，也可能是 ETL 今天没落数。
从事实表本身推不出区别。

**要分清需要外部信号**，两条路：
- 门店营业日历（今天本来就该关门）
- ETL 水位表（今天这张表最后写入是什么时候）

在此之前，日结对这两种情况都是「不推、不告警」。**这是有意的**：宁可漏推一天，
也不要每周一给一家周一休息的店发一条假告警。

真正可达的 rc=2 只有一个：**一个租户都没轮到**（名单为空）。已构造验证：
`DAILY CLOSE INSTRUMENT DEAD 2026-08-13 06:05:55`。

---

## 🔴 挂账（阻塞真实门店）：同一屏上两个数字用了两个营收口径，毛利没扣折扣

**2026-08-13 体检结论。** owner 问：营收 71.8 万、毛利 50.7 万、毛利率 70.6%，
而中餐一般 60% 左右 —— 是覆盖率算漏了，还是 demo 数据本来如此？

**都不是。是口径混用。**

### 实测（prod / MOCK_REST / 2026-08-12）

`revenue` 在登记表里有**两个粒度的表达式**：

```python
exprs={"txn": "SUM(t.net_amount)", "item": "SUM(i.amount)"}
```

| 段 | 走哪个粒度 | 值 |
|---|---|---|
| 营收 | `txn` = `SUM(t.net_amount)`（**扣了折扣**） | 717,883.41 |
| 毛利 | `item` = `SUM(i.amount)` − 食材成本（**没扣折扣**） | 749,009.00 − 242,259.58 = **506,749.42** |

506,749.42 与界面显示的毛利**逐分一致** —— 机制确认。

两个口径的差 = **31,125.59**，正是折扣额。

### 为什么这是缺陷不是口径选择

- 店长拿屏上两个数相除得 **70.6%**；内部自洽的是 506,749/749,009 = **67.7%**
- 更要紧的：**毛利没有扣折扣**，被高估了 31,125.59（约 6.5%）。
  一家靠满减跑量的店，折扣越多毛利虚高越多 —— 而这正是老板要看的那个数

### 覆盖率是清白的

同日实测：7913 行 item 明细**无一缺成本卡**，覆盖率 **100.0%**，
食材成本率 **32.3%**（逐菜 10.7%~41.4%，鲈鱼 37.4% / 水煮牛肉 41.4% / 红糖糍粑 10.7%）。
成本卡数据本身合理。⛔ 所以不要去改成本卡，问题不在那里。

### 为什么没有顺手修

改 `revenue` 在 item 粒度的语义会影响**整条问答读路径**，不只是日结。
这是承重改动，等 owner 定方向。两条候选：

1. 毛利改用 txn 口径营收（`SUM(t.net_amount) − 食材成本` = 475,623.83）——
   数字变小但与营收段自洽
2. 明细行按比例摊掉交易级折扣，两个粒度都拿到扣折扣后的营收

⚠️ 无论选哪条，**在修好之前不要给真实门店开日结** ——
店长看到的毛利会比真实值高一个折扣额。

---

## 🛑 前置 3 调查结论：不是「缺 grain 默认」，是 `all` 被当成了分组维度

**2026-08-13 prod 实测。** owner 让先查「planner 判据是不是太严」，查完了 —— **是**，
但成因和预想的不同：**planner 根本没有不肯定粒度，它正确地给出了 `all`。**

### 实测（deployed 代码 / prod 库 / 逐题清缓存）

| 问句 | 结果 | spec.dimensions | resolver 声明支持 | 差集 |
|---|---|---|---|---|
| 今天赚多少 | clarification | `('all',)` | `{store}` | `{all}` |
| 今天营业额多少 | clarification | `('all',)` | `{store}` | `{all}` |
| 今天多少单 | clarification | `('all',)` | `{store}` | `{all}` |

三题**同一个形状**。拦截点是 `_execution_mismatch` 里那句子集判断：

```python
if not set(_canonical_dimensions(spec.dimensions)).issubset(supported_dimensions):
    return "我不确定你要看的是哪一层的数"
```

### 为什么这是范畴错误

- `all` 在登记表里的定义是「**不分组**，整体汇总」（`Dimension.group_expr = None`）
- `_RESOLVER_DIMENSIONS` 列的是每个 resolver **能按什么分组**
- ⇒ `all` 不在任何一个 resolver 的集合里，**也不可能在** —— 它根本不是一种分组

所以 `{'all'} ⊆ {'store'}` 恒不成立。**任何「全店合计」类问句都会被拒**，
与指标无关、与 resolver 无关。这不是能力缺口，是拿「不分组」去比「分组清单」。

旁证：`RESTAURANT_OPS_SALES_SUMMARY` 自己的注释写着它返回
「a real per-store Top-N table **in addition to the chain aggregate**」——
chain aggregate 正是 `all` 要的东西，它一直都能出。
`AGGREGATIONS["summary"].needs_dimension` 也早就是 `False`。

### ⛔ 为什么没有动手

修法落点在 `_execution_mismatch`（或能力表）内部 —— owner 划的红线：
**不许动 `_execution_mismatch`，它是承重的**；且属「必须问我」的 B 类。
**已停下，等裁定。**

⚠️ 顺带排除了一个可能的回归：本轮给 registry 加 `caveat` 字段**没有**进指纹
（`_plan_semantics_materials` 只读 `_REQUEST_METRIC_RULES` / `sorted(_INTENT_DESCRIPTIONS)` /
`_REQUISITION_SPEND_RE`，都是纯字符串），HEAD 上算出的完整指纹 `fd5508a3` 与
今晨 02:25 那轮一致。

### 判据 3 预演（探针侧放行 `all`，**未改产品代码**）

| 指标 | 日结 | 通用问答 | 一致 |
|---|---|---|---|
| 订单数 | 1970 | 1,970 | ✅ |
| 营收 | 717,883.41 | ¥717,883.41 | ✅ |
| 毛利 | 506,749.42 | ¥506,749.42 | ✅ |

**逐个指标的数完全相同** —— 放行 `all` 之后 3 题里 2 题能答
（「昨天营业额多少」还差一步，卡在另一个原因：「没算出语义计划与实际执行范围」）。

### 🔴 但两条路给店长看的**毛利率**不一样

问答那条自己是自洽的，它把 item 口径营收摆出来：

> 全部销售营收 **¥749,009.00**；其中可计算毛利的营收 ¥749,009.00，营收覆盖率 100.0%
> 已覆盖部分毛利 **¥506,749.42**，加权毛利率 **67.7%**

而日结把 **717,883.41（txn 口径）** 和 **506,749.42（item 口径）** 并排放 → 店长算出 **70.6%**。

⇒ **同一天、同一家店、两个界面，毛利率 67.7% vs 70.6%。**
这就是上一节那条口径混用挂账的下游表现，而且是 owner 说的最贵那种形态 D：
两个数字都对外。⛔ 先定口径，再谈两条路对齐。

### 还有一处两份实现

问答那条**已经有自己的覆盖率**（「营收覆盖率 100.0%」「10/10 个销售菜品有完整成本数据」），
与本轮接进 `CellResult.coverage_ratio` 的是**两份**。同样是形态 D，一并记账。

---

## 📌 挂账：合计层改走 `generic_executor`（方案 b）

owner 2026-08-13 **不选** b，理由记在这里免得下次又被提出来：

> b 只消掉合计层那一份，分组层仍是 resolver 自己的 SQL —— 所以 b **并不能根治
> 形态 D**，只是把切分点换了个位置，代价却是动一个 5000 行 resolver 且可能丢掉
> 它现在产出的别的东西。

⇒ 当前处置是**立闸钉住两份一致**（`scripts/cron/margin-parity-daily.sh`
+ `tests/test_margin_parity.py`），等有更大的重构窗口再谈 b。

⚠️ 真做 b 的时候，先看那道闸有没有一直是绿的 —— 它绿着说明两份确实等价，
那时合并的风险才低。闸红过的时间段要先查清原因，⛔ 不要带着未解释的差异去合并。

---

## 📌 挂账：折扣摊到明细行（让拆开加得起来）

现状是「可以加不起来，但必须说明」——逐菜那层已经标 `provenance=ESTIMATED`，
限定语写「这里没扣折扣 —— 折扣是整单的，摊不到单道菜；所以按菜加起来会比合计高」。

要让它加得起来，得把交易级折扣摊到明细行，而**摊派规则本身有真争议**：
满减券摊到哪道菜？按金额还是按份数？赠品算不算？
这是产品决定，不是工程决定，现在定是仓促。

做的时候的判据：摊完之后 `sum(逐菜毛利) == 合计毛利`（容差 0.01），
并且那句 ESTIMATED 说明**跟着消失**（变异对照：不摊时它必须还在）。

---

## 📌 挂账：一个数字配三段免责，视觉上 1:3（owner 2026-08-13 自陈是自己造成的）

那一屏现在的形状：

```
今天全部门店毛利合计 **¥475,623.83**       ← 1 行，店长要的
> 这是毛利…人工、房租、水电都还没扣          ← 免责
> 其中 55.0% 的营收能算准，另外 45.0%…      ← 免责
> 补齐对应的成本卡，毛利就能从估变实          ← 唯一告诉他能怎么办的
>   ⚠️ 2026-08-14 已删: 这句兑现不了(毛利是 (b) 类结构性估算), 见上文 (a)/(b)
```

**店长打开看到的主体是「为什么这个数不能信」，而他要的是「今天赚了多少」。**

⚠️ owner 记了一句：这个比例是他造成的 ——「不是利润」那句是他坚持要的，
估算标注也是他定的。⛔ 所以不要把它读成「文案写得不好」，它是几条正确决定叠加
出来的副作用。

**推送场景（手机通知）下三段免责很可能整个被划走**，那时店长看到的只剩数字，
免责一句都没起作用 —— 比不写更糟。

挂账两条：
1. 推送形态上要不要**折叠免责**（默认一句，其余可展开）
2. 第三段是唯一的行动项，却压在三段免责最下面 —— 顺序要不要动（**本轮不动**）

做的时候的判据：折叠之后**默认那一句必须仍然挡得住「把毛利当利润」**，
否则折叠等于把最重要的那句藏起来。

---

## 🔴 订正：飞轮探针的阳性对照为什么死了（**不是 cron 自举**）

2026-08-13 查实。原假设是「cron 环境和手动环境不一样」，**读数否掉了它**：

```
env -i /bin/sh -c '<crontab 那一行原样>'   →  positive_control_hits 0/40
交互环境跑同一个脚本                        →  positive_control_hits 0/40
```

两个环境**同一个结果**。真因在代码：

- 探针 A 遍把活指纹打桩成存量行的值：`ri._routing_rules_fingerprint = lambda: row_fp`
- 而闸比的是 `_normalize_stored_fingerprint(stored)`：**裸 8-hex 的旧格式一律返回 `""`**
  （「不会等于任何真指纹」）
- ⇒ `"" != row_fp`，**探针再也开不了那道闸**

这是指纹分层那次修复的**预期行为**（它防的正是「静默复活 39 条人审晋升」），
但它让探针的开闸手法当天失效 —— 形态 B：机制在，只是探针那一侧没跟着改。

**影响范围（重要）**：只有飞轮那条台账受影响。
日结、对账两条 cron **不经过指纹闸**（直接走 `execute_cell` / `resolve_gross_margin`），
它们的台账不受此事牵连。⛔ 不要「三条一起作废」。

⛔ 没有动指纹逻辑一行（硬停止）。修法在探针侧，等 owner 定。

---

## 🔴🔴 订正：「21 家真实餐饮租户一行数据都没有」是**假读数**

2026-08-13。我上一轮报了这个结论，owner 据此做了方向判断（「没有真实门店可开」）。
**它是错的**，成因见规则里的「变体 A⁶：仪器自己的会话上下文」。

用会话级 RLS 上下文 + 每租户独立会话重测：

```
FACTORY                  LAST_DAY          TXN    ITEMS     STORES    REVENUE_30D    COVER%
MOCK_REST                2026-08-13      91102   364257         10    22179688.00     100.0
RES_3101_009 (QHJ_PROD)  2026-08-12     616887  2434373         38    11575900.20      49.0
DEMO_REST                2026-08-12     531377  2140717         27     8938691.20      61.3
R_GML_DEMO (桂满陇)      2026-01-15      16213    16213        132    88696414.90       (无成本卡)
R_XMX_CHAIN (唏嘛香)     2026-02-15        141      141          1      713090.00      22.0
其余 22 家                -                   0        0          0              0        -
```

⚠️ **`is_active = f` 不代表没有数据在流**：`RES_3101_009` 和 `DEMO_REST` 都标着
inactive，而数据到**昨天**。那个字段说的是别的事。

### 这改变了什么

owner 加那一步的理由是「40%~70% 覆盖率下那句限定语从来没被人读过」——
**现在有真租户正好落在那个区间**（49.0% / 61.3%），可以在真实数据上离线渲染，
不必再拿构造值代替。

⛔ 但**不改变**任何硬停止：真实门店仍未开，未激活任何租户，未灌任何数据。

---

## 🔧 待执行规格：毛利口径统一（owner 2026-08-13 裁定，**下一段照此实现**）

### 根因一句话

**毛利的分子和分母来自两个不同的口径。** 三个症状同源：

| 症状 | 现象 | 成因 |
|---|---|---|
| A | DEMO_REST 日结毛利率 **88.3%** | `COALESCE(food_cost, 0)` 把缺卡的菜按零成本计入 |
| B | 青花椒问答**整个拒答** | 全额分子 vs 覆盖额分母，`margin_invariant_pass` **正确开火** |
| C | DEMO_REST 问答正文自己算不平 | 全额与覆盖额的数并排显示 |

⚠️ 这个缺陷在 MOCK_REST 上**结构性地看不见**：覆盖率 100% 时「全额」与「覆盖额」
是同一个数，`margin_invariant_pass` 的左右两边**由构造相等** —— 它不是没抓到，
是**在那个环境里不可能红**。

### 判据（新，进规则）

> 一道比较式的闸，要问「**有没有一种输入能让它红**」——
> 如果那种输入恰好不在你的验收环境里，它在你的验收环境里就是恒真式。

同族：「左右两边来源相同」「只在一个样本上响的阳性对照」。

### 四处改动

1. **`metric_registry.METRICS["food_cost"].exprs["item"]`**
   `SUM(i.qty * COALESCE(c.food_cost, 0))` → `SUM(i.qty * c.food_cost)`
   ⛔ **没有成本卡的菜不是零成本的菜。** 把缺失当 0 是在编数据（撞
   CLAUDE.md 原则 1「禁止降级处理」），而且编出来的方向**让毛利虚高** ——
   正好是最不该错的那个方向。SQL 的 `SUM` 天然跳过 NULL，缺卡行自动退出。

2. **`generic_executor._execute_derived_split`**：营收侧换成同一批行的
   `SUM(i.amount) FILTER (WHERE c.food_cost IS NOT NULL)`，与成本侧同源同粒度。

3. **`restaurant_ops_router`**：`total_profit = 覆盖净营收 − 覆盖成本`；
   `margin_invariant_pass` 两边同口径。
   ⛔ **不许用「把闸放宽让它别响」的修法** —— 它在青花椒上开火是对的，
   拦下的正是一个不可能的数。要改的是被它拦下的那个算法，不是它。

4. **折扣按比例摊到覆盖部分**（owner 裁定，解掉「覆盖部分内部仍未扣折扣」）：

   ```
   覆盖净营收 = 覆盖明细金额 − 折扣总额 × (覆盖明细金额 / 全部明细金额)
   覆盖毛利   = 覆盖净营收 − 覆盖成本
   provenance = ESTIMATED
   estimation_basis = "交易级折扣按明细金额比例摊派"
   ```

   🔑 **关键区分**，它让这件事从「有争议」变成「可以定」：
   **折扣总额是实测的，只有它在明细行之间怎么分是估的。**
   我们不是在猜折扣，是在分配一个已知的数。

   📌 **为什么按比例摊比不摊好**（判据）：
   - 不摊 → 误差**系统性地偏向好看**（折扣永远是减项，不减就一律虚高）
   - 按比例摊 → 误差**不系统偏向任何一边**
   - ⇒ **一个系统性偏向好看的近似，和一个双向的近似，不是同一档的东西。**

   ⛔ 这**不解冻**「摊派规则有真争议」那条挂账：那条争的是**定向折扣该不该
   归给它针对的那道菜** —— 只改分配、不改总额。按比例摊是分配问题的**默认解**，
   不是对那条争议的裁定。它变的时候，换的是一个表达式。

### 验收对照（⛔ 不另造，用现成的）

- **42.2%**（`RES_3101_009`）与 **61.3%**（`DEMO_REST`）各跑一遍
- 改**之前**的红对照已经有了，不用再造：
  `[gross_margin] blocked impossible aggregate factory=RES_3101_009 profit=347578.81 covered_revenue=150326.01`
- 改**之后**：两条路的数必须相同，且 DEMO_REST 日结毛利率从 **88.3%** 落到与问答一致

⛔ 改完没预算验就别改。

---

## 📌 挂账：通用问答在覆盖率 <100% 的租户上会给出虚高毛利（**已知，不打紧急补丁**）

暴露面：日结 cron 只对 `MOCK_REST` 发（100% 覆盖率，不受此缺陷影响）；
但**通用问答对任何租户在线**，青花椒那种拒答现在就在线上。

⛔ **不加紧急闸**：往承重路径加闸也是改动，同样需要验证预算；
而且真实门店未开，暴露面只有我们自己。⇒ 处置是**记账不是补丁**，
下一段按上面的规格一次性修掉。

---

## 📌 挂账：`factories.is_active` 的调用点排查

已测（相关性，非定义）：

```
 is_active | factories | with_users | total_users
     f     |    37     |     19     |     51
     t     |     4     |      4     |     79
 RES_3101_009 (QHJ_PROD)  is_active=f  users=8  数据到 2026-08-12
 DEMO_REST                is_active=f  users=9  数据到 2026-08-12
```

结论够用：**不能拿它判断「这家店还在不在用」**。

⚠️ 但它真正的消费者没找到（Java 侧扫到的 `isActive` 都是同名不同表的字段）。
⇒ 不急，但**在有人拿它做判断之前必须查** —— 一个没人知道语义、却长得像
能用来做判断的字段，是**下一个假读数的现成来源**。

---

## 🎯 T2 的输出长什么样 —— 分析的四层（owner 2026-08-14，基于线上真实读数）

前面几节定的是 T2 的**机制**（`Metric.requires` 反查）。这一节定的是它的**输出**：
同一张表，哪些话能说、哪些只能问、哪些一个字都不能说。

⚠️ 本节的例子是 **RES_3101_009 / 2026-08-12 的线上实测**，不是构造样例。
   探针走的是产品自己的 `GRAINS["item_cost"]` + `resolve_cost_keys` +
   `dish_cost_is_implausible`，没有第二份实现。

### 素材：真实的一张表

| 菜名 | 份数 | 营收 | 均价 | 成本卡 | 成本合计 | 毛利率 | 判定 |
|---|---:|---:|---:|---:|---:|---:|---|
| 招牌青花椒味(单人份) | 479 | 27,576.26 | 57.59 | 8.58 | 4,108.10 | 85.1% | ✅ 算进毛利 |
| 招牌青花椒鱼可乐单人套餐 | 227 | 14,544.75 | 63.95 | 6.38 | 1,451.00 | 90.0% | ✅ |
| 招牌青花椒味(2-3人份) | 22 | 4,397.07 | 197.98 | 24.70 | 548.59 | 87.5% | ✅ |
| **米饭** | 1018 | 15,242.85 | 14.97 | **167.20** | 170,259.76 | **-1017%** | ⛔ 排除 |

营收前 18 里**只有 4 道有成本卡**；卖得最好的那批（顺德干蒸鲜排骨 ¥10,221 /
老广腊味煲仔饭 ¥9,271 / 豉油蒸海鲈鱼 ¥8,547 / 抖音松叶蟹368套餐 ¥6,936）**全部没卡**。
📌 42% 覆盖率的实际长相**不是均匀地缺，是卖得最好的那些反而没录**。

### 四层：什么能说到什么程度

| 层 | 内容 | 允许的语气 |
|---|---|---|
| ① **事实** | 4 道能算毛利；米饭那张卡是坏的；有卡的 3 道毛利率 85–90% | 直接陈述 |
| ② **带口径的计算** | 能算准的营收占 42%；缺的是哪几道（按营收排序）；补哪几道能抬到多少 | 陈述 + 必带口径 |
| ③ **有嫌疑** | 85–90% 高于餐饮常识 65–75%，卡**可能**只录了主料 | ⛔ 只能问，不能断 |
| ④ **不能说** | 「毛利率健康」「建议给某菜调价」「为什么卖得好」 | 一个字都不说 |

③ 之所以只能问：样本只有 3 道，而且有些菜天然高毛利。**证据不足以断言，但足以发问。**
这正是 T3 的形态（归因走到无痕层 → 把问题交回给人），只不过这里止步的原因是
**样本不足**而不是**数据无痕**。

### 目标输出（可直接当验收样例）

```
今天能算毛利的有 4 道菜，占营收 42%。

⚠️ 米饭的成本卡是 ¥167.20 一份，而它卖 ¥14.97 —— 多半是单位记错了
   （比如一袋当成一份）。这道菜暂时没算进毛利，改好就会自动算回来。

📌 卖得最好的几道反而没有成本卡：
     顺德干蒸鲜排骨  ¥10,221
     老广腊味煲仔饭  ¥ 9,271
     豉油蒸海鲈鱼    ¥ 8,547
   补上这 3 道，能算准的营收从 42% 提到约 50%。

❓ 已有的 3 张卡算出来毛利率 85~90%，比餐饮业常见的 65~75% 高一档，
   有可能这些卡只录了主料。
   [列出成本率最低的几道给我核]
```

四个成分正好是本卡那条回路：**数 → 坏数据指名 → 缺口带优先级(T2) → 存疑处问用户(T3)**。

### ⛔ 这张表**最不该**产出的分析

最容易被写成这一句：

> 「你的招牌菜毛利率 85–90%，盈利能力优秀。」

**每个字都能从表里查到，而它是错的。** 它建立在 3 张卡、42% 覆盖率、
其中一张刚被判定为坏卡的基础上。

这就是转录 `[15:49]`「他讨好心的……提供情绪价值」的具体长相 ——
**不是模型瞎编，是它只挑能说的说。**

⇒ 所以 T2 的输出里**必须出现三样它不愿意说的东西**：

| 必须出现 | 形态要求 |
|---|---|
| 我只算了 42% | 覆盖率与数字**同时**出现，不能分开放 |
| 有一张卡是坏的 | **指名道姓**，不是「1 个菜品异常」 |
| 剩下这几张我也不太信 | 说成**问题**，不是结论 |

前两句已经在线上（2026-08-14 部署）。**第三句还没有** —— 它需要成本率分布的
读数才能定阈值，见下一节的挂账。

### 📌 挂账：`dish_cost_is_implausible` 只有上界，没有下界

现有判据只抓「成本 > 5 倍售价」。一张只录了 3% 真实成本的卡会**静默通过并抬高毛利** ——
正是我们一直在对抗的那个方向。

⛔ **不要凭常识加下界**，两个理由：
1. 阈值必须从**真实分布**里来。需要先量两个租户所有有卡菜品的
   `卡成本 / 当期均价` 分位数（p10/p25/p50/p75/p90）+ 各区间菜数。
   ⛔ 不要只看均值 —— 要看的正是有没有**簇**（大量落在 10~15% 才支持「只录主料」）。
2. **下界比上界危险得多**：饮料/米饭/小料的食材成本天然极低，一刀切排除会误伤
   真实的高毛利菜。它更可能应该做成**披露**（「这几道成本率低于 X%，可能只录了主料」）
   而不是**排除**。

📌 这个量要排在 T2 实现**之前**：T2 要输出的是「补 X 就能算准 Y」，
**如果卡本身是半截的，这句话是错的** —— 用户照做了数还是不准，而我们已经承诺过。
# 🔴 2026-08-14 实测追加：T2 开工前必须先读的四条

**来源**：owner 提了两个假设（卡是半截的 / `米饭(单人份)` 其实是套餐），
这一轮的任务是**验它们**。两个真租户、只读、走产品自己的 join 与判据。
**结论：假设 A 部分支持（要拆成两半），假设 B 推翻。**

---

## 一 · 第三种「不准」：**卡是对的、价是对不上的**

我们此前只认识两种不准：**没有卡**（覆盖率）和 **卡明显错**（单位记错，上界抓）。
这一轮量出第三种，而且它**最隐蔽**：

```
                    卡        运营库目录价   POS 均价    倍数
米饭(单人份)      ¥2.20        ¥10.00      ¥58.00      5.8x
经典红糖冰粉      ¥3.99        ¥18.00      ¥60.85      3.4x
陈皮山楂饮        ¥1.65         ¥8.00      ¥25.99      3.2x
脆哨茶油蒸蛋      ——           ¥24.00       ¥8.87      0.4x
```

占**有卡营收**的比例：`RES_3101_009 14.8%` / `DEMO_REST 15.4%`。

**为什么隐蔽**：卡单独看正常（¥2.20 一份米饭合理），价单独看也正常
（¥58 是个像样的菜价），**只有放在一起才不对**。

**为什么现有判据抓不到**：`dish_cost_is_implausible` 只有上界
（成本 > 5×售价）。这一类是**分母被抬高**，方向相反 —— 成本率看起来
特别**低**，一路绿灯，并且**把毛利抬高**。

⇒ 米饭(单人份) 是青花椒营收第二高的条目（30 天 ¥739,707.74），
在 POS 口径下毛利率 96.2%。**那个 96.2% 不是真的。**

---

## 二 · 判据：**判成本卡对不对，分母用运营库目录价，不用 POS 当期均价**

🔑 理由是一句话：**成本卡是按目录价录的。**用 POS 均价当分母，是拿两个不同
基准的数在比 —— 形态 A 打在诊断工具上。

实测对照（换分母后跨过 15% 线的）：

```
RES_3101_009
  POS分母  3.8% → 目录价分母 22.0%   ¥739,707.74  [主食] 米饭(单人份)
  POS分母  6.6% → 目录价分母 22.2%   ¥139,888.46  [饮品] 经典红糖冰粉
  POS分母  6.3% → 目录价分母 20.6%     ¥2,005.67  [饮品] 陈皮山楂饮
```

三道全部落回 20~22% 的正常区间 —— **它们的卡从来就没问题。**

⚠️ 连法：按 `product_source_pk = product_types.id`，**不按名字**。
名字匹配已经有一处权威实现（`resolve_cost_keys`），再写一份就是这一周
一直在修的那个病。

⛔ 目录价拿不到的菜**单列一栏**，绝不用 POS 均价顶替 —— 那正是这次的病。
（本轮实测：两个租户 `no_catalog_row=0 / no_catalog_price=0`，全都连得上。）

---

## 三 · 环境定性：**136/136 全是 `PLAN_C_DEMO_SEED_2026_04_25`**

两个「真租户」的 `product_types` 每一行都带种子标记，**没有一行是商户录的**。

这是 MOCK_REST 那个教训的变体：我们以为在真租户上验收，其实是**另一份生成
数据，只是更脏**。⇒ 用途必须两分：

| 用途 | 能不能 | why |
|---|---|---|
| **算法**验收环境 | ✅ | 缺失形状 / 坏卡 / 价格不一致都是真实存在的形状 |
| **商户行为**证据 | ❌ | 「只录主料」是**生成器**造的，不是邓总的店 |

⇒ **不能据 A-1 去设计「提醒商户把卡录全」** —— 那句话的前提没有证据。

---

## 四 · 裁定：**不加下界，也不建「共用卡」检测器**

### 实测：假设 A 拆成两半

**A-1 支持（青花椒的鱼类主菜族）**：换成目录价后**仍然成簇**，而且更纯 ——
低簇 42 道**全部**是 `主菜`/`套餐`，饮品与主食全部搬走了。

判据不是低比值本身，是**卡只随份量变、不随菜变**：

```
卡 ¥11.66 → 目录价 ¥128.00 (6 道) / ¥152.00 (2 道)
卡 ¥17.20 → 目录价 ¥168.00 (2 道) / ¥192.00 (1 道)
卡 ¥24.70 → 目录价 ¥198.00 (2 道) / ¥268.00 (1 道)
卡 ¥13.08 → 小份 ¥158.00 与**小小份**同一张卡（份量小了卡没跟着小）
```

**A-2 推翻**：低簇里最扎眼的那三道是**分母**问题，见第一节。

**DEMO_REST 推翻**：单峰 20~30%（p25=19.6 / p50=24.2 / p75=26.9），
低簇只剩 8 道，全是天然低成本项（土豆粉/糍粑/白灼生菜/冰粉），**没有鱼族、没有簇**。

### 为什么不加下界

⚠️ **2026-08-14 订正：被推翻的是【理由】，不是【结论】。**
判据：**裁定被推翻的是理由还是结论，要分开记** —— 否则哪天有人看到第一条理由
被推翻，会以为整条裁定作废了。

| 原理由 | 现状 |
|---|---|
| 「会误伤 `米饭(单人份)`」 | ❌ **已被消解** —— 换成目录价分母后它是 22.0%，不再是最低 |
| 「形状只有种子数据支持，真实商户是否这样录未知」 | ✅ 仍成立 |
| 「平阈值会误伤 DEMO_REST 那 8 道天然低成本项」 | ✅ **新增** —— 土豆粉 5.1% / 山楂饮 8.1% / 糍粑 9.7% / 白灼生菜 10.0% 都是真的便宜 |

⇒ **结论不变（不加下界），但支撑它的是后两条。**


🔑 **一个会误报的提示比没有提示更糟 —— 它烧掉的是信任，而「敢说不准」这件事
的全部本钱就是每一句都站得住。**

具体到这里：下界会**先打在 `米饭(单人份)` 上**（两租户成本率最低的条目），
而那是误伤 —— 它的卡是对的（¥2.20/¥10.00 = 22%），错的是价格。
店长去查卡会发现卡没问题，**然后就不再信这个提示了**。

「共用卡」那个形状目前**只有种子数据支持**。为一个可能是生成器产物的形状
建检测器，是在建幻觉。

⇒ 这个形态**由 owner 定**，很可能应该做成**披露**而不是**排除**。本卡不做。

---

## 五 · 这四条对 T2 的约束（措辞收窄）

T2 要说的是「补 X 就能算准 Y」。**如果卡本身是半截的，这句话是错的。**
所以：

- ✅ 可以说：「补这 3 道，**能算进毛利的营收**从 42% 提到 50%」
- ⛔ 不能说：「补上就**准**了」

覆盖率的说法**不依赖卡录得全不全**，所以 T2 不被阻塞 —— 但**不许承诺准确性**。

📌 顺带：`米饭`（不带后缀）那张 ¥167.20 的卡，用目录价 ¥10.00 做分母是 **1672%**
（用 POS 均价是 1117%）。**换分母让那张坏卡更明显，不是更不明显** ——
这条是「分母该用目录价」的另一个佐证。那张卡按红线一个字未动。

---

## 📌 2026-08-14 追加：成本卡的**两种**病，别混成一条判据

| 病 | 长相 | 实例 |
|---|---|---|
| **只录主料** | 卡只随**份量**变，不随**菜**变 | `卡 ¥11.66` 同时用于目录价 ¥128.00 的 6 道鱼 |
| **份量变了卡没跟着变** | 同一张卡跨**不同份量** | `卡 ¥13.08` 同时用于「小份 ¥158.00」与「**小小份**」 |

第一种是「录了一半」，第二种是「复制粘贴时忘了改」。**成因不同、修法不同、
提示的措辞也不同** —— 合成一条判据会让提示对两种情况都不精确。

⛔ **两个都不做检测器**，理由同「不加下界」那一节（形状目前只有种子数据支持）。
这里只是把形状记下来，等有真实商户录入的卡再定。

---

## 📌 2026-08-14 起：这张卡有**两个写者**

owner 与执行侧都往这张卡里追加过内容，2026-08-14 撞过一次合并冲突
（`T2 的输出长什么样` 与 `T2 开工前必须先读的四条` 都追加在末尾）。

⇒ 约定：**owner 不再直接推这张卡**，内容交给执行侧统一落。

⚠️ 顺带一条通用判据：**跨 session 共享的文件，重放 patch 一律加 `--3way`** ——
让冲突显形。普通 `git apply` 会干净地打在一个**已经前进了的基底**上，
把对方那一节安静地覆盖掉。

---

## 📌 2026-08-14：逐日覆盖率的**验证限制**（记下来，别忘了）

日结台账那行 JSON 加了 `coverage_ratio`（4b）。但 owner 裁定
`DAILY_CLOSE_FACTORIES` **维持现状**（选 c）：

- ⛔ **a 不行** —— 青花椒是真实客户的租户，发通知给它就是开真实门店，红线不解冻
- ⛔ **b 不做** —— 「只算不推」的名单解决的问题**现在还不存在**：跨天比较只在
  有真实店长在用时才有意义，而那时那家店本来就会在推送列表里。
  为不存在的场景加配置链路 = 默认不加

⚠️⚠️ **限制**：推送列表里只有 `MOCK_REST`，而它的覆盖率**恒 100%** ——
于是这个字段**每天都写 100.0**。「它会不会跟着变」在 MOCK_REST 上**验不了**。

⇒ 又是那个太干净的环境（同
[[feedback_pick_the_dirtiest_tenant_for_acceptance]]）。
**等真实门店开的那天验**，⛔ 在那之前不要因为「台账里那一列一直是 100」
就以为它接对了 —— 它和「一直没变」长得一模一样。

---

## 📌 2026-08-14：整屏去重 —— 同一件事说了两遍，措辞还不一样

数了一下：**3 个数字 + 6 段话**（1 : 5.5，卡上挂着的目标是 1:3）。
其中两段说的是同一件事：

```
T2 那段：  先补这 3 道的成本卡（…）——能算进毛利的营收会从 40.2% 提到约 47.7%
进度感那段：…下一个最划算的是【成本卡】——补 3 道菜的成本卡，
           能算准的营收就从 40.2% 到约 47.7%
```

**「两份会漂」漂进了面向用户的正文。**

⇒ 分工定死：**T2 说「做什么」，进度感说「我在哪」。**
「先补这 N 道 → 覆盖率 a% 到 b%」**只由 `fill_offers.offers_for_cost_gaps` 一处产出**。
`data_progress.render` 只出视图，⛔ 不出建议。
闸：`test_the_next_step_sentence_has_exactly_one_home`（带阳性对照）+
`test_screen_says_each_thing_once`（同一个覆盖率在一屏上只许出现一次）。

⚠️ **仍然 1:4.6，还没到 1:3。** 剩下的重复见下一节。
