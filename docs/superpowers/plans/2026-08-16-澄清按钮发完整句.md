# 澄清按钮发完整句 Implementation Plan（计划 1 / 共 2）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 澄清按钮的 `question` 从光秃秃的词（`本月`）改成合成的完整问句（`本月米饭的销量是多少`），根治「一条对话的计划被另一条对话命中」。

**Architecture:** 新增一个纯函数 `_compose_clarification_question(prefix, seed, *, kind, store_names)`，把用户点的那一段**前置**到原问句上；两条准入不过就退回现在的行为。`_clarification_followups` 增加 `seed` 形参并调用它；`tiered_answer` 在唯一调用点把 `query` 传进去。顺带把重复的窗口字面量改成引用既有的 `_SWITCHABLE_WINDOWS`。

**Tech Stack:** Python 3.11 · pytest · 纯 Python 改动（⛔ 零 DB 迁移、零前端）

## Global Constraints

- **设计卡**：`docs/superpowers/specs/2026-08-16-时间词语料与澄清按钮-design.md`（件 1 一节）
- **改动只碰一个产品文件**：`backend/python/smartbi/gold/restaurant/restaurant_intent_service.py`
- **准入两条，任一不过就退回现在的行为**（返回光秃秃的词），⛔ 不发一个更坏的串：
  1. `seed` 非空
  2. 合成句自足 —— **按钮种类不同，检查的东西不同**：时间查 `_resolve_sales_date_range(合成句)[1] != "全部历史"`；门店查 `_split_store_scope(合成句, store_names)[0] == prefix`
- **⛔ 不改 `clarification_options`（LLM 自撰选项）那一支** —— 自撰选项不存在自足性概念
- **承重断言一律写在 `backend/python/tests/` 下** —— 手工探针 CI 抓到率实测 1/8
- **每条断言配变异对照**；阴性断言额外配阳性对照
- **部署通道**：纯 Python ⇒ `./scripts/deploy/deploy-smartbi-python.sh --env prod`，⛔ 不用 `release-cretas.sh`
- **提交用** `./scripts/safe-commit.sh "<msg>" <file>...`，⛔ 不用裸 `git commit`
- 当前分支 `codex/claude-time-phrase-corpus`（已 off origin/main）

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `backend/python/smartbi/gold/restaurant/restaurant_intent_service.py` | 澄清按钮的产出 | 改：加纯函数、`_clarification_followups` 加形参、调用点传 `query`、消除窗口字面量副本、`import` 加两个名字 |
| `backend/python/tests/test_clarification_buttons_compose_full_question.py` | 件 1 的全部承重断言 | 新建 |

⚠️ **⛔ 不新建产品模块** —— 合成逻辑只有十几行且只有一个消费者，抽文件是过度设计。
它与 `_split_store_scope` / `_strip_store_scope` 是同一族（都在处理「按钮问句的前缀」），
放在它们旁边。

---

## Task 1: 合成函数（纯逻辑，无接线）

**Files:**
- Modify: `backend/python/smartbi/gold/restaurant/restaurant_intent_service.py:50-58`（import）、`:1013` 附近（新增函数，紧跟 `_STRIPPABLE_TIME_PREFIXES` 那一族）
- Test: `backend/python/tests/test_clarification_buttons_compose_full_question.py`

**Interfaces:**
- Consumes: `_resolve_sales_date_range(query, today=None) -> Tuple[Tuple[date, date], str]`（`restaurant_ops_router`，第二个元素是窗口标签，解不出时是 `"全部历史"`）；`_split_store_scope(seed, store_names) -> Tuple[str, str]`（本文件 `:974`，返回 `(前缀, 余下)`，切不出时前缀是空串）
- Produces: `_compose_clarification_question(prefix: str, seed: str, *, kind: str, store_names: Sequence[str] = ()) -> str` —— Task 2 调用它。`kind` 只接受 `"time"` / `"store"`，别的值 **raise ValueError**

- [ ] **Step 1: 写失败的测试**

新建 `backend/python/tests/test_clarification_buttons_compose_full_question.py`：

```python
"""澄清按钮的 `question` 合成成完整问句。

## 为什么

生产日志实测（2026-08-16）：

    zero-token plan-cache hit: authority=validated_plan_cache
    intent=RESTAURANT_OPS_WASTAGE_TOP clarification=False query=本月

一条链问「损耗」→ 点「本月」→ 计划以字符串 `本月` 为键写进进程内计划缓存；
另一条链问「门店排名」→ 点「本月」→ **命中前一条链的计划**。

而「本月」是**产品自己发的按钮**（`_clarification_followups` 里 `question == label`）
⇒ 全系统最高频的缓存键，恰好是含义完全取决于上一轮的那批。

▎最容易被缓存的那批串，恰好是最不该被缓存的那批。

## 判据

按钮发的 `question` 必须是一句**自足**的话 —— 单独拿去问也能得到同一个答案。
"""
import pytest

from smartbi.gold.restaurant.restaurant_intent_service import (
    _compose_clarification_question,
    _split_store_scope,
)
from smartbi.gold.restaurant.restaurant_ops_router import _resolve_sales_date_range

SEED = "米饭的销量是多少"
STORE_NAMES = ("青花椒南方百联店", "模拟·闵行莘庄社区店")


# ── 正例：合成 ────────────────────────────────────────────────────
@pytest.mark.parametrize("window", ["本月", "上个月", "最近7天", "最近30天"])
def test_time_button_composes_a_full_question(window):
    assert _compose_clarification_question(window, SEED, kind="time") == f"{window}{SEED}"


@pytest.mark.parametrize("window", ["本月", "上个月", "最近7天", "最近30天"])
def test_composed_time_question_resolves_the_clicked_window(window):
    """🔴 承重：合成句单独拿去解析，解出的**正是用户点的那个窗口**。

    ⛔ 不是「解得出某个窗口就行」—— 那样把「本月」拼成「上个月X」也会过。
    """
    composed = _compose_clarification_question(window, SEED, kind="time")
    assert _resolve_sales_date_range(composed)[1] == window, composed


def test_store_button_composes_and_the_scope_is_recoverable():
    composed = _compose_clarification_question(
        "全部门店", SEED, kind="store", store_names=STORE_NAMES)
    assert composed == f"全部门店{SEED}"
    assert _split_store_scope(composed, STORE_NAMES)[0] == "全部门店"


def test_store_name_button_composes():
    composed = _compose_clarification_question(
        "青花椒南方百联店", SEED, kind="store", store_names=STORE_NAMES)
    assert composed == f"青花椒南方百联店{SEED}"
    assert _split_store_scope(composed, STORE_NAMES)[0] == "青花椒南方百联店"


# ── 准入 1：seed 非空 ────────────────────────────────────────────
@pytest.mark.parametrize("seed", ["", "   ", None])
def test_empty_seed_falls_back_to_the_bare_word(seed):
    """⚠️ 这一条**不能靠自足性检查兜**：`seed=''` 时合成句就是「本月」，
    而「本月」确实解得出窗口 —— 自足性检查会放行一个不是完整句子的串。
    """
    assert _compose_clarification_question("本月", seed, kind="time") == "本月"


# ── 准入 2：自足性，**按种类分**────────────────────────────────────
def test_a_time_word_the_parser_does_not_know_falls_back():
    """🔴 让准入 2（时间）真的红一次。

    ⚠️ 现有 4 个按钮词拼上任何 seed 都解得出窗口 ⇒ 这条准入在**当前词表下恒真**。
    它守的是「将来有人往按钮词表里加了解析器不认识的窗口词」。
    没有这条测试，那条准入从未被验证过。
    """
    assert _resolve_sales_date_range(f"第三季度末{SEED}")[1] == "全部历史"  # 前置事实
    assert _compose_clarification_question("第三季度末", SEED, kind="time") == "第三季度末"


def test_the_time_check_is_not_applied_to_store_buttons():
    """🔴 设计卡第一版把「解得出窗口」写成了通用准入 —— 那会让**每一个**
    门店合成句被判不自足，全部退回光秃秃的词，而我会以为改好了。

    实测：`全部门店米饭的销量是多少` 的窗口就是「全部历史」。
    """
    assert _resolve_sales_date_range(f"全部门店{SEED}")[1] == "全部历史"   # 前置事实
    assert _compose_clarification_question(
        "全部门店", SEED, kind="store", store_names=STORE_NAMES) == f"全部门店{SEED}"


def test_a_store_prefix_that_cannot_be_split_back_falls_back():
    """准入 2（门店）能红：店名不在 `store_names` 里就切不回来。"""
    assert _compose_clarification_question(
        "查无此店", SEED, kind="store", store_names=STORE_NAMES) == "查无此店"


# ── 兜底不许把「我不知道」翻译成一个值 ────────────────────────────
def test_unknown_kind_raises():
    """⛔ 不返回兜底值 —— 那会让拼错的 kind 静默退回旧行为，
    长得和「准入没过」一模一样（本仓形态 A¹⁰）。"""
    with pytest.raises(ValueError):
        _compose_clarification_question("本月", SEED, kind="dish")


# ── 阳性对照 ────────────────────────────────────────────────────
def test_composition_actually_changes_the_string():
    """⛔ 少了它，上面那些「退回光秃秃的词」的断言在
    「这个函数什么都不做」时**全部成立**。"""
    assert _compose_clarification_question("本月", SEED, kind="time") != "本月"
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd backend/python && python -m pytest tests/test_clarification_buttons_compose_full_question.py -q 2>&1 | tail -5
```

预期：`ImportError: cannot import name '_compose_clarification_question'`（collection error，全部失败）。

- [ ] **Step 3: 写实现**

先把两个名字加进 `restaurant_intent_service.py:50` 那个**已有**的 module-level import（⛔ 不新增 import 语句，⛔ 不函数内 import —— 该模块已在 module level 被 import，加名字零成本、无新循环）：

```python
from smartbi.gold.restaurant.restaurant_ops_router import (
    _SERVICE_DISPATCHED_WINDOW_AWARE,
    _resolve_sales_date_range,          # ← 新增
    _resolve_sales_query_spec,
    demo_data_factory_for_code,
    dish_catalogue_scope,
    extract_store_mentions,
    resolve_by_code as _resolve_tiered,
    resolver_supports_explicit_window,
)
```

然后在 `_STRIPPABLE_TIME_PREFIXES` 定义之后（约 `:1020`，紧跟那一族前缀工具）新增：

```python
def _compose_clarification_question(
    prefix: str,
    seed: Optional[str],
    *,
    kind: str,
    store_names: Sequence[str] = (),
) -> str:
    """澄清按钮的 `question`: 把用户点的那一段**前置**到原问句上。

    ## 为什么要合成

    发光秃秃的「本月」有两个后果, 都在生产上实测到了:

    1. **计划缓存串话题** —— 缓存键是 `(factory_id, 归一化问句, 版本)`, 不含会话。
       「本月」这种串的含义完全取决于上一轮, 而它是全系统最高频的键。
    2. **飞轮语料没价值** —— 记下来的是「本月」「上个月」这几个无意义的串,
       而语料的全部价值在于「完整、自足、下次能直接问的句子」。

    ## 为什么**前置**是安全的

    走到时间澄清的硬条件就是「LLM 没认出时间词 **且** 确定性层解不出窗口」
    ⇒ seed 里按定义没有时间段要替换, ⛔ 不需要 `_split_time_scope` 定位。

    ⚠️ 即使两层都漏了某个时间词(实测 `最近损耗怎么样` 在确定性层就是「全部历史」),
    合成句解出的仍是**用户点的那个窗口**:
    `本月最近损耗怎么样` → 「本月」。两条路都不产生坏结果。

    ## 准入(任一不过就退回 `prefix`, ⛔ 不发一个更坏的串)

    1. seed 非空 —— ⚠️ **不能靠下面那条兜**: seed 为空时合成句就是 `prefix`,
       而 `prefix` 自己解得出窗口, 自足性检查会放行一个不是完整句子的串。
    2. 合成句自足 —— 🔴 **按种类查不同的东西**:
       - time: 解得出窗口
       - store: 门店前缀能被 `_split_store_scope` 原样切回来

       ⛔ 不要拿「解得出窗口」去查门店 —— `全部门店米饭的销量是多少` 的窗口
       是「全部历史」, 那样**每一个**门店合成句都会退回光秃秃的词。
    """
    head = (prefix or "").strip()
    body = (seed or "").strip()
    if not head or not body:                       # 准入 1
        return prefix
    composed = f"{head}{body}"
    if kind == "time":
        ok = _resolve_sales_date_range(composed)[1] == head
    elif kind == "store":
        ok = _split_store_scope(composed, store_names)[0] == head
    else:
        # ⛔ 不兜底 —— 拼错的 kind 静默退回旧行为, 长得和「准入没过」一模一样。
        raise ValueError(f"unknown clarification button kind: {kind!r}")
    return composed if ok else prefix
```

⚠️ 时间那条写的是 `== head` 而**不是** `!= "全部历史"` —— 前者同时守住
「解出的是**用户点的那个**窗口」，后者只守「解出了某个窗口」。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd backend/python && python -m pytest tests/test_clarification_buttons_compose_full_question.py -q 2>&1 | tail -5
```

预期：`17 passed`。

- [ ] **Step 5: 变异对照 —— 证明这些断言在守东西**

逐条做，每条**先确认变异真的改变了行为**再看测试红不红（形态 C″）：

| # | 变异 | 预期红的测试 |
|---|---|---|
| M1 | `return composed if ok else prefix` → `return prefix` | 正例 + 阳性对照 |
| M2 | 去掉 `if not head or not body` 那一行 | `test_empty_seed_falls_back_to_the_bare_word` |
| M3 | time 分支 `== head` → `!= "全部历史"` | `test_a_time_word_the_parser_does_not_know_falls_back` 仍绿；⚠️ **需要新增一条**才抓得住（见下） |
| M4 | store 分支改成和 time 一样查窗口 | `test_the_time_check_is_not_applied_to_store_buttons` + 两条门店正例 |
| M5 | `raise ValueError` → `return prefix` | `test_unknown_kind_raises` |

🔴 **M3 会暴露一个缺口**：`第三季度末` 在两种写法下都退回，所以那条测试抓不住 M3。
补一条能区分的：

```python
def test_the_window_must_be_the_one_the_user_clicked():
    """M3 对照：`!= "全部历史"` 与 `== head` 的区别只在**解出了别的窗口**时显现。

    ⚠️ `上上上个月` 实测解出「上上个月」—— 解得出窗口(所以 `!=全部历史` 放行),
    但**不是**用户点的那个 ⇒ 必须退回。
    """
    assert _resolve_sales_date_range(f"上上上个月{SEED}")[1] == "上上个月"   # 前置事实
    assert _compose_clarification_question("上上上个月", SEED, kind="time") == "上上上个月"
```

跑一遍确认它在 M3 下红、在正确实现下绿。

- [ ] **Step 6: 提交**

```bash
cd /c/Users/Steve/cretas-lwb
./scripts/safe-commit.sh "feat(rest-ai): 澄清按钮问句合成函数 —— 前置到原问句 + 两条准入

计划缓存键不含会话, 而澄清按钮发的 question 就是光秃秃的「本月」
==> 最高频的缓存键恰好是含义随上一轮变化的那批(生产日志实测串话题)。

准入按按钮种类分: 时间查「解出的窗口 == 点的那个」, 门店查「前缀能切回来」。
⚠️ 设计卡第一版把查窗口写成通用准入, 那会让每个门店合成句都退回光秃秃的词。

本 commit 只加纯函数 + 测试, 尚未接线。" \
  backend/python/smartbi/gold/restaurant/restaurant_intent_service.py \
  backend/python/tests/test_clarification_buttons_compose_full_question.py
```

---

## Task 2: 接到 `_clarification_followups`（含消除窗口字面量副本）

**Files:**
- Modify: `backend/python/smartbi/gold/restaurant/restaurant_intent_service.py:1233-1252`（函数体）、`:1668`（唯一调用点）
- Test: `backend/python/tests/test_clarification_buttons_compose_full_question.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 `_compose_clarification_question(prefix, seed, *, kind, store_names)`；既有 `_SWITCHABLE_WINDOWS: Tuple[str, ...]`（`:1006`）；`RestaurantQuerySpec.store_options: Sequence[str]`、`.clarification_question: str`、`.clarification_options: Sequence[str]`
- Produces: `_clarification_followups(spec: RestaurantQuerySpec, seed: str = "") -> List[Dict[str, str]]` —— Task 3 走真实入口验它

- [ ] **Step 1: 写失败的测试**

追加到同一个测试文件末尾：

```python
# ══════════════════════════════════════════════════════════════════
# 接线：`_clarification_followups` 真的用上了合成函数
# ══════════════════════════════════════════════════════════════════
from smartbi.gold.restaurant.restaurant_intent import (          # noqa: E402
    STORE_SCOPE_CLARIFICATION_QUESTION,
    TIME_CLARIFICATION_QUESTION,
    RestaurantQuerySpec,
)
from smartbi.gold.restaurant.restaurant_intent_service import (  # noqa: E402
    _SWITCHABLE_WINDOWS,
    _clarification_followups,
)


def _spec(**kw):
    """⚠️ 用**真的** RestaurantQuerySpec，⛔ 不用 SimpleNamespace ——
    桩的自由度会让我构造出真实那侧永远不产出的形状（本仓形态 B‴）。"""
    return RestaurantQuerySpec(**kw)


def test_time_clarification_buttons_carry_the_whole_question():
    spec = _spec(clarification_question=TIME_CLARIFICATION_QUESTION)
    got = _clarification_followups(spec, SEED)
    assert [b["question"] for b in got] == [f"{w}{SEED}" for w in _SWITCHABLE_WINDOWS]
    # label 仍是短词 —— 按钮上显示的东西⛔ 不变
    assert [b["label"] for b in got] == list(_SWITCHABLE_WINDOWS)


def test_store_clarification_buttons_carry_the_whole_question():
    spec = _spec(clarification_question=STORE_SCOPE_CLARIFICATION_QUESTION,
                 store_options=list(STORE_NAMES))
    got = _clarification_followups(spec, SEED)
    assert got[0]["question"] == f"全部门店{SEED}"
    assert got[1]["question"] == f"{STORE_NAMES[0]}{SEED}"
    assert got[0]["label"] == "全部门店"


def test_no_bare_window_word_is_ever_sent_as_a_question():
    """🔴 承重：这是缺陷本身。任何按钮的 `question` 都不许**等于**一个窗口词。"""
    spec = _spec(clarification_question=TIME_CLARIFICATION_QUESTION)
    for b in _clarification_followups(spec, SEED):
        assert b["question"] not in _SWITCHABLE_WINDOWS, b


def test_empty_seed_still_produces_buttons():
    """阳性对照 + 退化路径：seed 为空时**仍然出按钮**（发光秃秃的词），
    ⛔ 不是「一个按钮都没有」—— 那会把澄清 UI 整个弄没。"""
    spec = _spec(clarification_question=TIME_CLARIFICATION_QUESTION)
    got = _clarification_followups(spec, "")
    assert [b["question"] for b in got] == list(_SWITCHABLE_WINDOWS)


def test_llm_authored_options_are_untouched():
    """⛔ 不动第三支：自撰选项是「先看营收」这种任意短语，
    不存在自足性概念，前置它没有意义。"""
    spec = _spec(clarification_options=["先看营收", "先看毛利"])
    got = _clarification_followups(spec, SEED)
    assert [b["question"] for b in got] == ["先看营收", "先看毛利"]


def test_the_window_list_has_exactly_one_definition():
    """形态 D：`_SWITCHABLE_WINDOWS` 与澄清按钮的窗口清单原本是**同一份字面量抄两遍**
    （`:1006` 的注释自己写着「两处一漂」，而没有闸钉住）。

    抽成一份之后这条断言由构造成立 —— 它守的是**别再抄回去**。
    """
    import ast
    import inspect

    import smartbi.gold.restaurant.restaurant_intent_service as svc

    src = inspect.getsource(svc._clarification_followups)
    tree = ast.parse(inspect.cleandoc(src))
    literals = {n.value for n in ast.walk(tree)
                if isinstance(n, ast.Constant) and isinstance(n.value, str)}
    assert not (set(_SWITCHABLE_WINDOWS) & literals), (
        f"窗口词又被抄进函数体了: {set(_SWITCHABLE_WINDOWS) & literals}")
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd backend/python && python -m pytest tests/test_clarification_buttons_compose_full_question.py -q 2>&1 | tail -8
```

预期：新增 6 条中至少 5 条失败（`TypeError: _clarification_followups() takes 1 positional argument but 2 were given`）。

- [ ] **Step 3: 写实现**

把 `:1233` 的函数改成：

```python
def _clarification_followups(
    spec: RestaurantQuerySpec, seed: str = "",
) -> List[Dict[str, str]]:
    """Render LLM-selected choices after they passed factual allowlists.

    `question` 是**合成的完整问句**, `label` 仍是短词。
    ⚠️ 两者曾经相同 —— 那让「本月」成了全系统最高频的计划缓存键,
    而它的含义完全取决于上一轮(生产实测串话题)。见
    `_compose_clarification_question`。
    """
    if spec.clarification_question == TIME_CLARIFICATION_QUESTION:
        return [
            {"label": window,
             "question": _compose_clarification_question(window, seed, kind="time")}
            for window in _SWITCHABLE_WINDOWS       # ⛔ 不再抄一份字面量
        ]
    if spec.clarification_question == STORE_SCOPE_CLARIFICATION_QUESTION:
        names = tuple(spec.store_options)
        scopes = ["全部门店", *list(spec.store_options[:3])]
        return [
            {"label": scope[:12],
             "question": _compose_clarification_question(
                 scope, seed, kind="store", store_names=names)}
            for scope in scopes
        ]
    if spec.clarification_options:
        # ⛔ 自撰选项不合成 —— 「先看营收」这种短语没有自足性可言。
        return [
            {"label": option[:12], "question": option}
            for option in spec.clarification_options[:6]
        ]
    return []
```

⚠️ 门店那支原来 `label` 是 `name[:12]` 而「全部门店」没有截断 —— 上面统一成
`scope[:12]`，对「全部门店」（4 字）无影响，行为不变。

改调用点 `:1668`：

```python
            followups = _clarification_followups(spec, query)
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd backend/python && python -m pytest tests/test_clarification_buttons_compose_full_question.py -q 2>&1 | tail -5
```

预期：`24 passed`。

- [ ] **Step 5: 变异对照**

| # | 变异 | 预期红 |
|---|---|---|
| M6 | 调用点改回 `_clarification_followups(spec)` | 走真实入口那条（Task 3）；⚠️ 本 task 的断言**抓不住**它 —— 它们直接调函数 |
| M7 | 时间那支改回 `"question": window` | `test_no_bare_window_word_is_ever_sent_as_a_question` |
| M8 | 时间那支的 `_SWITCHABLE_WINDOWS` 改回字面量元组 | `test_the_window_list_has_exactly_one_definition` |
| M9 | 门店那支不合成 | `test_store_clarification_buttons_carry_the_whole_question` |

🔴 **M6 抓不住这一点必须记下来** —— 它正是本仓形态 B（机制在、没接上）。
Task 3 存在的唯一理由就是抓它。

- [ ] **Step 6: 提交**

```bash
cd /c/Users/Steve/cretas-lwb
./scripts/safe-commit.sh "feat(rest-ai): 澄清按钮发完整问句 + 消除窗口清单副本

_clarification_followups 增加 seed 形参, question 改为合成的完整问句,
label 保持短词(按钮显示不变)。调用点传 tiered_answer 的 query。

顺带: 时间那支改为引用既有的 _SWITCHABLE_WINDOWS —— 那份字面量原本抄了
两遍, :1006 的注释自己写着「两处一漂」而没有闸钉住(形态 D, 能抽成一份
就别建闸)。AST 断言守住别再抄回去。

⛔ 不动 clarification_options(LLM 自撰选项): 任意短语不存在自足性概念。" \
  backend/python/smartbi/gold/restaurant/restaurant_intent_service.py \
  backend/python/tests/test_clarification_buttons_compose_full_question.py
```

---

## Task 3: 真实入口端到端 + 全量回归

**Files:**
- Test: `backend/python/tests/test_clarification_buttons_compose_full_question.py`（追加）

**Interfaces:**
- Consumes: `tiered_answer(query, pool, factory_id, role, *, history=None, precomputed_spec=None, ...) -> Dict[str, Any]`；澄清返回值里的 `suggested_followups: List[Dict[str, str]]`
- Produces: 无（终点 task）

⚠️ **这个 task 是唯一能抓住 M6（调用点没传 seed）的东西**。Task 2 的断言直接调
`_clarification_followups`，绕过了「谁调它」—— 本仓形态 B 的定义。

- [ ] **Step 1: 写失败的测试**

追加：

```python
# ══════════════════════════════════════════════════════════════════
# 产品真实入口：tiered_answer 的澄清返回值
# ══════════════════════════════════════════════════════════════════
import asyncio                                                    # noqa: E402

from smartbi.gold.restaurant.restaurant_intent_service import tiered_answer  # noqa: E402


def _clarify_spec(seed_question):
    """一个**真的**触发时间澄清的 spec，通过 `precomputed_spec` 注入
    ⇒ ⛔ 不调 LLM、⛔ 不连库，但走的是产品那条真实代码路径。"""
    return RestaurantQuerySpec(
        intent="RESTAURANT_OPS_SALES_SUMMARY",
        clarification_needed=True,
        clarification_question=TIME_CLARIFICATION_QUESTION,
    )


def test_real_entry_point_sends_full_questions():
    """🔴 承重：走 `tiered_answer`，⛔ 不直接调 `_clarification_followups`。

    Task 2 的断言在「调用点忘了传 seed」时**全绿** —— 那是本仓形态 B
    （机制在、没接上）。这一条是唯一抓得住的。
    """
    result = asyncio.run(tiered_answer(
        SEED, None, "MOCK_REST", "factory_super_admin",
        precomputed_spec=_clarify_spec(SEED),
    ))
    buttons = result.get("suggested_followups")
    assert buttons, f"一个按钮都没有 —— 下面的断言会恒真: {result}"
    for b in buttons:
        assert b["question"].endswith(SEED), b
        assert b["question"] not in _SWITCHABLE_WINDOWS, b
```

⚠️ **写这条测试时先跑一次确认 `precomputed_spec` 这条路真的到得了澄清返回**
（⛔ 不从代码推）。若 `tiered_answer` 在澄清之前就需要 `pool`，
改成传一个最小假 pool，并在测试 docstring 里写明桩掉了什么。

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd backend/python && python -m pytest tests/test_clarification_buttons_compose_full_question.py::test_real_entry_point_sends_full_questions -q 2>&1 | tail -20
```

预期：**先看它是不是因为「路径没走通」而失败**（`AttributeError` / `NoneType`），
那不算有效失败 —— 那说明夹具没搭对，先修夹具。
有效失败长这样：拿到了 `suggested_followups`，但 `question` 是光秃秃的词。

⇒ 做法：先在 Task 2 的实现**已经生效**的树上跑一次，看它**绿**；
再把调用点改回 `_clarification_followups(spec)`（M6），看它**红**。
两次都对，这条断言才算接上了。

- [ ] **Step 3: 补齐夹具直到路径走通**

（若 Step 2 显示路径不通才做；通了就跳过。）
按实际报错补最小假 pool / role，⛔ 不 mock `_clarification_followups` 本身
—— mock 掉它这条断言就恒真了。

- [ ] **Step 4: M6 变异对照**

```bash
cd backend/python
# 变异：调用点改回不传 seed
python - <<'PY'
import io
p = "smartbi/gold/restaurant/restaurant_intent_service.py"
s = io.open(p, encoding="utf-8", newline="").read()
assert s.count("_clarification_followups(spec, query)") == 1
io.open(p, "w", encoding="utf-8", newline="").write(
    s.replace("_clarification_followups(spec, query)", "_clarification_followups(spec)"))
print("M6 已注入")
PY
python -m pytest tests/test_clarification_buttons_compose_full_question.py -q 2>&1 | tail -4
git checkout -- smartbi/gold/restaurant/restaurant_intent_service.py
```

⚠️ 硬约束 5：文本模式 + `newline=''`，⛔ 不用 `bytes` 字面量、⛔ 不用
`pathlib.write_text`（Windows 会把整文件转 CRLF）。
预期：**恰好** `test_real_entry_point_sends_full_questions` 红，其余绿。
若一条都不红 ⇒ 先证明变异真的到达了（`grep -c "_clarification_followups(spec)"`），
再下「断言没用」的结论（形态 C″）。

- [ ] **Step 5: 全量回归（对当场重跑的 baseline）**

```bash
cd backend/python
python -m pytest tests/ smartbi/api/tests/ -q --continue-on-collection-errors \
  > /tmp/after.log 2>&1; echo "EXIT=$?"
grep -cE "^FAILED |^ERROR [a-z]" /tmp/after.log
tail -3 /tmp/after.log
```

⚠️ baseline **当场重跑**，⛔ 不引用记忆里的数：

```bash
git worktree add -q --detach ../cretas-baseline origin/main
cd ../cretas-baseline/backend/python && python -m pytest tests/ smartbi/api/tests/ -q \
  --continue-on-collection-errors > /tmp/base.log 2>&1; echo "EXIT=$?"
grep -E "^FAILED |^ERROR [a-z]" /tmp/base.log | sort > /tmp/base.txt
grep -E "^FAILED |^ERROR [a-z]" /tmp/after.log | sort > /tmp/after.txt
comm -13 /tmp/base.txt /tmp/after.txt | tee /tmp/new.txt
echo -n "新增失败条数: "; wc -l < /tmp/new.txt     # ⛔ 不无条件打「(空=没问题)」
cd - && git worktree remove ../cretas-baseline
```

⚠️ 两个全量**串行跑**，⛔ 不并发（互抢 CPU 会让 baseline 撞超时被杀，读数作废）。
判据：**新增失败条数 == 0**。

- [ ] **Step 6: 提交**

```bash
cd /c/Users/Steve/cretas-lwb
./scripts/safe-commit.sh "test(rest-ai): 澄清按钮走产品真实入口的断言

Task 2 的断言直接调 _clarification_followups, 绕过了「谁调它」——
调用点忘传 seed 时它们全绿(形态 B: 机制在、没接上)。
这一条走 tiered_answer, 是唯一抓得住的。

M6 变异对照: 调用点改回 _clarification_followups(spec) ==> 恰好这一条红。" \
  backend/python/tests/test_clarification_buttons_compose_full_question.py
```

---

## Task 4: 上线与验证

**Files:** 无代码改动

- [ ] **Step 1: 开 PR**

```bash
cd /c/Users/Steve/cretas-lwb
git diff origin/main...HEAD --stat        # 确认 scope 干净, 只有 2 个文件 + 2 个 doc
git push -u origin codex/claude-time-phrase-corpus
gh pr create --title "feat(rest-ai): 澄清按钮发完整问句 —— 根治计划缓存串话题" \
             --body-file /tmp/pr-body.md
```

⚠️ PR body 用 `--body-file`，⛔ 不用 `--body "..."` —— 反引号会被 shell 当命令替换
（本轮实测踩过）。

- [ ] **Step 2: 逐 job 看 CI**

```bash
gh pr checks <PR#>
gh run view <id> --json jobs --jq '.jobs[] | .name+" -> "+(.conclusion//.status)'
```

⛔ **不看汇总行**。⚠️ `failure` 先查是不是 `cancelled`（并发取消会被显示成失败）。
⚠️ `gh` 的时间戳是 UTC，本机 CST(+8)，⛔ 不直接相减。

- [ ] **Step 3: 合并后重开分支（硬约束 7）**

```bash
gh pr merge <PR#> --squash
git fetch origin main
# ⛔ 不在旧分支上继续 —— squash 让它不再是 main 的祖先
git worktree add -b codex/claude-time-phrase-corpus-2 ../cretas-tpc-2 origin/main
```

- [ ] **Step 4: 部署前量「之前」**

```bash
ssh root@47.100.235.168 "
  PID=\$(ss -lntp | grep :8083 | grep -oP 'pid=\K[0-9]+' | head -1)
  echo PID=\$PID; ps -o lstart= -p \$PID
  md5sum /www/wwwroot/cretas/code/backend/python/smartbi/gold/restaurant/restaurant_intent_service.py
"
```

- [ ] **Step 5: 部署**

```bash
cd <main worktree>
./scripts/deploy/deploy-smartbi-python.sh --env prod > /tmp/deploy.log 2>&1; echo "EXIT=$?"
wc -l /tmp/deploy.log; grep -nE "ERROR|FAIL|拒绝" /tmp/deploy.log
```

⛔ **不用 `release-cretas.sh`** —— 它只发 Java + web-admin，纯 Python 改动会得到
`DEPLOY_EXIT=0` 而东西根本没上线。
⛔ **不用 `| tail -N`** —— 管道会替换退出码，失败被读成成功（硬约束 1）。

- [ ] **Step 6: 部署后核对三样（⛔ `DEPLOY_EXIT=0` 不是判据）**

```bash
# ① 文件 md5 与本地一致
md5sum backend/python/smartbi/gold/restaurant/restaurant_intent_service.py
ssh root@47.100.235.168 "md5sum /www/wwwroot/cretas/code/backend/python/smartbi/gold/restaurant/restaurant_intent_service.py"
# ② 进程重启晚于制品
ssh root@47.100.235.168 "
  PID=\$(ss -lntp | grep :8083 | grep -oP 'pid=\K[0-9]+' | head -1)
  ps -o lstart= -p \$PID
  stat -c '%y' /www/wwwroot/cretas/code/backend/python/smartbi/gold/restaurant/restaurant_intent_service.py
"
# ③ 🔴 真跑一次那条路径 —— 找到标记 ≠ 它能跑
#    问一句缺时间的话, 看返回的 suggested_followups
curl -s -X POST http://47.100.235.168:8083/api/smartbi/... \
  -d '{"query":"米饭的销量是多少", ...}' > /tmp/probe.json 2>&1
python -c "
import json; d=json.load(open('/tmp/probe.json'))
fs=d.get('suggested_followups') or []
print('按钮数:', len(fs))
for b in fs: print('  ', b['label'], '->', b['question'])
assert fs, '一个按钮都没有 —— 下面的判据恒真'
assert all(b['question'] not in ('本月','上个月','最近7天','最近30天') for b in fs), '仍在发光秃秃的词'
print('✅ 按钮发的是完整问句')
"
```

⚠️ ③ 的具体 endpoint / payload **照生产日志里那条真实请求抄**，
⛔ 不手拼（本仓实测：手拼 payload + 探针用 `fetch` 而应用用 XHR，量的是别的东西）。

- [ ] **Step 7: 台账**

在 `docs/dispatch/ACTIVE.md` 记：PR 号、部署 commit、③ 的实测输出。
写给明天的自己：**哪一条读数证明它真的生效了**。

---

## 自查（写完计划后做的，⛔ 非计划步骤）

**1. 设计卡覆盖**：件 1 的每一条都有 task —— 合成（T1）· 接线（T2）· 真实入口（T3）·
上线（T4）· 消除窗口副本（T2）· ⛔ 不动自撰选项（T2 有断言钉住）。
⚠️ 写计划时发现设计卡**两处错**，已回改设计卡：准入 2 必须按种类分；
「自撰选项按同一条准入处理」那句话没有内容。

**2. 占位符**：Task 3 Step 1 的 endpoint / payload 与 Task 4 Step 6 的 `curl`
是**有意留给执行者按生产日志抄的**，且写明了「⛔ 不手拼」的理由 —— 这是判据不是占位符。
其余每一步都有可运行的代码。

**3. 类型一致**：`_compose_clarification_question(prefix, seed, *, kind, store_names)`
在 T1 定义、T2 调用，签名一致；`_clarification_followups(spec, seed="")`
在 T2 定义、T2 调用点与 T3 断言使用，一致；`_SWITCHABLE_WINDOWS: Tuple[str, ...]`
是既有符号（`:1006`），T2/T3 都从 `restaurant_intent_service` 导入。
