# 报工体系统一 — Phase D RN 实施计划（operator 逐道工序双量报工）

**日期**: 2026-06-01
**分支**: `feat/yield-rn-phase-d`（off main，已含 Phase A 后端 PR #350 LIVE prod）
**Spec**: `docs/superpowers/specs/2026-06-01-报工体系统一-phase-d-rn-design.md`（设计决策已定，本文为 no-placeholder 实施计划）
**REQUIRED SUB-SKILL**: `superpowers:executing-plans`（每 Task 完成走 review checkpoint）。Playwright 验证走 `.claude/rules/playwright-headed-mode.md`（强制 headed）。

---

## Goal

给产线工人（operator）一个"一道工序一张全屏卡片、填投入量+产出量双量、下道投入预填上道产出工人确认/改"的逐道报工 RN 界面，接已 LIVE 的 Phase A 出成率后端 5 端点。新建 1 个 API service + 1 个数量输入组件 + 3 个屏 + 导航注册 + 1 个后端 DTO 小改（Task 0 前置，A7 banner 依赖）。

## Architecture

- **后端（Task 0 前置）**: `WorkProcessTaskDTO` 加 `standardYieldMin`/`standardYieldMax` 两字段，从已 join 的 `WorkProcess` 定义透出（让卡片提交前预先显标准出成率区间）。Java，走 `deploy-backend.sh`。
- **前端**: 纯 RN（Expo），不走 jar 部署。
  - `yieldReportApi.ts`：照 `apiClient`/`processTaskApiClient` 模式，封装 Phase A 5 端点 + `listWorkProcessTasks`，定义 TS 类型 1:1 mirror Phase A DTO。
  - `YieldQuantityInput.tsx`：新数量输入组件（±stepper + 软上限 max + 小数 + 大字防呆 + 预填标注 slot）。
  - `YieldBatchSelectScreen.tsx` → `YieldStepReportScreen.tsx`：选批次 → 一道一卡片逐道报工 → 内部切完成态。
  - `OperatorNavigator.tsx`：注册 2 新屏 + `ScanReport` 顶部加"逐道工序报工"入口按钮。
- **状态管理**: 全部组件本地 `useState`，route params 传 batchId，不新建 zustand store（spec §3.6）。

## Tech Stack

Expo 53+ / TypeScript（严格，禁 `as any`）/ React Navigation 7 native-stack / react-native-paper（NeoButton 底层）/ axios（apiClient 已封装）/ Playwright headed（Expo Web 验证）。后端 Java 21 + Spring Boot 3.2 + Lombok。

---

## 关键事实（探查结果，全部直接核对真实代码 file:line，写死供实施引用）

> ⚠️ 以下标 **[纠正]** 的几条与 spec 文字有出入，已核对真实代码，实施按本节为准。

### F1 — apiClient 模式（`frontend/CretasFoodTrace/src/services/api/apiClient.ts`）
- 响应拦截器 `apiClient.ts:51-55` 统一 `return response.data`（解包 axios 一层）。所以 `apiClient.get<T>()` 返回的 `T` = 后端整个信封 `{success, code, message, data}`。
- JWT 注入：请求拦截器 `apiClient.ts:38-40` 从 `StorageService.getSecureItem('secure_access_token')` 取，设 `Authorization: Bearer`。
- 401 自动刷新：`apiClient.ts:60-86`。
- 方法签名 `apiClient.ts:135-158`：`get<T>(url, config?)` / `post<T>(url, data?, config?)` / `put<T>(url, data?, config?)` / `delete<T>` / `patch<T>`，全 `async` 返回 `Promise<T>`。
- 老 client 模式参照 `processTaskApiClient.ts:100-104`：`class X { private getBase(factoryId?) { const fid = factoryId || requireFactoryId(); return \`/api/mobile/${fid}\`; } ... } export const x = new X();`。`requireFactoryId` 从 `utils/factoryIdHelper.ts:312-318` import（REQUIRED 策略，工厂用户自动取，平台用户/未登录抛错）。

### F2 — work-process-task 列表 API（`backend/.../controller/WorkProcessTaskController.java`）
- 端点 `WorkProcessTaskController.java:93-99`：`GET /api/mobile/{factoryId}/production/batches/{batchId}/work-process-tasks` → `ApiResponse<List<WorkProcessTaskDTO>>`，`service.listByBatch` 按 `processOrder` 升序（`WorkProcessTaskServiceImpl.java:149-156` 调 `findByFactoryIdAndProductionBatchIdOrderByProcessOrderAsc`）。
- `WorkProcessTaskDTO`（`backend/.../dto/WorkProcessTaskDTO.java:23-71`，**注意包是 `com.cretas.aims.dto` 不是 `dto.response`**）字段：`id(Long)` / `factoryId(String)` / `productionBatchId(Long)` / `productWorkProcessId(Long)` / `workProcessId(String)` / `productTypeId(String)` / `processOrder(Integer)` / `status(enum PENDING/IN_PROGRESS/COMPLETED/SKIPPED/CANCELLED)` / `plannedQuantity(BigDecimal)` / `plannedUnit(String)` / `plannedStartAt` / `plannedEndAt` / `estimatedMinutes` / `actualQuantity(BigDecimal)` / `actualStartAt` / `actualEndAt` / `actualMinutes` / `assignedTo(Long)` / `completedBy` / `completedAt` / `notes` / `processName(String, join 提供, 选填)` / `processCategory` / `createdAt` / `updatedAt`。**无** `batchNumber` / `productName` / `productSpec` / `inputQuantity` / `standardYieldMin/Max`（Task 0 加后两个）。
- **[纠正]** spec §3.1.1 说"复用 `types/workProcess.ts:19-45` 的 `WorkProcessTask`"——**真实 `types/workProcess.ts` 没有 `WorkProcessTask` interface**（只有 `WorkProcess` line 34 + `ProductWorkProcess` line 80 + `CreateWorkProcessRequest` 等）。→ **本计划在 `yieldReportApi.ts` 内新定义 `WorkProcessTask` TS 类型并 export**（不 import workProcess.ts，避免引用不存在的类型）。
- **[纠正]** RN 现有 `processTaskApiClient.ts` 的 `ProcessTaskItem`（line 6-28）是**老 `ProcessTask` 实体**（路径 `/process-tasks`），**不是** Phase A 的 `WorkProcessTask`（路径 `/work-process-tasks`）。两者是不同实体不同端点，**不可复用** ProcessTaskItem。

### F3 — OperatorNavigator 报工 Tab（`frontend/.../navigation/OperatorNavigator.tsx`）
- `OperatorReportStackNavigator()` 在 `OperatorNavigator.tsx:21-29`，是 `createNativeStackNavigator<any>()`（`OperatorNavigator.tsx:15`）。当前 3 屏：`OperatorNavigator.tsx:24` `ScanReport`（首屏）/ `:25` `ScanReportSuccess` / `:26` `DraftReports`。
- import 区在 `OperatorNavigator.tsx:10-12`（`ScanReportScreen` / `ScanReportSuccessScreen` / `DraftReportsScreen`）。
- 报工 Tab 在 `OperatorNavigator.tsx:69-78`（`name="OperatorReportTab" component={OperatorReportStackNavigator}`，title `报工`）。
- 新屏注册位置：`OperatorNavigator.tsx:26` 后（`DraftReports` 之后）加两行 `<ReportStack.Screen>`。入口按钮加在 `ScanReportScreen` 顶部（保留 `ScanReport` 首屏，spec Q4 默认）。

### F4 — operator 角色判断（`frontend/.../store/authStore.ts` + `AppNavigator.tsx`）
- `getUserRole()` = `authStore.ts:94-109`：工厂用户取 `user.factoryUser.role`（`authStore.ts:104-106`）。
- `AppNavigator.tsx:113-116`：`if (userRole === "operator") return <OperatorNavigator />;`。→ operator 的 role 值 = 字符串 `"operator"`。入口天然由 navigator 隔离（只 operator 见）。
- factoryId 不存本地，API service 内 `requireFactoryId()` 自动从 `authStore` 取（`factoryIdHelper.ts:312-318`）。屏内若要拿 user 做业态门控，用 `useAuthStore.getState().user` 或 `useAuthStore(s => s.user)`。

### F5 — 数量输入组件 + 复用组件 prop（确切）
- **无现成数量 stepper 组件**。`ScanReportScreen.tsx:194-203` 用裸 `TextInput keyboardType="numeric"/"decimal-pad"` + `parseInt`/`parseFloat`，无 ±stepper / 无 max 防呆。→ **新建** `YieldQuantityInput.tsx`。
- **[纠正]** `NeoButton`（`components/ui/NeoButton.tsx`）继承 react-native-paper `ButtonProps`（`NeoButton.tsx:6` `extends Omit<ButtonProps,'theme'>`），props = `variant('primary'|'secondary'|'outline'|'ghost'|'danger')` / `size('small'|'medium'|'large')` / `style` / `labelStyle` + 透传 paper props（`onPress` / `disabled` / `loading` / `icon`）。**按钮文字是 `children`**（`NeoButton.tsx:67,97` `{children}`），**不是 `title` prop**（spec 写 `title` 是错的）。`size="large"` = height 56（`NeoButton.tsx:51`）。**无 `fullWidth` prop**（spec 写 `fullWidth` 是错的）；满宽用 `style={{ width: '100%' }}` 或父容器 `alignSelf:'stretch'`。
- `NeoCard`（`components/ui/NeoCard.tsx:5-11`）：props `children` / `style` / `variant('elevated'|'outlined'|'flat')` / `padding(keyof theme.custom.spacing)` / `onPress`。
- `ScreenWrapper`（`components/ui/ScreenWrapper.tsx:6-12`）：props `children` / `style` / `edges` / `backgroundColor`。**注意 `ScreenWrapper.tsx:47` content `maxWidth:500` 居中**——全屏卡片在宽屏会被约束到 500px 居中（手机端无影响，符合"一道一卡片"）。
- **[纠正]** `EmptyStateCard`（`components/common/EmptyStateCard.tsx`）是 **default export**（`EmptyStateCard.tsx:20` `export default function`），props（`EmptyStateCard.tsx:11-18`）= `icon?(MaterialCommunityIcons name)` / `title(必填)` / `description?` / `actionLabel?` / `onAction?` / `compact?`。**是 `description` 不是 `message`**（spec §3.3/§3.7 写 `message` 是错的）；**是 `actionLabel` 不是 `actionText`**（spec §5 截图验证点写 `actionText` 是错的，§3.7 已自纠为 `actionLabel`）。
- **无现成 ProgressBar**（`报工 3/7 ●●●○○○○`）→ 屏内自绘（`View` row + 圆点 `View` map + `Text`），不新建组件。
- 扫码 modal `BarcodeScannerModal`（`components/processing/BarcodeScannerModal.tsx`，`ScanReportScreen.tsx:12,253-257` 在用，props `visible`/`onClose`/`onScan`）。选批次屏可选复用。

### F6 — 批次品名来源（`frontend/.../services/api/processingApiClient.ts`）
- `processingApiClient.getBatchById(batchId: string, factoryId?)` → `Promise<ApiResponse<ProcessingBatch>>`（`processingApiClient.ts:174-176`，**batchId 参数是 string**）。
- `ProcessingBatch`（`processingApiClient.ts:62-90`）：品名字段 = **`productType`**（`processingApiClient.ts:65`，string，如"卤猪蹄"），**不是 `productName`**；**无 `productSpec`（规格）字段**。其余可用：`batchNumber`（line 64）/ `status`（66）/ `targetQuantity`（67）/ `actualQuantity`（68）。
- 批次列表 `processingApiClient.getBatches({ status?, page?, size? })` → `Promise<ApiResponse<PagedResponse<ProcessingBatch>>>`（`processingApiClient.ts:163-166`，分页 `content/totalElements/...`）。
- `processingApiClient.scanBatchByCode(code: string)` → `Promise<ApiResponse<ProcessingBatch>>`（`processingApiClient.ts:420-422`）扫码取批次。
- **结论**: 卡片头 context = `getBatchById`（品名 `productType` + 批次号 `batchNumber`）+ `listWorkProcessTasks`（工序名 `processName` + 计划量 `plannedQuantity`/`plannedUnit`）+ `getYield.steps[i-1].totalOutput`（下道预填）。规格省略（无字段，spec Q3 默认）。

### F7 — Task 0 后端小改（A7 区间透出，`WorkProcessTaskDTO` + 组装处）
- `WorkProcess` 实体（`backend/.../entity/WorkProcess.java:53-59`）已有 `standardYieldMin`（`standard_yield_min` precision 6 scale 4）+ `standardYieldMax`（`standard_yield_max`），Phase A V20260901_02 加的列。
- 组装处 `WorkProcessTaskServiceImpl.toDTO(WorkProcessTask task, WorkProcess definition)`（`WorkProcessTaskServiceImpl.java:337-368`）：`definition`（= 该 task 的 `WorkProcess`）**已作为参数传入**，line 363-366 已 `if (definition != null) { dto.setProcessName(...); dto.setProcessCategory(...); }`。→ **只需在该 if 块内加两行 setter**，无新 query。`listByBatch`（`:149-156`）/ `list`（`:131-146`）/ `getById`（`:158-166`）全走 `toDTO(task, def)`，定义已由 `loadDefinitionsForTasks`（`:295-304`）/ `lookupDefinition`（`:291-293`）加载 → 三条路径自动带上新字段。
- DTO 字段加在 `WorkProcessTaskDTO.java:65`（`processCategory` 后，时间字段前）。Lombok `@Data`+`@Builder`+`@AllArgsConstructor`（`WorkProcessTaskDTO.java:19-22`）→ 加字段会改 `@AllArgsConstructor` 形参顺序，但本类 `@Builder` 用 builder 链不用全参构造，影响小；现有 caller 全用 builder（`toDTO` `:338`）→ 安全。

---

## 文件结构（新建 / 改）

### 新建
| 文件 | 职责 |
|---|---|
| `frontend/CretasFoodTrace/src/services/api/yieldReportApi.ts` | Phase A 5 端点封装 + `listWorkProcessTasks` + 全部 TS 类型（`BatchYieldDTO`/`StepYieldDTO`/`YieldReportRequest`/`MaterialInputRequest`/`WorkProcessTask`/结果类型） |
| `frontend/CretasFoodTrace/src/components/processing/YieldQuantityInput.tsx` | 大字数量输入（−/+ stepper + 软上限 max + 小数 + 预填标注 slot） |
| `frontend/CretasFoodTrace/src/screens/processing/YieldBatchSelectScreen.tsx` | 选批次屏（IN_PROGRESS 批次列表 + 扫码入口 + 空态） |
| `frontend/CretasFoodTrace/src/screens/processing/YieldStepReportScreen.tsx` | 一道一卡片逐道报工屏 + 内部完成态切换 |

### 改
| 文件 | 改动 |
|---|---|
| `backend/.../dto/WorkProcessTaskDTO.java` | 加 `standardYieldMin`/`standardYieldMax` 两字段（Task 0） |
| `backend/.../service/workprocess/impl/WorkProcessTaskServiceImpl.java` | `toDTO` if(definition!=null) 块内加两 setter（Task 0） |
| `frontend/CretasFoodTrace/src/screens/processing/ScanReportScreen.tsx` | 顶部加"逐道工序报工"入口按钮（Task 7） |
| `frontend/CretasFoodTrace/src/navigation/OperatorNavigator.tsx` | import + 注册 2 新屏（Task 7） |

### Task 0 后端测试
| 文件 | 改动 |
|---|---|
| `backend/.../src/test/java/com/cretas/aims/service/workprocess/...WorkProcessTaskServiceImplTest.java`（若存在，否则新建最小测试） | 验 `toDTO` 透出 standardYieldMin/Max |

---

## Task 0（后端前置）— WorkProcessTaskDTO 透出 standardYieldMin/Max

> RN A7 banner（提交前显标准区间）依赖此字段。先做、先部署、先 PG 验证。Java 改动，走 `deploy-backend.sh`。

**Files**: `backend/.../dto/WorkProcessTaskDTO.java`、`backend/.../service/workprocess/impl/WorkProcessTaskServiceImpl.java`、对应测试。

**Step 0.1 — DTO 加两字段**
在 `WorkProcessTaskDTO.java:65`（`private String processCategory;` 之后）插入：
```java
    /** 标准出成率下限 (张权 A7; 来自该工序 WorkProcess, join 透出; null=不校验). */
    private java.math.BigDecimal standardYieldMin;

    /** 标准出成率上限 (张权 A7; 支持 >1 如滚揉保水 1.35; null=不校验). */
    private java.math.BigDecimal standardYieldMax;
```
（文件顶部已 `import java.math.BigDecimal;`（`WorkProcessTaskDTO.java:13`）→ 可直接写 `BigDecimal`；为防 import 漏，用全限定名 `java.math.BigDecimal` 最稳，或直接 `BigDecimal`。实施时用短名 `BigDecimal`，import 已在。）

最终（短名版，import 已在 line 13）：
```java
    /** 关联展示字段 (后端 join 提供, 选填). */
    private String processName;
    private String processCategory;

    /** 标准出成率下限 (张权 A7; 来自该工序 WorkProcess, join 透出; null=不校验). */
    private BigDecimal standardYieldMin;

    /** 标准出成率上限 (张权 A7; 支持 >1 如滚揉保水 1.35; null=不校验). */
    private BigDecimal standardYieldMax;
```

**Step 0.2 — toDTO 透出**
在 `WorkProcessTaskServiceImpl.java:363-366` 的 `if (definition != null) { ... }` 块内（`dto.setProcessCategory(...)` 之后）加：
```java
        if (definition != null) {
            dto.setProcessName(definition.getProcessName());
            dto.setProcessCategory(definition.getProcessCategory());
            dto.setStandardYieldMin(definition.getStandardYieldMin());
            dto.setStandardYieldMax(definition.getStandardYieldMax());
        }
```
（`definition.getStandardYieldMin()`/`getStandardYieldMax()` Lombok `@Data` 已生成，见 `WorkProcess.java:53-59`。）

**Step 0.3 — 单元测试**
先查测试是否存在：
```bash
ls /c/Users/Steve/cretas-yield-rn/backend/java/cretas-api/src/test/java/com/cretas/aims/service/workprocess/ 2>/dev/null
```
若有 `WorkProcessTaskServiceImplTest.java`，加一个 case：mock 一个 `WorkProcess` 设 `standardYieldMin=0.85`、`standardYieldMax=0.95`，断言 `toDTO`/`listByBatch` 返回的 DTO `getStandardYieldMin()` == 0.85、`getStandardYieldMax()` == 0.95。若无测试文件，新建最小 test 类只测 `toDTO`（`toDTO` 是 private → 通过 public `getById`/`listByBatch` 间接验，mock repository 返回单 task + 单 WorkProcess）。

**验证命令**:
```bash
cd /c/Users/Steve/cretas-yield-rn/backend/java/cretas-api
export PATH=$PATH:/c/tools/apache-maven-3.9.6/bin   # mvn 不在默认 PATH
mvn -q -Dtest=WorkProcessTaskServiceImplTest test   # 测试绿
mvn -q clean package -DskipTests                     # 编译通过
```

**部署 + PG 真实验证**（部到 test 10011 先验，再 prod；per server-operations.md）:
```bash
cd /c/Users/Steve/cretas-yield-rn
./scripts/deploy/deploy-backend.sh --env test
# 拿一个有工序任务的批次（test 库 cretas_db），调端点核对字段出现：
# 注: 端口 10011 仅 nginx 网关可达, 本地用 SSH tunnel:  ssh -L 10011:localhost:10011 root@47.100.235.168
curl -s -H "Authorization: Bearer <token>" \
  "http://localhost:10011/api/mobile/<factoryId>/production/batches/<batchId>/work-process-tasks" \
  | python -c "import sys,json; d=json.load(sys.stdin); print([{'processName':t.get('processName'),'standardYieldMin':t.get('standardYieldMin'),'standardYieldMax':t.get('standardYieldMax')} for t in d['data']])"
# 期望: 响应每个 task 含 standardYieldMin / standardYieldMax key (配了区间的工序非 null, 未配的 null)
```
> 若该工序未配 `standard_yield_min/max`，字段值 null（Lombok `@Data` 无 `@JsonInclude(NON_NULL)` → Jackson emit `"standardYieldMin": null`，前端 `number | null` 容忍）。验证点是 **key 出现**，不是非 null。

**commit**: `feat(yield): WorkProcessTaskDTO 透出 standardYieldMin/Max (A7 区间, Phase D RN 依赖)`（后端单独 commit，先合 main 再部 prod per worktree-and-main-only-deploy.md）。

---

## Task 1 — `yieldReportApi.ts`（5 方法 + listWorkProcessTasks + TS 类型）

**Files**: 新建 `frontend/CretasFoodTrace/src/services/api/yieldReportApi.ts`。

**Step 1.1 — 完整文件**
```ts
import { apiClient } from './apiClient';
import { requireFactoryId } from '../../utils/factoryIdHelper';

// ============ 后端统一响应信封 (拦截器已解包 axios 一层, 见 apiClient.ts:51-55) ============
export interface ApiResponse<T> {
  success: boolean;
  code: number;
  message: string;
  data: T;
}

// ============ 工序任务状态 (mirror WorkProcessTask.Status) ============
export type WorkProcessTaskStatus =
  | 'PENDING'
  | 'IN_PROGRESS'
  | 'COMPLETED'
  | 'SKIPPED'
  | 'CANCELLED';

// ============ WorkProcessTaskDTO (mirror backend dto/WorkProcessTaskDTO.java:23-71 + Task 0 两字段) ============
// 注: 后端 types/workProcess.ts 无此 interface (只有 WorkProcess/ProductWorkProcess), 故在此定义.
export interface WorkProcessTask {
  id: number;                       // = workProcessTaskId (报工 req 用这个)
  factoryId: string;
  productionBatchId: number;        // 注意: 不是 batchId
  productWorkProcessId: number;
  workProcessId: string;            // String (非 number)
  productTypeId: string;
  processOrder: number;             // 第几道 (升序)
  status: WorkProcessTaskStatus;
  plannedQuantity: number | null;   // BigDecimal → number; 计划数量 (Rule 1 max + Rule 2)
  plannedUnit: string | null;       // 计划单位 (非 unit)
  estimatedMinutes?: number | null;
  actualQuantity?: number | null;   // 累计产出 (Phase A 双写)
  assignedTo?: number | null;
  completedBy?: number | null;
  notes?: string | null;
  processName?: string | null;      // 工序名 (焯水/卤制..., join 提供, 选填) — 卡片头用
  processCategory?: string | null;
  standardYieldMin?: number | null; // Task 0 透出: A7 标准出成率下限 (null=未配)
  standardYieldMax?: number | null; // Task 0 透出: A7 标准出成率上限 (null=未配)
  plannedStartAt?: string | null;
  plannedEndAt?: string | null;
  actualStartAt?: string | null;
  actualEndAt?: string | null;
  actualMinutes?: number | null;
  completedAt?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
}

// ============ BatchYieldDTO (mirror backend dto/yield/BatchYieldDTO.java:16-28) ============
export interface StepYieldDTO {
  workProcessTaskId: number;
  processOrder: number;
  processName: string | null;
  totalInput: number | null;        // Σ input (BigDecimal → number)
  totalOutput: number | null;       // Σ output — 下道预填用这个
  inputUnit: string | null;
  outputUnit: string | null;
  yieldRate: number | null;         // Σoutput/Σinput; 量纲不可比时 null
  unitComparable: boolean | null;   // false → 只展示量
  carryover: number | null;         // 上道产出 − 本道投入 (>0 结转)
  yieldAlert: 'BELOW_MIN' | 'ABOVE_MAX' | null;  // A7 越界软告警
}

export interface BatchYieldDTO {
  batchId: number;
  batchNumber: string;
  firstStepInput: number | null;    // 998
  lastStepOutput: number | null;    // 382.08 或 3184 盒
  firstStepInputUnit: string | null;
  lastStepOutputUnit: string | null;
  cumulativeYieldRate: number | null;  // 0.3828
  steps: StepYieldDTO[];
  complete: boolean | null;         // 每道都有 input+output 才 true
}

// ============ 请求 DTO ============
// mirror backend dto/yield/YieldReportRequest.java:11-23
export interface YieldReportRequest {
  workProcessTaskId: number;
  inputQuantity: number;            // 本道投入 (前端预填上道产出, 可改)
  inputUnit?: string;
  outputQuantity: number;           // 本道产出 (必填)
  outputUnit?: string;
  workMinutes?: number;             // 选填工时 (后端 Integer)
  forceSubmit?: boolean;            // A4 超收软告警后强制提交
  sourceBatchRefs?: Array<Record<string, unknown>>;  // A3 跨批来源 (Phase D 默认不传)
  reporterName?: string;
  targetWorkerId?: number;          // 代报工 (Phase D operator 自报, 默认不传)
}

// mirror backend dto/yield/MaterialInputRequest.java:9-14
export interface MaterialInputRequest {
  workProcessTaskId: number;
  warehouseOutQuantity: number;     // 出库量 998
  feedInQuantity: number;           // 投料量 935.5
  inputUnit?: string;
}

// ============ Map 响应结果类型 (后端返 Map<String,Object>, 见 YieldReportServiceImpl.java) ============
// submitReport: 只 put reportId(always) + yieldRate(always, null if 量纲不可比) + alert(仅越界 put) — :95-106
export interface YieldReportResult {
  reportId: number;
  yieldRate: number | null;
  alert?: 'BELOW_MIN' | 'ABOVE_MAX';   // null 时 key 不出现
}

// recordMaterialInput: 只 put reportId — :160-162
export interface MaterialInputResult {
  reportId: number;
}

// settleDay: settledCount + batchYield(BatchYieldDTO) + completed — :182-196
export interface SettleDayResult {
  settledCount: number;
  batchYield: BatchYieldDTO;
  completed: boolean;
}

// ============ API Client ============
class YieldReportApi {
  private getBase(batchId: number, factoryId?: string): string {
    const fid = requireFactoryId(factoryId);
    return `/api/mobile/${fid}/production/batches/${batchId}`;
  }

  /** 1. POST /reports — 逐道报工 (投入+产出双量). workerId 后端从 JWT @RequestAttribute("userId") 取, body 不传. */
  async submitReport(
    batchId: number,
    req: YieldReportRequest,
    factoryId?: string,
  ): Promise<ApiResponse<YieldReportResult>> {
    return apiClient.post<ApiResponse<YieldReportResult>>(
      `${this.getBase(batchId, factoryId)}/reports`,
      req,
    );
  }

  /** 2. PUT /material-input — 首道领料双量 (出库量+投料量). Phase D 暂不在 UI 调用 (spec §4). */
  async recordMaterialInput(
    batchId: number,
    req: MaterialInputRequest,
    factoryId?: string,
  ): Promise<ApiResponse<MaterialInputResult>> {
    return apiClient.put<ApiResponse<MaterialInputResult>>(
      `${this.getBase(batchId, factoryId)}/material-input`,
      req,
    );
  }

  /** 3. GET /yield — 整批+单工序出成率 (派生, 下道预填用 steps[i-1].totalOutput). */
  async getYield(batchId: number, factoryId?: string): Promise<ApiResponse<BatchYieldDTO>> {
    return apiClient.get<ApiResponse<BatchYieldDTO>>(
      `${this.getBase(batchId, factoryId)}/yield`,
    );
  }

  /** 4. POST /settle-day?date=&triggerComplete= — 每日结清. */
  async settleDay(
    batchId: number,
    opts: { date?: string; triggerComplete?: boolean } = {},
    factoryId?: string,
  ): Promise<ApiResponse<SettleDayResult>> {
    const params = new URLSearchParams();
    if (opts.date) params.append('date', opts.date);
    if (opts.triggerComplete != null) {
      params.append('triggerComplete', String(opts.triggerComplete));
    }
    const qs = params.toString();
    return apiClient.post<ApiResponse<SettleDayResult>>(
      `${this.getBase(batchId, factoryId)}/settle-day${qs ? `?${qs}` : ''}`,
    );
  }

  /** 5. GET /reports — 报工流水汇总 (当前后端复用 getYield 的 steps 聚合视图). */
  async listReports(batchId: number, factoryId?: string): Promise<ApiResponse<BatchYieldDTO>> {
    return apiClient.get<ApiResponse<BatchYieldDTO>>(
      `${this.getBase(batchId, factoryId)}/reports`,
    );
  }

  /** 工序任务列表 (该批次, 按 processOrder 升序). 端点见 WorkProcessTaskController.java:93-99. */
  async listWorkProcessTasks(
    batchId: number,
    factoryId?: string,
  ): Promise<ApiResponse<WorkProcessTask[]>> {
    const fid = requireFactoryId(factoryId);
    return apiClient.get<ApiResponse<WorkProcessTask[]>>(
      `/api/mobile/${fid}/production/batches/${batchId}/work-process-tasks`,
    );
  }
}

export const yieldReportApi = new YieldReportApi();
```

**验证命令**:
```bash
cd /c/Users/Steve/cretas-yield-rn/frontend/CretasFoodTrace
npx tsc --noEmit 2>&1 | grep -i "yieldReportApi" || echo "yieldReportApi.ts: 0 ts errors"
```

**commit**: `feat(yield-rn): yieldReportApi.ts — Phase A 5 端点 + listWorkProcessTasks + TS 类型`

---

## Task 2 — workProcessTask RN service

**已并入 Task 1**（`listWorkProcessTasks` 在 `yieldReportApi.ts` 内，逻辑内聚同 base path，spec §3.1.1 默认）。**本 Task 无独立文件**——不另建 `workProcessTaskApi.ts`（避免与老 `processTaskApiClient.ts` 混淆，且 spec 默认内聚）。

> 已确认 RN 无现成接 `/work-process-tasks` 的 service（`processTaskApiClient.ts` 接的是老 `/process-tasks`，不同实体，F2 [纠正]）。

**验证**: 同 Task 1 的 tsc。**无独立 commit**。

---

## Task 3 — `YieldQuantityInput.tsx`（±stepper + 软上限 max + 小数 + 大字防呆）

**Files**: 新建 `frontend/CretasFoodTrace/src/components/processing/YieldQuantityInput.tsx`。

设计要点（防呆 Rule 1）:
- 大字 value（`fontSize 28`）+ `−`/`+` 步进按钮（大 hit area，48×48）+ unit 后缀。
- `keyboardType="decimal-pad"`，string 态受控（父传 value/onChangeText），允许小数 + 中间空值（不硬 clamp）。
- `max`（软上限）传入时，超 max 显黄色 inline 提示文案（不拦），下方 `hint` slot 显预填标注（"← 上道产出 X kg，请确认实际投了多少"，橙色高亮）。
- step 默认 1，可传（如 0.5）。`+`/`−` 在当前 parse 值上 ±step，`−` 不低于 0。

**Step 3.1 — 完整文件**
```tsx
import React, { useCallback, useMemo } from 'react';
import { View, Text, TextInput, TouchableOpacity, StyleSheet } from 'react-native';

interface YieldQuantityInputProps {
  /** 标签, 如 "投入量" / "产出量" */
  label: string;
  /** 受控 string 值 (父持有, 允许中间空/小数态) */
  value: string;
  onChangeText: (v: string) => void;
  /** 单位后缀, 如 "kg" / "盒" */
  unit?: string | null;
  /** 步进步长, 默认 1 */
  step?: number;
  /** 软上限 (不硬 clamp, 超过仅黄字提示). 如 plannedQuantity*1.3 */
  max?: number | null;
  /** 软上限超限提示文案 (Rule 1 预先显示边界), 如 "计划 998 kg, 可投上限约 1297 (含 30% 超收)" */
  maxHint?: string | null;
  /** 预填标注 (命门), 如 "← 上道产出 998 kg, 请确认实际投了多少". 橙色高亮显示. */
  prefillNote?: string | null;
  /** 是否禁用 (提交中) */
  disabled?: boolean;
  testID?: string;
}

const clampToZero = (n: number): number => (n < 0 ? 0 : n);

export const YieldQuantityInput: React.FC<YieldQuantityInputProps> = ({
  label,
  value,
  onChangeText,
  unit,
  step = 1,
  max,
  maxHint,
  prefillNote,
  disabled = false,
  testID,
}) => {
  const numeric = parseFloat(value);
  const overMax = useMemo(
    () => max != null && !Number.isNaN(numeric) && numeric > max,
    [max, numeric],
  );

  const handleStep = useCallback(
    (delta: number) => {
      const base = Number.isNaN(numeric) ? 0 : numeric;
      const next = clampToZero(base + delta);
      // 保留小数: 整数显整数, 否则最多 2 位
      onChangeText(Number.isInteger(next) ? String(next) : String(Number(next.toFixed(2))));
    },
    [numeric, onChangeText],
  );

  // 只允许数字 + 单个小数点
  const handleChange = useCallback(
    (raw: string) => {
      const cleaned = raw.replace(/[^0-9.]/g, '');
      const parts = cleaned.split('.');
      const normalized = parts.length > 2 ? `${parts[0]}.${parts.slice(1).join('')}` : cleaned;
      onChangeText(normalized);
    },
    [onChangeText],
  );

  return (
    <View style={styles.wrap} testID={testID}>
      <Text style={styles.label}>{label}</Text>
      <View style={styles.row}>
        <TouchableOpacity
          style={[styles.stepBtn, disabled && styles.stepBtnDisabled]}
          onPress={() => handleStep(-step)}
          disabled={disabled}
          testID={testID ? `${testID}-minus` : undefined}
          accessibilityLabel="减少"
        >
          <Text style={styles.stepText}>−</Text>
        </TouchableOpacity>

        <View style={styles.valueBox}>
          <TextInput
            style={styles.valueInput}
            keyboardType="decimal-pad"
            value={value}
            onChangeText={handleChange}
            editable={!disabled}
            placeholder="0"
            placeholderTextColor="#C0C4CC"
            testID={testID ? `${testID}-input` : undefined}
          />
          {unit ? <Text style={styles.unit}>{unit}</Text> : null}
        </View>

        <TouchableOpacity
          style={[styles.stepBtn, disabled && styles.stepBtnDisabled]}
          onPress={() => handleStep(step)}
          disabled={disabled}
          testID={testID ? `${testID}-plus` : undefined}
          accessibilityLabel="增加"
        >
          <Text style={styles.stepText}>＋</Text>
        </TouchableOpacity>
      </View>

      {prefillNote ? <Text style={styles.prefillNote}>{prefillNote}</Text> : null}
      {overMax && maxHint ? <Text style={styles.maxHint}>{maxHint}</Text> : null}
    </View>
  );
};

const styles = StyleSheet.create({
  wrap: { marginBottom: 16 },
  label: { fontSize: 16, fontWeight: '600', color: '#303133', marginBottom: 10 },
  row: { flexDirection: 'row', alignItems: 'center' },
  stepBtn: {
    width: 48, height: 48, borderRadius: 8, borderWidth: 1, borderColor: '#DCDFE6',
    backgroundColor: '#F5F7FA', alignItems: 'center', justifyContent: 'center',
  },
  stepBtnDisabled: { opacity: 0.4 },
  stepText: { fontSize: 26, color: '#303133', fontWeight: '600', lineHeight: 30 },
  valueBox: {
    flex: 1, flexDirection: 'row', alignItems: 'center', justifyContent: 'center',
    marginHorizontal: 10, borderWidth: 1, borderColor: '#DCDFE6', borderRadius: 8,
    backgroundColor: '#FFFFFF', height: 56, paddingHorizontal: 12,
  },
  valueInput: { flex: 1, fontSize: 28, fontWeight: '700', color: '#1A1A1A', textAlign: 'center', padding: 0 },
  unit: { fontSize: 16, color: '#909399', marginLeft: 6 },
  prefillNote: { fontSize: 14, color: '#E8732E', marginTop: 8, fontWeight: '500' },
  maxHint: { fontSize: 13, color: '#E6A23C', marginTop: 6 },
});

export default YieldQuantityInput;
```

**验证命令**:
```bash
cd /c/Users/Steve/cretas-yield-rn/frontend/CretasFoodTrace
npx tsc --noEmit 2>&1 | grep -i "YieldQuantityInput" || echo "YieldQuantityInput.tsx: 0 ts errors"
```

**commit**: `feat(yield-rn): YieldQuantityInput 组件 (±stepper + 软上限 max + 大字防呆)`

---

## Task 4 — `YieldStepReportScreen.tsx`（一道一卡片 + 完成态）

**Files**: 新建 `frontend/CretasFoodTrace/src/screens/processing/YieldStepReportScreen.tsx`。
**依赖**: Task 1（`yieldReportApi` + 类型）、Task 3（`YieldQuantityInput`）、复用 `NeoButton`/`NeoCard`/`ScreenWrapper`、`processingApiClient.getBatchById`。

**数据流**:
1. `useEffect` 进屏：`Promise.all([yieldReportApi.listWorkProcessTasks(batchId), processingApiClient.getBatchById(String(batchId)), yieldReportApi.getYield(batchId)])`。
2. tasks 按 `processOrder` 升序（后端已排，前端再 `sort` 兜底）。`currentStepIndex` 从首个未完成（无对应 step output 的）任务起，简单起见从 0 起（spec §3.6 一次性流程）。
3. 当前道卡片头 = batch.productType（品名）+ batch.batchNumber + task.processName + task.plannedQuantity/plannedUnit。
4. 投入量预填：若 `currentStepIndex>0`，取 `yieldData.steps` 里 processOrder == 当前道 processOrder−1 的 `totalOutput` 作 inputQty 初值 + prefillNote。首道不预填，note 提示"本道领料投入量"。
5. 提交：组 `YieldReportRequest`（inputUnit/outputUnit 用 task.plannedUnit 兜底），`yieldReportApi.submitReport` → 据 result.alert 显 A7 banner（不拦，已提交成功）→ re-fetch getYield → currentStepIndex+1 → reset 输入。
6. 末道提交后：`phase='done'`，显 cumulativeYieldRate + firstStepInput→lastStepOutput + 「标记今日结清」(settleDay 不带 triggerComplete) + 「返回选批次」。

**Step 4.1 — 完整文件**
```tsx
import React, { useCallback, useEffect, useMemo, useState } from 'react';
import { View, Text, ScrollView, StyleSheet, Alert, ActivityIndicator } from 'react-native';
import { useNavigation, useRoute, RouteProp } from '@react-navigation/native';
import { NativeStackNavigationProp } from '@react-navigation/native-stack';
import { ScreenWrapper } from '../../components/ui/ScreenWrapper';
import { NeoCard } from '../../components/ui/NeoCard';
import { NeoButton } from '../../components/ui/NeoButton';
import YieldQuantityInput from '../../components/processing/YieldQuantityInput';
import {
  yieldReportApi,
  WorkProcessTask,
  BatchYieldDTO,
  StepYieldDTO,
  YieldReportRequest,
} from '../../services/api/yieldReportApi';
import { processingApiClient } from '../../services/api/processingApiClient';
import { handleError } from '../../utils/errorHandler';

type YieldStepReportParams = { batchId: number; batchNumber?: string };
type RouteT = RouteProp<{ YieldStepReport: YieldStepReportParams }, 'YieldStepReport'>;
type NavT = NativeStackNavigationProp<Record<string, object | undefined>>;

const OVER_RECEIVE_TOLERANCE = 1.3; // A4 软上限: 计划 ×1.3 (含 30% 超收)

const YieldStepReportScreen: React.FC = () => {
  const navigation = useNavigation<NavT>();
  const route = useRoute<RouteT>();
  const { batchId } = route.params;

  const [loading, setLoading] = useState(true);
  const [submitting, setSubmitting] = useState(false);
  const [phase, setPhase] = useState<'reporting' | 'done'>('reporting');

  const [productType, setProductType] = useState<string>('');
  const [batchNumber, setBatchNumber] = useState<string>(route.params.batchNumber ?? '');
  const [tasks, setTasks] = useState<WorkProcessTask[]>([]);
  const [yieldData, setYieldData] = useState<BatchYieldDTO | null>(null);
  const [currentStepIndex, setCurrentStepIndex] = useState(0);

  const [inputQty, setInputQty] = useState('');
  const [outputQty, setOutputQty] = useState('');
  const [lastAlert, setLastAlert] = useState<'BELOW_MIN' | 'ABOVE_MAX' | null>(null);

  const currentTask = tasks[currentStepIndex] ?? null;
  const totalSteps = tasks.length;

  // 上道产出 (下道预填): 取 yield steps 里 processOrder == 当前道-1 的 totalOutput
  const prevOutput = useMemo<number | null>(() => {
    if (!currentTask || !yieldData) return null;
    const prevOrder = currentTask.processOrder - 1;
    const prevStep = yieldData.steps.find((s: StepYieldDTO) => s.processOrder === prevOrder);
    return prevStep?.totalOutput ?? null;
  }, [currentTask, yieldData]);

  const loadAll = useCallback(async () => {
    try {
      const [tasksRes, batchRes, yieldRes] = await Promise.all([
        yieldReportApi.listWorkProcessTasks(batchId),
        processingApiClient.getBatchById(String(batchId)),
        yieldReportApi.getYield(batchId),
      ]);
      if (tasksRes.success) {
        const sorted = [...tasksRes.data].sort((a, b) => a.processOrder - b.processOrder);
        setTasks(sorted);
      }
      if (batchRes.success && batchRes.data) {
        setProductType(batchRes.data.productType ?? '');
        if (batchRes.data.batchNumber) setBatchNumber(batchRes.data.batchNumber);
      }
      if (yieldRes.success) setYieldData(yieldRes.data);
    } catch (error) {
      handleError(error, { showAlert: false, logError: true });
      const msg = error instanceof Error ? error.message : '加载批次工序失败';
      Alert.alert('加载失败', msg);
    } finally {
      setLoading(false);
    }
  }, [batchId]);

  useEffect(() => {
    loadAll();
  }, [loadAll]);

  // 切道时: 投入预填上道产出, 产出清空, 清告警
  useEffect(() => {
    if (phase !== 'reporting') return;
    setInputQty(prevOutput != null ? String(prevOutput) : '');
    setOutputQty('');
    setLastAlert(null);
  }, [currentStepIndex, prevOutput, phase]);

  const unit = currentTask?.plannedUnit ?? 'kg';
  const planned = currentTask?.plannedQuantity ?? null;
  const inputMax = planned != null ? planned * OVER_RECEIVE_TOLERANCE : null;
  const inputMaxHint =
    planned != null
      ? `计划 ${planned} ${unit}, 可投上限约 ${Math.round(planned * OVER_RECEIVE_TOLERANCE)} (含 30% 超收)`
      : null;
  const prefillNote =
    prevOutput != null
      ? `← 上道产出 ${prevOutput} ${unit}, 请确认实际投了多少`
      : '本道为首道, 请填本道领料投入量';

  const handleSubmit = useCallback(async () => {
    if (!currentTask) return;
    const output = parseFloat(outputQty);
    if (Number.isNaN(output) || output <= 0) {
      Alert.alert('请填写本道产出量', '产出量必须大于 0');
      return;
    }
    const input = parseFloat(inputQty);
    const req: YieldReportRequest = {
      workProcessTaskId: currentTask.id,
      inputQuantity: Number.isNaN(input) ? 0 : input,
      inputUnit: unit,
      outputQuantity: output,
      outputUnit: unit,
    };
    setSubmitting(true);
    try {
      const res = await yieldReportApi.submitReport(batchId, req);
      if (!res.success) {
        Alert.alert('提交失败', res.message || '请重试');
        return;
      }
      setLastAlert(res.data.alert ?? null);

      // re-fetch yield 刷新下道预填
      const yieldRes = await yieldReportApi.getYield(batchId);
      if (yieldRes.success) setYieldData(yieldRes.data);

      const isLast = currentStepIndex >= totalSteps - 1;
      if (isLast) {
        setPhase('done');
      } else {
        setCurrentStepIndex((i) => i + 1);
      }
    } catch (error) {
      // 4位一体: 透传后端 message (含 hint), 用 Alert (RN sticky 模态)
      handleError(error, { showAlert: false, logError: true });
      const e = error as { response?: { data?: { message?: string; hint?: string } } };
      const backendMsg = e.response?.data?.message;
      const hint = e.response?.data?.hint;
      const msg = backendMsg
        ? hint
          ? `${backendMsg}\n${hint}`
          : backendMsg
        : error instanceof Error
          ? error.message
          : '提交失败, 请重试';
      // A4 后端补后会抛超收错 → 此处可扩展"确认超收提交"按钮重发 forceSubmit:true (spec §3.3); Phase A 未实现 A4, 暂只显错.
      Alert.alert('提交失败', msg);
    } finally {
      setSubmitting(false);
    }
  }, [currentTask, outputQty, inputQty, unit, batchId, currentStepIndex, totalSteps]);

  const handleSettleDay = useCallback(async () => {
    setSubmitting(true);
    try {
      const res = await yieldReportApi.settleDay(batchId, {});
      if (res.success) {
        Alert.alert('已标记今日结清', `本次结清 ${res.data.settledCount} 条报工`);
      } else {
        Alert.alert('结清失败', res.message || '请重试');
      }
    } catch (error) {
      handleError(error, { showAlert: false, logError: true });
      const e = error as { response?: { data?: { message?: string } } };
      Alert.alert('结清失败', e.response?.data?.message ?? '请重试');
    } finally {
      setSubmitting(false);
    }
  }, [batchId]);

  if (loading) {
    return (
      <ScreenWrapper>
        <View style={styles.centered}>
          <ActivityIndicator size="large" color="#E8732E" />
          <Text style={styles.loadingText}>加载批次工序...</Text>
        </View>
      </ScreenWrapper>
    );
  }

  if (totalSteps === 0) {
    return (
      <ScreenWrapper>
        <View style={styles.centered}>
          <Text style={styles.emptyTitle}>该批次未生成工序任务</Text>
          <Text style={styles.emptyDesc}>请联系主管为该批次 spawn 工序后再报工</Text>
          <NeoButton variant="outline" size="large" onPress={() => navigation.goBack()} style={styles.fullBtn}>
            返回选批次
          </NeoButton>
        </View>
      </ScreenWrapper>
    );
  }

  if (phase === 'done') {
    const cum = yieldData?.cumulativeYieldRate;
    const cumPct = cum != null ? `${(cum * 100).toFixed(2)}%` : '—';
    return (
      <ScreenWrapper>
        <ScrollView contentContainerStyle={styles.content}>
          <NeoCard variant="elevated" style={styles.doneCard}>
            <Text style={styles.doneTitle}>✓ {totalSteps}/{totalSteps} 道全部报完</Text>
            <Text style={styles.doneProduct}>{productType || '—'}</Text>
            <Text style={styles.doneBatch}>{batchNumber}</Text>
            <View style={styles.doneRow}>
              <Text style={styles.doneLabel}>累计出成率</Text>
              <Text style={styles.doneValue} testID="cumulative-yield-rate">{cumPct}</Text>
            </View>
            {yieldData?.firstStepInput != null && yieldData?.lastStepOutput != null ? (
              <Text style={styles.doneFlow}>
                {yieldData.firstStepInput}{yieldData.firstStepInputUnit ?? ''} → {yieldData.lastStepOutput}{yieldData.lastStepOutputUnit ?? ''}
              </Text>
            ) : null}
          </NeoCard>
          <NeoButton variant="primary" size="large" onPress={handleSettleDay} disabled={submitting} loading={submitting} style={styles.fullBtn}>
            标记今日结清
          </NeoButton>
          <NeoButton variant="outline" size="large" onPress={() => navigation.goBack()} style={styles.fullBtn}>
            返回选批次
          </NeoButton>
        </ScrollView>
      </ScreenWrapper>
    );
  }

  const alertText =
    lastAlert === 'ABOVE_MAX'
      ? '⚠ 出成率偏高, 请核对'
      : lastAlert === 'BELOW_MIN'
        ? '⚠ 出成率偏低, 请核对'
        : null;

  return (
    <ScreenWrapper>
      <ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled">
        {/* 进度条 报工 i/N + 圆点 */}
        <View style={styles.progressWrap}>
          <Text style={styles.progressText}>报工 {currentStepIndex + 1} / {totalSteps}</Text>
          <View style={styles.dotsRow}>
            {tasks.map((t, i) => (
              <View
                key={t.id}
                style={[styles.dot, i <= currentStepIndex ? styles.dotActive : styles.dotInactive]}
              />
            ))}
          </View>
        </View>

        <NeoCard variant="elevated" style={styles.card}>
          {/* 卡片头 context (Rule 2) */}
          <Text style={styles.product}>{productType || '—'}</Text>
          <Text style={styles.batchProcess}>
            {batchNumber}  ·  {currentTask?.processName ?? `第 ${currentTask?.processOrder} 道`}
          </Text>
          {planned != null ? (
            <Text style={styles.planned}>计划数量  {planned} {unit}</Text>
          ) : null}

          {/* 标准区间 (Task 0 透出) */}
          {currentTask?.standardYieldMin != null || currentTask?.standardYieldMax != null ? (
            <Text style={styles.stdRange}>
              标准出成率{' '}
              {currentTask?.standardYieldMin != null ? `${(currentTask.standardYieldMin * 100).toFixed(0)}%` : '—'}
              {' ~ '}
              {currentTask?.standardYieldMax != null ? `${(currentTask.standardYieldMax * 100).toFixed(0)}%` : '—'}
            </Text>
          ) : null}

          <View style={styles.divider} />

          <YieldQuantityInput
            label="投入量"
            value={inputQty}
            onChangeText={setInputQty}
            unit={unit}
            max={inputMax}
            maxHint={inputMaxHint}
            prefillNote={prefillNote}
            disabled={submitting}
            testID="yield-input-qty"
          />

          <YieldQuantityInput
            label="产出量"
            value={outputQty}
            onChangeText={setOutputQty}
            unit={unit}
            disabled={submitting}
            testID="yield-output-qty"
          />

          {alertText ? (
            <View style={styles.alertBanner} testID="yield-alert-banner">
              <Text style={styles.alertText}>{alertText}</Text>
            </View>
          ) : null}
        </NeoCard>

        <NeoButton
          variant="primary"
          size="large"
          onPress={handleSubmit}
          disabled={submitting}
          loading={submitting}
          style={styles.fullBtn}
          testID="yield-submit-btn"
        >
          {currentStepIndex >= totalSteps - 1 ? '提交  ·  完成' : '提交  ·  下一道  ▶'}
        </NeoButton>
      </ScrollView>
    </ScreenWrapper>
  );
};

const styles = StyleSheet.create({
  content: { padding: 16 },
  centered: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 24 },
  loadingText: { marginTop: 12, fontSize: 15, color: '#909399' },
  emptyTitle: { fontSize: 18, fontWeight: '600', color: '#303133', marginBottom: 8, textAlign: 'center' },
  emptyDesc: { fontSize: 14, color: '#909399', marginBottom: 24, textAlign: 'center' },
  progressWrap: { marginBottom: 16 },
  progressText: { fontSize: 16, fontWeight: '600', color: '#303133', marginBottom: 8 },
  dotsRow: { flexDirection: 'row', flexWrap: 'wrap' },
  dot: { width: 12, height: 12, borderRadius: 6, marginRight: 8, marginBottom: 6 },
  dotActive: { backgroundColor: '#E8732E' },
  dotInactive: { backgroundColor: '#DCDFE6' },
  card: { marginBottom: 16 },
  product: { fontSize: 22, fontWeight: '700', color: '#1A1A1A' },
  batchProcess: { fontSize: 15, color: '#606266', marginTop: 6 },
  planned: { fontSize: 14, color: '#909399', marginTop: 6 },
  stdRange: { fontSize: 13, color: '#409EFF', marginTop: 4 },
  divider: { height: 1, backgroundColor: '#EBEEF5', marginVertical: 16 },
  alertBanner: { backgroundColor: '#FDF6EC', borderRadius: 8, padding: 12, marginTop: 4 },
  alertText: { fontSize: 14, color: '#E6A23C', fontWeight: '500' },
  fullBtn: { width: '100%', marginBottom: 12 },
  doneCard: { marginBottom: 20, alignItems: 'center' },
  doneTitle: { fontSize: 20, fontWeight: '700', color: '#67C23A', marginBottom: 12 },
  doneProduct: { fontSize: 18, fontWeight: '600', color: '#1A1A1A' },
  doneBatch: { fontSize: 14, color: '#909399', marginTop: 4, marginBottom: 16 },
  doneRow: { flexDirection: 'row', alignItems: 'baseline', marginTop: 8 },
  doneLabel: { fontSize: 15, color: '#606266', marginRight: 12 },
  doneValue: { fontSize: 28, fontWeight: '700', color: '#E8732E' },
  doneFlow: { fontSize: 15, color: '#606266', marginTop: 12 },
});

export default YieldStepReportScreen;
```

> **NeoButton 用法核对（F5）**: 文字用 `children`（`<NeoButton ...>提交</NeoButton>`），不是 `title` prop；满宽用 `style={{ width:'100%' }}`（无 `fullWidth`）；`loading`/`disabled` 透传 paper props。✓

**验证命令**:
```bash
cd /c/Users/Steve/cretas-yield-rn/frontend/CretasFoodTrace
npx tsc --noEmit 2>&1 | grep -i "YieldStepReportScreen" || echo "YieldStepReportScreen.tsx: 0 ts errors"
```

**commit**: `feat(yield-rn): YieldStepReportScreen — 一道一卡片逐道双量报工 + 完成态`

---

## Task 5 — `YieldBatchSelectScreen.tsx`（选批次屏）

**Files**: 新建 `frontend/CretasFoodTrace/src/screens/processing/YieldBatchSelectScreen.tsx`。
**依赖**: `processingApiClient.getBatches`（列 IN_PROGRESS 批次）+ `scanBatchByCode`（可选扫码）+ `EmptyStateCard`（default import）+ `NeoCard`/`ScreenWrapper`/`BarcodeScannerModal`。

**数据流**: 进屏 `getBatches({ status: 'IN_PROGRESS', page: 1, size: 50 })`（仅在产批次可报工）→ 列表每行 `NeoCard onPress` → `navigation.navigate('YieldStepReport', { batchId: b.id, batchNumber: b.batchNumber })`。空列表 → `EmptyStateCard`（description/actionLabel）。顶部"扫码选批次"按钮 → `BarcodeScannerModal` → `scanBatchByCode` → 跳 YieldStepReport。

**Step 5.1 — 完整文件**
```tsx
import React, { useCallback, useEffect, useState } from 'react';
import { View, Text, ScrollView, StyleSheet, Alert, ActivityIndicator, TouchableOpacity } from 'react-native';
import { useNavigation, useFocusEffect } from '@react-navigation/native';
import { NativeStackNavigationProp } from '@react-navigation/native-stack';
import { ScreenWrapper } from '../../components/ui/ScreenWrapper';
import { NeoCard } from '../../components/ui/NeoCard';
import EmptyStateCard from '../../components/common/EmptyStateCard';
import { BarcodeScannerModal } from '../../components/processing/BarcodeScannerModal';
import { processingApiClient, ProcessingBatch } from '../../services/api/processingApiClient';
import { handleError } from '../../utils/errorHandler';

type NavT = NativeStackNavigationProp<Record<string, object | undefined>>;

const YieldBatchSelectScreen: React.FC = () => {
  const navigation = useNavigation<NavT>();
  const [loading, setLoading] = useState(true);
  const [batches, setBatches] = useState<ProcessingBatch[]>([]);
  const [scannerVisible, setScannerVisible] = useState(false);

  const loadBatches = useCallback(async () => {
    setLoading(true);
    try {
      const res = await processingApiClient.getBatches({ status: 'IN_PROGRESS', page: 1, size: 50 });
      if (res.success && res.data) {
        setBatches(res.data.content ?? []);
      } else {
        setBatches([]);
      }
    } catch (error) {
      handleError(error, { showAlert: false, logError: true });
      const msg = error instanceof Error ? error.message : '加载批次失败';
      Alert.alert('加载失败', msg);
      setBatches([]);
    } finally {
      setLoading(false);
    }
  }, []);

  useFocusEffect(
    useCallback(() => {
      loadBatches();
    }, [loadBatches]),
  );

  const goReport = useCallback(
    (batch: ProcessingBatch) => {
      navigation.navigate('YieldStepReport', { batchId: batch.id, batchNumber: batch.batchNumber });
    },
    [navigation],
  );

  const handleScan = useCallback(
    async (code: string) => {
      setScannerVisible(false);
      try {
        const res = await processingApiClient.scanBatchByCode(code);
        if (res.success && res.data) {
          goReport(res.data);
        } else {
          Alert.alert('未找到批次', res.message || '请检查条码是否正确');
        }
      } catch (error) {
        handleError(error, { showAlert: false, logError: true });
        const msg = error instanceof Error ? error.message : '扫码查询失败';
        Alert.alert('查询失败', msg);
      }
    },
    [goReport],
  );

  return (
    <ScreenWrapper>
      <ScrollView contentContainerStyle={styles.content}>
        <Text style={styles.title}>选择报工批次</Text>

        <TouchableOpacity style={styles.scanBtn} onPress={() => setScannerVisible(true)} testID="yield-scan-batch-btn">
          <Text style={styles.scanBtnText}>扫码选批次</Text>
        </TouchableOpacity>

        {loading ? (
          <View style={styles.centered}>
            <ActivityIndicator size="large" color="#E8732E" />
            <Text style={styles.loadingText}>加载在产批次...</Text>
          </View>
        ) : batches.length === 0 ? (
          <EmptyStateCard
            icon="clipboard-list-outline"
            title="暂无在产批次"
            description="只有'生产中'的批次可逐道报工, 请先开始生产或联系主管"
            actionLabel="刷新"
            onAction={loadBatches}
          />
        ) : (
          batches.map((b) => (
            <NeoCard key={b.id} variant="elevated" onPress={() => goReport(b)} style={styles.batchCard}>
              <Text style={styles.batchNo}>{b.batchNumber}</Text>
              <Text style={styles.batchProduct}>{b.productType || '—'}</Text>
              <Text style={styles.batchMeta}>
                计划 {b.targetQuantity ?? '—'}  ·  状态 {b.status}
              </Text>
            </NeoCard>
          ))
        )}
      </ScrollView>

      <BarcodeScannerModal
        visible={scannerVisible}
        onClose={() => setScannerVisible(false)}
        onScan={handleScan}
      />
    </ScreenWrapper>
  );
};

const styles = StyleSheet.create({
  content: { padding: 16 },
  title: { fontSize: 22, fontWeight: '700', color: '#1A1A1A', marginBottom: 16 },
  scanBtn: { backgroundColor: '#E8732E', borderRadius: 10, paddingVertical: 16, alignItems: 'center', marginBottom: 16 },
  scanBtnText: { color: '#FFFFFF', fontSize: 17, fontWeight: '600' },
  centered: { alignItems: 'center', paddingVertical: 48 },
  loadingText: { marginTop: 12, fontSize: 15, color: '#909399' },
  batchCard: { marginBottom: 12 },
  batchNo: { fontSize: 17, fontWeight: '600', color: '#1A1A1A' },
  batchProduct: { fontSize: 15, color: '#606266', marginTop: 4 },
  batchMeta: { fontSize: 13, color: '#909399', marginTop: 6 },
});

export default YieldBatchSelectScreen;
```

> **行内进度（spec 截图里"3/7 道"）**: 列表行只显批次号/品名/计划/状态，不显"已报 N/M 道"（需对每批额外调 `getYield` N 次，YAGNI；spec 截图是示意）。`scanBatchByCode` 返回 `ProcessingBatch`，含 `id`/`batchNumber` 直接 `goReport`。

**验证命令**:
```bash
cd /c/Users/Steve/cretas-yield-rn/frontend/CretasFoodTrace
npx tsc --noEmit 2>&1 | grep -i "YieldBatchSelectScreen" || echo "YieldBatchSelectScreen.tsx: 0 ts errors"
```

**commit**: `feat(yield-rn): YieldBatchSelectScreen — 选在产批次 + 扫码 + 空态`

---

## Task 6 — 整批出成率总结屏

**已并入 Task 4**（`YieldStepReportScreen` 内部 `phase='done'` 切换，显 `cumulativeYieldRate` + firstStepInput→lastStepOutput + 结清/返回，末道提交后自动进入）。不新建独立 `YieldDoneScreen`（spec §3.5 默认内部状态切换）。

**验证**: 完成态在 Task 4 tsc 内已覆盖。**无独立 commit**。

---

## Task 7 — operator 入口 + 导航注册（共享文件，主 chat 串行改）

> ⚠️ `OperatorNavigator.tsx` + `ScanReportScreen.tsx` 是共享文件，per `concurrent-edit-safety.md` 规则 4：改前 `git status --short <file>`，确认无并发未提交改动。

**Files**: `frontend/.../navigation/OperatorNavigator.tsx`、`frontend/.../screens/processing/ScanReportScreen.tsx`。

**Step 7.1 — 注册 2 新屏**
`OperatorNavigator.tsx:10-12` import 区后加：
```tsx
import YieldBatchSelectScreen from '../screens/processing/YieldBatchSelectScreen';
import YieldStepReportScreen from '../screens/processing/YieldStepReportScreen';
```
`OperatorNavigator.tsx:26`（`<ReportStack.Screen name="DraftReports" ... />` 之后、`</ReportStack.Navigator>` 之前）加：
```tsx
      <ReportStack.Screen name="YieldBatchSelect" component={YieldBatchSelectScreen} />
      <ReportStack.Screen name="YieldStepReport" component={YieldStepReportScreen} />
```

**Step 7.2 — ScanReport 顶部加入口按钮**
`ScanReportScreen.tsx` 需拿 navigation 跳 `YieldBatchSelect`。现有 `navigation` 类型是 `NativeStackNavigationProp<WSBatchesStackParamList, 'ScanReport'>`（`ScanReportScreen.tsx:37,40`），该 ParamList 不含 `YieldBatchSelect` → 直接 `navigation.navigate('YieldBatchSelect')` 会 TS 报错。**用类型安全方式**：在按钮回调里用 `(navigation as unknown as { navigate: (s: string) => void })` 会违反 `as any` 规范。**正确做法**：在文件顶部加一个最小局部联合类型并用 `useNavigation` 重取，或扩展。实施用：

在 `ScanReportScreen.tsx` 顶部 import 区加（与现有 import 同区）：
```tsx
import { useNavigation } from '@react-navigation/native';   // 已 import (line 7), 复用
```
现有已 `const navigation = useNavigation<NavProp>();`（line 40）。新增一个专跳 yield 的导航句柄（不污染现有 NavProp）：
```tsx
// 文件内, 组件顶部 (line 40 之后)
const yieldNav = useNavigation<NativeStackNavigationProp<Record<string, object | undefined>>>();
```
（`NativeStackNavigationProp` 已 import line 8。）

在 `ScanReportScreen.tsx:145`（`<Text style={styles.title}>扫码报工</Text>` 之后）插入入口按钮：
```tsx
        <TouchableOpacity
          style={styles.yieldEntryButton}
          onPress={() => yieldNav.navigate('YieldBatchSelect')}
          testID="yield-entry-button"
        >
          <Text style={styles.yieldEntryText}>逐道工序报工 ▶</Text>
        </TouchableOpacity>
```
在 `StyleSheet.create` 末尾（`ScanReportScreen.tsx:292` rescanText 后）加样式：
```tsx
  yieldEntryButton: {
    backgroundColor: '#E8732E', borderRadius: 10, paddingVertical: 16,
    alignItems: 'center', marginBottom: 16,
  },
  yieldEntryText: { color: '#FFFFFF', fontSize: 17, fontWeight: '600' },
```

> 入口角色门控天然由 `OperatorNavigator` 隔离（仅 operator 见，F4）。业态门控（餐饮 operator 不显）spec Q7 默认：可选地在按钮外包 `{hasProductionCapability(useAuthStore.getState().user) && (...)}`（`utils/factoryType.ts` import）。**默认实现加业态门控**（餐饮无工序，不显逐道入口；只显扫码）：
```tsx
// import 区加: import { hasProductionCapability } from '../../utils/factoryType';
//             import { useAuthStore } from '../../store/authStore';
// 组件内: const user = useAuthStore(s => s.user);
// 按钮外包: {hasProductionCapability(user) && ( <TouchableOpacity .../> )}
```

**验证命令**:
```bash
cd /c/Users/Steve/cretas-yield-rn/frontend/CretasFoodTrace
git status --short src/navigation/OperatorNavigator.tsx src/screens/processing/ScanReportScreen.tsx   # 确认无并发改动
npx tsc --noEmit 2>&1 | grep -iE "OperatorNavigator|ScanReportScreen" || echo "navigation/entry: 0 ts errors"
```

**commit**（per concurrent-edit-safety 规则 5b，commit 前 git status，用 `git commit -- <files>`）:
`feat(yield-rn): OperatorNavigator 注册逐道报工 2 屏 + ScanReport 顶部入口 (业态门控)`

---

## Task 8 — Expo Web headed Playwright 截图验证

> per `.claude/rules/playwright-headed-mode.md`：强制 `headless: false`、viewport 1920×1080（移动 case `page.setViewportSize({width:390,height:844})`）、`--lang=zh-CN`、`--font-render-hinting=none`。本任务用 `PLAYWRIGHT_PORT=9222 PLAYWRIGHT_CHAT_ID=yield-rn`。

**Files**: 新建 spec `frontend/CretasFoodTrace/e2e/yield-step-report.spec.ts`（或项目现有 e2e 目录，先 `ls frontend/CretasFoodTrace/e2e 2>/dev/null` / `ls frontend/CretasFoodTrace/playwright.config.ts` 确认目录与 config）。若无 Playwright config，按 playwright-headed-mode.md 的 `use:` 范例新建 `playwright.config.ts`（headed config 段落逐字照规则）。

**Step 8.1 — 启动 Expo Web**
```bash
cd /c/Users/Steve/cretas-yield-rn/frontend/CretasFoodTrace
npx expo start --web   # 或 npm run web; 记下 web URL (通常 http://localhost:8081 或 19006)
```
（后端用 prod 10010 或 test 10011 via tunnel；operator 账号见 `.env.test.example` 格式，本地配。需一个有 IN_PROGRESS 批次 + 工序任务 + 配了 standard_yield_min/max 的工厂数据；test 库 cretas_db。）

**Step 8.2 — spec 截图验证点**（每点 `page.screenshot({ fullPage: true })`）:
1. operator 登录 → 报工 Tab → 见"逐道工序报工 ▶"入口（中文真显，无方块 □）。
2. 点入口 → 选批次屏：在产批次列表（批次号+品名+计划+状态）渲染；空态显 EmptyStateCard + "刷新" actionLabel。
3. 选一批 → 一道一卡片：卡片头品名+批次+工序+计划数量齐全；进度条 `报工 1/N` + 圆点；标准区间（Task 0 透出，配了的工序显 `85% ~ 95%`）。
4. 第 2 道：投入量预填 = 上道产出值，prefillNote "← 上道产出 X kg, 请确认实际投了多少" 橙色可见。
5. 改投入量（998→360）+ 填产出 + 提交。
6. A7 软告警：构造越界数据（工序配 standardYieldMin/Max 且 yield 越界）→ 提交后据 `submitReport.alert` 显黄色 banner 且**不拦**（上步已成功）。
7. A4 软上限：投入 > 计划×1.3 → YieldQuantityInput 黄字 maxHint 显（不拦）。
8. 提交 → 进度 +1，切下道，新道预填新上道产出。
9. 末道提交 → 完成态：累计出成率 + firstStepInput→lastStepOutput + 「标记今日结清」+「返回选批次」。

**Step 8.3 — audit doc**
写 `docs/qa-audits/2026-06-01-yield-rn-phase-d-headed-audit.md`，末尾必含 Headed Mode Verification block（per playwright-headed-mode.md）：headless:false / viewport 1920×1080 / locale zh-CN / chromium 真弹 / 中文无方块 / screenshot fullPage / video webm / PLAYWRIGHT_PORT 9222 / PLAYWRIGHT_CHAT_ID yield-rn。

**验证命令**:
```bash
cd /c/Users/Steve/cretas-yield-rn/frontend/CretasFoodTrace
PLAYWRIGHT_PORT=9222 PLAYWRIGHT_CHAT_ID=yield-rn npx playwright test e2e/yield-step-report.spec.ts --headed
```

**commit**: `test(yield-rn): Expo Web headed Playwright — 逐道报工 9 截图验证 + audit doc`

---

## 验收标准

1. **Task 0 后端**: `mvn test` 绿 + `mvn clean package` 编译通过；test 10011 `GET .../work-process-tasks` 响应含 `standardYieldMin`/`standardYieldMax` key（PG 真实验证）。合 main 后从 main 部 prod（per worktree-and-main-only-deploy.md）。
2. **TS**: `npx tsc --noEmit` 全绿（0 error），无 `as any`（typescript-type-safety.md）。
3. **功能**: operator 登录 → 报工 Tab → 逐道工序报工 → 选在产批次 → 逐道填投入+产出双量 → 下道预填上道产出可改 → A7 banner（越界时）/ A4 maxHint（超 1.3 时）→ 末道完成态显累计出成率。9 截图全过（Task 8）。
4. **防呆 4 位一体**: 提交错误 Alert 透传后端 message+hint（sticky 模态）；卡片头带品名/批次/工序/计划（Rule 2）；大按钮 size="large"（56pt）；空态 EmptyStateCard 带 actionLabel（Rule 5）。
5. **字段命名 camelCase**（field-naming-convention.md）；TS 类型 1:1 mirror Phase A DTO，跨 task 一致。
6. **headed 验证**: audit doc 含 Headed Mode Verification block，中文无方块。

---

## 部署说明

- **RN 不走 jar 部署**。Expo：本地 `npx expo start --web`（Web 验证）/ `npx expo start`（真机 Expo Go 扫码）。真机 APK 可走 `build-android-apk` skill（非本期必需）。RN 改动 merge main 后随 App 发版生效。
- **Task 0 后端走 `deploy-backend.sh`**（per server-operations.md）:
  ```bash
  # 先 test 验证
  ./scripts/deploy/deploy-backend.sh --env test
  # 合 main 后从 main 部 prod (per worktree-and-main-only-deploy.md: prod 只从 main)
  git checkout main && git pull origin main
  ./scripts/deploy/deploy-backend.sh --env prod
  # 部完核对运行 jar 含修复 (per feedback_concurrent_deploy_r2_path_collision)
  ```
- **worktree 纪律**: 全程在 `feat/yield-rn-phase-d` worktree；完成 PR → merge main；prod 只从 main 部署。

---

## 不做什么（YAGNI，对齐 spec §4）

1. 不改老审批屏（`ProcessTaskReportScreen.tsx` / `ThreeStepReportScreen.tsx` / `TeamBatchReportScreen.tsx` / `DynamicReportScreen.tsx`），保留并存。
2. 离线报工（断网缓存 + 重连同步）→ Phase E+。Phase D 假设在线。
3. 首道领料双量独立屏（`PUT /material-input`）：`yieldReportApi.recordMaterialInput` 方法写好但本期 UI 不调用；首道投入直接当普通投入在逐道卡片填。
4. settle-day `triggerComplete=true`（回写批次完成）留管理端；operator 完成态只做"标记今日结清"汇总（不带 triggerComplete）。
5. 跨批来源（A3 `sourceBatchRefs`）录入 UI；`YieldReportRequest.sourceBatchRefs` 默认不传。
6. 代报工（`targetWorkerId`）：operator 自报自，不传（后端 fallback JWT workerId）。
7. 独立报工明细 append-only 流水屏（`GET /reports` 当前复用 getYield 聚合视图）。
8. 选批次行内"已报 N/M 道"进度（需逐批 getYield，YAGNI）。
9. **A4 后端补之前不实现客户端拒绝/forceSubmit 重发流程**——Phase A `YieldReportServiceImpl` 未实现 A4 超收检查（`submitReport` 不读 forceSubmit、不抛超收错，只校验 workProcessTaskId/outputQuantity 必填，已核实 `YieldReportServiceImpl.java:45-107`）。本期只做客户端 maxHint 软提示（基于 plannedQuantity）；后端补 A4 后扩展 Alert+forceSubmit（spec §3.3 / Q1）。

---

## 并行工作建议

### Subagent: ✅ 适合
- **Subagent A**: `yieldReportApi.ts`（Task 1+2，纯 API + 类型，无 UI 依赖，可独立 + tsc 验）。
- **Subagent B**: `YieldQuantityInput.tsx`（Task 3，无外部依赖，可与 A 并行先写）。
- **Subagent C**: `YieldBatchSelectScreen.tsx`（Task 5，依赖 A 类型 + processingApiClient，A 定完 interface 即可起）。
- **依赖**: 先定 Task 1 的 `interface`（~10 分钟），再 A/B/C 并行。`YieldStepReportScreen.tsx`（Task 4）依赖 A+B → A/B 完成后单独做（或 C 之后串行）。
- **共享文件串行**: Task 0（后端）+ Task 7（`OperatorNavigator.tsx`/`ScanReportScreen.tsx`）由主 chat 串行改，不并行（concurrent-edit-safety 规则 4/5b）。

### 多 Chat: ⚠️ 谨慎
- 全在 `frontend/CretasFoodTrace/` + 一处后端 DTO，与餐饮/SmartBI chat 无冲突可并行。
- **冲突风险**: `OperatorNavigator.tsx` 共享导航文件——本任务独占 navigator 改动；`WorkProcessTaskDTO.java`/`WorkProcessTaskServiceImpl.java` 若别 chat 也在改工序任务则冲突，本任务独占这两个后端文件。
- worktree 隔离（已在 `feat/yield-rn-phase-d`，per worktree-and-main-only-deploy.md）。
