# 美食广场「跳转壳」Demo — 设计文档

**日期**: 2026-07-22
**客户**: 天物书岛食集（美食广场，10 个独立档口）
**交付形态**: 静态 H5 Demo，部署到 139 网关 `:8089`，IP 直访
**需求来源**: 客户微信语音 9 分钟（转录见 `platform/foodcourt/assets/source/requirement-voice-transcript.txt`）+ 现状小程序截图 6 张 + 桌牌照片 1 张

---

## 1. 背景：客户现状与痛点

天物书岛食集是一个美食广场，场内有 10 个独立经营的档口，共用一套堂食扫码点餐。桌上贴桌牌二维码（如 `A01`），扫码进入客如云（客户如云）小程序「天物书岛食集」。

现状实现方式：把小程序的**菜品大类导航**改造成了**门店切换**——左侧竖栏塞下 10 个档口 logo，顶部才是真正的菜品分类。

### 1.1 体验层痛点（截图坐实）

| # | 痛点 | 证据 |
|---|---|---|
| 1 | 10 个彩色 logo 挤在左侧一条竖栏，红绿蓝黄互相打架，看不清 | 全部 6 张截图左栏 |
| 2 | 门店名与菜品分类混在同一屏，信息层级错乱 | 左栏=门店，顶栏=分类 |
| 3 | 菜品只有小方图，点击展不开大图，只能 `+` 加减或「选规格」 | 全部截图菜品列表 |
| 4 | 单个档口打烊时，**点进去才**弹「本店已打烊，仅可浏览菜单／您也可以尝试切换到其他门店进行点单」 | 6 张截图底部黑条 |
| 5 | 菜品图加载慢，出现「加载中…」转圈 | 青岛鲜啤截图 |

### 1.2 结算层痛点（客户核心诉求）

现状是**聚合收款**：顾客付的钱 100% 进总部账户，第二天总部再手工分 90% 给各档口，自留 10%。

- **已连续 4 个月没分过账** — 手工分账不可持续
- **税务风险** — 税务按 100% 收入认定总部，分出去的 90% 还需各档口回开发票，流程冗长

---

## 2. 解决方案：外面套一层「跳转壳」

**核心思路：不接入、不改造客如云内部系统，只在最外层套一个壳。**

```
桌牌二维码（A01）
      ↓ 扫码
┌─────────────────────────┐
│  跳转壳（总部小程序）      │   ← 我们做的，只有 10 个按钮，没有任何菜品/收银逻辑
│  显示桌号 A01 + 10 个档口 │
└─────────────────────────┘
      ↓ 点「磊子川菜」，桌号 A01 随之带入
┌─────────────────────────┐
│ 磊子川菜的独立点餐小程序    │   ← 客如云原有，各档口独立账号、独立收款
└─────────────────────────┘
      ↓ 顾客直接付给该档口
   钱进磊子川菜自己的账户，总部次日抽 10%
```

### 2.1 这样解决了什么

| 原痛点 | 解法 |
|---|---|
| 手工分账 4 个月没做 | 各档口独立收款，钱本来就在自己账上，**无需分账** |
| 税务按 100% 认定总部 | 总部只收 10% 抽成，只对 10% 计税 |
| 10 个 logo 挤一条竖栏 | 壳首页两列大卡，每家一张，留白充分 |
| 点进去才知道打烊 | **进店之前**就在卡片上标「已打烊」并置灰 |
| 图片展不开 | 档口页菜品大图，点击全屏展开 |

### 2.2 已明确接受的取舍

- **一次只能在一家档口下单**。想吃三家＝下三笔单。跨档口合并购物车需打通各档口收银，不在本方案范围内，客户已认可。
- 壳内**不做**任何菜品、库存、收银、订单逻辑，纯跳转。

### 2.3 桌号对齐（关键约束）

10 家档口必须共享同一套桌号。1 号桌扫码后跳进任何一家，该档口都必须知道这单来自 1 号桌，否则菜送不到桌。壳负责把桌号从扫码入口一路透传到跳转链接。

---

## 3. Demo 范围

本次交付的是**给客户看的高保真 H5 原型**，不是上线的小程序。目的：让客户在手机上真扫真点，认可方案后再走客如云授权、小程序开发、审核。

### 3.1 做什么

- 扫码落地页（壳首页）：桌号 + 10 个档口卡片
- 档口页（模拟）：真实菜名价格、大图可展开、桌号常驻
- 3 张演示桌牌二维码：A01 / A05 / B02
- 部署到 139:8089

### 3.2 不做什么

- 不做真实跳转（链接未拿到，跳转位置显示说明条）
- 不做购物车、下单、支付、订单
- 不做分账看板（分账由「各档口独立收款」天然解决，无需系统）
- 不做微信小程序（授权未到位，属下一阶段）

---

## 4. 技术方案

### 4.1 选型

纯静态 H5：单个 `index.html` + `config.json` + 图片资源，vanilla JS 做两屏切换。

**理由**：两屏体量，零构建零依赖当天可上；客户后续要改档口名/链接，直接改 `config.json` 即可，无需重新构建部署。

已否决方案：
- Vue + Vite SPA — 为两屏上完整构建链路过重，且改档口名要重新 build
- 直接做微信小程序 — 需客如云总账号授权 + appid + 审核，均未到位，属下一阶段

### 4.2 目录结构

```
platform/foodcourt/                    # 本地源
├── index.html                         # 单文件页面（两屏，hash 切换）
├── config.json                        # 唯一需要改的：档口名/图/跳转链接
├── assets/
│   ├── logo/                          # 10 个档口 logo（截图裁切）
│   ├── dish/                          # 菜品图
│   └── source/                        # 原始素材（截图、桌牌、语音转录）
├── qr/                                # A01.png / A05.png / B02.png 演示桌牌
└── tools/
    └── make_qr.py                     # 桌牌二维码生成脚本
```

部署目标：`139:/www/wwwroot/foodcourt/`（`assets/source/` 与 `tools/` 不上传）

### 4.3 nginx

139 端口现状：8086=web-admin，8087=client-request，8088=prototype，**8089 空闲且安全组已放行 0.0.0.0/0**。

新增 `/www/server/panel/vhost/nginx/foodcourt.conf`：

```nginx
server {
    listen 8089;
    server_name 139.196.165.140;
    root /www/wwwroot/foodcourt;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
}
```

`nginx -t` 通过后 reload。**不修改任何现有配置文件**（139 是网关，承载 API 反代，不可影响）。

访问：`http://139.196.165.140:8089/?table=A01`

---

## 5. 页面设计

### 5.1 第 1 屏 — 扫码落地页

```
天物书岛食集                    [演示]
FOOD IS NEW FASHION
━━━━━━━━━━━━━━━━━━━━━━━━━━━
              🪑 A01 桌
       您在 A01 桌 · 点哪家送哪家
━━━━━━━━━━━━━━━━━━━━━━━━━━━
┌───────────┐  ┌───────────┐
│  [logo]   │  │  [logo]   │
│ 磊子川菜   │  │ 咁好味     │
│ 川菜·小炒  │  │ 粤式烧腊   │
│ ● 营业中   │  │ ● 营业中   │
└───────────┘  └───────────┘
        …… 两列 × 5 行，共 10 家
```

- 桌号从 URL `?table=` 读取，无参数默认 `A01` 并显示「演示」角标
- 每张卡：logo（统一白色圆角容器承载）+ 店名 + 品类标签 + 营业状态
- **打烊档口整卡置灰 + 标「已打烊」，且不可点击** — 直接对冲现状「点进去才告知打烊」
- Demo 中固定 1-2 家为打烊态，用于现场演示该改进

### 5.2 第 2 屏 — 档口页（模拟）

```
← 磊子川菜                    A01 桌
━━━━━━━━━━━━━━━━━━━━━━━━━━━
┌─────────────────────────┐
│      [菜品大图]          │
│  水煮牛肉          ¥58   │
└─────────────────────────┘
        …… 6-8 道菜
━━━━━━━━━━━━━━━━━━━━━━━━━━━
 真实使用时此处跳转「磊子川菜」
 点餐小程序 · 桌号 A01 自动带入
```

- **桌号常驻顶部不消失** — 这是「10 家店桌号对齐」的可视化证明
- 菜品大图，**点击全屏展开** — 对冲现状「展不开大图」
- 底部说明条讲清这是壳、真实会跳走、桌号会带走

### 5.3 视觉方向

承客户现有桌牌物料：深黑底 + 米白字 + 白色手绘线条点缀 + `FOOD IS NEW FASHION`。

10 个 logo 本身彩色，各自装进统一的白色圆角容器，两列大卡、留白拉开——与现状「左侧竖条挤 10 个彩标」形成正面对比。客户可当场把现有小程序与 demo 并排对比。

---

## 6. 数据与素材

### 6.1 10 个档口（截图识别，顺序同现状左栏）

| # | 档口名 | 品类标签 |
|---|---|---|
| 1 | 东池 Plus | 日式便当 |
| 2 | 老城厢 | 汤包·点心 |
| 3 | 磊子川菜 | 川菜·小炒 |
| 4 | 代巴尼 DEI BAGNI | 手工窑炉披萨 |
| 5 | 海鲜面 福旺多多 | 海鲜面·套餐 |
| 6 | 咁好味 | 粤式烧腊 |
| 7 | CINTA BLUES 亲塔屋 | 东南亚菜 |
| 8 | 啡果 FEEGOO | 冰淇淋·特饮 |
| 9 | 现打鲜啤（青岛） | 啤酒 |
| 10 | 福记糖水铺子 | 糖水·刨冰 |

### 6.2 真实菜品数据（截图读取，直接使用）

**咁好味（粤式烧腊）**：咁好味四宝饭 ¥42 / 三拼饭 ¥38起 / 烧鸭饭 ¥29 / 烧鸭拼叉烧饭 ¥35 / 烧鸭拼油鸡 ¥35 / 玫瑰油鸡饭 ¥29 / 烧鹅饭 ¥45

**CINTA BLUES（东南亚）**：马来肉骨茶 ¥62 / 冬阴功海鲜汤 ¥58 / 泰式青柠蒸海鲈鱼 ¥78 / 泰式咖喱蛋香虾 ¥58 / 越式肉末生菜包 ¥42 / 椰汁咖喱鸡 ¥52 / 打包盒 ¥1

**海鲜面 福旺多多**："小网海鲜"与"大鸡排"套餐 ¥42起 / 台州渔民"海鲜粉干"套餐 ¥49起 / 海派"炒年糕"与"刨冰"套餐 ¥52 / "肥鲜黄鱼""搭"酸梅汤 ¥56 / 乐惠"海鲜小炒"单人套餐 ¥68 / 丰盛"海鲜热炒"双人套餐 ¥132 / 肥鲜黄鱼鲜虾面 ¥45

**啡果 FEEGOO（冰淇淋）**：柚子白茶 ¥22 / 茉莉山竹 ¥22 / 法国树莓荔枝 ¥22 / 墨西哥香草 ¥22 / 开心果冰淇淋 ¥22 / 巧克力冰淇淋 ¥22 / 双球冰淇淋 ¥35起

**现打鲜啤（青岛）**：青岛清爽 ¥10起 / 青岛小棕金 ¥8 / 青岛白啤瓶装 ¥10 / 青岛纯生瓶装 ¥12 / 逸品纯生 ¥15 / 奥古特A3 ¥15

**福记糖水铺子**：爽心刨冰雪泥双人套餐 ¥23起 / 女神闺蜜双人套餐 ¥39 / 夏日雪花冰双人套餐 ¥37 / 福记大满贯双人套餐 ¥46 / 招牌糖水三人老友记套餐 ¥68 / 福粿多多大满贯 ¥28 / 椰椰香芋西米露 ¥19起

其余 4 家（东池 Plus、老城厢、磊子川菜、代巴尼）截图未覆盖菜单，按其品类补充合理菜名与价位，并在 `config.json` 中标注 `"placeholder": true` 便于后续替换。

### 6.3 图片素材

- **10 个档口 logo**：从截图左栏裁切，真实
- **菜品图**：**全部为客户真实菜品图**，从截图裁切后 4× LANCZOS 放大。

  设计阶段原计划 AI 生成高清图（担心截图放大糊掉），实测后推翻：截图中菜品图约 128×127px，4× 放大后细节仍清晰可辨（虾、蛤蜊、咸蛋、麻将牌背景都看得清），完全支撑「点开看大图」的演示。用商家真图比 AI 生成图说服力更强，故不引入 AI 生成。正式上线仍应替换为商家原图（更高分辨率）。

  裁切参数（`assets/source/menu-*.png` → `assets/dish/`）：所有截图菜品行 **149px 等距、图框 127px、首行 y=194**，x 范围按各截图宽度比例取 0.255–0.475（啡果那张左栏偏宽，单独用 0.262–0.470）。已剔除 `tsingtao-4`（原截图该图带「加载中」灰遮罩）。

---

## 7. config.json 契约

```json
{
  "brand": {
    "name": "天物书岛食集",
    "tagline": "FOOD IS NEW FASHION"
  },
  "defaultTable": "A01",
  "stalls": [
    {
      "id": "leizi",
      "name": "磊子川菜",
      "tag": "川菜·小炒",
      "logo": "assets/logo/leizi.png",
      "open": true,
      "url": "https://客如云给的链接?tableNo={table}",
      "dishes": [
        { "name": "水煮牛肉", "price": "¥58", "img": "assets/dish/leizi-1.jpg" }
      ]
    }
  ]
}
```

### 7.1 跳转链接的两种形态（都必须支持）

档口跳转链接尚未拿到，其形态存在两种可能，代码需同时兼容：

| 形态 | config 写法 | 说明 |
|---|---|---|
| A. 链接可带桌号参数 | `"url": "https://...?tableNo={table}"` | `{table}` 运行时替换为当前桌号 |
| B. 一店一桌一码（每家每桌各一固定链接） | `"urlByTable": { "A01": "https://...", "A05": "https://..." }` | 按当前桌号查表 |

优先读 `urlByTable`，缺失则回落 `url` 并做 `{table}` 替换。因此链接未到手不阻塞开发。

---

## 8. 演示桌牌二维码

按客户现有桌牌版式生成 3 张：黑底、`A01` 大字、`就餐请扫描下方二维码`、底部品牌条 `天物书岛食集 / FOOD IS NEW FASHION`。

- 桌号：A01 / A05 / B02
- 内容：`http://139.196.165.140:8089/?table=<桌号>`
- 生成：`tools/make_qr.py`（qrcode + Pillow）
- 输出：`qr/A01.png` 等，可直接打印或屏幕展示

**演示脚本**：客户先扫 A01，再扫 A05，观察顶部桌号变化，并进入任一档口确认桌号跟随——「10 家店桌号对齐」当场演示完毕。

---

## 9. 待客户/客如云提供（不阻塞 demo 开发）

| # | 事项 | 阻塞什么 |
|---|---|---|
| 1 | 10 个档口的跳转链接（小程序 path 或 H5 URL） | 真实跳转，demo 用说明条占位 |
| 2 | 链接是否支持桌号参数，还是一店一桌一码 | 决定 config 用形态 A 还是 B，代码已双兼容 |
| 3 | 客如云总账号权限 | 真实小程序开发（下一阶段） |
| 4 | 各档口菜单原图 | 替换 AI 生成图（正式上线前） |
| 5 | 确认档口数量（语音说 8 家，截图数出 10 家） | demo 先按 10 家做，config 可删 |

---

## 10. 验收标准

1. `http://139.196.165.140:8089/?table=A01` 手机浏览器可访问，首屏 10 个档口卡片正确渲染
2. 换 `?table=A05` 顶部桌号随之变化
3. 点任一营业中档口进入档口页，顶部桌号与首页一致且常驻
4. 档口页点击菜品图可全屏展开大图
5. 打烊档口卡片置灰且不可点击
6. 3 张演示二维码扫描后分别落到对应桌号
7. 139 上 `nginx -t` 通过，现有站点（web-admin 8086、API 反代）不受影响

---

## 11. 实施记录（2026-07-22 完成并上线）

### 11.1 验收执行结果

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 首屏 10 卡片渲染 | ✅ 线上 200，截图确认 |
| 2 | `?table=` 切换桌号 | ✅ A01 / A05 / B02 均 200，顶部桌号随之变化 |
| 3 | 档口页桌号常驻 | ✅ B02 进 CINTA BLUES，顶栏红色 pill 显示 B02 |
| 4 | 点图全屏大图 | ✅ lightbox 展开，底部带菜名 + 价格 |
| 5 | 打烊置灰不可点 | ✅ 代巴尼灰度 + 压暗 + 「已打烊」印章 + 「明日 10:30 营业」 |
| 6 | 二维码落到对应桌号 | ✅ 3 张码内容分别指向 `?table=A01/A05/B02`，URL 均 200 |
| 7 | 不影响现有站点 | ✅ `nginx -t` 通过；reload 后 8086 web-admin 仍 200 |
| 8 | 需求素材不外泄 | ✅ `assets/source/`（客户截图）未上传，线上返回 404 |

### 11.2 打烊态的实现强化

设计原稿只写「整卡置灰」，实测视觉区分度不足——而「进店前就知道打烊」是本方案对冲现状的核心卖点，必须一眼可辨。最终实现为三重信号：logo 灰度 + 压暗遮罩、卡片中央「已 打 烊」印章、状态行显示 `closedNote`（如「明日 10:30 营业」，config 可配）。

### 11.3 部署踩坑：8089 需要开两道门

8089 端口在两个层面都是关着的，**两道都要开**：

1. **服务器 firewalld**：原本只放行到 8088。
   `firewall-cmd --permanent --add-port=8089/tcp && firewall-cmd --reload`
2. **阿里云安全组**（139 属账号 B，安全组 `sg-uf63xzexjj92s0srqjuz`）：原本无 8089 规则。
   已加 `tcp 8089/8089 0.0.0.0/0 accept`，Description 标注 `foodcourt demo static site (customer demo, 2026-07-22)`。

⚠️ **文档陷阱**：`server-operations/aliyun-credentials.md` 里「8086, 8087, 8088, 8089 | 0.0.0.0/0」那张表是 **47 服务器**（`sg-uf64n0hcl8w37d34zfmy`）的规则，不是 139 的。139 的安全组此前从未在文档中记录，误读会导致「以为端口开着」而排查方向跑偏。本次已补录到该文档。

### 11.4 部署命令（复现用）

```bash
# 1. 上传（排除需求素材与工具脚本）
cd platform/foodcourt
rsync -az --delete --exclude 'assets/source' --exclude '.gitignore' \
      ./ root@139.196.165.140:/www/wwwroot/foodcourt/

# 2. nginx（配置见 4.3，只新增 foodcourt.conf，不动现有配置）
ssh root@139.196.165.140 "nginx -t && nginx -s reload"

# 3. 二维码（改了访问地址时重新生成）
python tools/make_qr.py A01 A05 B02 --base http://139.196.165.140:8089
```

### 11.5 交付物

- 演示地址：`http://139.196.165.140:8089/?table=A01`（A05 / B02 同理）
- 演示桌牌：`platform/foodcourt/qr/{A01,A05,B02}.png`，900×1800，可直接打印或屏幕展示
